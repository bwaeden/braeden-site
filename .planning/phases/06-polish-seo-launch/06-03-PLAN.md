---
phase: 06-polish-seo-launch
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - data/projects.ts
  - .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [LNCH-01, LNCH-02, LNCH-03, LNCH-04]

must_haves:
  truths:
    - "`data/projects.ts` archived-braehods entry href is `https://github.com/bwaeden/braehods` (D-16) — committed and pushed to `main` BEFORE the DNS flip, so the live site never serves a self-loop link in the /work grid."
    - "The EXISTING Cloudflare DNS records for braehods.com (incl. proxy on/off state) are captured (screenshot or text) BEFORE any edit, so a NO-GO rollback restores a known-good config (D-19). DNS is in Cloudflare, not Namecheap."
    - "`braehods.com` + `www.braehods.com` are added to the Vercel `braeden-site` project Domains and show SSL `Valid` status BEFORE the Cloudflare DNS flip (D-13 Step 1+2 → LNCH-01)."
    - "Cloudflare DNS is flipped per D-13 (CORRECTED): apex `@` + `www` → CNAME `cname.vercel-dns.com`, BOTH grey-cloud (DNS-only / proxy OFF); old GitHub Pages records removed. `dig braehods.com +short` returns a Vercel edge IP (not Cloudflare 104.21/172.67, not GH Pages 185.199) and `curl -sI` shows no `cf-ray`."
    - "`https://braehods.com` returns HTTP 200 over valid HTTPS and serves the new site (contains 'Braeden'), NOT the old GH Pages content (D-14a)."
    - "A real Formspree submission from `https://braehods.com` (production URL, NOT preview) produces a real email at `fakegoat1@gmail.com` within 2 minutes (D-14b)."
    - "The `visual-checklist.md` sweep is walked clean against the live `https://braehods.com` (D-14c)."
    - "Production Lighthouse is ≥95 on Performance AND SEO (the lighthouse spec auto-applies strict 95 on a braehods.com base URL — closes deferred gates SEO≥95 and homepage-mobile-perf≥95) (D-14d)."
    - "A GO/NO-GO checkpoint confirms (a)–(d) of the D-14 pragmatic gate all pass on the live domain BEFORE any irreversible step; on NO-GO the Cloudflare records are reverted to the captured config (records + proxy state) (D-19)."
    - "Only AFTER a GO: old `bwaeden/braehods` GitHub Pages repo is archived (`gh repo archive bwaeden/braehods` exit 0 OR GitHub UI archive flag set) (LNCH-02 + D-15)."
    - "Only AFTER a GO: GitHub profile 'Website' field + Instagram bio 'Website' field both point at `https://braehods.com` — GitHub + Instagram ONLY, NOT YouTube (LNCH-03, user-driven external edits)."
    - "POST-LAUNCH (after GO, not a flip blocker): NVDA + VoiceOver iOS screen-reader walk on `https://braehods.com`; any P0 SR bug ships as a v1.0.1 hotfix (D-14 revised)."
  artifacts:
    - path: ".planning/phases/06-polish-seo-launch/06-03-SUMMARY.md"
      provides: "Plan 06-03 close: captured-Namecheap-records snapshot, DNS flip timestamps, prod URL smoke walk, prod Formspree real-email timestamp, prod Lighthouse Perf+SEO scores, visual sweep outcome, GO/NO-GO decision, GH archive confirmation, GH+IG bio update timestamps, post-launch SR walk outcome, Phase 6 close consolidation"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 row checked complete; Progress Table updated; v1 shipped marker"
  key_links:
    - from: "data/projects.ts archived braehods entry"
      to: "https://github.com/bwaeden/braehods (archived GH repo)"
      via: "single-line href edit per D-16, pushed to main before DNS flip"
      pattern: "github\\.com/bwaeden/braehods"
    - from: "braehods.com Cloudflare DNS"
      to: "Vercel braeden-site project deployment"
      via: "apex + www CNAME → cname.vercel-dns.com, grey-cloud/DNS-only (D-13 CORRECTED — Cloudflare, not Namecheap)"
      pattern: "cname\\.vercel-dns\\.com"
    - from: "https://braehods.com ContactModal submission"
      to: "fakegoat1@gmail.com"
      via: "Formspree endpoint xqeypnkw + NEXT_PUBLIC_FORMSPREE_ID (Vercel Production scope, Phase 1 D-13)"
      pattern: "real-email-on-prod"
---

<objective>
Ship v1 of braehods.com to the world with a safe, reversible launch sequence. Execute the DNS swap in the correct order (D-13 Namecheap: capture existing records → add Vercel domains + wait for SSL Valid → flip apex A + www CNAME → remove old GH Pages records), verify the PRAGMATIC production gate (D-14: HTTPS + real prod Formspree email + clean visual sweep + prod Lighthouse Perf/SEO ≥95), make an explicit GO/NO-GO decision (D-19), and ONLY on GO archive the old repo and update the GitHub + Instagram bios. The screen-reader walk runs POST-launch (not a flip blocker).

Purpose: Close the last 4 launch requirements (LNCH-01..04) and ship the personal site with a clean rollback path. The DNS flip is the only step with real blast radius; sequencing archive + bios AFTER a passing GO means a failed launch is a single CNAME-revert with nothing else to undo.

Output: braehods.com live with the new site over valid HTTPS; old `bwaeden/braehods` repo archived; GitHub + Instagram bios updated to braehods.com; post-launch SR walk recorded; 06-03-SUMMARY.md consolidating the Phase 6 close; ROADMAP/REQUIREMENTS/STATE.md flipped to v1-shipped state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-seo-launch/06-CONTEXT.md
@.planning/phases/06-polish-seo-launch/06-RESEARCH.md
@.planning/phases/06-polish-seo-launch/06-01-SUMMARY.md
@.planning/phases/06-polish-seo-launch/06-02-SUMMARY.md
@.planning/phases/06-polish-seo-launch/visual-checklist.md
@data/projects.ts
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From data/projects.ts (current state, verified at plan time):
- The archived entry is at lines 100-108 (`slug: 'braehods-archive'`, `title: 'braehods.com (v0)'`).
- Line 107 currently reads `href: 'https://braehods.com',` — this MUST change to `'https://github.com/bwaeden/braehods',` BEFORE the DNS flip (D-16). After the flip, the old URL serves the NEW site, so leaving it would render a self-loop card in the /work grid.
- The leading comment block (lines 33-36) currently says the href "swaps to a Wayback Machine snapshot or the archived GitHub repo URL — Phase 6 decides at the cutover." D-16 selected the archived GitHub repo URL.

From Vercel braeden-site project (verified in Plan 06-01 Task 4 + Plan 06-02 Task 9):
- Project linked to `bwaeden/braeden-site` GitHub repo. Production branch: `main`.
- Env vars: `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw` set across Production + Preview + Development (Phase 1 D-13).
- Domains `braehods.com` + `www.braehods.com` were added in Phase 1 W4-T1 with "Invalid Configuration" pending the DNS swap — re-confirm SSL `Valid` in Task 3 before flipping (re-add if absent).

From CONTEXT D-13 — Namecheap DNS flip (registrar is Namecheap, user-confirmed 2026-05-26):
- Namecheap does NOT support apex CNAMEs. Apex `@` → A record `76.76.21.21` (Vercel anycast IP). `www` → CNAME `cname.vercel-dns.com`.
- Capture the EXISTING records first (D-19 rollback contract), then add domains to Vercel + wait for SSL Valid, THEN set the A + CNAME and remove the old GitHub Pages records.
- Path: Namecheap → Domain List → braehods.com → Advanced DNS.

From CONTEXT D-14 (REVISED, pragmatic gate) — the DNS flip is hard-blocked on, and ONLY on:
- (a) `https://braehods.com` loads over valid HTTPS (SSL "Valid" + curl 200 with new content);
- (b) a real Formspree submission FROM the production URL arrives at fakegoat1@gmail.com;
- (c) the visual sweep (visual-checklist.md) is clean;
- (d) production Lighthouse ≥95 on Performance + SEO (lighthouse spec auto-applies strict 95 on a braehods.com base URL — verifies deferred gates SEO≥95 + homepage-mobile-perf≥95).
- MOVED TO POST-LAUNCH (NOT a flip blocker): NVDA + VoiceOver iOS screen-reader walk. A P0 SR bug found post-launch ships as a v1.0.1 hotfix.
- Already verified GREEN on preview at 06-02 close (NOT re-gated here): bundle ≤50KB (PERF-03 = 20.38 KB gz), full-spec-suite-green.

From CONTEXT D-19 — explicit rollback + go/no-go:
- Structure: DNS flip → verify D-14(a)–(d) → GO/NO-GO checkpoint → (only on GO) archive old repo + update bios.
- Rollback on NO-GO: restore the captured Namecheap records (GH Pages apex A-records + the `bwaeden.github.io` CNAME removed during the flip). DNS is reversible at the registrar.

From CONTEXT D-15:
- Old `bwaeden/braehods` repo handling = ARCHIVE ONLY, NO 301 redirect. `gh repo archive bwaeden/braehods` (CLI) OR GitHub UI Settings → "Archive this repository".

From CONTEXT § Claude's Discretion (bios, user-confirmed 2026-05-26):
- LNCH-03 bios = GitHub + Instagram ONLY. YouTube EXCLUDED (it was dropped from the v1 site per 02-SCOPE-AMENDMENT.md). The executor DRAFTS the exact copy + link; the user pastes it. The executor does NOT auto-edit external services.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1 (W0): Single-line edit data/projects.ts archived-braehods href (D-16) BEFORE DNS flip</name>
  <files>data/projects.ts</files>
  <read_first>
    - data/projects.ts (lines 100-108: the `braehods-archive` entry; line 107 href; lines 33-36 comment block)
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-16
    - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md § GH URL Verification (confirms the 3 active hrefs are 200; this task changes only the archived self-loop)
  </read_first>
  <behavior>
    - Test 1: `grep -c "github.com/bwaeden/braehods" data/projects.ts` returns ≥1 (the archived entry's href is now the GH archive URL).
    - Test 2: `grep -c "href: 'https://braehods.com'," data/projects.ts` returns 0 (the old self-loop URL is gone).
    - Test 3: `npm run test -- tests/work-grid-renders.spec.ts tests/work-status-badges.spec.ts tests/work-no-flagship.spec.ts tests/work-descriptions-cliche-scrub.spec.ts` GREEN (the specs assert entry count + status + cliché-scrub, NOT a specific archived href, so no regression).
  </behavior>
  <action>
    Single-line href edit per CONTEXT D-16. This MUST land and push to `main` BEFORE the DNS flip in Wave 1 — otherwise braehods.com will briefly serve a self-loop link in the /work grid for the archived card.

    Steps:
    1. EDIT `data/projects.ts` line 107: change `href: 'https://braehods.com',` to `href: 'https://github.com/bwaeden/braehods',` on the `braehods-archive` entry.
    2. Update the leading comment block (lines 33-36) to record that Phase 6 D-16 selected the archived-GitHub-repo URL (not a Wayback snapshot), resolving the "Phase 6 decides at the cutover" note.
    3. Verify locally: run `npm run typecheck`, then `npm run lint`, then `npm run build`, then the 4 work-grid specs from the Behavior block.
    4. Commit with message `feat(06-03): D-16 archived braehods href -> github archive URL (pre-DNS-flip)`.
    5. Push to `main` so the production deploy that the DNS flip routes to already carries the corrected href. Do NOT batch this with any DNS step — it must be a standalone earlier commit.
  </action>
  <verify>
    <automated>grep -c "github.com/bwaeden/braehods" data/projects.ts; grep -c "href: 'https://braehods.com'," data/projects.ts; npm run test -- tests/work-grid-renders.spec.ts tests/work-status-badges.spec.ts</automated>
    Expected: first grep ≥1, second grep 0, specs GREEN.
  </verify>
  <acceptance_criteria>
    - `data/projects.ts` contains ≥1 occurrence of `github.com/bwaeden/braehods` (the new href on the archived entry).
    - `data/projects.ts` contains 0 occurrences of `href: 'https://braehods.com',`.
    - All 4 work-grid Playwright specs GREEN locally; typecheck + lint + build clean.
    - A single commit lands the change and is pushed to `main` (production deploy re-triggered) before any Task 2 DNS work.
  </acceptance_criteria>
  <done>Archived-braehods href points at the GH archive URL and is live on main. Safe to flip DNS without a self-loop link.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (W1): Capture existing Cloudflare DNS records (D-19 rollback snapshot) BEFORE any edit</name>
  <files>(no files — Cloudflare dashboard external; output captured into 06-03-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-19 (rollback: capture existing records BEFORE editing so the restore is a known-good config, not a reconstruction)
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-13 (CORRECTED: DNS is in Cloudflare, not Namecheap; nameservers chris/katelyn.ns.cloudflare.com; currently proxied → GitHub Pages origin)
  </read_first>
  <what-built>
    The DNS flip is the one launch step with real blast radius (the live personal domain). DNS is managed in CLOUDFLARE (Namecheap is only the registrar). D-19 requires capturing the current Cloudflare DNS config + proxy (orange/grey) state BEFORE touching anything, so a NO-GO at the go/no-go checkpoint is a known-good restore rather than a reconstruction from memory.
  </what-built>
  <how-to-verify>
    1. Log in to Cloudflare → select the `braehods.com` zone → DNS → Records.
    2. Capture EVERY current record verbatim — type, name, content/value, TTL, AND proxy status (orange-cloud = Proxied vs grey-cloud = DNS only). Current config is proxied → GitHub Pages origin; record the apex + www records exactly as shown (whatever GH Pages target/IPs are present).
    3. Save the capture as BOTH a screenshot AND a verbatim text transcription. Store the text in the resume signal and (after) in 06-03-SUMMARY.md § Captured Cloudflare Records (Rollback Snapshot).
    4. Do NOT edit any record in this task. This task only reads + records.
  </how-to-verify>

  <action>This is a checkpoint task. See &lt;what-built&gt; for context and &lt;how-to-verify&gt; for the exact steps the human walks. The executor pauses until the &lt;resume-signal&gt; arrives, then writes the captured records into 06-03-SUMMARY.md § Captured Cloudflare Records before any DNS edit proceeds.</action>
  <verify>
    <automated>echo "checkpoint:human-action gate — executor pauses; verification is the captured-records transcription supplied in the resume signal per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "records-captured:" followed by the verbatim current Cloudflare record list (type/name/value/TTL/proxy for each), e.g. `records-captured: CNAME @ <gh-pages-target> proxied; CNAME www bwaeden.github.io proxied ...`. Do NOT proceed until the full list is provided.</resume-signal>
  <acceptance_criteria>
    - Every current Cloudflare record for braehods.com is captured verbatim (type, name, value, TTL, proxy on/off) in BOTH screenshot and text form.
    - The text capture is recorded so the NO-GO rollback can restore it exactly (including the original proxy state).
    - No DNS record was edited in this task.
  </acceptance_criteria>
  <done>Pre-flip Cloudflare config captured (incl. proxy state). Rollback target is known-good. Safe to add Vercel domains next.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (W1): Add braehods.com + www.braehods.com to Vercel + confirm SSL Valid (D-13 Step 1+2 → LNCH-01)</name>
  <files>(no files — Vercel dashboard external)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-13 (Step 1+2: add domain → SSL stages BEFORE the CNAME flip)
    - .planning/phases/01-foundation-design-tokens/01-CONTEXT.md § D-13 (env vars Production scope confirmed; domains were pre-added in W4-T1 with pending status)
  </read_first>
  <what-built>
    Vercel issues SSL via Let's Encrypt when a domain is added to a project. D-13's add-domain-FIRST ordering stages the cert at Vercel's edge BEFORE traffic routes to it, avoiding the brief HTTP-only window that a CNAME-first flip would cause (T-06-10). The domains may already exist from Phase 1 W4-T1 in a pending state — re-confirm or re-add.
  </what-built>
  <how-to-verify>
    1. Open https://vercel.com/dashboard → `braeden-site` project → Settings → Domains.
    2. If `braehods.com` + `www.braehods.com` are already listed (from Phase 1 W4-T1), note their current status. If absent, click "Add Domain", enter `braehods.com`, Add; repeat for `www.braehods.com` (accept Vercel's default redirect-to-apex setup for www).
    3. Vercel shows the required DNS records for each domain. With the Cloudflare CNAME-flatten approach (D-13), point apex + www at `cname.vercel-dns.com`; Vercel also accepts the apex A `76.76.21.21` if it prefers to show that. DO NOT change Cloudflare DNS yet — that is Task 4.
    4. Wait for the SSL status indicator to show "Valid" next to BOTH domains (typically <5 min once Vercel can complete the challenge; if it sticks on pending because DNS still points at GH Pages, the SSL still pre-provisions on the Vercel side — confirm the cert is issued).
    5. Optional sanity check (proves the Vercel side is ready before any propagation): `curl --resolve braehods.com:443:76.76.21.21 -sI https://braehods.com` → expect HTTP 200 + new-site headers (the Host header matches what Vercel provisioned).
    6. Capture "Domain added/confirmed" + "SSL Valid" timestamps for 06-03-SUMMARY.md § DNS Timeline.
  </how-to-verify>

  <action>This is a checkpoint task. See &lt;what-built&gt; for context and &lt;how-to-verify&gt; for the exact steps. The executor pauses until the &lt;resume-signal&gt; arrives. DO NOT advance to Task 4 (the Namecheap flip) until SSL shows Valid for both domains.</action>
  <verify>
    <automated>echo "checkpoint:human-action gate — executor pauses; SSL-Valid status is the human-verified gate per D-13 Step 2"</automated>
  </verify>
  <resume-signal>Type "vercel-domains: ssl-valid <timestamp>" OR "ssl-pending: <reason>" (DO NOT proceed to Task 4 until SSL is Valid for both braehods.com and www.braehods.com).</resume-signal>
  <acceptance_criteria>
    - `braehods.com` and `www.braehods.com` both appear in the Vercel Domains list with SSL status `Valid`.
    - The Vercel-shown required records are noted (CNAME `cname.vercel-dns.com`, or apex A `76.76.21.21`).
    - Optional `curl --resolve braehods.com:443:76.76.21.21` confirms Vercel serves the new site for the Host header.
    - Timestamps recorded in 06-03-SUMMARY.md § DNS Timeline.
  </acceptance_criteria>
  <done>Vercel side ready: domains configured + SSL staged. Safe to flip Cloudflare DNS.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 (W1): Flip Cloudflare DNS — apex + www CNAME → cname.vercel-dns.com, grey-cloud, remove GH Pages records (D-13)</name>
  <files>(no files — Cloudflare DNS dashboard external)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-13 (CORRECTED: Cloudflare CNAME-flatten; apex + www → cname.vercel-dns.com; BOTH grey-cloud / DNS-only; remove GH Pages records)
    - .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md § Captured Cloudflare Records (the Task 2 snapshot — confirm what is being removed)
  </read_first>
  <what-built>
    Task 3 staged the SSL cert at Vercel. This is the "go live" edit: pointing Cloudflare DNS at Vercel so the world starts resolving braehods.com to the new deployment. Cloudflare flattens apex CNAMEs, so apex + www both CNAME to Vercel. CRITICAL: the records must be grey-cloud (DNS-only / proxy OFF) — a Cloudflare proxy in front of Vercel breaks Vercel SSL + verification.
  </what-built>
  <how-to-verify>
    1. Cloudflare dashboard → `braehods.com` zone → DNS → Records.
    2. **Apex:** set a CNAME — Name `@` (braehods.com) → `cname.vercel-dns.com`, **Proxy status: DNS only (grey cloud)**, TTL Auto.
    3. **www:** set a CNAME — Name `www` → `cname.vercel-dns.com`, **Proxy status: DNS only (grey cloud)**, TTL Auto.
    4. **Remove the old GitHub Pages records** captured in Task 2 (the apex + www records pointing at the GH Pages origin). Confirm the final record set is exactly the two grey-cloud Vercel CNAMEs.
    5. Save. Record the "Cloudflare flipped" timestamp.
    6. Verify propagation (run from the Bash tool or a terminal):
       - `dig braehods.com +short` → expect a Vercel edge IP (e.g. `76.76.21.x`), NOT the Cloudflare 104.21.x/172.67.x IPs and NOT the GH Pages 185.199.x IPs.
       - `dig www.braehods.com +short` → expect `cname.vercel-dns.com` (or the Vercel edge IP it resolves to).
       - `curl -sI https://braehods.com` → confirm NO `cf-ray` / `Server: cloudflare` header (proves the proxy is off and Vercel serves directly).
    7. Record the "DNS propagated" timestamp in 06-03-SUMMARY.md § DNS Timeline.
  </how-to-verify>

  <action>This is a checkpoint task. See &lt;what-built&gt; for context and &lt;how-to-verify&gt; for the exact steps + records. The executor pauses until the &lt;resume-signal&gt; arrives, then proceeds to the automated prod smoke test (Task 5).</action>
  <verify>
    <automated>echo "checkpoint:human-action gate — executor pauses; dig result + no-cf-ray supplied in resume signal is the propagation gate"</automated>
  </verify>
  <resume-signal>Type "dns-flipped: <timestamp>; dig-apex: <ip>; dig-www: <cname-or-ip>; cf-proxy: off" OR "dns-pending: <details>".</resume-signal>
  <acceptance_criteria>
    - Cloudflare apex `@` and `www` are both CNAME → `cname.vercel-dns.com`, both grey-cloud (DNS only / proxy OFF).
    - The old GitHub Pages records are removed.
    - `dig braehods.com +short` returns a Vercel edge IP (not Cloudflare 104.21/172.67, not GH Pages 185.199); `curl -sI https://braehods.com` shows NO `cf-ray`/`Server: cloudflare`.
    - Propagation confirmed across ≥2 resolvers (whatsmydns.net).
    - Timestamps recorded in 06-03-SUMMARY.md § DNS Timeline.
  </acceptance_criteria>
  <done>Cloudflare DNS flipped to Vercel (grey-cloud). braehods.com now routes to the new deployment. DNS is the only blast-radius step and remains reversible via the Task 2 snapshot.</done>
</task>

<task type="auto">
  <name>Task 5 (W1): Prod smoke test — D-14(a) HTTPS + new content + all Phase 6 surfaces serve correctly</name>
  <files>(no files — verification only; outputs into 06-03-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-13 Step 6 + § D-14a
    - .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md § Authoritative Audit Results (the surfaces that must serve: sitemap, robots, OG, JSON-LD, 404, canonical, proxy noindex)
  </read_first>
  <behavior>
    - Test 1: `curl -sI https://braehods.com/` returns `200` over valid HTTPS (no `-k` needed; no TLS error).
    - Test 2: `curl -s https://braehods.com/ | grep -i "Braeden"` matches (proves new site, not old GH Pages).
    - Test 3: `curl -sI https://braehods.com/sitemap.xml` returns 200; `curl -sI https://braehods.com/robots.txt` returns 200.
    - Test 4: `curl -sI https://braehods.com/opengraph-image.png` returns 200 image/png; `/about/opengraph-image` + `/work/opengraph-image` return 200 image/png.
    - Test 5: `curl -sI https://braehods.com/about` + `/work` both return 200.
    - Test 6: `curl -sI https://braehods.com/this-route-does-not-exist` returns 404 (branded 404).
    - Test 7: `curl -sI https://braehods.com/` shows NO `X-Robots-Tag: noindex` header (production, not preview — proxy.ts only noindexes VERCEL_ENV=preview).
    - Test 8: `PLAYWRIGHT_BASE_URL=https://braehods.com npm run test -- tests/preview-noindex.spec.ts` SKIPs (the URL is not *.vercel.app — proves the noindex gate correctly does not fire on prod).
  </behavior>
  <action>
    Run curl smoke tests against the live production URL to confirm D-14(a) and that every Phase 6 surface serves correctly. This is the automated half of the pragmatic gate.

    Steps:
    1. `curl -sI https://braehods.com/` — capture status + headers; expect 200 over HTTPS.
    2. `curl -s https://braehods.com/ | grep -Eo "Braeden|Currently" | head -3` — confirm new-site content.
    3. `curl -sI https://braehods.com/sitemap.xml` and `curl -sI https://braehods.com/robots.txt` — both 200.
    4. `curl -sI https://braehods.com/opengraph-image.png`, `/about/opengraph-image`, `/work/opengraph-image` — all 200 image/png.
    5. `curl -sI https://braehods.com/about` and `curl -sI https://braehods.com/work` — both 200.
    6. `curl -sI https://braehods.com/this-route-does-not-exist` — expect 404.
    7. `curl -sI https://braehods.com/ | grep -i "x-robots-tag"` — expect EMPTY (no noindex on production).
    8. `PLAYWRIGHT_BASE_URL=https://braehods.com npm run test -- tests/preview-noindex.spec.ts` — expect the spec to SKIP (good — prod is not noindexed and the spec guard works).
    9. Document all curl outputs in 06-03-SUMMARY.md § Production Smoke Test.
    10. If any curl fails: stop, identify the failure mode (DNS not propagated → wait; SSL hiccup → re-check Vercel Task 3; build error → check Vercel build logs; missing OG → confirm Plan 06-02 commits reached prod via `main`), document, and resolve before Task 6. A hard failure here means D-14(a) is not satisfied → the go/no-go (Task 9) will be a NO-GO.
  </action>
  <verify>
    <automated>curl -sI https://braehods.com/ | head -1 && curl -sI https://braehods.com/sitemap.xml | head -1 && curl -sI https://braehods.com/robots.txt | head -1 && curl -sI https://braehods.com/opengraph-image.png | head -1 && curl -sI https://braehods.com/this-route-does-not-exist | head -1</automated>
    Expected: 5 status lines reading 200, 200, 200, 200, 404.
  </verify>
  <acceptance_criteria>
    - Tests 1-6 all pass (root 200 over HTTPS with "Braeden", sitemap/robots/OG 200, routes 200, 404 branded).
    - `X-Robots-Tag` is absent on production responses (Test 7).
    - `preview-noindex.spec.ts` SKIPs against the prod URL (Test 8).
    - All curl outputs documented in 06-03-SUMMARY.md § Production Smoke Test.
  </acceptance_criteria>
  <done>D-14(a) satisfied: production serves the new site over valid HTTPS with all Phase 6 surfaces healthy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (W1): D-14(b) Real Formspree submit from PRODUCTION URL → fakegoat1@gmail.com</name>
  <files>(no files — manual external test on prod)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-14b (real prod Formspree email = the single highest-stakes signal)
    - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md § Real Formspree Submit (preview-side baseline; this re-verifies on PROD because env vars are scoped per-environment)
  </read_first>
  <what-built>
    `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw` is set in the Vercel Production scope (Phase 1 D-13), but a CDN/SSL/DNS issue could break form submission silently in front of real visitors. D-14b makes a real production-URL submit the highest-stakes gate. The form path was verified on preview in Plan 06-01; this confirms it on production.
  </what-built>
  <how-to-verify>
    1. Open `https://braehods.com` in a desktop browser (production, NOT preview).
    2. Click Nav `Contact` → ContactModal opens.
    3. Wait ≥2 seconds (clears the 1500ms min-time bot gate).
    4. Fill: Name `Phase 6 PROD verify`; Email `fakegoat1@gmail.com`; Message `Phase 6 Plan 06-03 PROD URL verification. Timestamp: <ISO datetime>.`; leave the honeypot `company` field EMPTY.
    5. Click Send. Verify the success state renders (`Thanks — I'll get back to you within a day or two.`).
    6. Within ≤2 minutes, check the `fakegoat1@gmail.com` inbox; confirm the email arrives with the timestamp marker.
    7. Record submit + received timestamps in 06-03-SUMMARY.md § Production Formspree Verify.
    8. If no email within 2 min: check Vercel Settings → Environment Variables → Production scope for `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw`. If missing/wrong, fix in Vercel, redeploy, re-test. A failure here means D-14(b) is not satisfied → Task 9 will be a NO-GO.
  </how-to-verify>

  <action>This is a checkpoint task. See &lt;what-built&gt; for context and &lt;how-to-verify&gt; for the exact steps. The executor pauses until the &lt;resume-signal&gt; arrives.</action>
  <verify>
    <automated>echo "checkpoint:human-verify gate — email arrival is the human-verified signal per D-14b"</automated>
  </verify>
  <resume-signal>Type "prod-email-received: <timestamp>" OR "prod-email-failed: <details>".</resume-signal>
  <acceptance_criteria>
    - A real email arrives at fakegoat1@gmail.com within 2 minutes of submit on the PROD URL.
    - The email body contains the timestamp marker.
    - Submit + received timestamps documented in 06-03-SUMMARY.md § Production Formspree Verify.
  </acceptance_criteria>
  <done>D-14(b) satisfied: production Formspree path verified end-to-end. Highest-stakes launch signal is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7 (W1): D-14(c) Visual sweep clean on live braehods.com (visual-checklist.md)</name>
  <files>(no files — user walks visual-checklist.md against prod; outcomes annotated)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/visual-checklist.md (40-item dedup'd sweep; §1-5 = 34 chrome/home/about/work/modal rows; §6 = 404; §7 = OG sharing)
    - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md § Visual Checklist Outcome (deferred Gate 1)
    - .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md § Deferred to Plan 06-03 (§6+7 ship + walk here)
  </read_first>
  <what-built>
    The 40-item combined visual checklist (28 Phase 4 + 12 Phase 5, dedup'd) was scaffolded in Plan 06-01 and extended with §6 (404) + §7 (OG) in Plan 06-02. D-14(c) requires the full sweep to be walked CLEAN against the LIVE braehods.com (not preview) — this closes deferred 06-01 Gate 1 and is one of the four pragmatic-gate blockers.
  </what-built>
  <how-to-verify>
    1. Open `https://braehods.com` (production) on desktop.
    2. Walk every row of `visual-checklist.md` §1-7 against the live site: chrome (Nav/Footer/monogram/focus rings), home hero, /about, /work grid + status dots, ContactModal open/close/states, the branded 404, and the OG sharing surfaces (the §7 OG rows are also validated in Task 8 across platforms).
    3. Mark each row pass/fail in the file.
    4. Any P0 visual defect (broken layout, wrong content, missing surface): document, cycle back to an inline source fix, push to `main`, wait for the Vercel prod redeploy, re-walk the affected rows.
    5. Commit the annotated checklist: `docs(06-03): mark visual checklist outcomes on prod`.
    6. Record the sweep result (clean / N issues) in 06-03-SUMMARY.md § Visual Sweep.
  </how-to-verify>

  <action>This is a checkpoint task. See &lt;what-built&gt; for context and &lt;how-to-verify&gt; for the exact steps. The executor pauses until the &lt;resume-signal&gt; arrives.</action>
  <verify>
    <automated>echo "checkpoint:human-verify gate — visual sweep result is the human-verified signal per D-14c"</automated>
  </verify>
  <resume-signal>Type "visual-sweep-clean: 40/40" OR "visual-sweep: N issues: <bulleted details>".</resume-signal>
  <acceptance_criteria>
    - All 40 rows of visual-checklist.md walked against the LIVE braehods.com.
    - Any P0 defect fixed inline + redeployed + re-walked before signal.
    - Annotated checklist committed; result recorded in 06-03-SUMMARY.md § Visual Sweep.
  </acceptance_criteria>
  <done>D-14(c) satisfied: live visual sweep clean. Deferred 06-01 Gate 1 closed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8 (W1): D-14(d) Production Lighthouse ≥95 Performance + SEO + OG platform validation</name>
  <files>(no files — Lighthouse run + external platform unfurl checks; outputs into 06-03-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-14d (prod Lighthouse strict 95 on Perf+SEO; verifies deferred gates SEO≥95 + homepage-mobile-perf≥95)
    - .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md § Lighthouse (the spec is environment-aware: braehods.com base URL keeps STRICT 95 on all four categories; preview relaxations do NOT apply to prod)
    - tests/lighthouse.spec.ts (reads PLAYWRIGHT_BASE_URL; strict 95 when the base URL is braehods.com)
  </read_first>
  <what-built>
    Plan 06-02 made tests/lighthouse.spec.ts environment-aware: on a braehods.com base URL it applies STRICT 95 on Performance, Accessibility, Best-Practices, and SEO, with NO preview relaxation. This is where the two deferred gates close: SEO≥95 (preview was capped at ~69 by the intentional noindex) and homepage-mobile-perf≥95 (preview was ~90 on cold CDN; prod runs on warmed CDN). OG platform validation (LNCH-04) also runs here so the share aesthetic is confirmed on real platforms before GO.
  </what-built>
  <how-to-verify>
    1. **Production Lighthouse (D-14d gate):** run the lighthouse spec against prod —
       `PLAYWRIGHT_BASE_URL=https://braehods.com npm run test -- tests/lighthouse.spec.ts`
       Expect all 6 audits (3 routes × mobile+desktop) to pass at STRICT 95. The gate specifically requires Performance ≥95 AND SEO ≥95 (SEO now uncapped because production has no noindex; homepage-mobile perf now on warmed CDN). Capture the score matrix.
       - If homepage-mobile Performance is <95: warm the CDN by hitting `/` a few times, then re-run; if it persists, this is a real PERF-06 miss → NO-GO + inline fix.
       - If SEO is <95: inspect the failing audit (canonical, robots, meta) → fix → redeploy → re-run.
    2. **OG platform validation (LNCH-04):** paste `https://braehods.com` into ≥2 platforms (Slack + iMessage minimum; LinkedIn Post Inspector + X card validator if available) and confirm the unfurl shows the static 1200×630 `/` OG (Braeden wordmark + monogram on charcoal) with the correct title/description. Paste `/about` + `/work` to confirm the dynamic ImageResponse OG (route title in Fraunces 700 on charcoal). Screenshot each; save under `.planning/phases/06-polish-seo-launch/og-validation/`.
       - If a platform shows a stale/wrong thumbnail: force re-fetch via that platform's debugger (Facebook Sharing Debugger, LinkedIn Post Inspector) or re-paste after ~5 min (Slack respects cache-control).
    3. Record the Lighthouse score matrix + the OG platform matrix in 06-03-SUMMARY.md § Production Lighthouse and § OG Validation.
  </how-to-verify>

  <action>This is a checkpoint task. See &lt;what-built&gt; for context and &lt;how-to-verify&gt; for the exact steps + commands. The executor runs the Lighthouse spec, then pauses for the human OG-unfurl checks, resuming on the &lt;resume-signal&gt;.</action>
  <verify>
    <automated>PLAYWRIGHT_BASE_URL=https://braehods.com npm run test -- tests/lighthouse.spec.ts</automated>
    Expected: all 6 audits GREEN at strict 95 (Perf + SEO are the D-14d blockers).
  </verify>
  <resume-signal>Type "prod-lighthouse: perf=<n> seo=<n> (pass/fail); og-validated: <platforms>" e.g. `prod-lighthouse: perf=96 seo=100 pass; og-validated: slack, imessage` OR "lighthouse-failed: <details>" / "og-failed: <details>".</resume-signal>
  <acceptance_criteria>
    - Production Lighthouse ≥95 on Performance AND SEO across the audited routes (strict-95 spec on braehods.com base URL); homepage-mobile-perf and SEO deferred gates both closed.
    - OG unfurl renders the correct 1200×630 image with site typography on ≥2 platforms (static `/` + dynamic /about + /work confirmed).
    - Lighthouse score matrix + OG platform matrix recorded in 06-03-SUMMARY.md.
  </acceptance_criteria>
  <done>D-14(d) satisfied: production Lighthouse Perf+SEO ≥95 (deferred SEO + homepage-mobile-perf gates closed); OG share aesthetic confirmed on real platforms.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 9 (W2): GO / NO-GO launch decision (D-19) — gate on D-14(a)-(d) BEFORE any irreversible step</name>
  <files>(no files — decision gate; outcome recorded in 06-03-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-19 (go/no-go sits after the flip + gate verification, BEFORE archive + bios; NO-GO = revert Namecheap to the Task 2 snapshot)
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-14 (the four pragmatic-gate blockers (a)-(d))
    - .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md (the recorded outcomes of Tasks 5-8)
  </read_first>
  <decision>
    Is braehods.com clear to ship v1? GO proceeds to the irreversible/visible steps (archive old repo + bio updates). NO-GO triggers the rollback (restore the captured Namecheap records) and holds the launch.
  </decision>
  <context>
    This is the D-19 safety gate. The DNS flip (Task 4) is the only step with real blast radius, and it is reversible via the Task 2 snapshot. Archive (Task 10) and bio updates (Task 11) are deliberately sequenced AFTER this checkpoint so a NO-GO is a clean single-action CNAME-revert with nothing else to undo. Confirm ALL FOUR D-14 pragmatic-gate blockers passed on the live domain:
    - (a) Task 5: HTTPS 200 + new content + surfaces healthy.
    - (b) Task 6: real prod Formspree email arrived at fakegoat1@gmail.com.
    - (c) Task 7: visual sweep clean (40/40).
    - (d) Task 8: prod Lighthouse Perf ≥95 AND SEO ≥95.
    The screen-reader walk is NOT a blocker here (moved to post-launch, Task 12 — a P0 SR bug post-launch ships as a v1.0.1 hotfix).
  </context>
  <options>
    <option id="go">
      <name>GO — ship v1</name>
      <pros>All four D-14 blockers passed; site is live, secure, conversion-path verified, and meets the quality bar. Proceeds to archive + bios.</pros>
      <cons>None if (a)-(d) genuinely passed. SR polish is deferred to a post-launch hotfix lane (accepted per D-14 revision).</cons>
    </option>
    <option id="no-go">
      <name>NO-GO — roll back the DNS flip</name>
      <pros>Live domain returns to the known-good GH Pages config (Task 2 snapshot) with a single revert; nothing else was changed, so the old site is fully intact and re-pointable.</pros>
      <cons>v1 launch held until the failing D-14 blocker is fixed and the flip + gate are re-run.</cons>
    </option>
  </options>
  <action>This is a decision checkpoint. The executor pauses for the GO/NO-GO call. On GO, proceed to Task 10. On NO-GO, perform the rollback per &lt;rollback&gt; below, record it in 06-03-SUMMARY.md § GO/NO-GO Decision, and STOP — do NOT run Tasks 10-12 (archive + bios + SR walk) until a later flip + gate pass earns a GO.</action>
  <rollback>
    On NO-GO: in Cloudflare → DNS → Records, delete the Vercel CNAMEs (apex + www → `cname.vercel-dns.com`) and restore EXACTLY the records captured in Task 2 (the original GH Pages records AND their original proxy on/off state). Verify `dig braehods.com +short` returns the prior IPs again (Cloudflare 104.21/172.67 if proxied, or GH Pages 185.199 if grey). Record which D-14 blocker(s) failed and the rollback timestamp in 06-03-SUMMARY.md.
  </rollback>
  <verify>
    <automated>echo "checkpoint:decision gate — GO/NO-GO is the human decision; executor pauses per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "GO" (all of D-14 a-d confirmed pass — proceed to archive + bios) OR "NO-GO: <failing blocker(s)>" (executor performs the Task 2 rollback and holds the launch).</resume-signal>
  <acceptance_criteria>
    - The GO/NO-GO decision is recorded in 06-03-SUMMARY.md § GO/NO-GO Decision with each D-14 blocker's pass/fail state.
    - On GO: Tasks 10-12 proceed.
    - On NO-GO: Cloudflare is reverted to the Task 2 snapshot (records + proxy state), `dig` confirms the prior IPs, and Tasks 10-12 do NOT run.
  </acceptance_criteria>
  <done>Launch go/no-go decided. Irreversible steps are gated behind a passing GO; a NO-GO is a clean single-action rollback.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 10 (W2, GO-only): Archive bwaeden/braehods GitHub Pages repo (LNCH-02 + D-15)</name>
  <files>(no files — GitHub CLI/UI external)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-15 (archive-only, NO 301 redirect — redirect is a v1.x yak-shave)
    - .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md § GO/NO-GO Decision (confirm GO before running this)
  </read_first>
  <what-built>
    Per D-15, the old `bwaeden/braehods` GH Pages repo is archived (read-only), NOT given a 301 redirect. The DNS flip already moved traffic; the only remaining old-site URL is `bwaeden.github.io/braehods` (unlikely to be shared because the `braehods.com` CNAME masked it). Run ONLY after a GO (Task 9) — sequencing archive after the go/no-go keeps a NO-GO a clean CNAME-revert with nothing else to undo.
  </what-built>
  <how-to-verify>
    1. Confirm Task 9 returned GO. If NO-GO, skip this task.
    2. Prefer GitHub CLI (run from the Bash tool): `gh repo archive bwaeden/braehods --yes` (check `gh repo archive --help` for the confirm flag). Expect a success message.
    3. If `gh` is not installed/authenticated, fall back to the GitHub UI: https://github.com/bwaeden/braehods/settings → Danger Zone → "Archive this repository" → confirm.
    4. Verify: visit https://github.com/bwaeden/braehods — the page shows "This repository has been archived by the owner ... It is now read-only." (Archiving is reversible via Settings → Unarchive.)
    5. Confirm via CLI if available: `gh repo view bwaeden/braehods --json isArchived` returns `{"isArchived": true}`.
    6. Record the archive timestamp + method (CLI vs UI) in 06-03-SUMMARY.md § GH Archive.
  </how-to-verify>

  <action>This is a checkpoint task (GO-only). See &lt;what-built&gt; for context and &lt;how-to-verify&gt; for the exact steps + commands. The executor pauses until the &lt;resume-signal&gt; arrives.</action>
  <verify>
    <automated>echo "checkpoint:human-action gate (GO-only) — archive confirmation supplied in resume signal; gh repo view --json isArchived is the verifiable check if gh CLI present"</automated>
  </verify>
  <resume-signal>Type "archived: <timestamp> via <gh-cli|ui>" OR "archive-failed: <reason>".</resume-signal>
  <acceptance_criteria>
    - Task 9 returned GO before this task ran.
    - https://github.com/bwaeden/braehods shows the archived (read-only) banner.
    - If gh CLI present, `gh repo view bwaeden/braehods --json isArchived` returns `{"isArchived": true}`.
    - Archive timestamp + method recorded in 06-03-SUMMARY.md § GH Archive.
  </acceptance_criteria>
  <done>Old repo archived per D-15. LNCH-02 closed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 11 (W2, GO-only): Update bios — GitHub + Instagram ONLY (LNCH-03; executor drafts, user pastes)</name>
  <files>(no files — user-driven external edits; executor drafts copy + link, does NOT auto-edit external services)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § Claude's Discretion (LNCH-03 = GitHub + Instagram ONLY; YouTube EXCLUDED — dropped from v1 per 02-SCOPE-AMENDMENT.md; executor drafts copy, user pastes, no auto-edit)
    - .planning/phases/02-home-page/02-SCOPE-AMENDMENT.md (YouTube dropped from v1 — GH + IG are the only social surfaces)
    - .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md § GO/NO-GO Decision (confirm GO before running this)
  </read_first>
  <what-built>
    The user owns their GitHub + Instagram profiles; the executor cannot programmatically edit them. Per CONTEXT § Claude's Discretion: bios are GitHub + Instagram ONLY (YouTube is excluded — it was dropped from the v1 site, so a YT-bio update is out of scope and would be inconsistent). The executor DRAFTS the exact bio copy + the `https://braehods.com` link in the editorial-restrained register; the user pastes it. Run ONLY after a GO.
  </what-built>
  <how-to-verify>
    1. Confirm Task 9 returned GO. If NO-GO, skip this task.
    2. The executor first DRAFTS, in 06-03-SUMMARY.md § Bio Updates (Draft Copy), the exact strings the user will paste:
       - GitHub profile "Website" field value: `https://braehods.com`.
       - Instagram bio "Website" field value: `https://braehods.com` (plus, if the user wants a one-line bio refresh, an editorial-restrained line consistent with the site's "Business student and entrepreneur in LA" positioning — the URL is the required element).
    3. User edits **GitHub** (https://github.com/bwaeden → Edit profile → "Website" → `https://braehods.com` → Save; verify the profile page link resolves to braehods.com).
    4. User edits **Instagram** (app → Profile → Edit Profile → "Website"/links → `https://braehods.com` → Save; verify on the public profile view).
    5. Do NOT touch YouTube — it is out of scope (YT was dropped from the v1 site).
    6. Record which profiles were updated + timestamps in 06-03-SUMMARY.md § Bio Updates.
  </how-to-verify>

  <action>This is a checkpoint task (GO-only). The executor first drafts the GitHub + Instagram bio copy + the `https://braehods.com` link into 06-03-SUMMARY.md § Bio Updates (Draft Copy), then pauses for the user to paste it into each profile. See &lt;how-to-verify&gt; for the exact steps. The executor does NOT auto-edit external services. YouTube is explicitly excluded.</action>
  <verify>
    <automated>echo "checkpoint:human-action gate (GO-only) — bio update confirmation supplied in resume signal; GitHub + Instagram only, YouTube excluded"</automated>
  </verify>
  <resume-signal>Type "bios-updated: github, instagram" (both → braehods.com) OR "bios-updated: <subset>" / "bios-deferred: <reason>" (acceptable to defer if a platform is not actively maintained — document it).</resume-signal>
  <acceptance_criteria>
    - Task 9 returned GO before this task ran.
    - GitHub profile "Website" field → `https://braehods.com`.
    - Instagram bio "Website" → `https://braehods.com`.
    - YouTube is NOT touched (out of scope).
    - Draft copy + per-platform update timestamps recorded in 06-03-SUMMARY.md § Bio Updates.
  </acceptance_criteria>
  <done>GitHub + Instagram bios point at braehods.com. LNCH-03 closed (GH + IG scope per user decision).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12 (W2, POST-LAUNCH): NVDA + VoiceOver iOS screen-reader walk on braehods.com (D-14 revised — not a flip blocker)</name>
  <files>(no files — physical SR walk; outcomes recorded; P0 → v1.0.1 hotfix)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-14 (REVISED: SR walk MOVED TO POST-LAUNCH; a P0 SR bug ships as a v1.0.1 hotfix, NOT a launch blocker)
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-12 (the SR walk steps: tab all 3 routes; dialog role + aria-labelledby + focus-trap; aria-live idle/submitting/success/error; the role="alert" error region)
    - .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md § Deferred to Plan 06-03 item 1 (deferred T11 SR walk)
  </read_first>
  <what-built>
    Per the D-14 revision, the screen-reader walk is POST-launch accessibility polish — it runs AFTER the GO so it never blocks the flip. It still happens (it closes deferred 06-02 item 1), but a P0 SR finding ships as a v1.0.1 hotfix rather than holding v1. NVDA (Windows desktop) + VoiceOver iOS (audience-likely mobile SR) per D-12.
  </what-built>
  <how-to-verify>
    1. **NVDA (Windows):** with NVDA running, navigate `https://braehods.com` → tab through `/`, `/about`, `/work`; confirm each link's purpose is announced. Open ContactModal: confirm dialog role + aria-labelledby announce + focus trap. Submit the form: confirm the aria-live region announces idle → submitting → success. Trigger an error path: confirm the `role="alert"` error region announces with assertive urgency.
    2. **VoiceOver iOS (Safari):** repeat the walk on an iPhone — link announcements per route, ContactModal dialog announce + focus management, aria-live state transitions, error-region alert.
    3. Record findings in 06-03-SUMMARY.md § Post-Launch Screen-Reader Walk, classified by severity.
    4. For any P0 (e.g., dialog not announced, form state silent, focus escapes the trap): file/note a v1.0.1 hotfix and fix inline post-launch (push to `main` → Vercel prod redeploy). This does NOT reopen the launch gate — v1 is already shipped on GO.
  </how-to-verify>

  <action>This is a checkpoint task (post-launch). See &lt;what-built&gt; for context and &lt;how-to-verify&gt; for the walk steps. The executor pauses for the SR-walk outcomes. P0 findings route to a v1.0.1 hotfix lane; they do NOT block v1, which already shipped at the Task 9 GO.</action>
  <verify>
    <automated>echo "checkpoint:human-verify gate (post-launch) — SR-walk outcome supplied in resume signal; P0 findings route to v1.0.1 hotfix, not a launch block"</automated>
  </verify>
  <resume-signal>Type "sr-walk-clean: nvda=ok voiceover=ok" OR "sr-walk-findings: <severity-classified list>" (P0 → v1.0.1 hotfix; P1/P2 → tracked in Deferred Ideas).</resume-signal>
  <acceptance_criteria>
    - NVDA + VoiceOver iOS walks completed on `https://braehods.com` (3 routes + ContactModal dialog + aria-live states + role="alert" error region).
    - Findings recorded in 06-03-SUMMARY.md § Post-Launch Screen-Reader Walk, severity-classified.
    - Any P0 routed to a v1.0.1 hotfix (post-launch fix), NOT treated as a v1 launch blocker.
    - Deferred 06-02 SR-walk item closed.
  </acceptance_criteria>
  <done>Post-launch SR walk done. Deferred SR gate closed; any P0 handled as a v1.0.1 hotfix per the D-14 revision.</done>
</task>

<task type="auto">
  <name>Task 13 (W2): Write 06-03-SUMMARY.md + final Phase 6 close (ROADMAP/REQUIREMENTS/STATE)</name>
  <files>.planning/phases/06-polish-seo-launch/06-03-SUMMARY.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md + 06-02-SUMMARY.md (consolidation source)
    - .planning/ROADMAP.md (Phase 6 row + Progress Table)
    - .planning/REQUIREMENTS.md (LNCH-01..04 rows at lines 103-106 + traceability table lines 236-239)
    - .planning/STATE.md (progress.* frontmatter fields)
  </read_first>
  <action>
    Plan 06-03 = the v1 ship gate. Write the SUMMARY, flip final traceability, and mark Phase 6 (and the v1 release) complete. Reflect the GO/NO-GO outcome accurately — only mark v1 SHIPPED if Task 9 returned GO.

    Steps:
    1. CREATE `.planning/phases/06-polish-seo-launch/06-03-SUMMARY.md` per the SUMMARY template. Required:
       - Frontmatter: phase, plan (03), status (`complete` on GO; `blocked` on NO-GO), subsystem (launch/dns/prod-verify), tags ([dns-flip-namecheap, prod-formspree, prod-lighthouse, visual-sweep, go-no-go, rollback, gh-archive, bio-updates, sr-walk-post-launch, v1-shipped]), dependency graph (requires 06-01 + 06-02; provides braehods.com live), key-files (data/projects.ts D-16 edit + ROADMAP/REQUIREMENTS/STATE flips), key-decisions (D-13 Namecheap apex-A flip; D-14 pragmatic gate; D-19 go/no-go + rollback; D-15 archive-only; LNCH-03 GH+IG bios; SR walk post-launch), metrics (DNS propagation duration; total Phase 6 duration; commit count).
       - Body sections (required):
         - **Captured Namecheap Records (Rollback Snapshot):** the Task 2 verbatim record list.
         - **DNS Timeline:** domain-confirmed/SSL-valid (Task 3) + Namecheap-flipped/propagated (Task 4) timestamps.
         - **Production Smoke Test:** Task 5 curl outputs (8 checks) + preview-noindex SKIP confirmation.
         - **Production Formspree Verify:** Task 6 submit + received timestamps.
         - **Visual Sweep:** Task 7 result (40/40 clean or issues + fixes).
         - **Production Lighthouse:** Task 8 score matrix (Perf + SEO ≥95 confirmation — deferred SEO + homepage-mobile-perf gates closed).
         - **OG Validation:** Task 8 platform matrix + screenshot paths.
         - **GO/NO-GO Decision:** Task 9 outcome with each D-14 (a)-(d) pass/fail; rollback note if NO-GO.
         - **GH Archive:** Task 10 timestamp + method (GO-only).
         - **Bio Updates:** Task 11 draft copy + GH/IG timestamps (GO-only; YouTube explicitly excluded).
         - **Post-Launch Screen-Reader Walk:** Task 12 findings + any v1.0.1 hotfix notes.
         - **Anti-Patterns Avoided:** no 301 redirect (D-15); data/projects.ts href landed BEFORE the flip (D-16); SSL staged BEFORE the flip (D-13); archive + bios sequenced AFTER the GO (D-19); no bio auto-edits (user-driven); YouTube excluded from bios.
         - **Phase 6 Complete:** v1 of braehods.com shipped on <date> (on GO).
    2. UPDATE `.planning/ROADMAP.md`:
       - Phase 6 line (23) `[ ]` → `[x]`; append " — Shipped 2026-05-XX. braehods.com live over HTTPS; old bwaeden/braehods archived; GH+IG bios updated."
       - Phase 6 plan list: flip 06-01/06-02/06-03 to `[x]`.
       - Progress Table Phase 6 row → `3/3 | Complete | 2026-05-XX`.
    3. UPDATE `.planning/REQUIREMENTS.md`:
       - Checklist lines 103-106: `[ ]` → `[x]` for LNCH-01..04.
       - Traceability table lines 236-239:
         - LNCH-01 → `Complete (braehods.com on Vercel; SSL Valid before flip; Namecheap apex A 76.76.21.21 + www CNAME; flipped <timestamp>)`.
         - LNCH-02 → `Complete (bwaeden/braehods archived <timestamp>, no 301 per D-15)`.
         - LNCH-03 → `Complete (GitHub + Instagram bios → braehods.com; YouTube excluded per v1 scope)`.
         - LNCH-04 → `Complete (real-device via prior + prod Formspree email + OG validation ≥2 platforms on prod)`.
    4. UPDATE `.planning/STATE.md`: bump `progress.completed_phases` to 6, `progress.completed_plans` +1, `progress.percent` to 100; set Current focus → "v1 SHIPPED — braehods.com live"; Next Session → "v1.x triage (monogram refresh, portrait swap, SR P0 hotfixes if any, 301 redirect if referral traffic appears)"; append a detailed Last Session entry with the Phase 6 shipped marker.
    5. Commit: `gsd-sdk query commit "docs(06-03): plan 06-03 SUMMARY + Phase 6 complete (v1 SHIPPED)" --files .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`
    6. Push to `main`.
    (If Task 9 was NO-GO: set status `blocked`, do NOT mark Phase 6 complete or LNCH-01..04 Complete; record the failing blocker + rollback, and note the re-launch path.)
  </action>
  <verify>
    <automated>test -f .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md && grep -c "v1 SHIPPED" .planning/STATE.md && grep -c "\[x\] \*\*Phase 6" .planning/ROADMAP.md</automated>
    Expected (on GO): SUMMARY exists; STATE.md acknowledges v1 SHIPPED; ROADMAP Phase 6 marked complete.
  </verify>
  <acceptance_criteria>
    - 06-03-SUMMARY.md exists with full frontmatter + the required body sections (incl. Captured Namecheap Records, GO/NO-GO Decision, and Post-Launch SR Walk).
    - On GO: ROADMAP Phase 6 row checked + ship date; REQUIREMENTS LNCH-01..04 all Complete; STATE.md → v1 SHIPPED, Next Session → v1.x triage.
    - On NO-GO: status `blocked`, traceability NOT flipped to Complete, rollback + re-launch path recorded.
    - Single docs commit lands all 4 files + pushed to main.
  </acceptance_criteria>
  <done>Phase 6 closed (on GO: v1 of braehods.com SHIPPED, all 16 Phase 4+5 carry-forwards closed). Ready for v1.x triage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public DNS resolvers ↔ Namecheap DNS records | DNS propagation has a window where some resolvers serve old GH Pages IPs and some serve the new Vercel A record. SSL must be staged at Vercel (Task 3) BEFORE the flip (Task 4) so no resolver ever routes to an unprovisioned cert. |
| Vercel SSL provisioning ↔ Let's Encrypt | Vercel issues certs via Let's Encrypt automatically; if provisioning hangs, users see TLS errors. Task 3 waits for "Valid" before the flip. |
| Live braehods.com (blast-radius surface) ↔ rollback snapshot | The DNS flip is the only irreversible-feeling step. The Task 2 captured-records snapshot makes a NO-GO a known-good single-action restore (D-19), not a reconstruction. |
| GO/NO-GO gate ↔ irreversible/visible steps | Archive (Task 10) + bio updates (Task 11) are sequenced AFTER the go/no-go so a NO-GO leaves the old site fully intact and re-pointable. |
| User profile external services ↔ Phase 6 | The executor cannot edit GitHub/Instagram bios. LNCH-03 is user-driven; the executor only drafts copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Denial of service | DNS flip causes a brief HTTP-served / cert-not-ready window (SSL race) | medium | mitigate | D-13 + Task 3: add domains to Vercel FIRST + wait for SSL `Valid` BEFORE the Namecheap flip (Task 4). Cert is staged at Vercel's edge before any resolver routes to it. Task 5 verifies HTTPS 200 post-flip. |
| T-06-11 | Spoofing | Old GH Pages serves briefly post-flip via stale DNS cache | low | accept | TTL is short (Namecheap Automatic ≈ 30 min). Users on stale caches see Braeden's previous functional site (not an attacker), with no Formspree path to the new endpoint — no data leak. D-15 archive does not instantly disable the GH Pages subdomain; acceptable. |
| T-06-12 | Information disclosure | Production Formspree env var misconfigured → submits fail silently in front of real visitors | medium | mitigate | Task 6 (D-14b) is an explicit prod-URL real-submit gate; `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw` pre-staged in Vercel Production (Phase 1 D-13). On failure, fix env var + redeploy + re-test; a persistent failure forces a NO-GO at Task 9. |
| T-06-13 | Tampering | data/projects.ts archived-braehods href change lands AFTER the DNS flip (timing inversion → self-loop card) | low | mitigate | Task 1 lands + pushes BEFORE any DNS task (D-16). Commit order is verifiable: Task 1 commit precedes the Task 4 flip. Worst case is a cosmetic self-loop card, not a security issue. |
| T-06-14 | Repudiation | External bio updates (LNCH-03) are user-driven; no in-repo audit trail | low | accept | Task 11 records draft copy + per-platform timestamps in 06-03-SUMMARY.md as the best-available audit trail. External edits cannot be enforced programmatically. |
| T-06-15 | Elevation of privilege / irreversible action | Archive (Task 10) or bio updates (Task 11) run before launch is confirmed healthy | low | mitigate | D-19: archive + bios are gated behind the Task 9 GO. A NO-GO performs the Task 2 rollback and explicitly skips Tasks 10-12, so a failed launch undoes only the single DNS flip. |
| T-06-16 | Denial of service | NO-GO rollback fails because the prior records were not captured | low | mitigate | Task 2 captures the verbatim GH Pages records BEFORE any edit (D-19). The rollback in Task 9 restores that exact snapshot; `dig` re-confirms the GH Pages IPs. |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | Plan 06-03 installs ZERO new packages. The only source change is a single-line href value edit in data/projects.ts; no dependency surface. |
</threat_model>

<verification>
Plan 06-03 exit gate (= Phase 6 exit gate = v1 ship gate) — the D-14 PRAGMATIC gate (a)-(d) plus the D-19 go/no-go + post-GO steps:
1. `data/projects.ts` archived href = `https://github.com/bwaeden/braehods`, committed + pushed to main BEFORE the DNS flip (Task 1, D-16).
2. Existing Namecheap records captured before any edit (Task 2, D-19 rollback contract).
3. braehods.com + www added to Vercel with SSL `Valid` before the flip (Task 3, D-13 / LNCH-01).
4. Namecheap flipped: apex A `76.76.21.21` + www CNAME `cname.vercel-dns.com`; old GH Pages records removed; `dig` confirms (Task 4, D-13).
5. D-14(a): `https://braehods.com` 200 over HTTPS with new content + all surfaces healthy (Task 5).
6. D-14(b): real prod Formspree email arrives at fakegoat1@gmail.com (Task 6).
7. D-14(c): visual sweep clean on live braehods.com (Task 7).
8. D-14(d): prod Lighthouse Perf ≥95 AND SEO ≥95 (Task 8 — closes deferred SEO + homepage-mobile-perf gates); OG validated on ≥2 platforms (LNCH-04).
9. GO/NO-GO decision recorded; on NO-GO, Namecheap reverted to the Task 2 snapshot (Task 9, D-19).
10. GO-only: `bwaeden/braehods` archived (Task 10, LNCH-02 / D-15).
11. GO-only: GitHub + Instagram bios → braehods.com; YouTube excluded (Task 11, LNCH-03).
12. POST-launch (not a flip blocker): NVDA + VoiceOver iOS SR walk; P0 → v1.0.1 hotfix (Task 12, D-14 revised).
</verification>

<success_criteria>
Plan 06-03 is complete (= Phase 6 complete = v1 SHIPPED) when, on a GO decision:
- `data/projects.ts` archived href = GH archive URL, pushed to main before the flip (Task 1).
- Namecheap records captured pre-flip (Task 2); Vercel SSL Valid before flip (Task 3); Namecheap flipped to apex A 76.76.21.21 + www CNAME, GH Pages records removed (Task 4).
- D-14 pragmatic gate all green on live braehods.com: HTTPS + new content (Task 5), real prod Formspree email (Task 6), clean visual sweep (Task 7), prod Lighthouse Perf+SEO ≥95 + OG validation (Task 8).
- GO recorded at the Task 9 checkpoint (or, on NO-GO, a clean Namecheap rollback to the Task 2 snapshot with Tasks 10-12 skipped).
- GO-only: bwaeden/braehods archived (Task 10); GitHub + Instagram bios → braehods.com, YouTube excluded (Task 11).
- Post-launch SR walk recorded; any P0 routed to a v1.0.1 hotfix (Task 12).
- 06-03-SUMMARY.md + ROADMAP + REQUIREMENTS + STATE.md flipped to the v1-shipped state (Task 13).
- All 16 Phase 4 + Phase 5 carry-forwards closed across Plans 06-01 + 06-02 + 06-03.
- Working tree clean; v1 of braehods.com is LIVE over HTTPS.
</success_criteria>

<output>
Create `.planning/phases/06-polish-seo-launch/06-03-SUMMARY.md` on Task 13. Commit message: `docs(06-03): plan 06-03 SUMMARY + Phase 6 complete (v1 SHIPPED)`.

After this plan completes (on GO), no further phase work remains for v1. Subsequent work (designer monogram, portrait shoot, 301 redirect, /writing, light-mode toggle, /now page, any SR P0 v1.0.1 hotfix) is v1.x or v2 per CONTEXT § Deferred Ideas.
</output>
