---
phase: 06-polish-seo-launch
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - data/projects.ts
  - .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [LNCH-01, LNCH-02, LNCH-03, LNCH-04]

must_haves:
  truths:
    - "`data/projects.ts` archived-braehods entry href is `https://github.com/bwaeden/braehods` (D-16) — committed BEFORE the DNS flip in Wave 1."
    - "`braehods.com` + `www.braehods.com` are added to the Vercel `braeden-site` project Domains and show SSL `Valid` status BEFORE the CNAME flip."
    - "Registrar CNAME for `braehods.com` is flipped to `cname.vercel-dns.com` (or apex A `76.76.21.21`); `dig braehods.com +short` shows Vercel IPs."
    - "`https://braehods.com` returns HTTP 200 with valid SSL cert and serves the new site (not the old GH Pages content)."
    - "Smoke walk on `https://braehods.com` clicks Nav (Home, About, Work, Contact); each route loads; ContactModal opens; ESC closes; mailto fallback visible."
    - "Real Formspree submission from `https://braehods.com` (NOT preview URL) produces a real email at `fakegoat1@gmail.com` within 2 minutes."
    - "iPhone Safari + Android Chrome real-device walk on `https://braehods.com` confirms no horizontal scroll, all CTAs work, modal opens/closes cleanly, no iOS input auto-zoom (Pitfall 7), no dvh viewport jump (Pitfall 8)."
    - "OG validation: paste `https://braehods.com` URL in iMessage + Slack + LinkedIn + X — preview renders correct 1200×630 OG with site typography (≥2 platforms minimum per LNCH-04)."
    - "Old `bwaeden/braehods` GitHub Pages repo is archived (`gh repo archive` exit 0 OR GitHub UI archive flag set)."
    - "GitHub profile site link + Instagram bio link + YouTube About link all point at `https://braehods.com` (user-driven external edits)."
  artifacts:
    - path: ".planning/phases/06-polish-seo-launch/06-03-SUMMARY.md"
      provides: "Plan 06-03 close: DNS flip timestamps, prod URL smoke walk, real-device outcomes, real-email timestamp, OG validation matrix, GH archive confirmation, bio update timestamps, Phase 6 close consolidation"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 row checked complete; Progress Table updated; v1 shipped marker"
  key_links:
    - from: "data/projects.ts archived braehods entry"
      to: "https://github.com/bwaeden/braehods (archived GH repo)"
      via: "single-line href edit per D-16"
      pattern: "github\\.com/bwaeden/braehods"
    - from: "braehods.com DNS"
      to: "Vercel braeden-site project deployment"
      via: "CNAME cname.vercel-dns.com OR apex A 76.76.21.21"
      pattern: "vercel-dns|76\\.76\\.21\\.21"
    - from: "https://braehods.com ContactModal submission"
      to: "fakegoat1@gmail.com"
      via: "Formspree endpoint xqeypnkw + NEXT_PUBLIC_FORMSPREE_ID env var (Production scope)"
      pattern: "real-email-on-prod"
---

<objective>
Ship v1 of braehods.com to the world. Execute the DNS swap in the correct order (per CONTEXT D-13: Vercel domain add FIRST → SSL stages → THEN CNAME flip), confirm production URL serves the new site, run the full launch checklist (real-device + real-email + OG validation + GH archive + bio updates), and consolidate the Phase 6 close.

Purpose: Close the last 4 launch requirements (LNCH-01..04) and ship the personal site. After this plan, braehods.com serves the new site over HTTPS, the old GH Pages repo is archived, and Braeden's external bios link to the new URL.

Output: braehods.com live with new site over HTTPS; old `bwaeden/braehods` repo archived; GH/IG/YT bios updated; 06-03-SUMMARY.md consolidating Phase 6 close; ROADMAP/REQUIREMENTS/STATE.md flipped to v1-shipped state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-seo-launch/06-CONTEXT.md
@.planning/phases/06-polish-seo-launch/06-RESEARCH.md
@.planning/phases/06-polish-seo-launch/06-VALIDATION.md
@.planning/phases/06-polish-seo-launch/06-01-SUMMARY.md
@.planning/phases/06-polish-seo-launch/06-02-SUMMARY.md
@.planning/phases/05-contact-modal/05-03-SUMMARY.md
@data/projects.ts
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. -->

From data/projects.ts (current state):
- The archived entry at line ~107 has `href: 'https://braehods.com'` — this MUST change to `'https://github.com/bwaeden/braehods'` BEFORE the DNS flip (D-16). After DNS flip, the old URL serves the NEW site, so a self-link would render.

From Vercel braeden-site project (verified in Plan 06-01 Task 4 + Plan 06-02 Task 9):
- Project linked to `bwaeden/braeden-site` GitHub repo.
- Production branch: `main`.
- Env vars: `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw` set across Production + Preview + Development (Phase 1 D-13).

From CONTEXT D-13 — DNS flip 6-step sequence:
1. Vercel dashboard → braeden-site project → Domains → add `braehods.com` + `www.braehods.com`.
2. Wait for Vercel-issued SSL via Let's Encrypt → `Valid` status (typically <5 min).
3. Optional: test Vercel-provisioned domain with `curl --resolve` flag or /etc/hosts override.
4. Registrar CNAME: `braehods.com` → `cname.vercel-dns.com` (subdomain) OR apex A → `76.76.21.21`.
5. Verify propagation: `dig braehods.com +short` shows Vercel IPs.
6. `curl -sI https://braehods.com` → HTTP 200 + new site content.

From CONTEXT D-14 — production deploy gate:
- All of: (a) specs GREEN both projects on preview (closed in Plan 06-01 + 06-02), (b) Lighthouse ≥95 on 6 audits (closed in Plan 06-02), (c) 40-item visual checklist clean (closed in Plan 06-01 + 06-02), (d) real Formspree email arrives at fakegoat1@gmail.com (closed in Plan 06-01 on preview; this Plan re-verifies on PROD), (e) bundle ≤50KB gz (closed in Plan 06-02).

From CONTEXT D-15:
- Old `bwaeden/braehods` repo handling = ARCHIVE ONLY, NO 301 redirect. `gh repo archive bwaeden/braehods` (CLI) OR GitHub UI Settings → Archive this repository.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1 (W0): Single-line edit data/projects.ts archived-braehods href (D-16) BEFORE DNS flip</name>
  <files>data/projects.ts</files>
  <read_first>
    - data/projects.ts (locate the "archived braehods.com (v0)" entry; verify current href is `https://braehods.com`)
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-16
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "`data/projects.ts` (D-16 single-line edit)"
    - tests/work-grid-renders.spec.ts (regression canary — must still pass after the href change since the spec asserts presence of the archived entry, not a specific URL)
  </read_first>
  <behavior>
    - Test 1: `grep -E "github\\.com/bwaeden/braehods" data/projects.ts` returns 1 match (the archived entry's href is now the GH archive URL).
    - Test 2: `grep -cE "href: 'https://braehods\\.com'," data/projects.ts` returns 0 (the old self-loop URL is gone).
    - Test 3: `npm run test -- tests/work-grid-renders.spec.ts` GREEN locally (no regression — the spec asserts entry count + status, not href text).
    - Test 4: Full Playwright suite locally GREEN.
  </behavior>
  <action>
    Single-line edit per CONTEXT D-16 + PATTERNS.md. MUST land BEFORE the DNS flip in Wave 1 — otherwise braehods.com will briefly serve a self-loop link in the /work grid for the archived card.

    Steps:
    1. EDIT `data/projects.ts`: locate the archived entry (search for `archived braehods.com (v0)` or `archived` status). Change `href: 'https://braehods.com',` → `href: 'https://github.com/bwaeden/braehods',`.
    2. (Optional, per PATTERNS.md): update the file's leading comment block (lines 33-36 area) to record that Phase 6 D-16 selected option (a) "link to archived GitHub repo".
    3. Verify locally:
       - `npm run typecheck && npm run lint && npm run build`
       - `npm run test:full -- tests/work-grid-renders.spec.ts tests/work-status-badges.spec.ts tests/work-no-flagship.spec.ts tests/work-descriptions-cliche-scrub.spec.ts` — all 4 GREEN.
    4. Commit: `git commit -m "feat(06-03): D-16 archived braehods href → github archive URL (pre-DNS-flip)"`.
    5. Push to `main` so the preview re-builds with the new href before the DNS flip. (Optional: wait for the Vercel preview redeploy if you want to verify the link on preview first; otherwise the prod deploy from the DNS flip will pick it up.)

    Per D-16 timing: this commit MUST land before Task 2 (Vercel domain add) so the preview AND any subsequent production deploy already has the corrected href. Do not batch this with the DNS flip.
  </action>
  <verify>
    <automated>grep -c "github.com/bwaeden/braehods" data/projects.ts && grep -c "href: 'https://braehods.com'," data/projects.ts && npm run test -- tests/work-grid-renders.spec.ts tests/work-status-badges.spec.ts</automated>
    Expected: first grep ≥1, second grep 0, test suite GREEN.
  </verify>
  <acceptance_criteria>
    - `data/projects.ts` contains exactly 1 occurrence of `github.com/bwaeden/braehods` (the new href).
    - `data/projects.ts` contains 0 occurrences of `href: 'https://braehods.com',` (old self-loop URL removed).
    - All 4 work-grid Playwright specs GREEN locally.
    - Single commit lands the change.
    - Commit pushed to `main`; Vercel preview re-build triggered.
  </acceptance_criteria>
  <done>Archived-braehods href updated to the GH archive URL. Site is now safe to DNS-flip without a self-loop link.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (W1): Add braehods.com + www.braehods.com to Vercel project Domains (D-13 Step 1+2)</name>
  <files>(no files — Vercel dashboard external)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-13 (Step 1+2: add domain → SSL stages)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-03-02
    - .planning/phases/01-foundation-design-tokens/01-CONTEXT.md § D-13 (env vars Production scope confirmed)
  </read_first>
  <what-built>
    Vercel automatically issues SSL certificates via Let's Encrypt when a domain is added to a project — but ONLY if (a) the domain is configured in Vercel BEFORE the CNAME flip, OR (b) the verification challenge can be served from the existing DNS. The CONTEXT D-13 ordering avoids the SSL-cert-not-ready failure mode (which would briefly serve HTTP-only traffic).
  </what-built>
  <how-to-verify>
    1. Open https://vercel.com/dashboard.
    2. Navigate to the `braeden-site` project.
    3. Click `Settings` → `Domains`.
    4. Click `Add Domain`.
    5. Enter `braehods.com` → click `Add`.
    6. Vercel will prompt for DNS configuration (it will show a CNAME or A record). DO NOT update DNS at the registrar YET — that's Task 3.
    7. Repeat for `www.braehods.com` (Vercel will recommend a redirect-to-apex setup; accept the default).
    8. Wait for the SSL "Valid" status indicator next to each domain (usually <5 minutes; Vercel polls the DNS challenge then issues the cert).
       - Note: the SSL provisioning is what stages the cert; the CNAME flip in Task 3 routes traffic to the already-provisioned cert.
    9. (Optional sanity check) Test the Vercel-provisioned domain with curl override BEFORE the DNS flip:
       - `curl --resolve braehods.com:443:76.76.21.21 -sI https://braehods.com`
       - Expect HTTP 200 + new site content (because the Host header matches what Vercel has provisioned).
       - This proves the Vercel side is ready; the registrar side just needs the routing flip.
    10. Capture timestamps for "Domain added" + "SSL Valid" in 06-03-SUMMARY.md § DNS Timeline.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "domains-added: ssl-valid <timestamp>" OR "ssl-pending: <reason>" (DO NOT proceed to Task 3 until SSL is Valid).</resume-signal>
  <acceptance_criteria>
    - `braehods.com` shows in Vercel Domains list with SSL status `Valid`.
    - `www.braehods.com` shows in Vercel Domains list with SSL status `Valid`.
    - Optional curl --resolve test confirms Vercel serves the new site for the Host header.
    - Timestamps recorded in 06-03-SUMMARY.md.
  </acceptance_criteria>
  <done>Vercel side ready: domains configured + SSL staged. Safe to flip CNAME at registrar.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (W1): Flip registrar CNAME → cname.vercel-dns.com (D-13 Step 4)</name>
  <files>(no files — registrar UI external)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-13 (Step 4: registrar CNAME flip)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-03-03
  </read_first>
  <what-built>
    Task 2 staged the SSL cert at Vercel. This task is the actual "go live" moment: updating the registrar's DNS so the world starts resolving braehods.com to Vercel instead of the old GitHub Pages host.
  </what-built>
  <how-to-verify>
    1. Log in to the registrar that holds `braehods.com` (user knows which one — common: Namecheap, GoDaddy, Google Domains, Cloudflare). DO NOT auto-detect this; user-driven.
    2. Find the DNS settings panel for `braehods.com`.
    3. Locate the existing CNAME for `braehods.com` (currently points at GitHub Pages — likely `bwaeden.github.io` or similar).
    4. **Apex (root) record:** If the registrar supports apex CNAMEs (Cloudflare does; most don't):
       - Set CNAME `@` → `cname.vercel-dns.com`.
       Otherwise (most registrars require A record at apex):
       - Set A record `@` → `76.76.21.21` (Vercel's apex IP).
    5. **www subdomain:** Set CNAME `www` → `cname.vercel-dns.com`.
    6. SAVE the registrar changes. Note the timestamp.
    7. Wait for DNS propagation (typically 5-60 min; some TTLs are longer). Verify with multiple checks:
       - `dig braehods.com +short` → expect Vercel IPs (76.76.21.21 OR a Vercel CDN edge IP)
       - `dig www.braehods.com +short` → expect cname.vercel-dns.com OR Vercel CDN IPs
       - https://www.whatsmydns.net/#A/braehods.com — verify propagation across global resolvers
    8. Capture timestamps for "Registrar CNAME flipped" + "DNS propagated" in 06-03-SUMMARY.md.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "cname-flipped: <timestamp>; dig-result: <ip-or-cname>" OR "dns-pending: <details>".</resume-signal>
  <acceptance_criteria>
    - Registrar CNAME (or apex A record) updated to Vercel's IP/CNAME target.
    - `dig braehods.com +short` shows Vercel-controlled IPs (NOT the old GH Pages IP).
    - `dig www.braehods.com +short` shows Vercel-controlled IPs OR cname.vercel-dns.com.
    - Propagation confirmed via at least 2 external DNS resolvers (whatsmydns.net helps).
    - Timestamps recorded in 06-03-SUMMARY.md.
  </acceptance_criteria>
  <done>DNS flipped. braehods.com now resolves to Vercel. Production deploy live.</done>
</task>

<task type="auto">
  <name>Task 4 (W1): Verify https://braehods.com smoke walk + SSL + new content</name>
  <files>(no files — verification only)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-03-04
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-13 (Step 6: smoke test)
    - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md (Plan 06-01 preview smoke baseline)
  </read_first>
  <behavior>
    - Test 1: `curl -sI https://braehods.com` returns `HTTP/2 200` (or `HTTP/1.1 200`).
    - Test 2: HTTPS cert is valid (no curl `-k` flag needed; no TLS error).
    - Test 3: `curl -s https://braehods.com | grep -i "Braeden"` confirms the homepage body contains "Braeden" (proves the new site, NOT the old GH Pages site).
    - Test 4: `curl -sI https://braehods.com/sitemap.xml` returns 200 with content-type XML.
    - Test 5: `curl -sI https://braehods.com/robots.txt` returns 200.
    - Test 6: `curl -sI https://braehods.com/opengraph-image.png` returns 200 with content-type image/png.
    - Test 7: `curl -sI https://braehods.com/about` + `/work` both return 200.
    - Test 8: `curl -sI https://braehods.com/this-route-does-not-exist` returns 404 with the Page-not-found body.
    - Test 9 (preview-noindex sanity): `curl -sI https://braehods.com/` → expect NO `X-Robots-Tag: noindex` header (production-mode, NOT preview).
  </behavior>
  <action>
    Run a series of curl smoke tests against the live production URL to verify all Phase 6 surfaces are serving correctly.

    Steps:
    1. `curl -sI https://braehods.com/` — capture status + headers. Expect 200.
    2. `curl -s https://braehods.com/ | grep -Eo "Braeden|Hero|Currently" | head -3` — confirm new site content.
    3. `curl -sI https://braehods.com/sitemap.xml` — expect 200 + content-type XML.
    4. `curl -sI https://braehods.com/robots.txt` — expect 200.
    5. `curl -s https://braehods.com/robots.txt | grep -E "Sitemap|User-Agent"` — confirm robots.txt body shape.
    6. `curl -sI https://braehods.com/opengraph-image.png` — expect 200 + image/png.
    7. `curl -sI https://braehods.com/about/opengraph-image` — expect 200 + image/png (dynamic OG).
    8. `curl -sI https://braehods.com/work/opengraph-image` — expect 200 + image/png.
    9. `curl -sI https://braehods.com/about` + `curl -sI https://braehods.com/work` — both 200.
    10. `curl -sI https://braehods.com/this-route-does-not-exist` — expect 404.
    11. `curl -sI https://braehods.com/` | grep -i "x-robots-tag" — expect EMPTY (no noindex header on production; only preview gets noindex via proxy.ts).
    12. Run preview-noindex spec against production URL (it should SKIP because the URL does NOT contain `vercel.app`):
        `PLAYWRIGHT_BASE_URL=https://braehods.com npm run test -- tests/preview-noindex.spec.ts`
        Expected: spec skipped (good — proves the prod URL is not noindex'd, and the spec gate works correctly).
    13. Run full Playwright suite against production URL:
        `PLAYWRIGHT_BASE_URL=https://braehods.com npm run test:full`
        Expected: all specs GREEN (modulo any acceptable carry-forwards documented in Plans 06-01 + 06-02 SUMMARYs).
    14. Document curl outcomes + production-URL Playwright run result in 06-03-SUMMARY.md.
    15. If any curl fails: stop immediately, identify the failure mode (DNS not propagated → wait; SSL cert hiccup → re-check Vercel; build failure → check Vercel build logs; missing OG image → re-verify Plan 06-02 commits made it to prod), document, and resolve before Task 5.
  </action>
  <verify>
    <automated>curl -sI https://braehods.com/ | head -1 && curl -sI https://braehods.com/sitemap.xml | head -1 && curl -sI https://braehods.com/robots.txt | head -1 && curl -sI https://braehods.com/opengraph-image.png | head -1 && curl -sI https://braehods.com/this-route-does-not-exist | head -1</automated>
    Expected: 5 status lines: 200, 200, 200, 200, 404.
  </verify>
  <acceptance_criteria>
    - All 8 curl checks (Tests 1-8 in Behavior) pass.
    - `X-Robots-Tag` is NOT present on production responses (Test 9 confirms preview-noindex doesn't leak to prod).
    - `preview-noindex.spec.ts` correctly SKIPs against prod URL.
    - Full Playwright suite against prod URL exits 0 (or with documented carry-forwards).
    - All curl outputs documented in 06-03-SUMMARY.md.
  </acceptance_criteria>
  <done>Production URL serves the new site correctly across all Phase 6 surfaces. DNS + SSL + build all healthy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (W1): Real-device test on iPhone Safari + Android Chrome (LNCH-04 + Phase 5 carry-fwd #9)</name>
  <files>(no files — physical-device test)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-03-05
    - .planning/phases/05-contact-modal/05-03-SUMMARY.md § Phase 6 Carry-Forwards item #9 (Pitfall 7 input auto-zoom + Pitfall 8 dvh viewport)
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § "Plan 06-03 launch gate exit criteria" item 3
  </read_first>
  <what-built>
    Plan 06-02 verified VoiceOver iOS on preview URL. This task re-verifies the prod URL on TWO physical devices: iPhone Safari (the most likely social-share target — recruiters/investors on iPhone) and Android Chrome (the largest mobile market share). Real-device testing surfaces issues no emulator catches: input auto-zoom (iOS Safari when font-size <16px), dvh viewport visual jump, touch-target size, scroll inertia.
  </what-built>
  <how-to-verify>
    1. **iPhone Safari:**
       - Open Safari on a physical iPhone (any iOS 16+ device).
       - Navigate to `https://braehods.com`.
       - Verify: page loads, hero photo + name + Currently visible above the fold.
       - Tap Nav `About` — `/about` loads cleanly.
       - Tap Nav `Work` — `/work` loads cleanly; all 7 cards visible in scroll.
       - Tap Nav `Contact` — ContactModal opens (verify dialog renders centered, NO dvh viewport jump per Phase 5 Pitfall 8).
       - Tap Message textarea. Verify: iOS does NOT auto-zoom (the input's effective font-size is ≥16px per Phase 5 Pitfall 7).
       - Tap ESC area / outside dialog OR the close button (X) — modal closes.
       - Tap the inline `/about` `Get in touch` CTA. Verify: ContactModal re-opens.
       - Submit a real form (Name: `Phase 6 iOS Safari`, Email: `fakegoat1@gmail.com`, Message: `Real iPhone Safari test on prod URL. <timestamp>.`). Wait ≥1500ms. Tap Send.
       - Verify success state renders cleanly.
       - Confirm email arrives at `fakegoat1@gmail.com` within 2 min.
       - Navigate to `/this-route-does-not-exist` — 404 page renders with chrome + monogram + heading + return link.
       - Tap `← Back home` — returns to `/`.
       - Verify NO horizontal scroll on any page (smallest device width ~320-414px).
    2. **Android Chrome:**
       - Open Chrome on a physical Android device.
       - Same walk as iOS Safari steps above. Note: Android does NOT have the iOS Pitfall 7 + 8 concerns; this test is more about touch-target + scroll + Chrome rendering parity.
       - Submit a 2nd real form (Name: `Phase 6 Android Chrome`, Message: `Real Android Chrome test on prod URL. <timestamp>.`). Confirm email arrives.
    3. Document outcomes in 06-03-SUMMARY.md § Real-Device Test Matrix:
       - Per device: pass/fail per route + ContactModal behavior + form submit outcome + horizontal scroll observation.
    4. Any P0 failure (broken layout, modal can't close, form submit fails, auto-zoom triggers): document, cycle back to inline source fix, push, wait for Vercel redeploy, re-test. DO NOT proceed to Task 6 until clean.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "real-device-clean: ios=ok, android=ok" OR "real-device-failures: <details>".</resume-signal>
  <acceptance_criteria>
    - iPhone Safari walk-through completes cleanly on all routes + ContactModal.
    - Android Chrome walk-through completes cleanly on all routes + ContactModal.
    - No iOS input auto-zoom on ContactModal Message textarea.
    - No dvh viewport visual jump on modal open.
    - Real form submits from BOTH devices produce emails at fakegoat1@gmail.com.
    - No horizontal scroll on any device width.
    - Outcomes documented in 06-03-SUMMARY.md.
  </acceptance_criteria>
  <done>Real-device matrix clean. LNCH-04 real-device requirement + Phase 5 carry-fwd #9 closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (W1): OG validation on prod URL in iMessage + Slack + LinkedIn + X (LNCH-04)</name>
  <files>(no files — external platform tests)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-03-07
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § "Plan 06-03 launch gate exit criteria" (OG renders correctly in ≥2 platforms)
    - .planning/phases/06-polish-seo-launch/06-UI-SPEC.md § "Static `/` OG Image" + § "Dynamic /about + /work OG Images"
  </read_first>
  <what-built>
    Phase 6 shipped static `/` OG (1200×630 PNG) + dynamic `/about` + `/work` OG via ImageResponse. Plan 06-02 confirmed Slack + iMessage renders on PREVIEW URL. This task re-validates on PRODUCTION URL across ≥2 platforms (LNCH-04 minimum; 4 platforms = full coverage).
  </what-built>
  <how-to-verify>
    1. **Slack:** Open a Slack workspace (your own DMs to yourself work). Paste `https://braehods.com` into a chat. Verify unfurl preview shows:
       - 1200×630 thumbnail with the static `/` OG (Braeden wordmark + monogram on charcoal).
       - Title: `Braeden Hodson` (or whatever was set in `app/layout.tsx` metadata).
       - Description: the meta description from page metadata.
       - URL: `braehods.com`.
       Screenshot the unfurl. Save to `.planning/phases/06-polish-seo-launch/og-validation/slack-home.png`.
    2. Paste `https://braehods.com/about`. Verify unfurl shows the dynamic ImageResponse with "About" title in Fraunces 700 on charcoal.
    3. Paste `https://braehods.com/work`. Verify "Work" title.
    4. **iMessage:** Send `https://braehods.com` to yourself (Send to Self contact). Verify rich preview shows the static OG. Screenshot.
    5. **LinkedIn (optional):** Use LinkedIn's Post Inspector at https://www.linkedin.com/post-inspector/ — paste `https://braehods.com`. Verify OG image + title.
    6. **X (Twitter, optional):** Use https://cards-dev.twitter.com/validator (if still available) OR draft a tweet with `https://braehods.com` and check the card preview. Verify summary_large_image card.
    7. If any platform shows the WRONG thumbnail (e.g., a Vercel logo or a previous-cache hit): force re-fetch via that platform's debugger:
       - Facebook: https://developers.facebook.com/tools/debug/
       - Twitter: https://cards-dev.twitter.com/validator
       - LinkedIn: Post Inspector
       - Slack: simply paste again after ~5 min; Slack respects cache-control headers.
    8. Document platform-by-platform outcome in 06-03-SUMMARY.md § OG Validation Matrix.
    9. Acceptance: ≥2 platforms render correctly per LNCH-04. 4 = excellent.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "og-validated: <platforms-list>" e.g., `og-validated: slack, imessage, linkedin` OR "og-failed: <details>".</resume-signal>
  <acceptance_criteria>
    - At least 2 platforms (Slack + iMessage minimum) render correct 1200×630 OG with site typography.
    - Static `/` OG and dynamic `/about` + `/work` OG all confirmed in at least one platform.
    - Screenshots captured + paths documented in 06-03-SUMMARY.md.
  </acceptance_criteria>
  <done>OG validation across social-share platforms confirms branded-share aesthetic ships correctly.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7 (W1): Archive `bwaeden/braehods` GitHub Pages repo (LNCH-02 + D-15)</name>
  <files>(no files — GitHub CLI/UI external)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-15 (archive-only, NO 301 redirect)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-03-08
  </read_first>
  <what-built>
    The old `bwaeden/braehods` GitHub Pages repo served the previous site (Geist-font minimal page). Now that braehods.com points at Vercel, that GH Pages repo is dead — but the GitHub repo itself still exists with potentially-confusing READMEs / issues / commits. Per CONTEXT D-15: archive (read-only) only; do NOT serve a 301 redirect (yak-shave for v1; defer to v1.x).
  </what-built>
  <how-to-verify>
    1. Prefer GitHub CLI:
       - `gh repo archive bwaeden/braehods` (the `--yes` flag may be needed to skip the confirmation prompt; check with `gh repo archive --help`).
       - Expected output: success message indicating the repo is now archived.
       - If `gh` CLI is not installed or not authenticated, fall back to GitHub UI.
    2. Fallback GitHub UI:
       - Visit https://github.com/bwaeden/braehods/settings.
       - Scroll to "Danger Zone" → "Archive this repository".
       - Click "Archive this repository". Confirm in the popup.
    3. Verify the archive:
       - Visit https://github.com/bwaeden/braehods.
       - The repo page should show a banner "This repository has been archived by the owner on <date>. It is now read-only."
       - Issues should still be browsable but locked from new comments.
       - The repo can be UN-archived if needed (Settings → Unarchive); this is reversible.
    4. Confirm GH Pages auto-redirect implications:
       - GH Pages will STOP serving the old site immediately when DNS no longer points at GitHub (already done in Task 3). The archive is independent — even an unarchived repo would not serve GH Pages once the CNAME flipped.
       - Anyone visiting `bwaeden.github.io/braehods` (the GH-Pages subdomain, NOT `braehods.com`) will still see content there until GH Pages is explicitly disabled — OR they'll get a 404 if the build settings break. Per D-15, this is acceptable — the GH-Pages subdomain isn't widely shared.
    5. Document the archive timestamp + method (CLI vs UI) in 06-03-SUMMARY.md.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "archived: <timestamp> via <gh-cli|ui>" OR "archive-failed: <reason>".</resume-signal>
  <acceptance_criteria>
    - `https://github.com/bwaeden/braehods` page shows the archived banner.
    - `gh repo view bwaeden/braehods --json archived` returns `{"archived": true}` (if gh CLI is installed).
    - Archive timestamp documented in 06-03-SUMMARY.md.
  </acceptance_criteria>
  <done>Old repo archived per D-15. LNCH-02 closed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8 (W1): Update bios — GitHub profile + Instagram bio + YouTube About (LNCH-03)</name>
  <files>(no files — user-driven external edits; planner does NOT auto-edit)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § Claude's Discretion (LNCH-03 = user-driven, planner does NOT auto-edit external services)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-03-09
  </read_first>
  <what-built>
    The user owns their GitHub / Instagram / YouTube profiles. Phase 6 cannot programmatically edit these. Per CONTEXT § Claude's Discretion: planner does NOT auto-edit external services; user-driven action only.
  </what-built>
  <how-to-verify>
    1. **GitHub profile** (https://github.com/<bwaeden>/settings/profile):
       - Update the "Website" / "Profile URL" field to `https://braehods.com`.
       - Save.
       - Verify by visiting https://github.com/bwaeden (profile page) — site link should resolve to braehods.com.
    2. **Instagram bio** (Instagram app → Profile → Edit Profile):
       - Update the "Website" field to `https://braehods.com`.
       - Save.
       - Verify by viewing the profile (logged-out browser view if possible).
    3. **YouTube channel About** (YouTube Studio → Customization → Basic info OR channel About tab):
       - Update the channel description / website link to `https://braehods.com`.
       - Save.
       - Verify by viewing the channel About page.
    4. **LinkedIn / X (Twitter) / etc.** (if applicable — these are out of scope per LNCH-03 but optional):
       - Update profile website links if you have these.
    5. Document timestamps (which profile, when updated) in 06-03-SUMMARY.md § Bio Updates.
    6. Note: this task does NOT auto-detect bios that need updating; it's a checklist the user walks.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "bios-updated: <list of platforms updated>" e.g., `bios-updated: github, instagram, youtube` OR "bios-deferred: <reason>".</resume-signal>
  <acceptance_criteria>
    - GitHub profile "Website" field → `https://braehods.com`.
    - Instagram bio "Website" → `https://braehods.com`.
    - YouTube channel About → contains `https://braehods.com` link.
    - Timestamps documented in 06-03-SUMMARY.md.
    - Acceptable to defer 1-2 of the 3 if the user does NOT actively maintain that platform (e.g., no public YT channel) — document deferrals.
  </acceptance_criteria>
  <done>External bio links updated to braehods.com. LNCH-03 closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9 (W2): Real Formspree submit from PRODUCTION URL (CTCT-02 prod gate)</name>
  <files>(no files — manual external test on prod)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-03-06
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-14e (Formspree real-email gate)
    - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md (Plan 06-01 verified Formspree on PREVIEW; this task re-verifies on PROD)
  </read_first>
  <what-built>
    Plan 06-01 Task 6 verified the Formspree path on a preview URL. CONTEXT D-14e is the strictest gate — re-verify on the LIVE production URL too, because env vars are scoped per-environment (preview vs production) and a misconfigured production env var would silently break form submissions in front of real visitors.
  </what-built>
  <how-to-verify>
    1. Open `https://braehods.com` on a desktop browser (NOT preview URL; NOT real-device — separate from Task 5's mobile tests).
    2. Click Nav `Contact`. Verify ContactModal opens.
    3. Wait ≥2 seconds.
    4. Fill the form:
       - Name: `Phase 6 PROD verify`
       - Email: `fakegoat1@gmail.com`
       - Message: `Phase 6 Plan 06-03 PROD URL verification. Timestamp: <ISO datetime>.`
       - Honeypot `company`: leave EMPTY.
    5. Click Send.
    6. Verify success UI: `Thanks — I'll get back to you within a day or two.`
    7. Wait ≤2 minutes. Check `fakegoat1@gmail.com` inbox.
    8. Confirm email arrives with the timestamp marker.
    9. Document timestamp sent + received in 06-03-SUMMARY.md § Production Formspree Verify.
    10. If email does NOT arrive within 2 minutes: check Vercel dashboard env vars for Production scope — confirm `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw` is set. If env var is missing/wrong, this is a deploy gate blocker — fix in Vercel + redeploy + re-test.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "prod-email-received: <timestamp>" OR "prod-email-failed: <details>".</resume-signal>
  <acceptance_criteria>
    - Email arrives at fakegoat1@gmail.com within 2 minutes of submit on PROD URL.
    - Email body contains the timestamp marker.
    - Outcome documented in 06-03-SUMMARY.md.
  </acceptance_criteria>
  <done>Production Formspree path verified end-to-end. D-14e production deploy gate closed.</done>
</task>

<task type="auto">
  <name>Task 10 (W2): Write 06-03-SUMMARY.md + final Phase 6 close (ROADMAP/REQUIREMENTS/STATE)</name>
  <files>.planning/phases/06-polish-seo-launch/06-03-SUMMARY.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md
    - .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md
    - .planning/ROADMAP.md (current Phase 6 row)
    - .planning/REQUIREMENTS.md (locate LNCH-01..04 rows)
    - .planning/STATE.md (locate progress.* fields)
  </read_first>
  <action>
    Plan 06-03 = v1 ship gate. This task writes the Plan 06-03 SUMMARY, flips final traceability, and marks Phase 6 (and the v1 release) complete.

    Steps:
    1. CREATE `.planning/phases/06-polish-seo-launch/06-03-SUMMARY.md` per SUMMARY template. Required content:
       - Frontmatter: phase, plan (06-03), status (`complete`), subsystem (launch / dns / prod-verify), tags ([dns-flip, prod-formspree, real-device, og-validation, gh-archive, bio-updates, v1-shipped]), dependency graph (requires Plans 06-01 + 06-02 SUMMARYs; provides braehods.com live with new site), tech tracking, key-files (modified: data/projects.ts D-16 href edit; ROADMAP + REQUIREMENTS + STATE traceability flips), key-decisions (DNS sequence executed per D-13; old repo archived per D-15; bio updates per LNCH-03 user-driven), metrics (DNS propagation duration; total Phase 6 duration; commit count).
       - **Body sections (required):**
         - **DNS Timeline:** Domain-added timestamp, SSL-valid timestamp, CNAME-flip timestamp, DNS-propagated timestamp.
         - **Production Smoke Test:** Curl outputs from Task 4 (all 8 checks).
         - **Real-Device Matrix:** iOS Safari + Android Chrome outcomes from Task 5.
         - **OG Validation Matrix:** Platform-by-platform from Task 6.
         - **GH Archive:** Timestamp + method from Task 7.
         - **Bio Updates:** Per-platform timestamps from Task 8.
         - **Production Formspree Verify:** Submit + received timestamps from Task 9.
         - **Anti-Patterns Avoided:** No 301 redirect (D-15); no `meta refresh` on archived repo; data/projects.ts href landed BEFORE DNS flip (D-16 timing); no bio auto-edits (user-driven only).
         - **Phase 6 Complete:** v1 of braehods.com shipped on <date>.
    2. UPDATE `.planning/ROADMAP.md`:
       - Phase 6 row: `[ ]` → `[x]`. Append " — Shipped 2026-05-XX. braehods.com live; old bwaeden/braehods archived; bios updated."
       - Progress Table row Phase 6: `0/0 Not started` → `3/3 Complete | 2026-05-XX`.
       - Plans list under Phase 6: flip all 3 to `[x]`.
    3. UPDATE `.planning/REQUIREMENTS.md`:
       - LNCH-01 → `Complete (braehods.com resolves to Vercel + SSL valid; CNAME flipped <timestamp>)`.
       - LNCH-02 → `Complete (bwaeden/braehods archived <timestamp>)`.
       - LNCH-03 → `Complete (GH/IG/YT bios updated)` (or partial deferrals documented per Task 8).
       - LNCH-04 → `Complete (real-device + real-email + OG validation on prod)`.
    4. UPDATE `.planning/STATE.md`:
       - `progress.completed_phases: 5 → 6` (Phase 6 done).
       - `progress.completed_plans: <N> → N+1` (Plan 06-03 done).
       - `progress.percent` → 100 (or appropriate end value).
       - Current focus → "v1 SHIPPED — braehods.com live"; Next Session → "v1.x triage (monogram refresh, portrait swap, etc.)".
       - Last Session entry: detailed Plan 06-03 close + Phase 6 shipped marker.
    5. Commit: `gsd-sdk query commit "docs(06-03): plan 06-03 SUMMARY + Phase 6 complete (v1 SHIPPED)" --files .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`
    6. Push to main.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-polish-seo-launch/06-03-SUMMARY.md && grep -c "\\[x\\] \\*\\*Phase 6" .planning/ROADMAP.md && grep -c "v1 SHIPPED" .planning/STATE.md</automated>
    Expected: SUMMARY exists; ROADMAP Phase 6 marked complete; STATE.md acknowledges v1 ship.
  </verify>
  <acceptance_criteria>
    - 06-03-SUMMARY.md exists with full frontmatter + 8 body sections covering all Wave 1 + Wave 2 task outcomes.
    - ROADMAP Phase 6 row checked + ship date + completion note.
    - REQUIREMENTS LNCH-01..04 all Complete.
    - STATE.md acknowledges v1 ship; Current focus updated to v1.x triage.
    - Single docs commit lands all 4 files + pushed to main.
  </acceptance_criteria>
  <done>Phase 6 closed. v1 of braehods.com SHIPPED. All carry-forwards (16 from Phases 4 + 5) closed. Ready for v1.x triage (monogram refresh, portrait swap, etc. per Deferred Ideas).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public DNS resolvers ↔ registrar DNS record | DNS propagation has a window where some resolvers serve old IPs and some serve new IPs. During this window, users may briefly see the old GH Pages site. SSL cert must be staged BEFORE the flip (Task 2 enforces) to prevent HTTP-only fallback. |
| Vercel SSL cert provisioning ↔ Let's Encrypt | Vercel issues certs via Let's Encrypt automatically. If the cert provisioning hangs or fails, users see TLS errors. Task 2 explicitly waits for "Valid" status before proceeding. |
| GitHub repo archive action ↔ GitHub API | Archiving is idempotent but irreversible-ish (un-archive is possible but the archived banner stays in commit history). Low-risk operation. |
| User profile external services ↔ Phase 6 | Planner cannot edit external services (GitHub profile, Instagram bio, YouTube About). LNCH-03 is user-driven only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Denial of service | DNS flip causes brief HTTP-served period (SSL race) | medium | mitigate | CONTEXT D-13 + Task 2: add domain to Vercel FIRST + wait for SSL Valid status BEFORE flipping registrar CNAME. The cert is staged at Vercel's edge before any traffic routes to it. Task 4 verifies HTTPS post-flip. |
| T-06-11 | Spoofing | Old GH Pages serves briefly post-flip via stale DNS cache | low | accept | DNS TTL is typically short (300-3600s). Users hitting stale caches see the OLD site for minutes-to-hours; this is acceptable — they see Braeden's previous (functional) site, not an attacker's site. The old site does not have a contact form connected to Formspree on the new endpoint, so no data leak. D-15 archive (Task 7) does NOT immediately disable GH Pages — that requires explicit deactivation; acceptable per D-15. |
| T-06-12 | Information disclosure | Production Formspree env var misconfigured → submits fail silently | low | mitigate | Task 9 explicit production-URL real-submit verifies the prod env var is set + working. Pre-staged in Phase 1 D-13 (`NEXT_PUBLIC_FORMSPREE_ID` across all envs). If missing, Task 9 fails → fix Vercel env vars + redeploy + re-test. |
| T-06-13 | Tampering | data/projects.ts archived-braehods href change lands AFTER DNS flip (timing inversion) | low | mitigate | Task 1 explicit pre-DNS-flip sequencing per D-16. Plan-checker can verify via commit order: Task 1 commit landed before Task 3 (registrar CNAME flip). If the timing is wrong, the prod site briefly serves a self-loop link in the /work grid — visual bug but not a security issue. |
| T-06-14 | Repudiation | External bio updates (LNCH-03) are user-driven; no audit trail in repo | low | accept | Task 8 documents timestamps in 06-03-SUMMARY.md as best-available audit trail. Cannot enforce external-service edits programmatically. |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | Plan 06-03 installs ZERO new packages. data/projects.ts edit is a single-line value change; no dependency surface. |
</threat_model>

<verification>
Plan 06-03 exit gate (= Phase 6 exit gate = v1 ship gate; per CONTEXT § "Plan 06-03 launch gate exit criteria" + § D-14):
1. `https://braehods.com` resolves to new Vercel deploy over HTTPS (Task 4)
2. Smoke walk: visit `https://braehods.com`, click through Nav (Home + About + Work + Contact), submit a real Formspree message, verify email arrives (Tasks 4 + 9)
3. Real-device test: iPhone Safari + Android Chrome (Task 5)
4. Old `bwaeden/braehods` repo archived (Task 7)
5. GitHub profile + IG bio + YT About bio links updated (Task 8)
6. `data/projects.ts` archived-braehods href = github URL committed BEFORE the DNS flip (Task 1)
7. OG validation: paste prod URL in iMessage / Slack / LinkedIn / X — preview renders correct OG image in ≥2 platforms (Task 6)
</verification>

<success_criteria>
Plan 06-03 is complete (= Phase 6 complete = v1 SHIPPED) when:
- All 10 tasks done and committed.
- `https://braehods.com` resolves to the Vercel deployment with valid SSL.
- Production-URL Playwright suite GREEN (Task 4).
- iPhone Safari + Android Chrome real-device walks pass (Task 5).
- OG validation on ≥2 platforms confirms branded share thumbnails (Task 6).
- `bwaeden/braehods` GH repo archived (Task 7).
- GitHub + Instagram + YouTube bios updated to `braehods.com` (Task 8).
- Real Formspree submit on prod URL produces real email at fakegoat1@gmail.com (Task 9).
- 06-03-SUMMARY.md + ROADMAP + REQUIREMENTS + STATE.md all flipped to v1-shipped state (Task 10).
- All 16 Phase 4 + Phase 5 carry-forwards closed across Plans 06-01 + 06-02 + 06-03.
- Working tree clean; v1 of braehods.com is LIVE.
</success_criteria>

<output>
Create `.planning/phases/06-polish-seo-launch/06-03-SUMMARY.md` on Task 10. Commit message: `docs(06-03): plan 06-03 SUMMARY + Phase 6 complete (v1 SHIPPED)`.

After this plan completes, no further phase work remains for v1. Subsequent work (designer monogram, portrait shoot, /writing, light-mode toggle, /now page) is v1.x or v2 per CONTEXT § Deferred Ideas.
</output>
