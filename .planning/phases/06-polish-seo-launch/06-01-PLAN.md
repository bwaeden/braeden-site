---
phase: 06-polish-seo-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/live-url.ts
  - tests/contact-modal-opens-from-nav.spec.ts
  - tests/contact-modal-opens-from-about.spec.ts
  - tests/contact-modal-esc-closes.spec.ts
  - tests/contact-modal-states.spec.ts
  - tests/contact-modal-honeypot.spec.ts
  - tests/contact-modal-min-time.spec.ts
  - tests/contact-modal-mailto-fallback.spec.ts
  - components/contact/ContactModal.tsx
  - app/globals.css
  - app/%5Ftokens/
  - .planning/phases/06-polish-seo-launch/visual-checklist.md
  - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md
autonomous: false
requirements: [CTCT-02, WORK-05, A11Y-05, PERF-05]

must_haves:
  truths:
    - "All 7 contact-modal-*.spec.ts files run GREEN on both chromium-mobile + chromium-desktop Playwright projects."
    - "Mailto fallback link in ContactModal renders a `→` glyph via CSS `::after` content (NOT a `<span>` child element)."
    - "Error region in ContactModal has both `role=\"alert\"` AND `aria-live=\"assertive\"` (already at line 227 — verify-only per Pattern Map refinement)."
    - "Silent bot-rejection render path (honeypot OR min-time) sets `bypassedSuccess` flag but never fires Formspree submit and never sets `state.succeeded` (verify trace)."
    - "`page.url()` reads after `history.replaceState` are replaced with `await liveUrl(page)` in `contact-modal-esc-closes.spec.ts`."
    - "`app/%5Ftokens/` URL-encoded folder is fully deleted with no live references remaining in `app/`, `components/`, `tests/`, or `lib/`."
    - "`<Analytics />` and `<SpeedInsights />` are verified-mounted in `app/layout.tsx` (researcher Pivot #2 — already present at lines 22-23)."
    - "Vercel preview deploy succeeds — preview URL resolves to HTTP 200 and serves the new site."
    - "`PLAYWRIGHT_BASE_URL=<preview> npm run test:full` runs GREEN on both projects against the real Vercel CDN."
    - "Real Formspree submission from the preview URL produces an email arriving at fakegoat1@gmail.com within 2 minutes."
    - "40-item combined visual checklist (28 P4 + 12 P5 dedup'd) walked clean against preview URL with all items ✓."
    - "3 placeholder GitHub repo URLs (prediction-market-bot, no-more-short-form, mc-packet-client) return HTTP 200 OR are flagged for href revision."
    - "WebAIM contrast ratios for `#707070` on `#1a1a1f` and `#c8a86a` on `#0a0a0a` are measured and documented numerically in 06-01-SUMMARY.md."
  artifacts:
    - path: "tests/helpers/live-url.ts"
      provides: "liveUrl(page) wrapper for post-history.replaceState URL reads"
      exports: ["liveUrl"]
    - path: ".planning/phases/06-polish-seo-launch/visual-checklist.md"
      provides: "40-item dedup'd combined visual sweep with 4-prefix taxonomy (visual/interaction/screen-reader/responsive)"
      contains: "7 sections: Chrome, /, /about, /work, ContactModal, 404, OG sharing"
    - path: ".planning/phases/06-polish-seo-launch/06-01-SUMMARY.md"
      provides: "Plan 06-01 close: preview URL, spec scoreboard delta, real-email screenshot/timestamp, WebAIM ratios, GH URL outcomes, visual checklist completion"
  key_links:
    - from: "tests/contact-modal-esc-closes.spec.ts"
      to: "tests/helpers/live-url.ts"
      via: "import { liveUrl } from './helpers/live-url'"
      pattern: "import.*liveUrl.*helpers/live-url"
    - from: "components/contact/ContactModal.tsx mailto link"
      to: "rendered DOM"
      via: "CSS `::after { content: '→' }` (Tailwind arbitrary `after:content-['→']` OR globals.css class)"
      pattern: "after:content-\\['→'\\]|mailto-arrow"
    - from: "Vercel preview URL"
      to: "fakegoat1@gmail.com"
      via: "Formspree endpoint xqeypnkw + NEXT_PUBLIC_FORMSPREE_ID env var"
      pattern: "live-email-arrives"
---

<objective>
Deliver a clean Vercel preview URL on which all originally-RED Phase 5 specs (22 Categories A–D failures) now pass on both Playwright projects, with the real Formspree submission verified end-to-end and the 40-item combined visual sweep clean.

Purpose: Close every "deferred-to-Phase-6" carry-forward from Plans 04-01 + 05-03 inline BEFORE the audit (Plan 06-02) lands on a known-RED state. Per CONTEXT D-06: fix all 22 spec failures inline in Wave 0 so the deploy gate is meaningful.

Output: Preview URL committed, 06-01-SUMMARY.md documenting exit-gate evidence (preview URL, spec scoreboard delta, real-email timestamp, WebAIM ratios, GH URL outcomes, 40-item checklist completion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-seo-launch/06-CONTEXT.md
@.planning/phases/06-polish-seo-launch/06-RESEARCH.md
@.planning/phases/06-polish-seo-launch/06-PATTERNS.md
@.planning/phases/06-polish-seo-launch/06-UI-SPEC.md
@.planning/phases/06-polish-seo-launch/06-VALIDATION.md
@.planning/phases/05-contact-modal/05-03-SUMMARY.md
@.planning/phases/04-work-projects/04-01-SUMMARY.md
@.planning/.continue-here.md
@components/contact/ContactModal.tsx
@components/layout/Nav.tsx
@tests/contact-modal-esc-closes.spec.ts
@tests/contact-modal-states.spec.ts
@tests/contact-modal-opens-from-nav.spec.ts
@app/layout.tsx
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from existing codebase. -->

From components/contact/ContactModal.tsx (current state — Cat C role="alert" already present at line 227):
- The component is the SOLE 'use client' island per FOUND-07 carve-out.
- `bypassedSuccess` state at lines 173-194 implements honeypot + min-time silent reject; `state.succeeded` never flips on bypass.
- Line 205: `const showSuccess = state.succeeded || bypassedSuccess;`
- Line 227: `<div role="alert" aria-live="assertive" aria-atomic="true">` — VERIFY ONLY, do not re-add.
- Mailto link rendered at idle/submitting (lines 411-420) AND success branches (lines 265-274) — both need the `::after` arrow refactor.

From components/layout/Nav.tsx (mobile hamburger):
- Line 40: `<details className="nav-mobile">` with `<summary>` — clicked first on mobile viewport before the Contact link is reachable.

From app/layout.tsx (D-09 verify target — researcher Pivot #2):
- Lines 1-2: `import { Analytics } from '@vercel/analytics/next';` + `import { SpeedInsights } from '@vercel/speed-insights/next';`
- Lines 22-23: `<Analytics />` + `<SpeedInsights />` already mounted as siblings of `<Footer />`.
- VERIFY only — NO mount needed.

From tests/contact-modal-esc-closes.spec.ts (Cat D single-line target):
- Line ~49: `expect(page.url(), ...).not.toContain('#contact');` → swap to `expect(await liveUrl(page), ...).not.toContain('#contact');`
- Add import: `import { liveUrl } from './helpers/live-url';`

Cat A common helper signature (per RESEARCH.md Pattern 9):
```ts
async function openModal(page: import('@playwright/test').Page) {
  await page.goto('/');
  const viewport = page.viewportSize();
  if (viewport && viewport.width < 640) {
    await page.locator('details.nav-mobile > summary').click();
  }
  await page.getByRole('link', { name: 'Contact' }).first().click();
  await expect(page.locator('dialog[data-test="contact-modal"]')).toHaveAttribute('open', /.*/);
}
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (W0): Atomic Cat A + Cat D spec refactor (single commit per D-07)</name>
  <files>tests/helpers/live-url.ts, tests/contact-modal-opens-from-nav.spec.ts, tests/contact-modal-opens-from-about.spec.ts, tests/contact-modal-esc-closes.spec.ts, tests/contact-modal-states.spec.ts, tests/contact-modal-honeypot.spec.ts, tests/contact-modal-min-time.spec.ts, tests/contact-modal-mailto-fallback.spec.ts</files>
  <read_first>
    - tests/contact-modal-opens-from-nav.spec.ts
    - tests/contact-modal-opens-from-about.spec.ts
    - tests/contact-modal-esc-closes.spec.ts
    - tests/contact-modal-states.spec.ts
    - tests/contact-modal-honeypot.spec.ts
    - tests/contact-modal-min-time.spec.ts
    - tests/contact-modal-mailto-fallback.spec.ts
    - components/layout/Nav.tsx (verify `details.nav-mobile > summary` selector at line 40)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "Contact modal specs (Category A atomic-binding refactor)"
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-06 + D-07
  </read_first>
  <behavior>
    - Test 1 (Cat A): On chromium-mobile (Pixel 5 viewport <640px width), every spec opens the `details.nav-mobile > summary` hamburger via click before clicking the Contact link. On chromium-desktop (1280×800), no hamburger open is performed (selector skipped because `viewport.width >= 640`).
    - Test 2 (Cat A): Each spec's modal-open precondition asserts `<dialog data-test="contact-modal">` has the `open` attribute after the trigger click; assertion holds on BOTH projects.
    - Test 3 (Cat D): After `await modal close + history.replaceState(null, '', window.location.pathname + window.location.search)`, the assertion `expect(await liveUrl(page)).not.toContain('#contact')` PASSES; the previous `page.url()` assertion was stale due to Playwright frame URL caching.
    - Test 4 (helper): `liveUrl(page)` returns the current `window.location.href` evaluated in the browser context, NOT the cached Playwright URL.
  </behavior>
  <action>
    Per CONTEXT D-07 + RESEARCH.md § Pattern 7 atomic-binding, this entire refactor MUST land in a SINGLE git commit. Stage all 8 files together, commit once, verify via `git log -n 1 --name-only HEAD` that the commit lists exactly these files.

    Steps:
    1. CREATE `tests/helpers/live-url.ts` exporting `liveUrl(page)` per PATTERNS.md § `tests/helpers/live-url.ts` (3-line wrapper around `page.evaluate(() => window.location.href)`).
    2. In each of the 7 contact-modal-*.spec.ts files, ADD a shared `openModal(page)` helper at the top (per `<interfaces>` block above) that detects `page.viewportSize().width < 640` and opens `details.nav-mobile > summary` before clicking the Contact link. Inline-define or extract — planner prefers inline per-file to keep the atomic commit self-contained without an additional shared file.
    3. Replace existing inline modal-open sequences in each spec with calls to the new `openModal(page)`. PRESERVE all post-open assertions; only the open path changes.
    4. In `tests/contact-modal-esc-closes.spec.ts` ONLY: ADD `import { liveUrl } from './helpers/live-url';` at the top. REPLACE the single occurrence of `page.url()` with `await liveUrl(page)`. Keep the surrounding `expect().not.toContain('#contact')` shape identical.
    5. Stage all 8 files: `git add tests/helpers/live-url.ts tests/contact-modal-*.spec.ts`
    6. Commit: `git commit -m "test(06-01): atomic Cat A viewport detection + Cat D liveUrl helper (D-07)"`
    7. Verify atomic binding: `git log -n 1 --name-only HEAD` — output MUST list all 8 files; if any are missing, `git reset --soft HEAD~1` and re-stage.

    Do NOT split this across waves; do NOT split the live-url.ts creation into a separate commit; do NOT edit ContactModal.tsx source in this commit (Cat B + Cat C are Task 2). Use Edit tool, not heredoc.
  </action>
  <verify>
    <automated>git log -n 1 --name-only HEAD | grep -E "^tests/" | sort -u | wc -l</automated>
    Expected: 8 lines (7 spec files + 1 helper). Then run `npm run test:full -- tests/contact-modal-*.spec.ts` — GREEN on both chromium-mobile and chromium-desktop projects for Category A and Category D failure subset.
  </verify>
  <acceptance_criteria>
    - `git log -n 1 --name-only HEAD` lists exactly: `tests/helpers/live-url.ts`, `tests/contact-modal-opens-from-nav.spec.ts`, `tests/contact-modal-opens-from-about.spec.ts`, `tests/contact-modal-esc-closes.spec.ts`, `tests/contact-modal-states.spec.ts`, `tests/contact-modal-honeypot.spec.ts`, `tests/contact-modal-min-time.spec.ts`, `tests/contact-modal-mailto-fallback.spec.ts` (8 entries total, no others).
    - `grep -E "^export const liveUrl" tests/helpers/live-url.ts` returns 1 match.
    - `grep -c "viewport.width < 640" tests/contact-modal-*.spec.ts` returns 7 (one per file).
    - `grep -c "page.url()" tests/contact-modal-esc-closes.spec.ts` returns 0 (replaced).
    - `grep -c "await liveUrl(page)" tests/contact-modal-esc-closes.spec.ts` returns ≥1.
    - `npm run test:full -- tests/contact-modal-*.spec.ts` exit code 0 (both projects GREEN on all 7 files for the Cat A + Cat D subset).
  </acceptance_criteria>
  <done>Single atomic commit lands 8 files. Cat A 7-spec viewport detection + Cat D liveUrl helper both wired. Specs GREEN on both projects for Cat A and Cat D coverage. ContactModal source untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (W0): Cat B mailto arrow `::after` + Cat C verify role="alert" + delete %5Ftokens</name>
  <files>components/contact/ContactModal.tsx, app/globals.css, tests/contact-modal-states.spec.ts, app/%5Ftokens/</files>
  <read_first>
    - components/contact/ContactModal.tsx (current state — locate the two mailto link blocks at idle/submitting lines 411-420 AND success branch lines 265-274; verify role="alert" presence at line 227)
    - app/globals.css (existing rules; identify where to append the `mailto-arrow` class OR confirm Tailwind arbitrary `after:content-['→']` is preferred)
    - tests/contact-modal-states.spec.ts (locate the existing mailto assertion that needs the `::after` content read)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "Cat B — mailto `→` arrow refactor" + § "Cat C — error region role='alert'"
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-06 (Cat B + Cat C)
    - .planning/.continue-here.md § Critical Anti-Patterns (no 2nd 'use client' addition; `transition-[border-color,color,opacity]` arbitrary-list discipline)
  </read_first>
  <behavior>
    - Test 1 (Cat B mailto arrow): The mailto link's pseudo-element `::after` returns `content: "→"` via `getComputedStyle(linkEl, '::after').content` in the rendered DOM. No `<span aria-hidden>→</span>` child element exists inside the mailto anchor.
    - Test 2 (Cat B hover translate): On `:hover`, the `::after` pseudo-element transform shifts `translateX(4px)` (matches the existing `group-hover:translate-x-1` 4px convention from `CTAArrowLink`).
    - Test 3 (Cat C role alert): The error region element has BOTH `role="alert"` AND `aria-live="assertive"` attributes (already present at line 227 — assertion VERIFIES no regression, does not re-add).
    - Test 4 (Cat C silent-bypass audit): Triggering honeypot fill (the `name="company"` input) or sub-1500ms submit sets `bypassedSuccess=true` and renders the success UI without invoking the wrapped `useForm.handleSubmit`; `state.succeeded` remains `false` (verify via console.log instrumentation OR by tracing the code path manually — DO NOT mock Formspree).
    - Test 5 (%5Ftokens deletion): `git grep -l "_tokens\|%5Ftokens" -- 'app/**' 'components/**' 'tests/**' 'lib/**'` returns empty; `npm run build` exits 0.
  </behavior>
  <action>
    Three independent edits batched into this task (NOT one atomic commit — each is a logically separable change with its own verification):

    Sub-task A (Cat B mailto arrow refactor — single commit):
    1. In `components/contact/ContactModal.tsx`, locate the two `<a href={MAILTO_HREF}>` blocks (lines ~265-274 success branch + ~411-420 idle/submitting branch).
    2. REMOVE the `<span aria-hidden className="inline-block transition-transform group-hover:translate-x-1">→</span>` child element from BOTH.
    3. ADD Tailwind arbitrary classes `after:content-['→'] after:ml-2 after:inline-block after:transition-transform group-hover:after:translate-x-1` to the anchor's `className`. Use the `transition-[transform]` arbitrary-list discipline (avoid the bare `transition-colors` shorthand per the established anti-pattern; `transition-transform` is acceptable here because no color is interpolating on hover).
    4. Update `tests/contact-modal-states.spec.ts` mailto-arrow assertion to use `await mailtoLink.evaluate(el => getComputedStyle(el, '::after').content)` — expected return value is `'"→"'` (note the quotes because `content` returns the CSS-parsed quoted form).
    5. Commit: `git commit -m "feat(06-01): Cat B mailto arrow via CSS ::after pseudo-element"`

    Sub-task B (Cat C verify — NO source edit; only documentation in SUMMARY):
    1. CONFIRM `components/contact/ContactModal.tsx` line 227 has `<div role="alert" aria-live="assertive" aria-atomic="true">`. If absent (regression), re-add it.
    2. TRACE the honeypot + min-time silent-bypass path: read lines 173-194 + 205 to confirm `state.succeeded` is NEVER set during bypass and only `bypassedSuccess` flips. Document the trace outcome in `.planning/phases/06-polish-seo-launch/06-01-SUMMARY.md` § Cat C audit.
    3. Run `npm run test:full -- tests/contact-modal-states.spec.ts tests/contact-modal-honeypot.spec.ts tests/contact-modal-min-time.spec.ts` — all GREEN on both projects.
    4. No commit if no source edit; if Cat B Sub-task A edited contact-modal-states.spec.ts, the Cat C verification rides in the same commit.

    Sub-task C (%5Ftokens folder deletion — single commit):
    1. Verify no live references: `git grep -l "_tokens\|%5Ftokens" -- 'app/**' 'components/**' 'tests/**' 'lib/**' 'data/**'` — must return empty BEFORE delete (Phase 1 D-11 leftover; should already be unreferenced).
    2. DELETE the entire folder: `git rm -rf 'app/%5Ftokens'` (use the URL-encoded literal name; on Windows PowerShell the path is `app\%5Ftokens` — quote it).
    3. Verify build remains clean: `npm run build`.
    4. Commit: `git commit -m "chore(06-01): delete app/%5Ftokens/ Phase 1 leftover (carry-fwd #14)"`

    Total task output: 2 commits (Sub-task A + Sub-task C); Sub-task B is verification-only embedded in Sub-task A's commit message OR SUMMARY notes.

    Do NOT add a 2nd `'use client'` directive (no new client islands). Do NOT touch `tests/single-client-island.spec.ts` or `tests/no-client-components.spec.ts`. Use Edit tool, not heredoc.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npm run test:full -- tests/contact-modal-states.spec.ts tests/contact-modal-honeypot.spec.ts tests/contact-modal-min-time.spec.ts tests/contact-modal-mailto-fallback.spec.ts && git grep -l "_tokens\\|%5Ftokens" -- 'app/**' 'components/**' 'tests/**' 'lib/**' 'data/**' ; echo "REFS=$?"</automated>
    Expected: typecheck/lint/build/test all exit 0; the final grep should print no files AND echo `REFS=1` (grep returns 1 when no matches — the desired state).
  </verify>
  <acceptance_criteria>
    - `grep -c "after:content-\\['→'\\]" components/contact/ContactModal.tsx` returns ≥2 (both mailto link branches use the pseudo-element).
    - `grep -c '<span aria-hidden[^>]*>→</span>' components/contact/ContactModal.tsx` returns 0 (the trailing `<span>→</span>` child is removed from mailto blocks).
    - `grep -c 'role="alert"' components/contact/ContactModal.tsx` returns ≥1 (Cat C role still present after Cat B edits).
    - `ls "app/%5Ftokens"` errors out (folder deleted).
    - `git grep -l "_tokens\\|%5Ftokens" -- 'app/**' 'components/**' 'tests/**' 'lib/**' 'data/**'` returns no matches.
    - `npm run build` exit code 0 (Turbopack production build clean).
    - `npm run test:full -- tests/contact-modal-states.spec.ts tests/contact-modal-honeypot.spec.ts tests/contact-modal-min-time.spec.ts tests/contact-modal-mailto-fallback.spec.ts` exit code 0 (Cat B + Cat C specs GREEN on both projects).
    - `grep -E "(import.*'use client'|^'use client')" components/contact/ContactModal.tsx | wc -l` returns 1 (the single carve-out preserved).
    - `tests/single-client-island.spec.ts` + `tests/no-client-components.spec.ts` still GREEN (no client island count drift).
  </acceptance_criteria>
  <done>Cat B mailto `::after` arrow pattern landed; Cat C role="alert" verified + silent-bypass path audited and documented; `app/%5Ftokens/` folder fully deleted. Build + typecheck + lint clean. All Cat B + Cat C specs GREEN on both Playwright projects.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (W1): Verify Analytics + SpeedInsights mount (D-09 researcher Pivot #2)</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (verify lines 1-2 imports + lines 22-23 `<Analytics />` + `<SpeedInsights />` JSX presence)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-01-06
    - .planning/phases/06-polish-seo-launch/06-RESEARCH.md § Pivot #2
    - package.json (confirm `@vercel/analytics@2.0.1` + `@vercel/speed-insights@2.0.0` dependencies present)
  </read_first>
  <what-built>
    Automation completed by Task 3a (auto subtask before this checkpoint):
    - `grep -nE "Analytics|SpeedInsights" app/layout.tsx` confirms imports + JSX mounts present.
    - Production build emits the Analytics + Speed Insights client scripts (verify in `.next/static/` bundle).
    - This is a VERIFY task per researcher Pivot #2 — both components are already mounted at lines 22-23.
  </what-built>
  <how-to-verify>
    1. Read `app/layout.tsx` and confirm:
       - Line 1 OR 2: `import { Analytics } from '@vercel/analytics/next';`
       - Line 1 OR 2: `import { SpeedInsights } from '@vercel/speed-insights/next';`
       - Lines 22-23 (or adjacent): `<Analytics />` and `<SpeedInsights />` as JSX siblings of `<Footer />` (or `<ContactModal />`).
    2. Run `npm run build` and verify the output reports no warnings about missing analytics initialization.
    3. After the preview deploy in Task 4, open the preview URL in a browser with DevTools → Network tab. Reload. Confirm at least one request to `*.vercel-insights.com` (Speed Insights beacon) AND one to `va.vercel-scripts.com` (Analytics script load).
    4. Open Vercel dashboard → braeden-site → Analytics tab; within ~30 minutes of the preview load, at least 1 pageview should appear in the Analytics chart. (If not, the env var `VERCEL_ANALYTICS_ID` may be missing — flag for follow-up but DO NOT block the plan exit.)
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "approved" once analytics endpoint hit confirmed OR document outcome (any failure here is informational, not blocking — Vercel often takes 30-60 min to surface preview analytics data).</resume-signal>
  <acceptance_criteria>
    - `grep -cE "^import.*Analytics.*from '@vercel/analytics'" app/layout.tsx` returns ≥1.
    - `grep -cE "^import.*SpeedInsights.*from '@vercel/speed-insights'" app/layout.tsx` returns ≥1.
    - `grep -cE "<Analytics ?/>" app/layout.tsx` returns ≥1.
    - `grep -cE "<SpeedInsights ?/>" app/layout.tsx` returns ≥1.
    - Human confirms via DevTools Network tab that analytics + speed-insights endpoints are hit on preview load.
  </acceptance_criteria>
  <done>Both components verified mounted; analytics endpoint hits confirmed against preview. No source edit needed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 (W2): Vercel preview deploy + verify URL resolves</name>
  <files>(no files — Vercel pipeline external)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-13 (deploy sequence + env vars)
    - .planning/phases/01-foundation-design-tokens/01-CONTEXT.md § D-13 (Formspree env var across all envs)
    - .planning/STATE.md § branch state (verify `main` is target deploy branch)
  </read_first>
  <what-built>
    Tasks 1-3 produced atomic commits closing Cat A, B, C, D inherited failures + %5Ftokens deletion + Analytics verify. The branch `main` is now in a deploy-ready state. This task pushes those commits and confirms the Vercel preview URL serves HTTP 200.
  </what-built>
  <how-to-verify>
    1. Run `git status` — confirm working tree clean.
    2. Run `git log --oneline -5` — confirm Tasks 1+2 commits are at HEAD.
    3. Run `git push origin main` (or appropriate branch). Vercel webhook auto-triggers a deploy.
    4. Wait for Vercel pipeline to report success (~2-4 min). Get the preview URL from Vercel dashboard (e.g., `https://braeden-site-<hash>-bwaeden.vercel.app`).
    5. Run `curl -sI <preview-url>/` — expect `HTTP/2 200`.
    6. Open the preview URL in a browser; confirm the homepage renders (Hero + Currently + ChannelButton row + CTAs).
    7. Copy the preview URL value into the orchestrator's state so Task 5 can use it as `PLAYWRIGHT_BASE_URL`.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Paste the preview URL (e.g., `https://braeden-site-abc123.vercel.app`) OR "deploy-failed: <reason>".</resume-signal>
  <acceptance_criteria>
    - `git status` reports clean working tree.
    - `curl -sI <preview-url>/` returns `HTTP/2 200` (or `HTTP/1.1 200`).
    - Preview URL renders the homepage with Hero/CurrentlyLine/ChannelButton row visible.
    - Preview URL is documented in 06-01-SUMMARY.md.
  </acceptance_criteria>
  <done>Preview deploy succeeds and is reachable. URL captured for downstream tasks.</done>
</task>

<task type="auto">
  <name>Task 5 (W2): Playwright full suite against Vercel preview URL</name>
  <files>(no source edits — env-driven existing suite)</files>
  <read_first>
    - playwright.config.ts (verify `baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'`)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md § "Preview-URL full run"
    - tests/lighthouse.spec.ts (note: still threshold=0 here — Plan 06-02 bumps to 95; this run only verifies non-lighthouse specs)
  </read_first>
  <behavior>
    - Test 1: Running `PLAYWRIGHT_BASE_URL=<preview> npm run test:full` exits 0.
    - Test 2: Both chromium-mobile (Pixel 5) AND chromium-desktop (1280×800) projects pass.
    - Test 3: The originally-failing 22 Category A–D specs now GREEN.
    - Test 4: The 7 pre-existing `/`-route specs (lighthouse, photo-lcp, no-bare-outline-none) — researcher RESEARCH.md notes some may flip GREEN on real Vercel CDN bundle; document outcome.
  </behavior>
  <action>
    1. Set the env var with the preview URL from Task 4: `export PLAYWRIGHT_BASE_URL=<preview-url>` (PowerShell: `$env:PLAYWRIGHT_BASE_URL = '<preview-url>'`).
    2. Run the full suite: `npm run test:full`.
    3. Capture exit code + per-project pass/fail summary.
    4. For any remaining failures: document file + project + failure reason in 06-01-SUMMARY.md § Spec Scoreboard Delta. Specs that fail because of lighthouse-threshold-0 (Phase 1 spec still has 0 threshold) are EXPECTED — Plan 06-02 owns the threshold bump.
    5. The 22 Cat A–D specs MUST be GREEN; if any are still RED, return to Task 1 or Task 2.
    6. Do NOT commit anything from this task — verification-only against the deployed preview.
  </action>
  <verify>
    <automated>PLAYWRIGHT_BASE_URL=$PREVIEW_URL npm run test:full --silent 2>&1 | tee /tmp/preview-full-run.log; cat /tmp/preview-full-run.log | grep -E "passed|failed"</automated>
    Expected: line(s) like "X passed (Y skipped)" with the 22 Cat A–D specs in the passed set; any failures documented separately (lighthouse-threshold-0 expected to fail and acceptable here).
  </verify>
  <acceptance_criteria>
    - `npm run test:full` against the preview URL exits 0 OR exits non-zero ONLY due to known-pending Plan-06-02 specs (lighthouse threshold=0, the 8 new SEO specs not yet created).
    - All 7 contact-modal-*.spec.ts files GREEN on BOTH chromium-mobile AND chromium-desktop.
    - The pre-existing 7 `/`-route specs (lighthouse, photo-lcp, no-bare-outline-none) outcome documented in 06-01-SUMMARY.md.
    - No new regressions introduced beyond Phase 5's known carry-forward set.
  </acceptance_criteria>
  <done>Preview-URL Playwright run captures the post-fix spec state on real Vercel CDN. Cat A–D coverage GREEN. Remaining failures (if any) documented for Plan 06-02 to address.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (W2): Real Formspree end-to-end email verification (CTCT-02 deploy gate)</name>
  <files>(no files — manual external test)</files>
  <read_first>
    - .planning/phases/05-contact-modal/05-03-SUMMARY.md § Phase 6 Carry-Forwards item #7
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-01-09
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-14 (production deploy gate exit criteria item d)
    - components/contact/ContactModal.tsx (note Formspree endpoint env var `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw`)
  </read_first>
  <what-built>
    Plan 05 shipped the ContactModal client island wired to Formspree endpoint `xqeypnkw`. Phase 1 D-13 set the env var across Production+Preview+Development. This task verifies the end-to-end submit-to-inbox path works against a real Vercel runtime.
  </what-built>
  <how-to-verify>
    1. Open the preview URL from Task 4 in a desktop browser (Chrome or Firefox).
    2. Click the `Contact` link in Nav. Verify the dialog opens and Name input is focused.
    3. Wait ≥ 2 seconds (to clear the 1500ms min-time gate).
    4. Fill the form:
       - Name: `Phase 6 Plan 01 Verify`
       - Email: `fakegoat1@gmail.com` (sender — your own address; reply-to)
       - Message: `Phase 6 Plan 06-01 deploy gate verification. Timestamp: <ISO datetime>.` (≥50 chars)
       - Honeypot field `company`: leave EMPTY (do not fill).
    5. Click `Send` button.
    6. Verify the UI transitions to the success state with copy: `Thanks — I'll get back to you within a day or two.`
    7. Wait ≤ 2 minutes. Check `fakegoat1@gmail.com` inbox.
    8. Confirm an email arrives with:
       - Subject: includes "braehods" OR "Formspree"
       - From: `noreply@formspree.io` (or similar)
       - Body: includes the timestamp message from step 4
    9. Document in 06-01-SUMMARY.md § Real Formspree Submit: timestamp sent, timestamp received, inbox screenshot path OR confirmation text.
    10. ALSO: repeat the form open BUT fill the honeypot `company` field (DevTools → inspect → set value programmatically) and submit ≥1500ms after open. Verify success UI renders BUT no email arrives within 2 minutes. Document the silent-rejection outcome.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "email-received: <timestamp>" OR "email-not-received: <details>" (if failure).</resume-signal>
  <acceptance_criteria>
    - Real email arrives at fakegoat1@gmail.com within 2 minutes of the submit (success path).
    - Email body contains the timestamp/marker string from the form submission.
    - Honeypot fill path renders success UI but produces NO email (silent rejection verified).
    - Both outcomes documented in 06-01-SUMMARY.md.
  </acceptance_criteria>
  <done>Production-path Formspree integration verified end-to-end on the Vercel preview. CTCT-02 carry-forward closed at the preview level (production verification happens again in Plan 06-03).</done>
</task>

<task type="auto">
  <name>Task 7 (W2): Emit 40-item combined visual checklist file</name>
  <files>.planning/phases/06-polish-seo-launch/visual-checklist.md</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-UI-SPEC.md § "Visual Checklist Taxonomy" (7 sections × ~40 rows, 4-prefix shape: visual/interaction/screen-reader/responsive)
    - .planning/phases/04-work-projects/04-01-SUMMARY.md (28 Phase 4 visual items source)
    - .planning/phases/05-contact-modal/05-03-SUMMARY.md (12 Phase 5 visual items source — search for "Phase Exit Visual Verification" 12-item list)
  </read_first>
  <action>
    Create `.planning/phases/06-polish-seo-launch/visual-checklist.md` as a markdown checkbox list, dedup'd from Phase 4's 28-item checklist and Phase 5's 12-item checklist. Use the 4-prefix taxonomy from UI-SPEC.md § Visual Checklist Taxonomy:
    - `visual` — observable property at a viewport
    - `interaction` — action → outcome
    - `screen-reader` — SR walk announces phrase
    - `responsive` — viewport renders cleanly

    Section structure (7 sections per UI-SPEC):
    1. **Chrome** (5 items): Nav monogram alignment, hamburger toggle, link focus rings, Contact hash-trigger; Footer monogram size + copyright + socials + View source + braehods.com text.
    2. **`/` (Home)** (8 items): Hero photo LCP, Fraunces wordmark, CurrentlyLine, ChannelButton row, CTAArrowLinks to /about + /work, 320px overflow, on-load stagger.
    3. **`/about`** (5 items): Two-column composition, single-column mobile, bio copy no-clichés, Get in touch CTA fires modal, photo treatment matches hero.
    4. **`/work`** (8 items): 7-card grid, CapitolLens leads, orphan card alignment, hairline border + accent hover, status dot+label per card, archived dot contrast, tags line, external `↗` glyph translate.
    5. **ContactModal** (8 items): Trigger from Nav + /about, focus trapped, ESC closes + focus return, field labels + asterisks, char counter color flip at 800 chars, submit → success state, error state preserves form, mailto fallback with `::after` arrow.
    6. **404** (3 items): Renders with full chrome + monogram + heading + body line + return link; inline `get in touch` opens modal; `← Back home` returns to `/`. (Note: 404 page itself is created in Plan 06-02 — these items are placeholders that get verified in Plan 06-02's visual sweep, but listed here for completeness per D-08 single-combined-checklist.)
    7. **OG sharing** (3 items): `/` static OG renders in Slack/iMessage; `/about` dynamic OG with "About" title; `/work` dynamic OG with "Work" title. (Note: same as 404 — these items belong to Plan 06-02 surfaces but listed here for unified single-walk per D-08.)

    Each row begins with the 4-prefix label. Total: aim for 40 items (some sections may have ±1 after dedup). Mark sections 6 and 7 with a header note: "Verified after Plan 06-02 ships the SEO surfaces."

    The file is generated text (not source code). Use the Write tool to emit the entire file in one pass.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-polish-seo-launch/visual-checklist.md && grep -cE '^- \\[ \\] (visual|interaction|screen-reader|responsive)' .planning/phases/06-polish-seo-launch/visual-checklist.md</automated>
    Expected: file exists; grep returns 35-45 (target 40 items with the 4-prefix shape; tolerance for dedup variance).
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/06-polish-seo-launch/visual-checklist.md`.
    - Total checkbox rows (lines starting with `- [ ]`) ≥ 35.
    - Each row starts with one of: `visual `, `interaction `, `screen-reader `, `responsive ` (4 prefixes).
    - 7 section headers present (Chrome, /, /about, /work, ContactModal, 404, OG sharing).
    - Sections 6 and 7 carry an explicit "Verified after Plan 06-02 ships the SEO surfaces" note.
    - File committed with message: `docs(06-01): emit 40-item combined visual checklist (D-08)`.
  </acceptance_criteria>
  <done>visual-checklist.md committed and ready for the manual sweep in Task 8.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8 (W2): Walk 40-item visual checklist against preview URL</name>
  <files>.planning/phases/06-polish-seo-launch/visual-checklist.md</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/visual-checklist.md (the file emitted by Task 7)
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-08
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-01-10
  </read_first>
  <what-built>
    Task 7 emitted the 40-item dedup'd checklist. This task walks each item against the deployed preview URL from Task 4, marking ✓ or ✗ and screenshotting failures.
  </what-built>
  <how-to-verify>
    1. Open the preview URL on a desktop browser (Chrome recommended).
    2. For each row in `visual-checklist.md`:
       - `visual` rows: navigate to the surface, observe the visual property, mark ✓ or ✗.
       - `interaction` rows: perform the action, observe the outcome, mark ✓ or ✗.
       - `screen-reader` rows: SKIP for Plan 06-01 (NVDA + VoiceOver audit lives in Plan 06-02). Mark as "Plan 06-02 owns".
       - `responsive` rows: DevTools → Toggle Device Toolbar → set viewport (320px iPhone SE, 1280px desktop), reload, verify no horizontal scroll.
    3. For sections 6 (404) and 7 (OG sharing): MARK as "deferred — Plan 06-02 ships these surfaces". Sections 1-5 must all be ✓.
    4. For any ✗ row: take a screenshot, save to `.planning/phases/06-polish-seo-launch/visual-checklist-failures/<row-id>.png`, document in 06-01-SUMMARY.md.
    5. Commit the marked-up `visual-checklist.md` (replace `- [ ]` with `- [x]` for passing items) with message `docs(06-01): mark visual checklist outcomes`.
    6. Any P0 visual failure (broken layout, missing chrome, illegible text) CYCLES BACK to inline source fix; do not proceed to Task 9 until clean.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "visual-clean: <count-passed>/<count-total>" (e.g., `visual-clean: 32/34` — items deferred to Plan 06-02 are excluded from the count) OR "visual-failures: <details>".</resume-signal>
  <acceptance_criteria>
    - All non-deferred rows in `visual-checklist.md` marked ✓.
    - Any ✗ rows produce a screenshot + a SUMMARY entry + a follow-up commit OR a deliberate "deferred" tag.
    - Sections 1-5 of the checklist are 100% ✓ on the preview URL.
    - File committed.
  </acceptance_criteria>
  <done>40-item visual sweep clean (excluding Plan-06-02-deferred section 6 + 7). Plan 06-01 visual gate passed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9 (W2): 3 placeholder GitHub URL real-link verify + WebAIM contrast measurement</name>
  <files>.planning/phases/06-polish-seo-launch/06-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-work-projects/04-01-SUMMARY.md § Phase 6 Carry-Forwards items #2 + #3
    - data/projects.ts (locate the 3 placeholder entries — `prediction-market-bot`, `no-more-short-form`, `mc-packet-client` — and confirm each href is `https://github.com/bwaeden/<slug>`)
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § "Plan 06-01 deploy gate exit criteria" items 8 + 9
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md tasks 06-01-11 + 06-01-12
  </read_first>
  <what-built>
    Phase 4 deferred two manual checks to Phase 6: (a) the 3 placeholder GH URLs need a real-link 200 check, and (b) WebAIM contrast on `#707070` archived-dot color and `#c8a86a` paper-trading char-counter color need numerical measurement (axe-core does NOT cover WCAG 1.4.11 graphical 3:1 per researcher Finding #4).
  </what-built>
  <how-to-verify>
    1. **GitHub URL check** — Run each of these three commands (substitute the correct bwaeden username; expect 200 for live OR 404 for missing repos):
       - `curl -L -o /dev/null -s -w "%{http_code}" https://github.com/bwaeden/prediction-market-bot`
       - `curl -L -o /dev/null -s -w "%{http_code}" https://github.com/bwaeden/no-more-short-form`
       - `curl -L -o /dev/null -s -w "%{http_code}" https://github.com/bwaeden/mc-packet-client`
    2. For each: document the HTTP status code in 06-01-SUMMARY.md § GH URL Verification. If 404: flag for user follow-up — Phase 6 does NOT auto-rewrite hrefs (D-16 only covers the archived braehods entry).
    3. **WebAIM contrast** — Open the preview URL on Chrome:
       - Navigate to `/work`. Use DevTools color picker (or Chrome's contrast tool in Elements panel) on the archived project status dot. Confirm rendered color is `#707070`.
       - Visit https://webaim.org/resources/contrastchecker/. Foreground: `#707070`. Background: `#1a1a1f`. Record the numerical contrast ratio in 06-01-SUMMARY.md. Expected: ≥3.0:1 (WCAG 1.4.11 UI minimum).
       - Open the ContactModal. Type into Message textarea until char counter color flips to amber (~800 chars). Confirm color is `#c8a86a`.
       - WebAIM: Foreground: `#c8a86a`. Background: `#0a0a0a`. Record the ratio in 06-01-SUMMARY.md. Expected: ≥3.0:1.
    4. If EITHER ratio is below 3.0:1, document the fallback recipe per Phase 4 D-09 (archived dot fallback to `#7a7a7a`) or flag for the user. DO NOT auto-revise tokens — Phase 6 inherits all design tokens verbatim per UI-SPEC.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "contrast-clean: archived=<ratio>, char-counter=<ratio>; gh-urls: <status-codes>" — e.g., `contrast-clean: archived=3.4:1, char-counter=8.2:1; gh-urls: 200/404/200`.</resume-signal>
  <acceptance_criteria>
    - Three numerical HTTP status codes documented in 06-01-SUMMARY.md (one per GH URL).
    - Two numerical contrast ratios documented (archived dot + char counter).
    - Any 404 GH URL flagged for follow-up (NOT auto-fixed).
    - Any ratio < 3.0:1 documented with fallback recipe reference (Phase 4 D-09 archived-dot fallback to `#7a7a7a`); ratios ≥3.0:1 marked PASS.
  </acceptance_criteria>
  <done>3 GH URLs verified; 2 contrast ratios measured + documented. Plan 06-01 carry-forward items #2/#3/#11 closed.</done>
</task>

<task type="auto">
  <name>Task 10 (W2): Write 06-01-SUMMARY.md + traceability flips</name>
  <files>.planning/phases/06-polish-seo-launch/06-01-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
    - .planning/phases/05-contact-modal/05-03-SUMMARY.md (recent SUMMARY style reference)
    - .planning/phases/04-work-projects/04-01-SUMMARY.md (consolidated-SUMMARY style reference)
    - .planning/REQUIREMENTS.md (locate CTCT-02 + PERF-05 + WORK-05 + A11Y-05 rows for traceability flip)
    - .planning/STATE.md (locate Last Session + progress.* fields)
  </read_first>
  <action>
    1. CREATE `.planning/phases/06-polish-seo-launch/06-01-SUMMARY.md` per the SUMMARY template. Required sections:
       - Frontmatter: phase, plan, status, subsystem, tags, dependency graph (requires Phase 1-5 + carry-forwards from 04 + 05; provides clean preview URL + closed Cat A-D inherited failures + visual sweep + Formspree-on-preview verify), tech tracking, key-files (created/modified/deleted), key-decisions, patterns-established, requirements-completed (list IDs: CTCT-02, PERF-05, WORK-05, A11Y-05 — preview-verify level), metrics (commit count, duration).
       - **Body:** Performance, Key Findings (Cat A-D closure mechanism), Spec Scoreboard Delta (22 specs RED → GREEN), Real Formspree submit timestamp, Visual checklist outcome (passed/deferred counts), GH URL verification results, WebAIM contrast ratios (numeric), Anti-Patterns Avoided (no 2nd 'use client'; atomic D-07 commit verified; no `transition-colors` shorthand introduced), Phase 6 Carry-Forwards remaining (Plan 06-02 + Plan 06-03 ownership).
    2. FLIP traceability in `.planning/REQUIREMENTS.md`:
       - CTCT-02: status → `Verified on preview (real email arrived at fakegoat1@gmail.com); production verify in Plan 06-03`.
       - PERF-05: status → `Mounted in app/layout.tsx + verified analytics endpoint hit on preview deploy`.
       - WORK-05: append note `(3 placeholder GH URLs HTTP-status verified; results in 06-01-SUMMARY.md)`.
       - A11Y-05: append note `(archived dot + char counter contrast ratios measured; results in 06-01-SUMMARY.md)`.
    3. UPDATE `.planning/STATE.md`:
       - Append a Last Session entry summarizing Plan 06-01 close.
       - Bump `progress.completed_plans` by 1.
       - Set Current focus → "Phase 6 Plan 06-02 (A11Y + PERF + SEO audit)".
    4. Commit all 3 files via:
       ```
       gsd-sdk query commit "docs(06-01): plan 06-01 SUMMARY + traceability flip" --files \\
         .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md \\
         .planning/REQUIREMENTS.md \\
         .planning/STATE.md
       ```
  </action>
  <verify>
    <automated>test -f .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md && grep -c "## " .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md && grep -c "Verified on preview" .planning/REQUIREMENTS.md</automated>
    Expected: SUMMARY file exists with ≥6 H2 sections; REQUIREMENTS.md shows the verify-on-preview note for CTCT-02.
  </verify>
  <acceptance_criteria>
    - 06-01-SUMMARY.md exists with frontmatter, performance section, spec scoreboard delta, Formspree submit log, visual checklist outcome, contrast ratios, GH URL results, anti-patterns avoided, carry-forwards remaining.
    - REQUIREMENTS.md CTCT-02 + PERF-05 status updated.
    - STATE.md Last Session entry appended; progress fields bumped.
    - All 3 files committed in a single docs commit.
  </acceptance_criteria>
  <done>Plan 06-01 closed at SUMMARY level. Traceability up-to-date. Ready for Plan 06-02 dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Vercel preview CDN | First time Phase 6 work hits a real network — preview URLs are public but obscured; no auth gate yet (Plan 06-02 adds `X-Robots-Tag: noindex`). |
| ContactModal ↔ Formspree API | `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw` injected at build time; Formspree handles the SMTP path. No direct SMTP/secret leak in repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information disclosure | Vercel preview URL public-but-obscure | low | accept | Plan 06-02 adds `X-Robots-Tag: noindex` via `proxy.ts` so search engines do not index the preview; Plan 06-01 accepts brief exposure window (~hours-to-days between deploy and Plan 06-02 ship) — preview URLs are random-hash subdomains, not enumerable. |
| T-06-02 | Spoofing | Formspree submit from preview URL | low | mitigate | Honeypot (`name="company"`) + 1500ms min-time gate already in place; Task 6 verifies the silent-rejection path doesn't render success-leak-to-attacker UI. No new code change needed; verify-only. |
| T-06-03 | Tampering | Atomic D-07 spec commit could be split accidentally | medium | mitigate | Task 1's acceptance criterion uses `git log -n 1 --name-only` to enforce the 8-file atomic commit; if any file is missing, `git reset --soft HEAD~1` and re-stage. Plan-checker also validates per CONTEXT D-07. |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | NO new package installs in Plan 06-01. Researcher confirmed zero new runtime/dev deps required. No legitimacy gate needed. |
</threat_model>

<verification>
Plan 06-01 exit gate (combined acceptance per CONTEXT § "Plan 06-01 deploy gate exit criteria"):
1. `npm run typecheck` clean (Task 2 + 5 verify)
2. `npm run lint` clean (acceptable carry-forward: pre-existing `_staggerIndex` warning per Phase 5 deferred-items.md)
3. `npm run build` clean (Turbopack) (Task 2 + 4 verify)
4. `npm run test:full` GREEN on chromium-mobile + chromium-desktop projects against preview URL — explicit acceptance that all 22 Cat A–D failures are CLOSED (Task 5)
5. Vercel preview deploy succeeds + URL resolves 200 (Task 4)
6. Real Formspree submission from preview URL lands at fakegoat1@gmail.com (Task 6)
7. 40-item visual checklist clean on preview (Task 7 + 8)
8. 3 GH URL real-link verifications HTTP-status documented (Task 9)
9. WebAIM contrast ratios for `#707070` + `#c8a86a` documented numerically (Task 9)
</verification>

<success_criteria>
Plan 06-01 is complete when:
- All 10 tasks done and committed.
- 22 Categories A–D inherited spec failures are GREEN on both chromium-mobile + chromium-desktop projects against the deployed preview URL.
- `app/%5Ftokens/` folder deleted.
- Real email confirmed received at fakegoat1@gmail.com from a preview-URL form submit within 2 minutes.
- 40-item visual checklist sections 1-5 100% ✓; sections 6-7 deferred to Plan 06-02 with explicit notes.
- 3 GH URL HTTP statuses + 2 WebAIM contrast ratios documented in 06-01-SUMMARY.md.
- 06-01-SUMMARY.md committed with full traceability flips.
- Working tree clean; branch is in deploy-verified state for Plan 06-02 to take over.
</success_criteria>

<output>
Create `.planning/phases/06-polish-seo-launch/06-01-SUMMARY.md` on Task 10. Commit message: `docs(06-01): plan 06-01 SUMMARY + traceability flip`.
</output>
