# Plan 06-02 — CHECKPOINT: Task 3 (font vendoring) blocking human gate

**Written:** 2026-05-26 by gsd-executor (sequential, on `phase-6/audit-preview`)
**Status:** PAUSED at Task 3 (`checkpoint:human-action`, gate=blocking)

## Where we stopped

All Plan 06-02 auto tasks that do NOT depend on the font gate are DONE + committed.
The next task in plan order — **Task 3: vendor Fraunces-Bold + GeistSans-Bold
TTFs to `assets/`** — is a blocking human-action checkpoint. The executor cannot
download binary font files from external sources (Google Fonts / vercel/geist-font
releases); a human must do this. Tasks 5, 6 (OG images), 9 (preview deploy +
Lighthouse), and 11 (manual A11Y/SR/Rich-Results) all depend on either the fonts
or a human, so the executor halted here.

## Completed tasks (committed on phase-6/audit-preview)

| Task | Description | Commit(s) |
|------|-------------|-----------|
| T1 | Stub 8 new SEO specs RED + bump lighthouse threshold to 95 | `e5a12a0` |
| T2 | Monogram triple-source extraction (lib/og/monogram-path.ts) | `62a3fa1` |
| T4a | app/sitemap.ts dynamic sitemap (SEO-04a) | `0ab11df` |
| T4b | app/robots.ts allow-all + sitemap ref (SEO-04b) | `56ee5b0` |
| T4c | proxy.ts X-Robots-Tag noindex on preview (SEO-09) | `2824e7a` |
| T7 | CTAArrowLink direction=back + branded 404 (SEO-08) | `93ea470` |
| T8a | layout metadataBase + canonical + openGraph + Person JSON-LD (SEO-05/06) | `078b9e3` |
| T8b | per-route metadata + canonical on /, /about, /work (SEO-01/06) | `abd06e3` |
| T10 | bundle-measurement script via Turbopack-native analysis (PERF-03) | `3c8b2d7` |

Local verification status (against `npm run build` + `npm run start` on localhost):
- typecheck / lint / build all clean (only 2 pre-existing warnings: `_staggerIndex`, `.lcp-element-runner.mjs`).
- favicon spec GREEN (monogram extraction byte-identical, 930 chars across all 3 surfaces).
- not-found-renders, jsonld-person, canonical-urls, metadata-per-route specs GREEN locally.
- FOUND-07 invariant holds: `single-client-island` + `no-client-components` GREEN (still exactly 1 client island = ContactModal).
- PERF-03 preliminary (local prod build): application-only first-page JS = 20.38 KB gz (target 50 KB) -> pass. Framework baseline 108.81 KB gz; ContactModal chunk 12.55 KB gz (separate, not first-load).

The 8 new SEO specs (sitemap/robots/og-images/jsonld-person/not-found/canonical/
metadata) are RED-by-construction until the preview deploy (T9); preview-noindex is
skip-gated until `PLAYWRIGHT_BASE_URL` contains `vercel.app`.

## DEVIATIONS logged (for the eventual 06-02-SUMMARY.md)

1. **[Rule 1 - Bug] canonical-urls spec accepted Next root-canonical normalization.**
   Next.js resolves the `/` canonical against `metadataBase` to the bare origin
   `https://braehods.com` (no trailing slash) and cannot be coerced to a
   trailing-slash form via metadata config (verified: both relative `'/'` and an
   absolute `'https://braehods.com/'` emit the bare origin). The W0 spec I authored
   expected `https://braehods.com/`. Fixed the spec (committed in `abd06e3`) to
   accept both bare-origin and trailing-slash forms for `/`; `/about` + `/work`
   stay exact. This matches the Phase 6 precedent of spec-side framework-reality
   fixes (Cat E/F/G in 06-01). NOTE: the must_haves truth line says
   `https://braehods.com/` — flag for verifier; the SEO intent (root canonicalizes
   to the homepage origin) is satisfied.

2. **proxy.ts dropped the unused `_request` param** (RESEARCH Pattern 5 included it
   but it triggered a no-unused-vars warning). Next's proxy function accepts zero
   args; signature is now `export function proxy()`. No behavior change.

## What the HUMAN must do for Task 3 (resume gate)

Per 06-02-PLAN.md Task 3 `<how-to-verify>`:

1. Create `assets/` at repo root (NOT `public/` — it must not be served publicly;
   `assets/` is a build-time source folder).
2. **assets/Fraunces-Bold.ttf** — download the STATIC Bold (weight 700) TTF from
   https://fonts.google.com/specimen/Fraunces (Download family zip -> extract
   `static/Fraunces-Bold.ttf`). ~100-300 KB. Do NOT use the variable .ttf
   (~600KB) — ImageResponse does not support variable fonts at edge.
3. **assets/GeistSans-Bold.ttf** — download from
   https://github.com/vercel/geist-font/releases (fonts zip) -> extract
   `Geist-Bold.ttf` / `GeistSans-Bold.ttf`, rename to `GeistSans-Bold.ttf`.
   ~50-200 KB.
4. **assets/OFL.txt** — copy the SIL OFL 1.1 license text from either font's
   download (one file covers both).
5. Record each TTF's MD5: PowerShell `Get-FileHash -Algorithm MD5 assets/*.ttf`.
6. Confirm `git check-ignore assets/Fraunces-Bold.ttf` exits NON-zero (file tracked).
7. ATOMIC commit (3 files):
   `git add assets/Fraunces-Bold.ttf assets/GeistSans-Bold.ttf assets/OFL.txt`
   `git commit -m "assets(06-02): vendor Fraunces-Bold + GeistSans-Bold TTFs for OG ImageResponse (SIL OFL)"`
   Verify `git log -n 1 --name-only HEAD` lists exactly those 3 paths.

**Resume signal:** `fonts-vendored: fraunces=<MD5-12chars>, geist=<MD5-12chars>`
OR `vendor-failed: <details>`.

## What runs NEXT (after Task 3 resume)

- **T5 (auto)**: `scripts/build-static-og.mjs` -> generate `app/opengraph-image.png`
  (1200×630). Imports MONOGRAM_PATH; reads `assets/Fraunces-Bold.ttf`. UI-SPEC
  Dimension-5 FLAG: try monogram y=276 / wordmark y=444 (multiples of 4) FIRST;
  fall back to spec y=275 / y=445 if optical drift is worse (documented in T11).
  Static `/` OG composition: 96px monogram + 48px gap + 180px Fraunces "Braeden",
  135deg gradient #1a1a1f->#0a0a0a, grain opacity 0.04 (no mix-blend-mode),
  letter-spacing -0.02em.
- **T6 (auto)**: `app/about/opengraph-image.tsx` + `app/work/opengraph-image.tsx`
  (edge runtime, fontSize 128, import MONOGRAM_PATH, fetch BOTH TTFs, monogram at
  (80,80) 64×64, alt "About Braeden Hodson" / "Work — Braeden Hodson").
- **T9 (checkpoint:human-action, blocking)**: push to Vercel preview, run the 8 SEO
  specs + 6 Lighthouse audits (≥95) + regression canaries against the preview URL.
- **T11 (checkpoint:human-verify, blocking)**: 200% zoom + 320px + NVDA + VoiceOver
  iOS + Google Rich Results + OG platform validation + visual-checklist §6/§7.
- **T12 (auto)**: write 06-02-SUMMARY.md + flip REQUIREMENTS.md traceability + STATE.md.

## Cross-plan notes

- Preview URL from 06-01: `https://braeden-site-evecb9bdz-bwaedens-projects.vercel.app`
  (auto-refreshes after each push to `phase-6/audit-preview`; use the latest for T9).
- 06-02 formally depends on 06-01's 3 still-open manual gates (visual sweep / real
  Formspree submit / WebAIM contrast). None of the auto tasks above depended on those
  outcomes; they were executed independently.
- Branch `phase-6/audit-preview` — sequential mode, NO worktree isolation, NO new
  branches (project branching_strategy = "none"). Orchestrator owns the eventual push.
- STATE.md + config.json have pre-existing uncommitted edits from the resume-work
  session (NOT this executor's work) — left untouched.
