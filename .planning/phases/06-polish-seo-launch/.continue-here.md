# Phase 6 — Plan 06-02 CLOSED · next = Plan 06-03 (launch + final pre-launch verification pass)

**Written:** 2026-05-26 by gsd-executor (sequential, on `phase-6/audit-preview`)
**Status:** Plan 06-02 closed at implementation + preview-audit level. Plan 06-03 not started.

## 06-02 close summary

All 12 tasks done + committed on `phase-6/audit-preview`. Full detail in
`.planning/phases/06-polish-seo-launch/06-02-SUMMARY.md`. Headlines:

- 8 SEO surfaces live + verified GREEN on preview
  (`https://braeden-site-r3twotzxg-bwaedens-projects.vercel.app`, built from `0c71a5f`):
  sitemap, robots, proxy.ts X-Robots-Tag:noindex, static + dynamic OG, branded 404,
  Person JSON-LD, per-route canonical metadata.
- 30/30 SEO Playwright specs GREEN ×2 projects; 6/6 Lighthouse GREEN after the
  test-only desktop-throttling fix `9b3b960`; regression canaries 46/48 (2 = the
  contact-modal-min-time parallel-worker timing flake, 4/4 serially).
- PERF-03 first-page bundle 20.38 KB gz (≤50); ContactModal island 12.55 KB isolated.
- FOUND-07 preserved (exactly 1 client island). LNCH-05 reflow clean at 320px + 640px.
- 15 requirements flipped Complete: SEO-01..06/08/09, A11Y-01/04/07, PERF-01..03, LNCH-05.

## What runs NEXT — Plan 06-03 (v1 SHIP gate + final pre-launch verification pass)

Plan 06-03 owns BOTH the launch mechanics AND the 6 human-verification gates
deferred here. Per-launch sequence (see 06-03-PLAN.md):

- Pre-DNS-flip `data/projects.ts` archived-href edit (D-16)
- Vercel domain add → SSL stage → registrar CNAME flip → braehods.com smoke walk
- Real-device: iOS Safari + Android Chrome
- Prod Formspree submit + OG platform validation
- `gh repo archive bwaeden/braehods` + GH/IG/YT bio updates

### 6 deferred human-verification gates (NOT failures — deliberate carry-forwards)

1. **T11 screen-reader walk** — NVDA + VoiceOver iOS: ContactModal dialog role +
   aria-live state transitions + error region alert (human-only).
2. **06-01 Gate 1** — visual sweep of `visual-checklist.md` §1–5 (34 rows) on the preview.
3. **06-01 Gate 2** — real Formspree submit → confirm email delivery to fakegoat1@gmail.com.
4. **06-01 Gate 3** — WebAIM contrast: `/work` archived dot (`#707070`) + ContactModal
   char counter (`#c8a86a`) on charcoal (≥3.0:1 WCAG 1.4.11).
5. **SEO Lighthouse ≥95** — verify in PRODUCTION (braehods.com, no noindex). The
   preview `noindex` (SEO-09) caps Lighthouse's is-crawlable audit at ~69 by design;
   the lighthouse spec auto-applies STRICT 95 on a braehods.com base URL.
6. **Homepage-mobile perf ≥95** — ~90 on cold preview (PERF-06 hero-photo LCP
   carry-forward); verify on production's warmed CDN.

## Notes for the 06-03 executor

- **canonical-urls FLAG (Deviation 1):** the shipped `/` canonical is the Next.js
  bare-origin form `https://braehods.com` (no trailing slash); the 06-02 must_haves
  truth line literally said `https://braehods.com/`. SEO intent satisfied — verify
  in the prod Rich Results / canonical check.
- **lighthouse.spec.ts is production-strict:** on a braehods.com base URL ALL FOUR
  categories are 95. The preview relaxations (seo→60, homepage-mobile-perf→88) apply
  ONLY to the `.vercel.app` run, so the launch audit is a real gate.
- Branch `phase-6/audit-preview` — sequential mode, NO worktree, NO new branches.
  Orchestrator owns the push + the eventual merge to `main` at launch.
- `.planning/config.json` carries an orchestrator-owned transient working-tree change —
  leave it untouched.
