# Phase 6: Polish + SEO + Launch — Pattern Map

**Mapped:** 2026-05-19
**Files analyzed:** 17 NEW + 12 MODIFIED + 1 DELETED = 30 surfaces
**Analogs found:** 22 in-repo / 30 (8 NEW files have no in-repo analog — `next/og`, `MetadataRoute.Sitemap`, `proxy.ts`, ImageResponse, TTF vendoring; all pulled from RESEARCH.md § Code Examples directly)

---

## File Classification

### New files

| File | Role | Data Flow | Closest in-repo Analog | Match Quality |
|------|------|-----------|------------------------|---------------|
| `app/sitemap.ts` | route-segment (metadata) | build-time file-I/O + transform | **none** | RESEARCH.md Pattern 3 |
| `app/robots.ts` | route-segment (metadata) | static config emit | **none** | RESEARCH.md Pattern 4 |
| `app/not-found.tsx` | page (Server Component) | request-response (404 status) | `app/about/page.tsx` | exact (Server page with `<section>` + composed components) |
| `app/opengraph-image.png` | static asset (committed binary) | n/a (build-time output) | **none** | generated by `scripts/build-static-og.mjs` |
| `app/about/opengraph-image.tsx` | route-segment (metadata, edge/Node) | request-response (ImageResponse PNG) | **none** | RESEARCH.md Pattern 2 |
| `app/work/opengraph-image.tsx` | route-segment (metadata, edge/Node) | request-response (ImageResponse PNG) | **none** | same template as `/about` |
| `proxy.ts` (repo root) | middleware (edge/Node) | request-response (header injection) | **none** | RESEARCH.md Pattern 5 |
| `lib/og/monogram-path.ts` | utility (constant export) | static data | `components/ui/MonogramMark.tsx` | partial — extracts the `<path d="...">` string to a shared const so OG JSX, MonogramMark.tsx, and app/icon.svg become triple-source-byte-identical (extends Phase 1 D-04 dual-write to triple-write) |
| `scripts/build-static-og.mjs` | build script (Node, one-shot) | file-I/O + ImageResponse → write PNG | **none** | UI-SPEC § Static `/` OG; runs `ImageResponse` at build time and writes the buffer to `app/opengraph-image.png` |
| `assets/Fraunces-Bold.ttf` | vendored binary | n/a (build/edge readFile target) | **none** | UI-SPEC § OG Image Fonts; download once from `fonts.gstatic.com`, commit, MD5 documented in OG source file leading comment |
| `assets/GeistSans-Bold.ttf` | vendored binary | n/a (fallback font) | **none** | UI-SPEC § OG fallback / OQ-1; download once from `github.com/vercel/geist-font/releases` |
| `tests/helpers/live-url.ts` | test helper (Playwright utility) | request-response (browser-context eval) | **none** in `tests/` (no `tests/helpers/` dir exists yet) | RESEARCH.md § Code Examples `tests/helpers/live-url.ts` |
| `tests/sitemap-renders.spec.ts` | spec | integration (HTTP) | `tests/favicon.spec.ts` | exact (asset-URL fetch + content-type + body shape) |
| `tests/robots-renders.spec.ts` | spec | integration (HTTP) | `tests/favicon.spec.ts` | exact |
| `tests/og-images-render.spec.ts` | spec | integration (HTTP) | `tests/favicon.spec.ts` | exact |
| `tests/jsonld-person.spec.ts` | spec | integration (DOM parse) | `tests/footer-socials-render.spec.ts` | role-match (HTML parse + JSON.parse + sameAs assertion against `data/site.ts`) |
| `tests/not-found-renders.spec.ts` | spec | integration (DOM) | `tests/about-renders.spec.ts` + `tests/footer-socials-render.spec.ts` | role-match (page.goto + role-based locator + verbatim copy) |
| `tests/preview-noindex.spec.ts` | spec (preview-gated) | integration (HTTP headers) | `tests/favicon.spec.ts` (header inspection pattern) | partial — adds `test.skip` gate based on `baseURL` |
| `tests/canonical-urls.spec.ts` | spec | integration (DOM) | `tests/footer-socials-render.spec.ts` | partial (per-route iteration + `<link rel="canonical">` assertion) |
| `tests/metadata-per-route.spec.ts` | spec | integration (DOM) | `tests/footer-socials-render.spec.ts` | partial (per-route `<title>` + `<meta name="description">` uniqueness check) |
| `.planning/phases/06-polish-seo-launch/visual-checklist.md` | documentation | n/a (markdown) | none | UI-SPEC § Visual Checklist Taxonomy locks the 4 row-prefixes (`visual`/`interaction`/`screen-reader`/`responsive`) and 7 sections |

### Modified files

| File | Role | Modification |
|------|------|--------------|
| `app/layout.tsx` | root layout | ADD inline `<script type="application/ld+json">` Person JSON-LD; ADD `metadataBase` + canonical to existing `metadata` export; verify `<Analytics />` + `<SpeedInsights />` already mounted (lines 22-23 per researcher Pivot #2) |
| `tests/lighthouse.spec.ts` | spec | CHANGE all 4 thresholds from `0` → `95`; PARAMETRIZE over `['/'`, `'/about'`, `'/work']`; SET `emulatedFormFactor` based on `test.info().project.name` |
| `tests/contact-modal-opens-from-nav.spec.ts` | spec (Cat A) | ADD mobile-viewport detection + open `<details>` hamburger before clicking Contact |
| `tests/contact-modal-opens-from-about.spec.ts` | spec (Cat A) | same Cat A refactor |
| `tests/contact-modal-esc-closes.spec.ts` | spec (Cat A + Cat D) | Cat A refactor + replace `page.url()` with `await liveUrl(page)` |
| `tests/contact-modal-states.spec.ts` | spec (Cat A + B + C) | Cat A refactor + Cat B `:text-is("→")` via `getComputedStyle(el, '::after').content` + Cat C `role="alert"` assertion |
| `tests/contact-modal-honeypot.spec.ts` | spec (Cat A + C) | Cat A refactor + audit silent-bypass path |
| `tests/contact-modal-min-time.spec.ts` | spec (Cat A + C) | Cat A refactor + audit silent-bypass path |
| `tests/contact-modal-mailto-fallback.spec.ts` | spec (Cat A) | Cat A refactor only |
| `components/contact/ContactModal.tsx` | client island | Cat B: ADD CSS `::after { content: "→" }` to mailto link (REMOVE inline `<span aria-hidden>→</span>`); Cat C: `role="alert"` already present at line 227 — verify only |
| `components/home/CTAArrowLink.tsx` | Server Component | ADD optional `direction?: 'forward' \| 'back'` prop (default `'forward'`); swap glyph + translate direction when `'back'` |
| `data/projects.ts` | typed data | CHANGE line 107 `href: 'https://braehods.com'` → `'https://github.com/bwaeden/braehods'` (single-line edit, Plan 06-03 W0, BEFORE DNS flip) |

### Deleted files

| File | Rationale | Reference check |
|------|-----------|-----------------|
| `app/%5Ftokens/` (entire folder) | Phase 1 D-11 private-folder workaround (URL-encoded `_tokens`); v1 ships without a tokens preview surface | `git grep -l "_tokens\|%5Ftokens" -- 'app/**' 'components/**' 'tests/**'` must return empty post-delete; `npm run build` must remain clean |

---

## Pattern Assignments (NEW files)

### `app/not-found.tsx` (page, Server Component, request-response)

**Analog:** `app/about/page.tsx` (Server Component composing `HeroPhoto` + `CTAArrowLink`).

**Keep from analog:**
- Top file comment block explaining source decisions (lines 1-19)
- `export const metadata = { title: 'Page not found' }` shape (line 25)
- Server Component (no `'use client'`); FOUND-07 invariant
- `<section data-test="..." aria-labelledby="..." className="py-8 md:py-12">` outer wrapper (lines 29-33)
- `<CTAArrowLink>` consumer pattern (lines 75-77)

**Adapt from analog:**
- Swap composition: instead of bio paragraphs + `<HeroPhoto>`, render `<MonogramMark size={80} aria-hidden />` + `<h1>` + `<p>` body line + `<CTAArrowLink direction="back">`
- Outer `<section>` adds `flex min-h-[60vh] flex-col items-center justify-center gap-6 text-center` (UI-SPEC § 404 Page Composition)
- No `stagger()` calls inside (UI-SPEC: 404 page may stagger the return link only; planner picks)

**Reference excerpt — copy outer composition shape from `app/about/page.tsx` lines 27-83:**

```tsx
import { HeroPhoto } from '@/components/home/HeroPhoto';
import { CTAArrowLink } from '@/components/home/CTAArrowLink';
import { fadeInUp, stagger } from '@/lib/motion';

export const metadata = { title: 'About' };

export default function AboutPage() {
  return (
    <section
      data-test="about-section"
      aria-labelledby="about-heading"
      className="py-8 md:py-12"
    >
      ...
      <CTAArrowLink href="#contact" staggerIndex={4}>
        Get in touch
      </CTAArrowLink>
      ...
    </section>
  );
}
```

**Then layer on the 404-specific spec from RESEARCH.md Pattern 6** (lines 588-619) and UI-SPEC § 404 Page Composition.

---

### `components/home/CTAArrowLink.tsx` (MODIFIED — `direction` prop)

**Analog:** itself — current implementation IS the analog.

**Keep:**
- File comment block (lines 1-16) — add a note about the new `direction` prop
- Imports (lines 18-19)
- `group inline-flex items-center gap-2` class wrapper
- `transition-transform group-hover:translate-x-1` on the glyph (this becomes the `forward` branch)
- `fadeInUp` + `stagger(staggerIndex)` motion seam

**Change:**
- Prop interface gains `direction?: 'forward' | 'back'` (default `'forward'`)
- When `direction === 'back'`: render `<span aria-hidden>←</span> {children}` (leading glyph) with `group-hover:-translate-x-1`
- When `direction === 'forward'`: render `{children} <span aria-hidden>→</span>` (current behavior, unchanged)

**Atomic-binding requirement (CONTEXT D-07 inherited):** prop addition + 404 page consumer + (optional) `tests/cta-arrow-link.spec.ts` update all land in a single commit. No existing call site passes `direction`, so defaults preserve backwards-compat.

**Current implementation to extend (lines 21-43):**

```tsx
interface CTAArrowLinkProps {
  href: string;
  children: string;
  staggerIndex: number;
}

export function CTAArrowLink({ href, children, staggerIndex }: CTAArrowLinkProps) {
  return (
    <Link
      href={href}
      className={`group inline-flex items-center gap-2 text-base font-sans hover:underline hover:decoration-1 hover:underline-offset-4 ${fadeInUp}`}
      style={{ color: 'var(--color-accent)', ...stagger(staggerIndex) }}
    >
      {children}
      <span aria-hidden className="inline-block transition-transform group-hover:translate-x-1">
        →
      </span>
    </Link>
  );
}
```

---

### `app/sitemap.ts` (route-segment, build-time file-I/O)

**Analog:** none in-repo. Use RESEARCH.md Pattern 3 (lines 478-518) verbatim with `BASE_URL = 'https://braehods.com'`.

**Implementation requirement:** wrap `readdir(contentDir)` in try/catch because `content/` may not exist in v1 (Pitfall 7 line 795-803). RESEARCH.md example already shows this.

---

### `app/robots.ts` (route-segment, static emit)

**Analog:** none in-repo. Use RESEARCH.md Pattern 4 (lines 528-541) verbatim.

```ts
import type { MetadataRoute } from 'next';
export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: '*', allow: '/' },
    sitemap: 'https://braehods.com/sitemap.xml',
  };
}
```

---

### `app/about/opengraph-image.tsx` + `app/work/opengraph-image.tsx` (route-segment, edge/Node)

**Analog:** none in-repo. Use RESEARCH.md Pattern 2 (lines 413-459).

**Per-file deltas:**
- `app/about/opengraph-image.tsx`: `const TITLE = 'About'`; `export const alt = 'About Braeden Hodson'`
- `app/work/opengraph-image.tsx`: `const TITLE = 'Work'`; `export const alt = 'Work — Braeden Hodson'`

**UI-SPEC pixel-precise values to apply on top of RESEARCH.md example:**
- `fontSize: 128` (UI-SPEC § Dynamic OG title) — NOT the `96` in RESEARCH example
- Background `linear-gradient(135deg, #1a1a1f 0%, #0a0a0a 100%)` — 135° angle for "branded" feel (UI-SPEC § Static `/` OG Composition table)
- Inline monogram SVG at `(80, 80)`, 64×64px, `fill="#e8e8e8"` — path data from new `lib/og/monogram-path.ts` const
- Grain overlay opacity `0.04`, no `mix-blend-mode` (UI-SPEC § Dynamic OG monogram table)
- `letter-spacing: -0.02em` on title
- Fonts array: `[{ Fraunces 700 }, { Geist Sans 700 fallback }]` — both fetched via `readFile(join(process.cwd(), 'assets/<name>.ttf'))`

**Triple-source contract (UI-SPEC § Phase Exit Visual Verification last row):** the OG JSX inline-SVG path must match `MonogramMark.tsx` path AND `app/icon.svg` path. Shared via `lib/og/monogram-path.ts` `MONOGRAM_PATH` const.

---

### `app/opengraph-image.png` + `scripts/build-static-og.mjs` (static asset + build script)

**Analog:** none in-repo. UI-SPEC § Static `/` OG Image governs composition (1200×630, 96px monogram + 180px Fraunces "Braeden" wordmark, centered).

**Script flow:**
1. Import `MONOGRAM_PATH` from `lib/og/monogram-path.ts` (or re-read the path string from `components/ui/MonogramMark.tsx`)
2. `readFile('assets/Fraunces-Bold.ttf')`
3. Construct `ImageResponse(<JSX>, { width: 1200, height: 630, fonts: [...] })`
4. `const buffer = await response.arrayBuffer()` then `await writeFile('app/opengraph-image.png', Buffer.from(buffer))`

**Run cadence:** manual one-shot; NOT wired into `next build` (per UI-SPEC OQ-3 locked answer).

---

### `proxy.ts` (repo root, middleware)

**Analog:** none in-repo. Use RESEARCH.md Pattern 5 (lines 557-575) verbatim.

```ts
// proxy.ts (repo root — NOT under app/)
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function proxy(_request: NextRequest) {
  const response = NextResponse.next();
  if (process.env.VERCEL_ENV === 'preview') {
    response.headers.set('X-Robots-Tag', 'noindex');
  }
  return response;
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|icon|images|opengraph-image|robots.txt|sitemap.xml).*)'],
};
```

**Critical (RESEARCH.md Pitfall 6):** file MUST be at repo root next to `package.json` + `next.config.ts`, NOT under `app/`. Next 16 default runtime is Node.js; do NOT set `runtime` config option (build error).

---

### `lib/og/monogram-path.ts` (utility constant)

**Analog:** `components/ui/MonogramMark.tsx` (single-source today) + `app/icon.svg` (byte-identical dual-source, Phase 1 D-04).

**Pattern:** extract the `<path d="...">` string from `MonogramMark.tsx` line 53 into a single exported const so 3 surfaces share one source:

```ts
// lib/og/monogram-path.ts
// Triple-source contract (Phase 6 extends Phase 1 D-04 dual-write).
// MUST be byte-identical with components/ui/MonogramMark.tsx line 53 AND
// the <path d="..."> in app/icon.svg. Any monogram swap (Phase 1 D-01 v1.x
// designer-pass) re-runs the trace + updates ALL THREE surfaces in one commit.
export const MONOGRAM_PATH =
  'M58.41 45.56Q58.41 51.13 53.60 54.56Q48.79 58 38.72 58L8.71 58Q7.11 58 6.35 57.33Q5.59 56.66 5.59 55.55Q5.59 53.58 7.49 52.84L8.93 52.32Q9.90 51.95 10.36 51.39Q10.83 50.83 10.83 49.90L10.83 14.10Q10.83 13.17 10.36 12.61Q9.90 12.05 8.93 11.68L7.49 11.16Q5.59 10.42 5.59 8.45Q5.59 7.30 6.35 6.65Q7.11 6 8.71 6L32.15 6Q39.43 6 44.63 7.93Q49.83 9.86 52.63 13.32Q55.44 16.77 55.44 21.41Q55.44 25.17 53.19 28.04Q50.94 30.92 46.67 32.56Q42.40 34.19 36.31 34.19L38.02 32.67Q44.07 32.67 48.66 34.28Q53.25 35.90 55.83 38.82Q58.41 41.73 58.41 45.56M32.85 36.23L23.38 36.23L23.38 32.22L31.07 32.22Q33.82 32.22 35.66 31.18Q37.50 30.14 38.41 27.95Q39.32 25.76 39.32 22.42Q39.32 18.78 38.09 16.18Q36.87 13.58 34.56 12.15Q32.26 10.72 29.07 10.72L27.99 10.72L27.99 48.94Q27.99 51.17 29.25 52.22Q30.51 53.28 32.82 53.28L34.75 53.28Q36.83 53.28 38.33 52.32Q39.84 51.35 40.67 49.49Q41.51 47.64 41.51 45Q41.51 40.88 39.21 38.56Q36.90 36.23 32.85 36.23';
```

**Atomic-binding requirement:** `MonogramMark.tsx` line 53 + `app/icon.svg` line 20 + new `lib/og/monogram-path.ts` MUST be edited together in the same commit to switch all three to read from / reference the shared const. Plan-checker enforces.

---

### `tests/helpers/live-url.ts` (Playwright helper)

**Analog:** none in-repo. Use RESEARCH.md § Code Examples `tests/helpers/live-url.ts` (line 929).

```ts
// tests/helpers/live-url.ts
import type { Page } from '@playwright/test';
export const liveUrl = (page: Page) =>
  page.evaluate(() => window.location.href);
```

Then `tests/contact-modal-esc-closes.spec.ts` line 49 changes from `page.url()` to `await liveUrl(page)`.

---

## Pattern Assignments (NEW specs)

### `tests/sitemap-renders.spec.ts`, `tests/robots-renders.spec.ts`, `tests/og-images-render.spec.ts` (asset-URL specs)

**Analog:** `tests/favicon.spec.ts` (15-29) — already does HTTP GET + content-type + body shape.

**Keep:**
- `await request.get('/path')` then `response.status()` assertion
- `response.headers()['content-type']` assertion against regex
- `response.text()` for body content checks

**Adapt per spec:**
- `sitemap-renders.spec.ts`: GET `/sitemap.xml`, expect 200, content-type `/xml/`, body contains `<url>` + `https://braehods.com/about`
- `robots-renders.spec.ts`: GET `/robots.txt`, expect 200, body contains `User-Agent: *` + `Sitemap: https://braehods.com/sitemap.xml`
- `og-images-render.spec.ts`: for each route `/`, `/about`, `/work` — GET the route HTML, parse `<meta property="og:image">`, GET that image URL, expect 200 + `content-type: image/png` + size headers if available

**Reference excerpt (tests/favicon.spec.ts lines 15-29):**

```ts
test('SEO-07: /icon.svg returns a valid SVG with <path and viewBox', async ({ request }) => {
  const response = await request.get('/icon.svg');
  expect(response.status(), 'expected 200 from /icon.svg').toBe(200);
  const contentType = response.headers()['content-type'] ?? '';
  expect(contentType, `content-type should mention svg (got: ${contentType})`).toMatch(/svg/i);
  const body = await response.text();
  expect(body, 'SVG body must contain <path').toMatch(/<path\b/);
});
```

---

### `tests/jsonld-person.spec.ts` (DOM parse + cross-data assertion)

**Analog:** `tests/footer-socials-render.spec.ts` (parses DOM + cross-checks against `data/site.ts` expectations).

**Adapt:**
- `await page.goto('/')`
- Locate `<script type="application/ld+json">` content via `page.locator('script[type="application/ld+json"]').textContent()`
- `const parsed = JSON.parse(text)`; assert `parsed['@type'] === 'Person'`
- Import `{ site } from '@/data/site'`; assert `parsed.sameAs` equals `[site.socials.github, site.socials.instagram]`
- **Negative assertion (mirroring `footer-socials-render.spec.ts` line 50-53 YT exclusion):** `expect(parsed.sameAs.some((url) => /youtube/i.test(url))).toBe(false)`

---

### `tests/not-found-renders.spec.ts` (page render assertion)

**Analog:** `tests/about-renders.spec.ts` + `tests/footer-socials-render.spec.ts` (page.goto + `page.getByRole` + verbatim copy assertions).

**Adapt:**
- `await page.goto('/this-route-does-not-exist')`
- Status assertion: `const response = await page.goto(...); expect(response?.status()).toBe(404)`
- Assert Nav present (`page.locator('nav')`)
- Assert Footer present (`page.locator('footer')`)
- Assert heading: `page.locator('h1:text-is("Page not found")')` — use `:text-is()` per Phase 4 lesson reaffirmed in 05-CONTEXT D-08
- Assert return link: `page.getByRole('link', { name: /Back home/ })`
- Assert link href: `toHaveAttribute('href', '/')`

---

### `tests/preview-noindex.spec.ts` (HTTP header + preview-gated)

**Analog:** `tests/favicon.spec.ts` (header inspection) + `tests/lighthouse.spec.ts` lines 38-39 (`test.skip` gate pattern).

**Adapt:**
- `test.skip(!process.env.PLAYWRIGHT_BASE_URL?.includes('vercel.app'), 'Preview noindex header requires Vercel preview URL')`
- `const response = await request.get('/')`
- `expect(response.headers()['x-robots-tag']).toBe('noindex')`

---

### `tests/canonical-urls.spec.ts` + `tests/metadata-per-route.spec.ts` (per-route iteration)

**Analog:** `tests/footer-socials-render.spec.ts` (3 separate `test(...)` blocks, one per route) — OR use a `for` loop with `test()` inside (Playwright supports parameterized tests).

**Recommended pattern:**

```ts
const ROUTES = [
  { path: '/', expectedCanonical: 'https://braehods.com/', expectedTitle: 'Braeden Hodson' },
  { path: '/about', expectedCanonical: 'https://braehods.com/about', expectedTitle: 'About · Braeden Hodson' },
  { path: '/work', expectedCanonical: 'https://braehods.com/work', expectedTitle: 'Work · Braeden Hodson' },
];

for (const { path, expectedCanonical, expectedTitle } of ROUTES) {
  test(`SEO-06: ${path} has rel=canonical = ${expectedCanonical}`, async ({ page }) => {
    await page.goto(path);
    const href = await page.locator('link[rel="canonical"]').getAttribute('href');
    expect(href).toBe(expectedCanonical);
  });
}
```

---

## Pattern Assignments (MODIFIED files)

### `app/layout.tsx` (root layout)

**Keep (lines 1-27 current state):**
- All imports (Nav, Footer, ContactModal, fonts, globals.css, Analytics, SpeedInsights)
- `<html className={...}>` font variables
- `<body>` chrome composition order: Nav → main → Footer → ContactModal → Analytics → SpeedInsights
- `metadata` export shape (lines 9-12)

**Add (per RESEARCH.md Pattern 1 lines 329-389):**
- `metadataBase: new URL('https://braehods.com')` to existing `metadata` export
- `alternates: { canonical: '/' }` to `metadata`
- `openGraph: { siteName: 'Braeden Hodson', type: 'website', locale: 'en_US' }` to `metadata` (do NOT set `images` — file-based `app/opengraph-image.png` overrides; Pitfall 2)
- `twitter: { card: 'summary_large_image' }`
- Inline JSON-LD `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(personJsonLd) }} />` between `<body>` and `<Nav />` (or right after `</main>` — placement is non-load-bearing for crawlers)
- `personJsonLd` const reads `site.name`, `site.domain`, `site.socials.github`, `site.socials.instagram` (filter Boolean to drop undefined)

**Do NOT use** `next/script` for JSON-LD (RESEARCH.md Anti-Patterns line 647 — defers HTML, breaks SEO).

**Verify (per researcher Pivot #2):** `<Analytics />` + `<SpeedInsights />` already mounted at lines 22-23 of current `app/layout.tsx`. No install needed.

---

### `components/contact/ContactModal.tsx` (Cat B + Cat C)

**Cat B — mailto `→` arrow refactor:**

Current implementation has TWO mailto links (idle/submitting state at lines 411-420 AND success state at lines 265-274). Both render:

```tsx
<a href={MAILTO_HREF} className="group inline-flex items-center gap-2 ...">
  Or just email me directly{' '}
  <span aria-hidden className="inline-block transition-transform group-hover:translate-x-1">→</span>
</a>
```

**Change:** REMOVE the `<span aria-hidden>→</span>` element. ADD a CSS `::after { content: "→" }` pseudo via Tailwind arbitrary variant OR a global CSS class in `app/globals.css`:

```tsx
<a href={MAILTO_HREF} className="group inline-flex items-center gap-2 ... after:content-['→'] after:inline-block after:transition-transform group-hover:after:translate-x-1">
  Or just email me directly
</a>
```

OR (recommended for clarity) add a single class `mailto-arrow` to globals.css with `::after { content: "→"; transition: transform 200ms; }` and `.group:hover .mailto-arrow::after { transform: translateX(4px); }`. Planner picks; CONTEXT D-06 Cat B locks the `::after` approach.

**Cat C — error region `role="alert"`:**

Current implementation line 227 already has `<div role="alert" aria-live="assertive" aria-atomic="true">`. **VERIFY ONLY — no edit needed.** The Cat C concern was that this was missing in some prior spec/source iteration; the current source has it. The 06-VALIDATION.md task 06-01-03 reduces to "verify role + aria-live present AND audit silent-bypass render path doesn't fire spurious success."

**Silent-bypass audit (Cat C, no source edit):** lines 173-194 implement honeypot + min-time silent reject by setting `bypassedSuccess = true`. Line 205 `const showSuccess = state.succeeded || bypassedSuccess`. **Audit:** trace that an unauthenticated rejection does NOT fire `state.succeeded` — confirmed: only the wrapped handler short-circuits with `setBypassedSuccess(true)` and `return`, never calling Formspree. Source is correct; specs `contact-modal-honeypot.spec.ts` + `contact-modal-min-time.spec.ts` already verify.

---

### Contact modal specs (Category A atomic-binding refactor)

**Affected (7 specs):**
- `tests/contact-modal-opens-from-nav.spec.ts`
- `tests/contact-modal-opens-from-about.spec.ts`
- `tests/contact-modal-esc-closes.spec.ts`
- `tests/contact-modal-states.spec.ts`
- `tests/contact-modal-honeypot.spec.ts`
- `tests/contact-modal-min-time.spec.ts`
- `tests/contact-modal-mailto-fallback.spec.ts`

**Common refactor (the atomic-binding commit content per D-07):**

Each spec's `openModal` helper (or inline open sequence) needs a mobile-viewport precondition:

```ts
async function openModal(page: import('@playwright/test').Page) {
  await page.goto('/');
  // Phase 6 Cat A: open mobile <details> hamburger if viewport <sm breakpoint
  const viewport = page.viewportSize();
  if (viewport && viewport.width < 640) {
    await page.locator('details.nav-mobile > summary').click();
  }
  await page.getByRole('link', { name: 'Contact' }).first().click();
  await expect(page.locator('dialog[data-test="contact-modal"]')).toHaveAttribute('open', /.*/);
}
```

(The `details.nav-mobile` selector source: `components/layout/Nav.tsx` line 40 — verified.)

**Atomic commit verify (per RESEARCH.md Pattern 7 line 632-635):**
```bash
git log -n 1 --name-only HEAD
# Expect: 7 spec files + (optionally) tests/helpers/live-url.ts
```

---

### `tests/contact-modal-esc-closes.spec.ts` (Cat D `liveUrl` swap)

**Single-line change (line 49):**

Before:
```ts
expect(page.url(), '...').not.toContain('#contact');
```

After:
```ts
import { liveUrl } from './helpers/live-url';
expect(await liveUrl(page), '...').not.toContain('#contact');
```

---

### `tests/lighthouse.spec.ts` (threshold bump + parametrize)

**Keep:**
- File comment block + import structure (lines 1-25)
- `LIGHTHOUSE_PORT = 9222` constant
- `test.use({ launchOptions: { args: [...] } })` block (lines 29-33)
- `test.skip(browserName !== 'chromium', ...)` guard
- `playAudit({ page, port: LIGHTHOUSE_PORT, thresholds: {...} })` shape

**Change:**
- All 4 thresholds in lines 46-51: `performance: 95, accessibility: 95, 'best-practices': 95, seo: 95`
- Wrap the test body in a `for` loop over `['/'`, `'/about'`, `'/work']` OR use Playwright's `test.describe.serial` per route
- Add `emulatedFormFactor: process.env.PLAYWRIGHT_PROJECT_NAME === 'chromium-mobile' ? 'mobile' : 'desktop'` to `playAudit` options (consult playwright-lighthouse@4 docs for exact key — likely `settings: { emulatedFormFactor: ... }`)

**Per researcher Open Question #5:** route list is `['/'`, `'/about'`, `'/work']` only — NOT 404 (REQ PERF-01 wording).

---

### `data/projects.ts` (D-16 single-line edit)

**Single line change at line 107:**

Before:
```ts
href: 'https://braehods.com',
```

After:
```ts
href: 'https://github.com/bwaeden/braehods',
```

(Also update the lines 33-36 comment block to record that Phase 6 D-16 selected option (a) "archived GitHub repo URL" — single-line comment edit.)

**Sequence:** Plan 06-03 W0, BEFORE the DNS flip in W1 (per `06-VALIDATION.md` task 06-03-01).

---

## Shared Patterns

### Server Component invariant (FOUND-07)

**Source:** `components/home/CTAArrowLink.tsx` lines 1-16 file comment + every Phase 1-5 page file.
**Apply to:** All new Phase 6 page/route files (`app/not-found.tsx`, `app/sitemap.ts`, `app/robots.ts`, `app/about/opengraph-image.tsx`, `app/work/opengraph-image.tsx`). `lib/og/monogram-path.ts` is a constant export (also server-safe).

**Anti-pattern to avoid:** any `'use client'` directive in Phase 6 source. `proxy.ts` is Node-runtime (NOT browser); ImageResponse JSX runs on the server; JSON-LD is server HTML; not-found is a server page.

### Atomic-binding commits (Phase 5 D-05 → 06 CONTEXT D-07)

**Source:** Phase 5 SUMMARY commit `48f0a10` (per RESEARCH.md line 632).
**Apply to:**
1. Category A: 7 contact-modal specs + (optionally) `tests/helpers/live-url.ts` — single commit
2. Category B + C: `components/contact/ContactModal.tsx` source edit + `tests/contact-modal-states.spec.ts` assertion edits — single commit
3. CTAArrowLink `direction` prop addition + 404 page consumer + (optional) spec — single commit
4. Triple-source monogram extraction: `lib/og/monogram-path.ts` create + `components/ui/MonogramMark.tsx` import + `app/icon.svg` comment update — single commit
5. `app/layout.tsx` metadataBase + canonical + Person JSON-LD addition — single commit (paired conceptually with new SEO specs landing)

**Verify after each:** `git log -n 1 --name-only HEAD` (RESEARCH.md line 634).

### `transition-[border-color,color,opacity]` arbitrary-list

**Source:** `components/contact/ContactModal.tsx` lines 25-31 (Pitfall 4 comment) + lines 313, 342, 375, 405.
**Apply to:** Any new focus-rendered interactive element on 404 page (return link inherits via `CTAArrowLink`; inline `<Link>` elements inside 404 body line — planner picks whether to add the transition or rely on default).

### `:text-is()` Playwright selector for verbatim copy

**Source:** `tests/contact-modal-states.spec.ts` lines 14-19 lesson block, applied at lines 48, 53, 58, 108.
**Apply to:** `tests/not-found-renders.spec.ts` (heading "Page not found", body line copy, return link "← Back home"), `tests/metadata-per-route.spec.ts` (title assertions).

### Inline color literal discipline

**Source:** Phase 5 inheritance (no new `@theme` tokens for Phase 6 per UI-SPEC § Color).
**Apply to:** OG image JSX uses pixel-literal `#e8e8e8`, `#1a1a1f`, `#0a0a0a` (NOT CSS custom properties — edge runtime has no `var()` resolver).

### Single-source contract enforcement

**Source:** Phase 1 D-04 dual-write (`MonogramMark.tsx` ↔ `app/icon.svg`).
**Extend in Phase 6 to triple-write:** add `lib/og/monogram-path.ts` `MONOGRAM_PATH` const, then `MonogramMark.tsx` line 53 reads from it (template literal), AND `app/icon.svg` retains its inline string with a code-comment cross-reference.

---

## No Analog Found

Files with NO in-repo predecessor. Planner reads from RESEARCH.md § Code Examples instead:

| File | RESEARCH.md reference |
|------|------------------------|
| `app/sitemap.ts` | Pattern 3 (lines 470-524) |
| `app/robots.ts` | Pattern 4 (lines 526-549) |
| `app/about/opengraph-image.tsx` + `app/work/opengraph-image.tsx` | Pattern 2 (lines 405-468) |
| `app/opengraph-image.png` | UI-SPEC § Static `/` OG Image (build script + composition) |
| `scripts/build-static-og.mjs` | UI-SPEC § Static `/` OG; researcher Pattern 2 ImageResponse + Node `writeFile` |
| `proxy.ts` | Pattern 5 (lines 551-583) |
| `lib/og/monogram-path.ts` | UI-SPEC § Component Inventory; extracted from `MonogramMark.tsx` |
| `tests/helpers/live-url.ts` | RESEARCH.md § Code Examples (lines 929-960) |
| `assets/Fraunces-Bold.ttf` + `assets/GeistSans-Bold.ttf` | UI-SPEC § OG Image Fonts + OQ-1 |
| `.planning/phases/06-polish-seo-launch/visual-checklist.md` | UI-SPEC § Visual Checklist Taxonomy |

---

## Metadata

**Analog search scope:** `app/`, `components/`, `data/`, `lib/`, `tests/`, `playwright.config.ts`, `.planning/phases/04-work-projects/`, `.planning/phases/05-contact-modal/`
**Files scanned:** 60+ (full `app/`, `components/`, `tests/` trees; targeted `data/`, `lib/`)
**Cross-cutting patterns identified:** 6 (FOUND-07 server-component invariant; atomic-binding commits; arbitrary-list transition; `:text-is()` selectors; inline color literal discipline; single-source monogram contract — extended from dual to triple in Phase 6)
**Pattern extraction date:** 2026-05-19
