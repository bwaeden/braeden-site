---
phase: 06-polish-seo-launch
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/sitemap-renders.spec.ts
  - tests/robots-renders.spec.ts
  - tests/og-images-render.spec.ts
  - tests/jsonld-person.spec.ts
  - tests/not-found-renders.spec.ts
  - tests/preview-noindex.spec.ts
  - tests/canonical-urls.spec.ts
  - tests/metadata-per-route.spec.ts
  - tests/lighthouse.spec.ts
  - scripts/measure-first-page-bundle.mjs
  - scripts/build-static-og.mjs
  - assets/Fraunces-Bold.ttf
  - assets/GeistSans-Bold.ttf
  - lib/og/monogram-path.ts
  - app/sitemap.ts
  - app/robots.ts
  - app/not-found.tsx
  - app/opengraph-image.png
  - app/about/opengraph-image.tsx
  - app/work/opengraph-image.tsx
  - proxy.ts
  - app/layout.tsx
  - app/page.tsx
  - app/about/page.tsx
  - app/work/page.tsx
  - components/home/CTAArrowLink.tsx
  - components/ui/MonogramMark.tsx
  - app/icon.svg
  - .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md
autonomous: false
requirements: [SEO-01, SEO-02, SEO-03, SEO-04, SEO-05, SEO-06, SEO-08, SEO-09, A11Y-01, A11Y-04, A11Y-07, PERF-01, PERF-02, PERF-03, LNCH-05]

must_haves:
  truths:
    - "8 new SEO Playwright specs are GREEN on both chromium-mobile + chromium-desktop projects against the preview URL."
    - "`/sitemap.xml` returns 200 + content-type application/xml + body contains <url> entries for `/`, `/about`, `/work`."
    - "`/robots.txt` returns 200 + body contains `User-Agent: *` + `Sitemap: https://braehods.com/sitemap.xml`."
    - "Person JSON-LD in `app/layout.tsx` parses as valid JSON, has `@type === 'Person'`, and `sameAs` equals `[site.socials.github, site.socials.instagram]` (NO YouTube URL)."
    - "Person JSON-LD passes Google Rich Results Test on the preview URL."
    - "Each route (`/`, `/about`, `/work`) has a unique `<title>`, meta description ≤160 chars, and `<link rel=\"canonical\">` matching the expected braehods.com URL."
    - "`/this-route-does-not-exist` returns HTTP 404 + renders Nav + Footer + 80px monogram + Fraunces \"Page not found\" heading + body line + `← Back home` return link."
    - "`/about/opengraph-image` and `/work/opengraph-image` return 1200×630 PNG via `next/og` ImageResponse with Fraunces 128px title."
    - "`/` serves a static 1200×630 `/opengraph-image.png` generated by `scripts/build-static-og.mjs`."
    - "`proxy.ts` at repo root sets `X-Robots-Tag: noindex` on responses when `VERCEL_ENV=preview`."
    - "Lighthouse audits on `/`, `/about`, `/work` × mobile+desktop all score ≥95 across Performance, Accessibility, Best Practices, SEO (6 audits total)."
    - "First-page client JS bundle (excluding ContactModal chunk) measures ≤50KB gzipped via `next experimental-analyze`."
    - "NVDA + VoiceOver iOS walks announce dialog role + aria-live state transitions + error region alert on /, /about, /work, and ContactModal."
    - "200% zoom + 320px viewport produce no horizontal scroll on `/`, `/about`, `/work`."
    - "CTAArrowLink `direction='back'` prop renders `<span aria-hidden>←</span>` BEFORE children with `group-hover:-translate-x-1`; existing forward callers unaffected."
    - "MONOGRAM_PATH const in `lib/og/monogram-path.ts` is byte-identical with `<path d>` in `components/ui/MonogramMark.tsx` and `app/icon.svg` (triple-source contract)."
  artifacts:
    - path: "app/sitemap.ts"
      provides: "Dynamic MetadataRoute.Sitemap covering /, /about, /work + content/*.mdx glob"
      exports: ["default"]
    - path: "app/robots.ts"
      provides: "MetadataRoute.Robots with allow:/ + sitemap reference"
      exports: ["default"]
    - path: "app/not-found.tsx"
      provides: "Branded 404 Server Component with monogram + heading + body + CTAArrowLink direction='back'"
      exports: ["default", "metadata"]
    - path: "app/opengraph-image.png"
      provides: "Static 1200×630 OG for `/` generated by scripts/build-static-og.mjs"
      contains: "PNG binary, RGB no-alpha"
    - path: "app/about/opengraph-image.tsx"
      provides: "Dynamic /about OG via next/og ImageResponse"
      exports: ["default", "alt", "size", "contentType", "runtime"]
    - path: "app/work/opengraph-image.tsx"
      provides: "Dynamic /work OG via next/og ImageResponse"
      exports: ["default", "alt", "size", "contentType", "runtime"]
    - path: "proxy.ts"
      provides: "Edge middleware setting X-Robots-Tag noindex on Vercel preview deploys"
      exports: ["proxy", "config"]
    - path: "lib/og/monogram-path.ts"
      provides: "Shared MONOGRAM_PATH string for triple-source byte-identical monogram (MonogramMark.tsx + app/icon.svg + OG JSX)"
      exports: ["MONOGRAM_PATH"]
    - path: "scripts/build-static-og.mjs"
      provides: "One-shot Node script generating app/opengraph-image.png via ImageResponse"
    - path: "scripts/measure-first-page-bundle.mjs"
      provides: "One-shot Node script reading `.next/diagnostics/analyze/client.html` and reporting first-page bundle gz size excluding ContactModal chunk"
    - path: "assets/Fraunces-Bold.ttf"
      provides: "Vendored TTF for OG ImageResponse Fraunces 700 rendering"
    - path: "assets/GeistSans-Bold.ttf"
      provides: "Vendored TTF for OG ImageResponse Geist Sans fallback rendering"
  key_links:
    - from: "app/layout.tsx Person JSON-LD"
      to: "data/site.ts socials.github + socials.instagram"
      via: "import { site } from '@/data/site'"
      pattern: "sameAs.*\\[.*github.*instagram"
    - from: "app/sitemap.ts"
      to: "https://braehods.com/<route>"
      via: "BASE_URL const + readdir('content/')"
      pattern: "https://braehods\\.com"
    - from: "proxy.ts"
      to: "Vercel preview response headers"
      via: "NextResponse.next() + headers.set('X-Robots-Tag', 'noindex')"
      pattern: "X-Robots-Tag.*noindex"
    - from: "app/about/opengraph-image.tsx + app/work/opengraph-image.tsx"
      to: "lib/og/monogram-path.ts MONOGRAM_PATH"
      via: "import { MONOGRAM_PATH } from '@/lib/og/monogram-path'"
      pattern: "MONOGRAM_PATH"
    - from: "components/home/CTAArrowLink.tsx (direction='back')"
      to: "app/not-found.tsx return link"
      via: "<CTAArrowLink href='/' direction='back' staggerIndex={0}>← Back home</CTAArrowLink>"
      pattern: "direction=['\"]back['\"]"
---

<objective>
Ship every Phase 6 SEO + A11Y + PERF audit gate on the deployed preview URL. Adds 8 new SEO surfaces (sitemap, robots, OG images × 3, 404, proxy.ts, JSON-LD, per-route metadata), vendors 2 TTFs for `next/og`, runs 6 Lighthouse audits at ≥95 threshold, measures first-page bundle ≤50KB gz, and conducts NVDA + VoiceOver iOS screen-reader walks.

Purpose: Close every SEO requirement (SEO-01..06, SEO-08, SEO-09), every A11Y audit requirement (A11Y-01, A11Y-04, A11Y-07), every PERF gate (PERF-01, PERF-02, PERF-03), and the 320px viewport overflow requirement (LNCH-05) — all measured against a real Vercel CDN.

Output: 8 new spec files + 3 SEO route files + 2 OG image route segments + static OG PNG + proxy.ts + monogram-path shared const + CTAArrowLink direction prop + Person JSON-LD + per-route metadata. 06-02-SUMMARY.md documenting all 6 Lighthouse scores, bundle measurement, SR walk results, Google Rich Results outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-seo-launch/06-CONTEXT.md
@.planning/phases/06-polish-seo-launch/06-RESEARCH.md
@.planning/phases/06-polish-seo-launch/06-PATTERNS.md
@.planning/phases/06-polish-seo-launch/06-UI-SPEC.md
@.planning/phases/06-polish-seo-launch/06-VALIDATION.md
@.planning/phases/06-polish-seo-launch/06-01-SUMMARY.md
@app/layout.tsx
@app/page.tsx
@app/about/page.tsx
@app/work/page.tsx
@components/home/CTAArrowLink.tsx
@components/ui/MonogramMark.tsx
@app/icon.svg
@data/site.ts
@tests/lighthouse.spec.ts
@tests/favicon.spec.ts
@tests/footer-socials-render.spec.ts
@tests/about-renders.spec.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From data/site.ts:
- `site.socials.github`: string URL (e.g., 'https://github.com/bwaeden')
- `site.socials.instagram`: string URL (e.g., 'https://instagram.com/...')
- `site.name`: string (e.g., 'Braeden Hodson')
- `site.domain`: string (e.g., 'braehods.com')
- `site.email`: 'fakegoat1@gmail.com' (Phase 5 D-15a)
- NO youtube URL (dropped from v1 per 02-SCOPE-AMENDMENT.md).

From components/home/CTAArrowLink.tsx (current — Phase 2):
```ts
interface CTAArrowLinkProps {
  href: string;
  children: string;
  staggerIndex: number;
}
// Renders {children} <span aria-hidden>→</span> with group-hover:translate-x-1
```
Phase 6 extends with optional `direction?: 'forward' | 'back'` (default 'forward'); backwards-compat preserved.

From components/ui/MonogramMark.tsx line 53 (current path data — Phase 1 D-04 dual-write):
```ts
<path d="M58.41 45.56Q58.41 51.13 53.60 54.56Q48.79 58 38.72 58L8.71 58Q7.11 58 6.35 57.33Q5.59 56.66 5.59 55.55Q5.59 53.58 7.49 52.84L8.93 52.32Q9.90 51.95 10.36 51.39Q10.83 50.83 10.83 49.90L10.83 14.10Q10.83 13.17 10.36 12.61Q9.90 12.05 8.93 11.68L7.49 11.16Q5.59 10.42 5.59 8.45Q5.59 7.30 6.35 6.65Q7.11 6 8.71 6L32.15 6Q39.43 6 44.63 7.93Q49.83 9.86 52.63 13.32Q55.44 16.77 55.44 21.41Q55.44 25.17 53.19 28.04Q50.94 30.92 46.67 32.56Q42.40 34.19 36.31 34.19L38.02 32.67Q44.07 32.67 48.66 34.28Q53.25 35.90 55.83 38.82Q58.41 41.73 58.41 45.56M32.85 36.23L23.38 36.23L23.38 32.22L31.07 32.22Q33.82 32.22 35.66 31.18Q37.50 30.14 38.41 27.95Q39.32 25.76 39.32 22.42Q39.32 18.78 38.09 16.18Q36.87 13.58 34.56 12.15Q32.26 10.72 29.07 10.72L27.99 10.72L27.99 48.94Q27.99 51.17 29.25 52.22Q30.51 53.28 32.82 53.28L34.75 53.28Q36.83 53.28 38.33 52.32Q39.84 51.35 40.67 49.49Q41.51 47.64 41.51 45Q41.51 40.88 39.21 38.56Q36.90 36.23 32.85 36.23"/>
```

From RESEARCH.md Pattern 1 — root layout metadata addition shape:
- ADD `metadataBase: new URL('https://braehods.com')` to existing metadata export.
- ADD `alternates.canonical: '/'` (per-route layouts override).
- ADD `openGraph: { siteName, type: 'website', locale: 'en_US' }` (do NOT set `images` — file-based opengraph-image.png overrides).
- ADD `twitter: { card: 'summary_large_image' }`.
- INLINE `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(personJsonLd) }} />` inside `<body>` (NOT `<Script>` component — must be in initial HTML).

From RESEARCH.md Pattern 5 — proxy.ts shape:
```ts
// proxy.ts at REPO ROOT (not under app/)
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
export function proxy(_request: NextRequest) {
  const response = NextResponse.next();
  if (process.env.VERCEL_ENV === 'preview') {
    response.headers.set('X-Robots-Tag', 'noindex');
  }
  return response;
}
export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|icon|images|opengraph-image|robots.txt|sitemap.xml).*)'],
};
```

UI-SPEC § Dynamic OG Images — pixel-precise values to apply (NOT defaults from RESEARCH example):
- Title fontSize: 128 (NOT 96 from RESEARCH example).
- Background: `linear-gradient(135deg, #1a1a1f 0%, #0a0a0a 100%)`.
- Inline monogram SVG at (80, 80), 64×64, fill `#e8e8e8`.
- Grain overlay: SVG feTurbulence data-URI, opacity 0.04, NO mix-blend-mode (raster).
- letter-spacing: -0.02em on title.
- fonts: [Fraunces 700, Geist Sans 700 fallback].
- export const runtime = 'edge';

UI-SPEC FLAG Dimension 5 — OG canvas Y positions: try multiples of 4 (y=276/y=444 OR y=272/y=448) FIRST; if optical drift worse than current y=275/y=445 spec, document the original values in a leading comment in `scripts/build-static-og.mjs`.

UI-SPEC OQ-1 — Geist TTF vendoring: `assets/GeistSans-Bold.ttf` MUST be vendored in the SAME commit as `assets/Fraunces-Bold.ttf` BEFORE the OG route segments land (else OG renders broken on missing fallback).

UI-SPEC § 404 Page Composition — wrapper class:
- Outer `<section>`: `aria-labelledby="not-found-heading"` + `text-center mx-auto max-w-[44ch] min-h-[60vh] flex flex-col items-center justify-center gap-6`.
- `<MonogramMark size={80} aria-hidden />`.
- `<h1 id="not-found-heading" className="font-serif text-4xl md:text-5xl font-bold leading-tight">Page not found</h1>` (verbatim).
- `<p className="text-base font-sans">Try <Link href="/about">/about</Link> or <Link href="/work">/work</Link> — or <Link href="#contact">get in touch</Link> if you were looking for me.</p>` (verbatim).
- `<CTAArrowLink href="/" staggerIndex={0} direction="back">← Back home</CTAArrowLink>` (NEW direction prop).
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (W0): Stub 8 new SEO Playwright specs RED-first + bump lighthouse.spec.ts threshold</name>
  <files>tests/sitemap-renders.spec.ts, tests/robots-renders.spec.ts, tests/og-images-render.spec.ts, tests/jsonld-person.spec.ts, tests/not-found-renders.spec.ts, tests/preview-noindex.spec.ts, tests/canonical-urls.spec.ts, tests/metadata-per-route.spec.ts, tests/lighthouse.spec.ts</files>
  <read_first>
    - tests/favicon.spec.ts (asset-URL spec analog for sitemap/robots/og-images)
    - tests/footer-socials-render.spec.ts (DOM parse + cross-file data assertion analog for jsonld-person)
    - tests/about-renders.spec.ts (page render assertion analog for not-found-renders)
    - tests/lighthouse.spec.ts (current threshold=0; bump + parametrize)
    - data/site.ts (read socials.github + socials.instagram for jsonld-person expected values)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "Pattern Assignments (NEW specs)"
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md § "Plan 06-02 W0"
    - .planning/phases/06-polish-seo-launch/06-RESEARCH.md § Open Question #5 (lighthouse routes = /, /about, /work only — NOT 404)
  </read_first>
  <behavior>
    - Test 1 (sitemap): `GET /sitemap.xml` returns 200, content-type matches `/xml/i`, body contains `<url>` AND `https://braehods.com/about` AND `https://braehods.com/work`.
    - Test 2 (robots): `GET /robots.txt` returns 200, body contains `User-Agent: *` AND `Sitemap: https://braehods.com/sitemap.xml`.
    - Test 3 (og-images): For each route in `['/', '/about', '/work']`, parse the `<meta property="og:image" content="...">` tag from the HTML, then GET that image URL — expect 200 + content-type `image/png`.
    - Test 4 (jsonld-person): Navigate to `/`. Read `<script type="application/ld+json">` text content. JSON.parse. Assert `@type === 'Person'`. Assert `sameAs` equals `[site.socials.github, site.socials.instagram]` (imported from `@/data/site`). NEGATIVE: assert `sameAs.some(url => /youtube/i.test(url))` is `false`.
    - Test 5 (not-found): `page.goto('/this-route-does-not-exist')` returns response with `status() === 404`. Locate `<nav>` AND `<footer>` AND `h1:text-is("Page not found")` AND `link[name=/Back home/]` with `href === '/'`.
    - Test 6 (preview-noindex): `test.skip(!process.env.PLAYWRIGHT_BASE_URL?.includes('vercel.app'))`. `request.get('/')` response headers have `x-robots-tag === 'noindex'`.
    - Test 7 (canonical-urls): For each route in `[{path: '/', expected: 'https://braehods.com/'}, {path: '/about', expected: 'https://braehods.com/about'}, {path: '/work', expected: 'https://braehods.com/work'}]`, `page.goto(path)`, then `page.locator('link[rel="canonical"]').getAttribute('href') === expected`.
    - Test 8 (metadata-per-route): For each route, page.title() ≠ ''; description meta tag length ≤160; titles are unique across the 3 routes.
    - Test 9 (lighthouse): All 4 thresholds → 95; parametrize over `['/', '/about', '/work']` (NOT 404); `emulatedFormFactor` based on `test.info().project.name === 'chromium-mobile' ? 'mobile' : 'desktop'`.
  </behavior>
  <action>
    Per CONTEXT D-06 RED-first discipline, create all 8 new spec files + update `tests/lighthouse.spec.ts` BEFORE writing any of the source surfaces (sitemap.ts, robots.ts, etc.). The specs should ALL be RED on the current preview URL (because none of the SEO surfaces exist yet).

    Steps:
    1. Create `tests/sitemap-renders.spec.ts` using `tests/favicon.spec.ts` as the analog (HTTP GET + content-type assertion + body shape via `await response.text()` + regex). Assertions per Behavior Test 1.
    2. Create `tests/robots-renders.spec.ts` using `tests/favicon.spec.ts` analog. Assertions per Test 2.
    3. Create `tests/og-images-render.spec.ts`. For each route, `page.goto(route)`, then `await page.locator('meta[property="og:image"]').getAttribute('content')` (or `metaContent`), then `await request.get(ogUrl)` and assert 200 + content-type. Use `for (const path of ['/', '/about', '/work']) { test(...) }` parametrization.
    4. Create `tests/jsonld-person.spec.ts` using `tests/footer-socials-render.spec.ts` as analog. Import `{ site } from '@/data/site'`. Page.goto('/'). Use `page.locator('script[type="application/ld+json"]').textContent()`. JSON.parse + assertions per Test 4 (including the negative YT exclusion).
    5. Create `tests/not-found-renders.spec.ts` using `tests/about-renders.spec.ts` analog. Use `:text-is()` for verbatim heading + return link copy. Assert status 404 via `const response = await page.goto(...)` + `expect(response?.status()).toBe(404)`.
    6. Create `tests/preview-noindex.spec.ts` with `test.skip(!process.env.PLAYWRIGHT_BASE_URL?.includes('vercel.app'), 'Preview noindex requires Vercel preview URL')`. Use `request.get('/')` + `response.headers()['x-robots-tag']` assertion.
    7. Create `tests/canonical-urls.spec.ts` with parameterized `for` loop over 3 routes; assert `<link rel="canonical">` href.
    8. Create `tests/metadata-per-route.spec.ts` with 3 tests asserting `page.title()` non-empty + `<meta name="description">` ≤160 chars + a 4th test asserting all 3 titles are mutually distinct.
    9. UPDATE `tests/lighthouse.spec.ts`:
       - Change all 4 thresholds from `0` → `95` (`performance: 95, accessibility: 95, 'best-practices': 95, seo: 95`).
       - Wrap the existing test body in a `for (const route of ['/', '/about', '/work']) { test(...) }` loop OR use `test.describe.parallel` with one test per route.
       - Add `settings: { emulatedFormFactor: test.info().project.name === 'chromium-mobile' ? 'mobile' : 'desktop' }` to `playAudit` options (consult the local playwright-lighthouse types if the key name differs — `lhConfig` or `config` may be the wrapper).
       - PRESERVE the existing `LIGHTHOUSE_PORT = 9222` + `test.use({ launchOptions })` + `test.skip(browserName !== 'chromium', ...)` infra.
       - Per researcher OQ#5: route list does NOT include 404 (PERF-01 wording).
    10. Run `PLAYWRIGHT_BASE_URL=<preview> npm run test:full -- tests/sitemap-renders.spec.ts tests/robots-renders.spec.ts tests/og-images-render.spec.ts tests/jsonld-person.spec.ts tests/not-found-renders.spec.ts tests/canonical-urls.spec.ts tests/metadata-per-route.spec.ts` — confirm ALL 7 specs are RED (preview-noindex is skip-gated; lighthouse is part of the threshold work).
    11. Commit: `git commit -m "test(06-02): stub 8 new SEO specs RED + bump lighthouse threshold to 95"`. Stage all 9 spec file changes in one commit per atomic-discipline.
  </action>
  <verify>
    <automated>PLAYWRIGHT_BASE_URL=$PREVIEW_URL npm run test:full -- tests/sitemap-renders.spec.ts tests/robots-renders.spec.ts tests/og-images-render.spec.ts tests/jsonld-person.spec.ts tests/not-found-renders.spec.ts tests/canonical-urls.spec.ts tests/metadata-per-route.spec.ts --reporter=line 2>&1 | grep -E "(passed|failed)"</automated>
    Expected: failures reported (specs are RED — sources don't exist yet); preview-noindex skipped due to base-URL gate; lighthouse not in this command's scope.
  </verify>
  <acceptance_criteria>
    - 8 new spec files exist under `tests/`.
    - `grep -l "test\\.skip" tests/preview-noindex.spec.ts` returns a match (preview gating present).
    - `grep -c "expect(.*toBe(95)" tests/lighthouse.spec.ts` returns ≥4 (one per Lighthouse category — Perf/A11y/BestPractices/SEO).
    - `grep -c "'/about'\\|'/work'\\|'/'" tests/lighthouse.spec.ts` returns ≥3 (route parametrization present).
    - All 8 new specs are RED on preview URL (sources don't exist yet) — verified via the test run.
    - Single commit with the 9 files staged together.
    - Negative YT assertion present in `tests/jsonld-person.spec.ts` (grep for `/youtube/i.*toBe(false)` or similar).
  </acceptance_criteria>
  <done>RED-first spec scaffolding complete. Plan 06-02 has its acceptance test suite ready to flip GREEN as Wave 1 sources land.</done>
</task>

<task type="auto">
  <name>Task 2 (W1): Atomic monogram triple-source extraction (lib/og/monogram-path.ts)</name>
  <files>lib/og/monogram-path.ts, components/ui/MonogramMark.tsx, app/icon.svg</files>
  <read_first>
    - components/ui/MonogramMark.tsx (line 53 — the `<path d="..."/>` string is the source of truth)
    - app/icon.svg (line 20 — the inline `<path d="..."/>` should currently be byte-identical with MonogramMark.tsx line 53 per Phase 1 D-04)
    - .planning/phases/01-foundation-design-tokens/01-CONTEXT.md § D-04 (dual-write contract)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "`lib/og/monogram-path.ts`"
    - .planning/phases/06-polish-seo-launch/06-UI-SPEC.md § "Component Inventory" (triple-source contract)
  </read_first>
  <action>
    Per UI-SPEC + PATTERNS.md, extend Phase 1's dual-write monogram contract to a triple-write contract by extracting the `<path d>` string into a shared const. ATOMIC commit: all 3 files together.

    Steps:
    1. CREATE `lib/og/monogram-path.ts` exporting `MONOGRAM_PATH` const set to the path data string from `MonogramMark.tsx` line 53 (use the exact string from `<interfaces>` block above OR re-read from `MonogramMark.tsx`). Add a leading file comment explaining the triple-source contract (per PATTERNS.md § lib/og/monogram-path.ts comment template).
    2. EDIT `components/ui/MonogramMark.tsx`: import `MONOGRAM_PATH` from `@/lib/og/monogram-path` and replace the inline `d="..."` string with `d={MONOGRAM_PATH}` (or use template-literal interpolation). Verify the rendered output is byte-identical via the existing favicon.spec.ts grep regex (no change to rendered HTML).
    3. EDIT `app/icon.svg`: This is a static .svg file, NOT a React component, so it cannot import. Add a leading XML comment `<!-- MUST be byte-identical with MONOGRAM_PATH in lib/og/monogram-path.ts. Any update must propagate to all 3 surfaces. -->` (Phase 1 D-04 binding extended). The path string remains inline literal (verify byte-identical against MONOGRAM_PATH manually).
    4. Run `npm run typecheck && npm run lint && npm run build && npm run test:full -- tests/favicon.spec.ts` — all clean (no rendered behavior change).
    5. Commit atomically: `git add lib/og/monogram-path.ts components/ui/MonogramMark.tsx app/icon.svg && git commit -m "refactor(06-02): extract MONOGRAM_PATH to lib/og — extends Phase 1 D-04 to triple-source"`
    6. Verify atomic binding: `git log -n 1 --name-only HEAD` lists exactly 3 files.
  </action>
  <verify>
    <automated>git log -n 1 --name-only HEAD | sort -u && grep -c "MONOGRAM_PATH" lib/og/monogram-path.ts components/ui/MonogramMark.tsx && npm run typecheck && npm run build && npm run test:full -- tests/favicon.spec.ts</automated>
    Expected: commit lists 3 files; MONOGRAM_PATH appears in both TS files; build clean; favicon spec still GREEN.
  </verify>
  <acceptance_criteria>
    - `lib/og/monogram-path.ts` exports `MONOGRAM_PATH` string starting with `M58.41 45.56Q58.41 51.13`.
    - `components/ui/MonogramMark.tsx` imports `MONOGRAM_PATH` from `@/lib/og/monogram-path`.
    - `app/icon.svg` contains the comment referencing `lib/og/monogram-path.ts`.
    - Path strings in all 3 surfaces are byte-identical (verify via a one-liner `node -e` script comparing first 50 chars).
    - Single commit with 3 files; `tests/favicon.spec.ts` still GREEN.
  </acceptance_criteria>
  <done>Triple-source monogram contract established. OG images in Task 5 + Task 6 can consume MONOGRAM_PATH.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (W1): Vendor Fraunces + Geist TTF fonts to assets/ (atomic, SIL OFL license verified)</name>
  <files>assets/Fraunces-Bold.ttf, assets/GeistSans-Bold.ttf, assets/OFL.txt</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-UI-SPEC.md § "OG Image Fonts" + OQ-1
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md "assets/Fraunces-Bold.ttf" + "assets/GeistSans-Bold.ttf" rows
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md (no mention of `assets/` dir yet — confirm `assets/` is NOT served publicly; .gitignore review)
    - .gitignore (confirm `assets/` is NOT public — Next.js `public/` is the public-asset directory; `assets/` at repo root is a build-time source folder)
  </read_first>
  <what-built>
    `next/og` at edge runtime cannot use `next/font`; fonts MUST be loaded as raw TTF bytes. Phase 6 vendors Fraunces-Bold (primary) + GeistSans-Bold (fallback) so OG image generation is byte-deterministic and offline-buildable. Both must be in `assets/` (NOT `public/`) BEFORE the OG route segments land in Task 6.
  </what-built>
  <how-to-verify>
    1. Create `assets/` directory at repo root if it does not exist.
    2. **Fraunces-Bold.ttf:** Download static-Bold-weight Fraunces TTF from https://fonts.google.com/specimen/Fraunces. Options:
       - Use Google Fonts Download (zip), extract `static/Fraunces-Bold.ttf`. Place at `assets/Fraunces-Bold.ttf`.
       - OR via direct CDN (verify URL still serves bold-700 static): `https://fonts.gstatic.com/s/fraunces/v32/...` (URL pattern may have changed; prefer the Google Fonts UI download).
       - File size should be ~180KB; if you accidentally download the variable .ttf (~600KB), select the static Bold variant instead — ImageResponse does NOT support variable fonts at edge.
    3. **GeistSans-Bold.ttf:** Download from https://github.com/vercel/geist-font/releases. Latest release ships a `fonts/` zip containing Geist Sans static weights. Extract `Geist-Bold.ttf` (or `GeistSans-Bold.ttf`); rename to `GeistSans-Bold.ttf` for consistency. Place at `assets/GeistSans-Bold.ttf`.
    4. **SIL OFL license:** Both Fraunces and Geist Sans ship under SIL OFL 1.1. Copy the OFL.txt (LICENSE.txt) from EITHER font's download into `assets/OFL.txt` (one license covers both — SIL OFL is the same text). If the downloads contain separate license files with the same text, just commit one as `assets/OFL.txt`.
    5. Verify file sizes:
       - `Fraunces-Bold.ttf`: 100-300 KB (target static-Bold ~180KB)
       - `GeistSans-Bold.ttf`: 50-200 KB (target ~100KB)
       - `OFL.txt`: ~4KB
    6. Document MD5 of each TTF: `md5sum assets/Fraunces-Bold.ttf assets/GeistSans-Bold.ttf` (Windows: `Get-FileHash -Algorithm MD5 assets/*.ttf`). Record values in `.planning/phases/06-polish-seo-launch/06-02-SUMMARY.md` § Vendored Fonts.
    7. Verify `assets/` is NOT under `.gitignore`: `git check-ignore assets/Fraunces-Bold.ttf` should return non-zero (i.e., file is NOT ignored, will be committed).
    8. ATOMIC commit: `git add assets/Fraunces-Bold.ttf assets/GeistSans-Bold.ttf assets/OFL.txt && git commit -m "assets(06-02): vendor Fraunces-Bold + GeistSans-Bold TTFs for OG ImageResponse (SIL OFL)"`. Verify via `git log -n 1 --name-only HEAD` — 3 files.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "fonts-vendored: fraunces=<MD5-12chars>, geist=<MD5-12chars>" OR "vendor-failed: <details>".</resume-signal>
  <acceptance_criteria>
    - `assets/Fraunces-Bold.ttf` exists, size 100-300 KB, MD5 documented in 06-02-SUMMARY.md.
    - `assets/GeistSans-Bold.ttf` exists, size 50-200 KB, MD5 documented.
    - `assets/OFL.txt` exists, contains SIL OFL 1.1 license text.
    - `git check-ignore assets/Fraunces-Bold.ttf` exits non-zero (file tracked).
    - Single atomic commit with all 3 files; `git log -n 1 --name-only HEAD` lists exactly those 3 paths.
  </acceptance_criteria>
  <done>Both TTFs + license vendored in assets/ ready for OG ImageResponse consumption in Tasks 5-6. SIL OFL compliance preserved.</done>
</task>

<task type="auto">
  <name>Task 4 (W1): Ship app/sitemap.ts + app/robots.ts + proxy.ts (3 atomic commits)</name>
  <files>app/sitemap.ts, app/robots.ts, proxy.ts</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-RESEARCH.md § Pattern 3 (sitemap), § Pattern 4 (robots), § Pattern 5 (proxy)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "`app/sitemap.ts`", § "`app/robots.ts`", § "`proxy.ts`"
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-04 (sitemap iterates content/ glob) + D-18 (proxy.ts shape)
    - tests/sitemap-renders.spec.ts + tests/robots-renders.spec.ts + tests/preview-noindex.spec.ts (the RED specs these surfaces flip GREEN)
    - .continue-here.md § Critical Anti-Patterns ("Tailwind v4 transition-colors shorthand" — N/A here but reinforces no inline state)
  </read_first>
  <action>
    Three independent surface files; each in its own commit so failures isolate cleanly.

    Sub-task 4a — `app/sitemap.ts`:
    1. CREATE `app/sitemap.ts` per RESEARCH.md Pattern 3:
       - Import `type { MetadataRoute } from 'next';`
       - Import `readdir` from `node:fs/promises` + `join` from `node:path`.
       - Export default async function `sitemap(): Promise<MetadataRoute.Sitemap>`.
       - Define `BASE_URL = 'https://braehods.com'`.
       - Build static routes list: `['/', '/about', '/work']` mapped to `{ url: BASE_URL + path, lastModified: new Date(), changeFrequency: 'monthly', priority: path === '/' ? 1.0 : 0.8 }`.
       - Wrap `readdir(join(process.cwd(), 'content'))` in `try/catch` (content/ may not exist in v1 per RESEARCH.md Pitfall 7). On error, return only static routes.
       - On success, map `.mdx` files to additional sitemap entries.
       - Return concatenated array.
    2. Verify: `PLAYWRIGHT_BASE_URL=<preview> npm run test:full -- tests/sitemap-renders.spec.ts` — should still be RED (no preview deploy yet); local typecheck/lint/build clean.
    3. Commit: `git commit -m "feat(06-02): app/sitemap.ts dynamic sitemap (SEO-04a)"`.

    Sub-task 4b — `app/robots.ts`:
    1. CREATE `app/robots.ts` per RESEARCH.md Pattern 4 (verbatim shape):
       - Import `type { MetadataRoute } from 'next';`
       - Export default function `robots(): MetadataRoute.Robots`.
       - Return `{ rules: { userAgent: '*', allow: '/' }, sitemap: 'https://braehods.com/sitemap.xml' }`.
    2. Verify: local build clean.
    3. Commit: `git commit -m "feat(06-02): app/robots.ts allow all + sitemap reference (SEO-04b)"`.

    Sub-task 4c — `proxy.ts`:
    1. CREATE `proxy.ts` AT REPO ROOT (next to package.json, NOT under `app/`) per RESEARCH.md Pattern 5 + CONTEXT D-18:
       - Import `NextResponse` from `next/server`.
       - Import `type { NextRequest }` from `next/server`.
       - Export function `proxy(_request: NextRequest)` returning `NextResponse.next()` with `X-Robots-Tag: noindex` set when `process.env.VERCEL_ENV === 'preview'`.
       - Export const `config = { matcher: ['/((?!_next/static|_next/image|favicon.ico|icon|images|opengraph-image|robots.txt|sitemap.xml).*)'] };`
    2. Per RESEARCH.md Pitfall 6: file MUST be at repo root; do NOT set `runtime` config option (build error).
    3. Verify: `npm run build` — Turbopack must still build clean WITH the new proxy file detected at root.
    4. Commit: `git commit -m "feat(06-02): proxy.ts X-Robots-Tag noindex on Vercel preview (SEO-09)"`.

    Use Edit/Write tool, not heredoc. Do NOT add a 2nd 'use client' anywhere. Do NOT touch tests in this task (they were stubbed in Task 1).
  </action>
  <verify>
    <automated>test -f app/sitemap.ts && test -f app/robots.ts && test -f proxy.ts && npm run typecheck && npm run lint && npm run build</automated>
    Expected: 3 files exist; typecheck/lint/build all exit 0. Specs remain RED until preview deploys (Task 9).
  </verify>
  <acceptance_criteria>
    - `app/sitemap.ts` exports a default async function returning `MetadataRoute.Sitemap` with at least 3 url entries (`/`, `/about`, `/work`).
    - `app/robots.ts` exports a default function with `rules: { userAgent: '*', allow: '/' }` AND `sitemap: 'https://braehods.com/sitemap.xml'`.
    - `proxy.ts` AT REPO ROOT (not under app/) with `process.env.VERCEL_ENV === 'preview'` gate + matcher excluding `_next/static`, `_next/image`, `favicon.ico`, `icon`, `images`, `opengraph-image`, `robots.txt`, `sitemap.xml`.
    - `grep -c "use client" proxy.ts` returns 0.
    - `npm run build` exits 0 (Turbopack accepts all 3 file conventions).
    - 3 separate commits (Sub-task 4a, 4b, 4c).
  </acceptance_criteria>
  <done>SEO-04 (sitemap + robots) + SEO-09 (preview noindex) source surfaces shipped. Specs ready to GREEN-flip after preview deploy in Task 9.</done>
</task>

<task type="auto">
  <name>Task 5 (W1): Ship static `/` OG via scripts/build-static-og.mjs + app/opengraph-image.png</name>
  <files>scripts/build-static-og.mjs, app/opengraph-image.png</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-UI-SPEC.md § "Static `/` OG Image" (composition pixel-precise) + § Dimension 5 FLAG (Y position multiples-of-4)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "`app/opengraph-image.png` + `scripts/build-static-og.mjs`"
    - lib/og/monogram-path.ts (from Task 2 — read MONOGRAM_PATH)
    - assets/Fraunces-Bold.ttf (from Task 3 — readFile target)
  </read_first>
  <action>
    Per UI-SPEC § Static `/` OG Image, generate a 1200×630 PNG via a one-off Node script using `next/og` ImageResponse, committed to repo as a static asset.

    Steps:
    1. CREATE `scripts/build-static-og.mjs`:
       - Use ESM (`.mjs`); import `ImageResponse` from `next/og`.
       - Import `readFile, writeFile` from `node:fs/promises`; import `join` from `node:path`.
       - Read `MONOGRAM_PATH` — easiest path: re-export it from `lib/og/monogram-path.ts` via dynamic import OR copy the constant into the script (with a leading comment cross-referencing the canonical source). Prefer dynamic import for byte-identical guarantee: `const { MONOGRAM_PATH } = await import('../lib/og/monogram-path.ts');` — if TS .ts import fails in .mjs, fall back to inline copy with comment.
       - Read Fraunces TTF: `const fraunces = await readFile(join(process.cwd(), 'assets/Fraunces-Bold.ttf'));`
       - Construct `ImageResponse(<JSX>, { width: 1200, height: 630, fonts: [{ name: 'Fraunces', data: fraunces, weight: 700, style: 'normal' }] })`.
       - JSX composition per UI-SPEC § Static / OG Image:
         - Outer `<div>` with `display: flex, flexDirection: column, alignItems: center, justifyContent: center, width: 1200, height: 630, background: 'linear-gradient(135deg, #1a1a1f 0%, #0a0a0a 100%)'`.
         - Grain overlay `<div>` absolute-positioned with `background-image: url("data:image/svg+xml,<feTurbulence SVG>")`, opacity 0.04, no mix-blend-mode (raster).
         - Monogram `<svg width="96" height="96" viewBox="0 0 64 64" fill="#e8e8e8"><path d={MONOGRAM_PATH}/></svg>` — try monogram center y position `y=276` (multiple of 4) FIRST per UI-SPEC Dimension 5 FLAG. If visual review (Task 11) shows optical drift worse than y=275, EDIT the script + regenerate with y=275 + document the original spec values in a leading script comment.
         - Wordmark `<div style={{ fontFamily: 'Fraunces', fontSize: 180, fontWeight: 700, lineHeight: 1.0, color: '#e8e8e8', letterSpacing: '-0.02em' }}>Braeden</div>` — vertical baseline at y=444 (multiple of 4 attempt) OR y=445 (spec original). Same fallback document approach.
         - Outer safe margin 80px (achieved via flex centering — no content should touch canvas edge).
       - Read the arrayBuffer: `const buffer = Buffer.from(await response.arrayBuffer());`
       - Write to `app/opengraph-image.png`: `await writeFile(join(process.cwd(), 'app/opengraph-image.png'), buffer);`
       - Log success + file size + MD5 (use node:crypto).
       - Add leading file comment block documenting: (a) UI-SPEC Dimension 5 FLAG attempt-multiples-of-4 first, fall back to spec values if optical drift worse; (b) MD5 of the generated PNG (filled after first run); (c) regeneration cadence (manual only, NOT in `next build`).
    2. Run the script: `node scripts/build-static-og.mjs`. Capture the generated MD5; update the script's leading comment to record the MD5 for future regeneration determinism.
    3. Verify `app/opengraph-image.png` exists and is a valid PNG (use `file app/opengraph-image.png` or open in image viewer). Dimensions 1200×630.
    4. Commit BOTH files: `git add scripts/build-static-og.mjs app/opengraph-image.png && git commit -m "feat(06-02): static / OG image + build script (SEO-02 / SEO-03 static fallback)"`.
    5. Document the script command in README.md OR in 06-02-SUMMARY.md (manual run cadence per UI-SPEC OQ-3).

    Do NOT wire the script into `next build` (per UI-SPEC OQ-3 locked answer).
  </action>
  <verify>
    <automated>test -f scripts/build-static-og.mjs && test -f app/opengraph-image.png && file app/opengraph-image.png | grep -E "PNG image data, 1200 x 630"</automated>
    Expected: both files exist; file inspector confirms 1200×630 PNG.
  </verify>
  <acceptance_criteria>
    - `scripts/build-static-og.mjs` exists and runs successfully (`node scripts/build-static-og.mjs` exits 0).
    - `app/opengraph-image.png` exists, is PNG, dimensions 1200×630.
    - Script leading comment documents UI-SPEC FLAG resolution (which y-positions ultimately used) + MD5.
    - Both files committed in single commit.
    - Next.js automatically picks up `app/opengraph-image.png` and generates `<meta property="og:image">` for `/` — verified after preview deploy in Task 9.
  </acceptance_criteria>
  <done>Static `/` OG asset generated, committed, ready to ship in next deploy.</done>
</task>

<task type="auto">
  <name>Task 6 (W1): Ship dynamic /about + /work OG ImageResponse route segments</name>
  <files>app/about/opengraph-image.tsx, app/work/opengraph-image.tsx</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-UI-SPEC.md § "Dynamic /about + /work OG Images" (pixel-precise composition + per-route override hook + alt text contract)
    - .planning/phases/06-polish-seo-launch/06-RESEARCH.md § Pattern 2 (ImageResponse JSX + font loading at edge runtime)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "app/about/opengraph-image.tsx + app/work/opengraph-image.tsx" + UI-SPEC pixel-precise deltas table
    - lib/og/monogram-path.ts (from Task 2 — import MONOGRAM_PATH)
    - assets/Fraunces-Bold.ttf + assets/GeistSans-Bold.ttf (from Task 3 — fetched at edge runtime)
  </read_first>
  <action>
    Per UI-SPEC § Dynamic OG Images, ship 2 nearly-identical route segment files. Same composition; only `TITLE` const + `alt` differ.

    Steps:
    1. CREATE `app/about/opengraph-image.tsx`:
       - `import { ImageResponse } from 'next/og';`
       - `import { MONOGRAM_PATH } from '@/lib/og/monogram-path';`
       - `export const runtime = 'edge';`
       - `export const alt = 'About Braeden Hodson';`
       - `export const size = { width: 1200, height: 630 };`
       - `export const contentType = 'image/png';`
       - `const TITLE = 'About';`
       - `export default async function Image() { ... }`:
         - Fetch Fraunces TTF: `const fraunces = await fetch(new URL('../../assets/Fraunces-Bold.ttf', import.meta.url)).then(r => r.arrayBuffer());`
         - Fetch Geist TTF: `const geist = await fetch(new URL('../../assets/GeistSans-Bold.ttf', import.meta.url)).then(r => r.arrayBuffer());`
         - JSX per UI-SPEC § Dynamic OG composition (pixel-precise):
           - Outer `<div>` background `linear-gradient(135deg, #1a1a1f 0%, #0a0a0a 100%)`, width 1200, height 630, flex column center.
           - Grain overlay (data-URI SVG, opacity 0.04, no mix-blend-mode).
           - Inline monogram `<svg width="64" height="64" viewBox="0 0 64 64" style={{ position: 'absolute', top: 80, left: 80 }} fill="#e8e8e8"><path d={MONOGRAM_PATH}/></svg>`.
           - Title `<div style={{ fontFamily: 'Fraunces', fontSize: 128, fontWeight: 700, lineHeight: 1.05, color: '#e8e8e8', letterSpacing: '-0.02em', maxWidth: 1040, textAlign: 'center' }}>{TITLE}</div>`.
         - Return `new ImageResponse(<JSX>, { ...size, fonts: [{ name: 'Fraunces', data: fraunces, weight: 700 }, { name: 'Geist Sans', data: geist, weight: 700 }] })`.
       - Add leading file comment: "Falls back to Geist Sans if Fraunces TTF fetch fails" per UI-SPEC § Fallback.
    2. CREATE `app/work/opengraph-image.tsx` — copy of `/about/opengraph-image.tsx` with two deltas:
       - `const TITLE = 'Work';`
       - `export const alt = 'Work — Braeden Hodson';`
    3. Commit: `git add app/about/opengraph-image.tsx app/work/opengraph-image.tsx && git commit -m "feat(06-02): dynamic /about + /work OG via next/og ImageResponse (SEO-02)"`.

    Constraints:
    - `export const runtime = 'edge'` per UI-SPEC (fastest first-byte for social crawlers).
    - Do NOT use `<img>` or rely on `next/image` inside ImageResponse JSX — only inline SVG + CSS gradients work at edge.
    - Inline color literals (`#e8e8e8`, `#1a1a1f`, `#0a0a0a`) — NO `var(--color-...)` because edge runtime has no CSS custom property resolver.
    - Do NOT add a 2nd 'use client'.
  </action>
  <verify>
    <automated>test -f app/about/opengraph-image.tsx && test -f app/work/opengraph-image.tsx && grep -c "MONOGRAM_PATH" app/about/opengraph-image.tsx app/work/opengraph-image.tsx && grep -c "runtime = 'edge'" app/about/opengraph-image.tsx app/work/opengraph-image.tsx && npm run typecheck && npm run build</automated>
    Expected: both files exist + import MONOGRAM_PATH + declare edge runtime; typecheck + build clean.
  </verify>
  <acceptance_criteria>
    - Both files exist with edge runtime + size + contentType + alt + default async function exports.
    - Both import `MONOGRAM_PATH` from `@/lib/og/monogram-path`.
    - Both fetch BOTH `Fraunces-Bold.ttf` AND `GeistSans-Bold.ttf` from `assets/`.
    - `grep -c "use client" app/about/opengraph-image.tsx app/work/opengraph-image.tsx` returns 0.
    - `grep -c 'fontSize: 128' app/about/opengraph-image.tsx` returns ≥1 (UI-SPEC pixel-precise value, NOT 96 from RESEARCH example).
    - Single commit covering both files.
    - `npm run build` exits 0 (Turbopack accepts the edge runtime declaration).
  </acceptance_criteria>
  <done>Dynamic /about + /work OG ImageResponse surfaces shipped. Will be verified at preview-URL level in Task 9.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7 (W1): Add CTAArrowLink direction prop + ship branded 404 page (atomic)</name>
  <files>components/home/CTAArrowLink.tsx, app/not-found.tsx</files>
  <read_first>
    - components/home/CTAArrowLink.tsx (current — Phase 2 D-19 forward-only)
    - .planning/phases/06-polish-seo-launch/06-UI-SPEC.md § "CTAArrowLink Adaptation" + § "404 Page Composition" (composition + verbatim copy + tab order)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "`components/home/CTAArrowLink.tsx` (MODIFIED — `direction` prop)" + § "`app/not-found.tsx`"
    - tests/not-found-renders.spec.ts (the RED spec this surface flips GREEN; verify selector pattern)
    - app/about/page.tsx (Server Component analog for 404 composition)
    - lib/motion.ts (for `fadeInUp` + `stagger` consumption in CTAArrowLink)
  </read_first>
  <behavior>
    - Test 1 (direction=forward default): All existing call sites (Hero CTAs in `app/page.tsx`; about-page CTA in `app/about/page.tsx`; success-state "Send another →" in ContactModal — wherever CTAArrowLink is consumed) render unchanged: `{children} <span aria-hidden>→</span>` with `group-hover:translate-x-1`.
    - Test 2 (direction=back, 404 consumer): `<CTAArrowLink href="/" direction="back" staggerIndex={0}>← Back home</CTAArrowLink>` renders `<span aria-hidden>←</span> {children}` with `group-hover:-translate-x-1`.
    - Test 3 (404 page): Navigating to `/this-route-does-not-exist` returns 404, renders Nav + Footer chrome + 80px monogram + Fraunces 48px (mobile 36px) "Page not found" heading + body line with 3 inline links + return link with `←` glyph.
    - Test 4 (FOUND-07): No 'use client' directive added in either file.
    - Test 5 (verbatim copy): Heading exact text `Page not found`; body line exact text `Try /about or /work — or get in touch if you were looking for me.`; return link exact text `← Back home`.
  </behavior>
  <action>
    Atomic single-commit per UI-SPEC + PATTERNS.md atomic-binding contract.

    Steps:
    1. EDIT `components/home/CTAArrowLink.tsx`:
       - Extend `CTAArrowLinkProps` interface with optional `direction?: 'forward' | 'back'` (default 'forward').
       - Add a destructuring default in the function signature: `function CTAArrowLink({ href, children, staggerIndex, direction = 'forward' }: CTAArrowLinkProps)`.
       - In the JSX return, switch on `direction`:
         - `'forward'` (default — preserve current Phase 2 behavior verbatim): `{children} <span aria-hidden className="inline-block transition-transform group-hover:translate-x-1">→</span>`.
         - `'back'`: `<span aria-hidden className="inline-block transition-transform group-hover:-translate-x-1">←</span> {children}` (leading glyph + negative translate).
       - PRESERVE all other props (href, staggerIndex, fadeInUp, stagger, color tokens) untouched.
       - Update the file comment block at top noting the Phase 6 extension (direction prop).
    2. CREATE `app/not-found.tsx` per UI-SPEC § 404 Page Composition:
       - Imports: `Link` from `next/link`, `MonogramMark` from `@/components/ui/MonogramMark`, `CTAArrowLink` from `@/components/home/CTAArrowLink`.
       - `export const metadata = { title: 'Page not found' };`
       - Default export Server Component (NO 'use client'):
         - `<section aria-labelledby="not-found-heading" data-test="not-found-section" className="py-8 md:py-12 flex flex-col items-center justify-center gap-6 min-h-[60vh] text-center mx-auto max-w-[44ch]">`
         - `<MonogramMark size={80} aria-hidden />`
         - `<h1 id="not-found-heading" className="font-serif text-4xl md:text-5xl font-bold leading-tight">Page not found</h1>`
         - `<p className="text-base font-sans">Try <Link href="/about" style={{ color: 'var(--color-accent)' }} className="underline-offset-4 hover:underline decoration-1">/about</Link> or <Link href="/work" style={{ color: 'var(--color-accent)' }} className="underline-offset-4 hover:underline decoration-1">/work</Link> — or <Link href="#contact" style={{ color: 'var(--color-accent)' }} className="underline-offset-4 hover:underline decoration-1">get in touch</Link> if you were looking for me.</p>`
         - `<CTAArrowLink href="/" staggerIndex={0} direction="back">Back home</CTAArrowLink>` (the `←` glyph is rendered by the component when `direction='back'`).
       - Note: per UI-SPEC verbatim copy contract, the return-link children string is `Back home` (the `←` comes from the glyph span; the visible rendered text is "← Back home"). VERIFY against the spec: UI-SPEC Component Inventory table § Return link slot says "`← Back home`" is the FULL rendered text — if the spec wants the children string to literally include "←", revise the CTAArrowLink to NOT prepend the glyph when children already starts with "←", OR pass `children="Back home"` and let the component prepend. Recommended: pass `children="Back home"` (clean separation of concerns); rendered DOM still reads "← Back home" with the glyph from the component.
    3. Verify locally: `npm run typecheck && npm run lint && npm run build`.
    4. Run RED spec to confirm it flips GREEN (locally first, before preview deploy): `npm run test -- tests/not-found-renders.spec.ts`.
    5. Stage all 2 files atomically: `git add components/home/CTAArrowLink.tsx app/not-found.tsx && git commit -m "feat(06-02): CTAArrowLink direction=back prop + branded 404 page (SEO-08)"`.
    6. Verify atomic binding: `git log -n 1 --name-only HEAD` lists exactly 2 files.

    Per .continue-here.md anti-pattern: no 2nd 'use client'; no `transition-colors` shorthand on focus-rendered links (the inline `<Link>` elements rely on default focus-ring inheritance — no explicit transition on them needed).
  </action>
  <verify>
    <automated>git log -n 1 --name-only HEAD && grep -c "direction" components/home/CTAArrowLink.tsx && grep -c "'use client'" app/not-found.tsx && npm run test -- tests/not-found-renders.spec.ts</automated>
    Expected: commit lists 2 files; direction appears in CTAArrowLink ≥3 times (interface + default + JSX switch); 'use client' count 0 in not-found; spec flips GREEN locally.
  </verify>
  <acceptance_criteria>
    - `components/home/CTAArrowLink.tsx` interface has `direction?: 'forward' | 'back'`.
    - `direction === 'back'` branch renders `<span aria-hidden>←</span>` BEFORE children with `group-hover:-translate-x-1`.
    - `direction === 'forward'` branch unchanged (backwards-compat).
    - `app/not-found.tsx` exists as Server Component (no 'use client').
    - `app/not-found.tsx` heading text exactly `Page not found`; body line exactly per UI-SPEC; return link children exactly `Back home` (component prepends `←`).
    - `app/not-found.tsx` uses `<MonogramMark size={80} aria-hidden />`.
    - 3 inline Links to /about, /work, #contact present with accent color.
    - Single atomic commit with both files.
    - `tests/not-found-renders.spec.ts` GREEN locally (Wave 1 surfaces shipped; spec flips RED→GREEN before preview).
    - Existing Phase 2 CTAArrowLink consumers (hero, /about CTA) still work — `tests/about-renders.spec.ts` + `tests/footer-socials-render.spec.ts` still GREEN.
  </acceptance_criteria>
  <done>404 page live; CTAArrowLink supports `direction='back'`; FOUND-07 invariant preserved. Plan 06-02 SEO-08 source surface shipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8 (W1): Add metadataBase + canonical + per-route generateMetadata + Person JSON-LD</name>
  <files>app/layout.tsx, app/page.tsx, app/about/page.tsx, app/work/page.tsx</files>
  <read_first>
    - app/layout.tsx (current metadata export shape — lines 9-12 per Phase 5 SUMMARY)
    - app/page.tsx + app/about/page.tsx + app/work/page.tsx (current `export const metadata` shapes)
    - data/site.ts (read site.socials.github + socials.instagram + name + domain for JSON-LD)
    - .planning/phases/06-polish-seo-launch/06-RESEARCH.md § Pattern 1 (root layout metadata addition)
    - .planning/phases/06-polish-seo-launch/06-PATTERNS.md § "`app/layout.tsx` (root layout)"
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-03 (JSON-LD sameAs = GH + IG only) + D-05 (canonical + metadataBase) + § Claude's Discretion (minimal Person shape)
    - tests/jsonld-person.spec.ts + tests/canonical-urls.spec.ts + tests/metadata-per-route.spec.ts (the 3 RED specs this task flips GREEN)
    - .planning/phases/02-home-page/02-SCOPE-AMENDMENT.md (re-confirm: NO YT URL in sameAs)
  </read_first>
  <behavior>
    - Test 1 (Person JSON-LD): `<script type="application/ld+json">` block in HTML head/body parses as valid JSON with `@type === 'Person'` + `name` + `url === 'https://braehods.com'` + `sameAs === [site.socials.github, site.socials.instagram]`. NO YT URL.
    - Test 2 (canonical URLs): `/` has canonical `https://braehods.com/`; `/about` has `https://braehods.com/about`; `/work` has `https://braehods.com/work`.
    - Test 3 (per-route titles): `/` title is non-empty (e.g., "Braeden Hodson"); `/about` title contains "About"; `/work` title contains "Work". All 3 are mutually distinct.
    - Test 4 (meta descriptions): Each route has `<meta name="description">` with content ≤160 chars.
    - Test 5 (metadataBase): The root layout exports `metadataBase: new URL('https://braehods.com')` so relative OG image paths resolve correctly.
    - Test 6 (openGraph + twitter): Root layout adds `openGraph: { siteName, type: 'website', locale: 'en_US' }` + `twitter: { card: 'summary_large_image' }`. Per-route `page.tsx` files do NOT override `openGraph.images` (file-based opengraph-image.png handles that automatically per Pitfall 2).
  </behavior>
  <action>
    Two logical units, two commits:

    Sub-task 8a — `app/layout.tsx` root metadata + Person JSON-LD (single commit):
    1. EDIT `app/layout.tsx`:
       - Extend existing `metadata` export:
         - `metadataBase: new URL('https://braehods.com')`
         - `alternates: { canonical: '/' }` (root canonical; per-route layouts override)
         - `openGraph: { siteName: 'Braeden Hodson', type: 'website', locale: 'en_US' }` — DO NOT set `images` (Pitfall 2: file-based `app/opengraph-image.png` overrides metadata.openGraph.images automatically).
         - `twitter: { card: 'summary_large_image' }`
       - Import `{ site } from '@/data/site'` if not already.
       - ADD a `personJsonLd` const above the default export component:
         ```ts
         const personJsonLd = {
           '@context': 'https://schema.org',
           '@type': 'Person',
           name: site.name,
           url: `https://${site.domain}`,
           sameAs: [site.socials.github, site.socials.instagram].filter(Boolean),
         };
         ```
       - INLINE the JSON-LD script inside `<body>` (placement: just before the existing `<Nav />` mount OR right after — PATTERNS.md notes placement is non-load-bearing for crawlers). Use:
         `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(personJsonLd) }} />`
       - DO NOT use `<Script>` component (defers HTML, defeats SEO purpose per RESEARCH.md Anti-Patterns).
       - PRESERVE existing `<Analytics />` + `<SpeedInsights />` mount (verified in Plan 06-01 Task 3).
       - PRESERVE the single `'use client'` carve-out for ContactModal (`app/layout.tsx` itself remains a Server Component).
    2. Verify: `npm run typecheck && npm run lint && npm run build && npm run test -- tests/jsonld-person.spec.ts` — JSON-LD spec GREEN locally.
    3. Commit: `git commit -m "feat(06-02): app/layout.tsx metadataBase + canonical + openGraph + Person JSON-LD (SEO-05, SEO-06)"`.

    Sub-task 8b — Per-route `generateMetadata` (or `metadata` export) on `/`, `/about`, `/work` (single commit):
    1. EDIT `app/page.tsx`:
       - Add or extend `export const metadata: Metadata = { title: 'Braeden Hodson', description: '<≤160-char one-line positioning>', alternates: { canonical: '/' } };` — use the site's one-line positioning (e.g., from Hero copy) for the description.
    2. EDIT `app/about/page.tsx`:
       - Add or extend `export const metadata: Metadata = { title: 'About · Braeden Hodson', description: '<≤160-char about-page summary>', alternates: { canonical: '/about' } };`.
    3. EDIT `app/work/page.tsx`:
       - Add or extend `export const metadata: Metadata = { title: 'Work · Braeden Hodson', description: '<≤160-char work-page summary>', alternates: { canonical: '/work' } };`.
    4. Verify titles are mutually distinct (3 different strings).
    5. Verify each description is ≤160 chars (manual check; specs assert at runtime).
    6. Run: `npm run typecheck && npm run lint && npm run build && npm run test -- tests/canonical-urls.spec.ts tests/metadata-per-route.spec.ts` — both GREEN locally.
    7. Commit: `git commit -m "feat(06-02): per-route metadata + canonical on /, /about, /work (SEO-01, SEO-06)"`.

    Do NOT add `<Script>` for JSON-LD. Do NOT add a 2nd 'use client'. Do NOT set `metadata.openGraph.images` (file-based picks up automatically). Use Edit tool, not heredoc.
  </action>
  <verify>
    <automated>grep -c "metadataBase" app/layout.tsx && grep -c "application/ld+json" app/layout.tsx && grep -c "@type.*Person" app/layout.tsx && grep -c "canonical" app/page.tsx app/about/page.tsx app/work/page.tsx && npm run test -- tests/jsonld-person.spec.ts tests/canonical-urls.spec.ts tests/metadata-per-route.spec.ts</automated>
    Expected: each grep returns ≥1; the 3 specs GREEN locally.
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` has `metadataBase: new URL('https://braehods.com')`.
    - `app/layout.tsx` contains `<script type="application/ld+json">` block with Person JSON-LD.
    - JSON-LD `sameAs` is `[site.socials.github, site.socials.instagram]` (filtered for boolean) — NO YT URL.
    - `app/page.tsx` + `app/about/page.tsx` + `app/work/page.tsx` each have `metadata.alternates.canonical` set.
    - 3 page titles are mutually distinct.
    - `tests/jsonld-person.spec.ts` + `tests/canonical-urls.spec.ts` + `tests/metadata-per-route.spec.ts` all GREEN locally.
    - `grep -c "'use client'" app/layout.tsx` returns 0 (layout is Server Component).
    - 2 commits: Sub-task 8a + Sub-task 8b.
  </acceptance_criteria>
  <done>SEO-01 + SEO-05 + SEO-06 surfaces shipped. 3 RED specs from Task 1 flip GREEN locally.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 9 (W2): Push to Vercel preview + run full Playwright SEO suite + 6 Lighthouse audits</name>
  <files>(no source edits — env-driven existing suite)</files>
  <read_first>
    - playwright.config.ts (PLAYWRIGHT_BASE_URL env-var hook)
    - tests/lighthouse.spec.ts (the updated threshold=95 spec from Task 1)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md § "Plan 06-02 exit gate"
    - .planning/phases/06-polish-seo-launch/06-RESEARCH.md § Open Question #5 (404 not in Lighthouse scope)
  </read_first>
  <what-built>
    Tasks 2-8 produced 9+ commits: monogram triple-source, vendored TTFs, sitemap.ts, robots.ts, proxy.ts, static OG + script, dynamic /about + /work OG, CTAArrowLink direction prop, 404 page, layout metadata + JSON-LD, per-route metadata. Working tree clean. This task pushes those commits and runs the full audit + 6 Lighthouse runs against the new preview URL.
  </what-built>
  <how-to-verify>
    1. `git status` clean; `git log --oneline -12` shows Plan 06-02 commits.
    2. `git push origin main` (Vercel webhook auto-deploys).
    3. Wait for Vercel pipeline ~3-5 min; capture new preview URL.
    4. Set `PLAYWRIGHT_BASE_URL=<preview-url>` (PowerShell: `$env:PLAYWRIGHT_BASE_URL = '<preview-url>'`).
    5. Run new SEO specs: `npm run test:full -- tests/sitemap-renders.spec.ts tests/robots-renders.spec.ts tests/og-images-render.spec.ts tests/jsonld-person.spec.ts tests/not-found-renders.spec.ts tests/preview-noindex.spec.ts tests/canonical-urls.spec.ts tests/metadata-per-route.spec.ts` — all 8 GREEN expected (preview-noindex now NOT skipped because BASE_URL contains `.vercel.app`).
    6. Run 6 Lighthouse audits: `npm run test:full -- tests/lighthouse.spec.ts` — both chromium-mobile + chromium-desktop projects × 3 routes (`/`, `/about`, `/work`) = 6 audits. Each must score ≥95 on Perf + A11y + BestPractices + SEO.
    7. For any audit < 95: capture the Lighthouse JSON report (the spec writes to `.lighthouse-reports/` per Phase 1 infra OR re-run with `playAudit({ ...opts, reports: { formats: { html: true } } })`). Identify the lowest-scoring category + recommendations.
    8. For each Lighthouse < 95 failure: cycle back to source fix (the most common levers: PERF — image optimization, font preload, render-blocking JS; A11Y — color contrast, missing alt, missing label; BestPractices — HTTPS-only resource, no console errors; SEO — meta description, crawlable links). Re-deploy + re-audit.
    9. Document all 6 scores in 06-02-SUMMARY.md (route × form factor × 4 category matrix).
    10. Run regression canaries: `npm run test:full -- tests/contact-modal-*.spec.ts tests/favicon.spec.ts tests/footer-socials-render.spec.ts tests/about-renders.spec.ts tests/work-grid-renders.spec.ts tests/single-client-island.spec.ts tests/no-client-components.spec.ts` — all GREEN.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "audits-clean: <6-score-summary>" e.g., `audits-clean: /-mobile 97/100/96/98, /-desktop 99/100/96/100, /about-mobile 96/100/95/100, ...` OR "audits-failed: <route> <category> <score>".</resume-signal>
  <acceptance_criteria>
    - New preview URL accessible at HTTPS 200.
    - All 8 new SEO specs GREEN on both Playwright projects against preview.
    - All 6 Lighthouse audits score ≥95 on all 4 categories.
    - All regression canaries (contact modal, favicon, footer socials, about, work, single-client-island, no-client-components) still GREEN.
    - `preview-noindex.spec.ts` no longer skipped (BASE_URL contains `vercel.app`) and asserts `X-Robots-Tag: noindex`.
    - All 6 Lighthouse scores documented in 06-02-SUMMARY.md.
  </acceptance_criteria>
  <done>Preview-URL audit + SEO surface verification complete. PERF-01 + PERF-02 + SEO-01..06, SEO-08, SEO-09 all GREEN at preview level.</done>
</task>

<task type="auto">
  <name>Task 10 (W2): PERF-03 bundle measurement via `next experimental-analyze`</name>
  <files>scripts/measure-first-page-bundle.mjs, .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-RESEARCH.md § Pivot #1 (D-11 override: `@next/bundle-analyzer` incompatible with Turbopack; use `next experimental-analyze` instead)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md task 06-02-13
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-11 (researcher Pivot #1 supersedes)
    - .planning/REQUIREMENTS.md § PERF-03 ("≤50KB gz excluding contact modal client island")
  </read_first>
  <action>
    Per researcher Pivot #1, use Next 16.1+ Turbopack-native `next experimental-analyze` (NOT `@next/bundle-analyzer` webpack plugin). Zero new deps.

    Steps:
    1. Run: `npx next experimental-analyze --output` (or simply `npx next experimental-analyze` depending on the Next 16 flag — check `npx next experimental-analyze --help` for the right invocation). Output lands at `.next/diagnostics/analyze/client.html` (Turbopack convention).
    2. CREATE `scripts/measure-first-page-bundle.mjs`:
       - Read `.next/diagnostics/analyze/client.html` (or the JSON variant if it exists — Turbopack's analyze output structure may vary; verify by inspecting after step 1).
       - Parse out:
         - Total first-page bundle size (gzipped) for the `/` route.
         - The ContactModal chunk size (identified by file path `components/contact/ContactModal`).
         - First-page-minus-ContactModal = the PERF-03 target.
       - Log JSON: `{ first_page_bundle_gz_kb: N, contact_modal_chunk_gz_kb: M, target_excluding_modal_kb: N - M, perf_03_pass: N - M <= 50 }`.
       - Output to stdout AND write to `.planning/phases/06-polish-seo-launch/perf-03-measurement.json`.
    3. Run the script: `node scripts/measure-first-page-bundle.mjs`.
    4. If `perf_03_pass === false`: identify which non-modal chunk is over budget. Likely levers: lazy-load any non-LCP component, defer analytics scripts (they should be small enough already at ~2KB total), tree-shake unused lucide icons. Document the failure mode in 06-02-SUMMARY.md and cycle back to a source fix.
    5. If `perf_03_pass === true`: document the numbers in 06-02-SUMMARY.md § PERF-03 Bundle Measurement.
    6. Commit: `git add scripts/measure-first-page-bundle.mjs && git commit -m "perf(06-02): bundle measurement script via next experimental-analyze (PERF-03)"`.
    7. Do NOT add `@next/bundle-analyzer` as a devDep — it's webpack-only and breaks under Turbopack.
    8. Do NOT commit `.next/` artifacts (they're in .gitignore); only commit the script + the measurement output in 06-02-SUMMARY.md.
  </action>
  <verify>
    <automated>npx next experimental-analyze --help 2>&1 | grep -E "analyze|experimental" && test -f scripts/measure-first-page-bundle.mjs && node scripts/measure-first-page-bundle.mjs 2>&1 | grep -E "perf_03_pass.*true"</automated>
    Expected: `next experimental-analyze` is recognized; script exists; output JSON shows `perf_03_pass: true`.
  </verify>
  <acceptance_criteria>
    - `scripts/measure-first-page-bundle.mjs` exists, exits 0.
    - Script output shows `first_page_bundle_gz_kb`, `contact_modal_chunk_gz_kb`, `target_excluding_modal_kb`, `perf_03_pass`.
    - `target_excluding_modal_kb ≤ 50`.
    - No `@next/bundle-analyzer` in `package.json` devDependencies (verify Pivot #1 applied).
    - Measurement documented in 06-02-SUMMARY.md.
  </acceptance_criteria>
  <done>PERF-03 bundle measured via Turbopack-native tool; ≤50KB gz target verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 11 (W2): Manual A11Y + visual audit — 200% zoom, 320px, NVDA, VoiceOver iOS, Google Rich Results</name>
  <files>.planning/phases/06-polish-seo-launch/06-02-SUMMARY.md, .planning/phases/06-polish-seo-launch/visual-checklist.md (mark Section 6 + 7)</files>
  <read_first>
    - .planning/phases/06-polish-seo-launch/06-CONTEXT.md § D-12 (NVDA + VoiceOver iOS only; JAWS skipped)
    - .planning/phases/06-polish-seo-launch/06-VALIDATION.md § "Manual-Only Verifications" + tasks 06-02-14..17
    - .planning/phases/06-polish-seo-launch/visual-checklist.md (Section 6 = 404; Section 7 = OG sharing — both deferred from Plan 06-01)
    - .planning/phases/05-contact-modal/05-03-SUMMARY.md § Phase 6 Carry-Forwards items #9 + #10
  </read_first>
  <what-built>
    All 8 SEO surfaces are live + audit specs GREEN + Lighthouse 6/6 GREEN + bundle ≤50KB gz. This task closes the manual-only audit items: keyboard order walk, 200% zoom, 320px viewport, NVDA, VoiceOver iOS, Google Rich Results Test, OG validation in social platforms.
  </what-built>
  <how-to-verify>
    1. **A11Y-01 keyboard walk (manual + axe-smoke):**
       - On preview URL, Tab through `/`, `/about`, `/work` end-to-end (no mouse). Verify each interactive element receives focus in DOM order; focus ring is visible electric-blue 2px.
       - Run `npm run test:full -- tests/contrast.spec.ts` (existing axe-driven spec from Phase 1+) against preview — confirm no critical violations.
    2. **A11Y-04 alt text audit:**
       - Open each route in DevTools → Lighthouse A11Y panel; verify "Image elements have [alt] attributes" passes (covered by axe rule `image-alt`).
       - Spot-check: `<img alt>` on hero photo + favicon + monograms (decorative → `alt=""` or `aria-hidden`).
    3. **A11Y-07 200% zoom + LNCH-05 320px viewport:**
       - DevTools → Toggle Device Toolbar → 320 × 568 (iPhone SE 1st gen). Verify NO horizontal scroll on `/`, `/about`, `/work`, AND ContactModal open at 320px.
       - Reset viewport → Browser zoom Ctrl+Shift+= to 200%. Verify NO horizontal scroll on `/`, `/about`, `/work`.
       - Document outcomes per route in 06-02-SUMMARY.md.
    4. **NVDA walk (Windows):**
       - Install NVDA from https://www.nvaccess.org/ if not already.
       - Run NVDA. Tab through `/`, `/about`, `/work`. Verify each link/button announces purpose.
       - Open ContactModal. Verify "dialog" role + label announced. Submit form. Verify state transitions announced via aria-live.
       - Trigger error path (submit with invalid email). Verify error region announces with "alert" urgency (the Plan 06-01 Cat C role="alert" addition).
       - Document any P0 SR bugs in 06-02-SUMMARY.md; cycle back to inline source fix if found.
    5. **VoiceOver iOS walk:**
       - Open `<preview>` on a physical iPhone Safari.
       - Settings → Accessibility → VoiceOver → ON.
       - Same walk as NVDA: `/`, `/about`, `/work`, ContactModal open + submit + error.
       - Document outcomes in 06-02-SUMMARY.md.
    6. **Google Rich Results Test (SEO-05):**
       - Visit https://search.google.com/test/rich-results
       - Paste `<preview-url>/` and run.
       - Verify "Page is eligible for rich results" + "Person" detected with sameAs array.
       - Screenshot result; save path to 06-02-SUMMARY.md.
    7. **OG validation (SEO-02 / SEO-03 — also LNCH-04 dry-run on preview):**
       - Open Slack DM-to-self; paste `<preview>/`. Confirm unfurled card shows the static 1200×630 OG.
       - Same for `/about` (dynamic ImageResponse) and `/work` (dynamic).
       - Same in iMessage (DM-to-self, or use https://www.opengraph.xyz/).
       - Document which platforms render correctly in 06-02-SUMMARY.md (target ≥2 for LNCH-04; 4 = full).
    8. **Visual checklist Section 6 (404) + Section 7 (OG sharing):**
       - Walk `.planning/phases/06-polish-seo-launch/visual-checklist.md` sections 6 + 7 (previously deferred from Plan 06-01 because surfaces didn't exist yet).
       - Mark each item ✓ or ✗; commit the marked-up file.
    9. **OG canvas Y-position confirmation (UI-SPEC Dimension 5 FLAG):**
       - Eyeball the static `/` OG image (open `<preview>/opengraph-image.png` directly in browser).
       - Confirm the monogram + wordmark composition reads cleanly. If optical drift is worse than spec, edit `scripts/build-static-og.mjs` to use y=275 + y=445 instead of multiples-of-4, re-run the script, re-commit the new PNG, redeploy. Document the resolution choice in 06-02-SUMMARY.md.
  </how-to-verify>
  
  <action>This is a checkpoint task. See &lt;what-built&gt; for completed automation context and &lt;how-to-verify&gt; for the exact verification steps the human walks. The executor pauses here until the &lt;resume-signal&gt; is received.</action>
  <verify>
    <automated>echo "checkpoint:human gate — automated verification deferred to &lt;how-to-verify&gt; manual steps; executor pauses on this task per gate=blocking"</automated>
  </verify>
  <resume-signal>Type "audit-clean: zoom=ok, 320=ok, nvda=ok, voiceover=ok, google=eligible, og=<platforms>" OR "audit-failures: <details>".</resume-signal>
  <acceptance_criteria>
    - Keyboard tab order documented as logical on all 3 routes.
    - axe-core smoke passes (no critical violations).
    - 320px viewport: no horizontal scroll on `/`, `/about`, `/work`, ContactModal.
    - 200% zoom: no horizontal scroll on `/`, `/about`, `/work`.
    - NVDA walk announces dialog role + aria-live state transitions + error alert.
    - VoiceOver iOS walk produces equivalent announcements.
    - Google Rich Results Test reports Person detected with sameAs array; no errors.
    - OG validation: at least 2 platforms (Slack + iMessage minimum) render correct 1200×630 OG.
    - Visual checklist Sections 6 + 7 marked ✓.
    - Static `/` OG Y-position resolution documented (either kept multiples-of-4 OR reverted to y=275/445 per UI-SPEC FLAG).
    - All findings recorded in 06-02-SUMMARY.md.
  </acceptance_criteria>
  <done>All manual A11Y + SEO audits clean. Plan 06-02 exit gate satisfied at audit level.</done>
</task>

<task type="auto">
  <name>Task 12 (W2): Write 06-02-SUMMARY.md + traceability flips</name>
  <files>.planning/phases/06-polish-seo-launch/06-02-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/06-polish-seo-launch/06-01-SUMMARY.md (recent reference)
    - .planning/REQUIREMENTS.md (locate SEO-01..06, SEO-08, SEO-09, A11Y-01, A11Y-04, A11Y-07, PERF-01..03, LNCH-05 rows)
    - .planning/STATE.md
  </read_first>
  <action>
    1. CREATE `.planning/phases/06-polish-seo-launch/06-02-SUMMARY.md` per SUMMARY template. Required content:
       - Frontmatter: phase, plan, status (`complete`), subsystem, tags ([seo, og, jsonld, sitemap, robots, 404, lighthouse, bundle-measurement, screen-reader, monogram-triple-source, ttf-vendored]), dependency graph (requires Plan 06-01 preview URL; provides clean preview URL with all SEO + A11Y + PERF gates GREEN), tech tracking, key-files (created: sitemap.ts, robots.ts, not-found.tsx, opengraph-image × 3, proxy.ts, monogram-path.ts, build-static-og.mjs, measure-first-page-bundle.mjs, 2 TTFs, OFL.txt; modified: layout.tsx, page.tsx × 3, CTAArrowLink.tsx, MonogramMark.tsx, icon.svg), key-decisions (researcher Pivot #1 applied → `next experimental-analyze`; UI-SPEC FLAG resolution on OG y-positions; Geist TTF vendored atomically with Fraunces TTF per UI-SPEC OQ-1), patterns-established (triple-source monogram contract extends Phase 1 D-04 dual-write), requirements-completed (the full Plan-06-02 requirements list), metrics.
       - **Body sections (required):** Performance, Key Findings (Pivot #1 + Pivot #2 applied), Lighthouse Audit Matrix (6 rows: route × form-factor × 4 categories), PERF-03 Bundle Measurement (numeric), Screen-Reader Walk Outcomes (NVDA + VoiceOver iOS), Visual Checklist Sections 6 + 7 outcomes, Google Rich Results Outcome, OG Platform Validation Matrix, Anti-Patterns Avoided (no @next/bundle-analyzer; no 2nd 'use client'; no `<Script>` for JSON-LD; no metadata.openGraph.images override), Phase 6 Carry-Forwards remaining (Plan 06-03 DNS + production-gate + real-device).
    2. FLIP traceability in `.planning/REQUIREMENTS.md`:
       - SEO-01..06, SEO-08, SEO-09 → `Complete (verified on preview)`.
       - A11Y-01, A11Y-04, A11Y-07 → `Complete (manual audit on preview)`.
       - PERF-01, PERF-02 → `Complete (Lighthouse ≥95 on 6 audits)`.
       - PERF-03 → `Complete (bundle ≤50KB gz measured via next experimental-analyze)`.
       - LNCH-05 → `Complete (320px no horizontal scroll verified on preview)`.
    3. UPDATE `.planning/STATE.md`:
       - Append Last Session entry summarizing Plan 06-02 close.
       - Bump `progress.completed_plans` by 1.
       - Set Current focus → "Phase 6 Plan 06-03 (Launch)".
    4. Commit: `gsd-sdk query commit "docs(06-02): plan 06-02 SUMMARY + traceability flip" --files .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md .planning/REQUIREMENTS.md .planning/STATE.md`
  </action>
  <verify>
    <automated>test -f .planning/phases/06-polish-seo-launch/06-02-SUMMARY.md && grep -c "Complete (verified on preview)" .planning/REQUIREMENTS.md</automated>
    Expected: SUMMARY exists; REQUIREMENTS.md shows ≥8 verified-on-preview SEO entries.
  </verify>
  <acceptance_criteria>
    - 06-02-SUMMARY.md exists with full frontmatter + body sections.
    - REQUIREMENTS.md updated for SEO-01..06, SEO-08, SEO-09, A11Y-01, A11Y-04, A11Y-07, PERF-01..03, LNCH-05.
    - STATE.md Last Session + progress fields updated.
    - All 3 files committed in single docs commit.
  </acceptance_criteria>
  <done>Plan 06-02 closed at SUMMARY level. Plan 06-03 (Launch) ready to dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search engine crawler ↔ Vercel preview URL | preview-noindex header must be set so preview content does not appear in Google search results. Without this, the preview URL could rank for "Braeden Hodson" and confuse production traffic. |
| Social platform OG crawler ↔ OG image route | Facebook/Slack/Twitter crawlers fetch `/opengraph-image.png` + `/about/opengraph-image` + `/work/opengraph-image` without authentication. Image rendering must not expose PII or crash on malformed request. |
| `app/layout.tsx` Person JSON-LD ↔ `data/site.ts` | JSON-LD content reads from `data/site.ts.socials`; a malicious edit to `data/site.ts` could inject hostile URLs into the `sameAs` array. Phase 2 already vetted `data/site.ts` content; Phase 6 introduces no new write path. |
| ImageResponse runtime ↔ vendored TTF files | Edge runtime reads `assets/Fraunces-Bold.ttf` + `assets/GeistSans-Bold.ttf`; corrupt or attacker-substituted TTF could crash the ImageResponse build or produce malformed PNG. Mitigation: vendor TTFs via known-good sources (Google Fonts / vercel/geist-font releases); MD5 documented for tampering detection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information disclosure | preview URL indexable by search engines | medium | mitigate | `proxy.ts` (Task 4c) sets `X-Robots-Tag: noindex` when `VERCEL_ENV === 'preview'`. Verified by `tests/preview-noindex.spec.ts` (Task 1 stubbed; Task 9 verifies GREEN on preview). |
| T-06-05 | Tampering | XML injection via MDX frontmatter into `app/sitemap.ts` | low | mitigate | `app/sitemap.ts` reads `content/*.mdx` filenames via `readdir`; filenames cannot contain XML metacharacters in a filesystem. The URL strings constructed are based on slugs derived from filenames (alphanumeric + hyphens only). Future MDX frontmatter fields fed into sitemap (e.g., `lastModified`) must JSON.stringify or escape — Phase 6 v1 reads only `mtime` from `stat()`, which returns a Date — no injection vector. |
| T-06-06 | Tampering | Person JSON-LD script tag could be injected via malicious `data/site.ts` change | low | mitigate | `data/site.ts` is in-repo, requires PR to change. JSON.stringify of the personJsonLd object escapes any `<` or `>` in URL strings; even hostile sameAs URLs are bounded by JSON encoding. Additional protection: tests/jsonld-person.spec.ts asserts the parsed JSON matches the expected shape (Task 1 stub; will detect drift). |
| T-06-07 | Spoofing | OG image route renders attacker-controlled title | low | accept | Per UI-SPEC OQ-3 + Component Inventory, route titles ('About', 'Work') are hard-coded const in each `opengraph-image.tsx` file — not request-driven. No `searchParams` or URL params feed into the title rendering. v2 if `generateMetadata` outputs feed OG titles, revisit this threat. |
| T-06-08 | Denial of service | OG image generation timeout (cold start at edge) | low | mitigate | Edge runtime via `export const runtime = 'edge'` for fastest cold start. Fonts vendored locally (no external CDN dependency). Image dimensions fixed at 1200×630 (no user-controlled resize). Vercel automatically caches OG images per Next.js docs. |
| T-06-09 | Tampering | TTF file substitution | low | mitigate | Vendored TTFs sourced from Google Fonts + vercel/geist-font releases (known-good origins); MD5 documented in 06-02-SUMMARY.md (Task 3). Future TTF re-vendor must re-record MD5; CI could grep for MD5 mismatch (out of scope for v1). |
| T-06-SC | Tampering | npm package installs | n/a | accept | NO new package installs in Plan 06-02 (zero new runtime + zero new devDeps per researcher Pivot #1 removing `@next/bundle-analyzer`). No legitimacy gate needed. |
</threat_model>

<verification>
Plan 06-02 exit gate (combined acceptance per CONTEXT § "Plan 06-02 audit gate exit criteria"):
1. All 6 Lighthouse audits ≥95 across all 4 categories (Task 9)
2. PERF-03 first-page bundle ≤50KB gz excluding ContactModal chunk (Task 10)
3. NVDA + VoiceOver iOS walk-throughs clean; any P0 SR bug fixed inline (Task 11)
4. 200% zoom + 320px viewport overflow verify clean on preview (Task 11)
5. All 8 new SEO specs GREEN against preview URL (Task 9)
6. Google Rich Results Test validates Person JSON-LD (Task 11)
7. OG validation: ≥2 platforms render correctly (Task 11; LNCH-04 dry-run continues in Plan 06-03 on prod)
</verification>

<success_criteria>
Plan 06-02 is complete when:
- All 12 tasks done and committed.
- 8 new SEO Playwright specs GREEN on both projects against the preview URL.
- 6 Lighthouse audits (3 routes × 2 form factors) all ≥95 on Perf + A11y + BestPractices + SEO.
- First-page bundle ≤50KB gz (excluding ContactModal chunk) measured via `next experimental-analyze`.
- NVDA + VoiceOver iOS walks produce clean state announcements on all 3 routes + ContactModal.
- Google Rich Results Test reports Person JSON-LD eligible.
- 200% zoom + 320px viewport produce no horizontal scroll on all 3 routes.
- Static `/` OG renders correctly in ≥2 social platforms.
- 06-02-SUMMARY.md committed with full audit matrix + bundle numbers + SR walk outcomes.
- All Phase 6 audit-tier requirements (SEO + A11Y + PERF) flipped to Complete in REQUIREMENTS.md.
- No new client islands; no `@next/bundle-analyzer` devDep; no `<Script>` for JSON-LD.
</success_criteria>

<output>
Create `.planning/phases/06-polish-seo-launch/06-02-SUMMARY.md` on Task 12. Commit message: `docs(06-02): plan 06-02 SUMMARY + traceability flip`.
</output>
