---
phase: 02-home-page
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - components/home/HeroPhoto.tsx
  - components/home/CurrentlyLine.tsx
  - lib/format.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-04
  - PERF-04
  - PERF-06
must_haves:
  truths:
    - "D-23: components/home/HeroPhoto.tsx renders an unanimated next/image with priority + width=320 + height=320 + quality=90 + placeholder='blur' from a static import of /public/portrait.jpg — explicit dimensions and static-import blur per D-23 (HOME-04, PERF-04)"
    - "D-03: HeroPhoto content dimensions are 320×320px on desktop (medium tile per D-03) via width=320 height=320 + className w-60 h-60 md:w-80 md:h-80 for the responsive 240/320px reflow"
    - "D-04: HeroPhoto static-imports the v1 portrait from @/public/portrait.jpg (Plan 01 asset copied from ~/Projects/braehods/images/photo.jpg per D-04)"
    - "D-22: View-transition seam is a per-element inline style on the HeroPhoto wrapper only (D-22 — NO project-wide <ViewTransition> wrapper on the root layout)"
    - "HeroPhoto wraps the image in a div with className='rounded border border-[var(--color-border)] p-2' and inline style={{ viewTransitionName: 'hero-photo' }} carrying data-test='hero-photo-tile' (D-02, D-21, D-22)"
    - "D-10: components/home/CurrentlyLine.tsx consumes the CurrentlyStatement shape from data/currently.ts (D-10 source = data/currently.ts; statement + updatedAt + optional link)"
    - "D-11: CurrentlyLine renders accent dot + statement in --color-text + middle dot + <time dateTime=...> in --color-muted with formatDate output — visual treatment satisfies D-11 (updatedAt date present in muted micro-format; AA contrast on both gradient endpoints per CD-02)"
    - "lib/format.ts exports formatDate(iso: string): string producing 'May 9' from '2026-05-09' using Intl.DateTimeFormat with timeZone: 'UTC' (CD-02 server-rendered, no timezone surprises)"
    - "All three files are Server Components (zero 'use client' string) (FOUND-07 / D-25)"
    - "HeroPhoto carries no animation classes — no fadeInUp, no opacity transition, no transform on entry (D-24, HOME-04 — LCP element)"
    - "CurrentlyLine applies className={fadeInUp} + style={stagger(2)} so the line participates in the hero stagger at 160ms delay (CD-03)"
  artifacts:
    - path: "components/home/HeroPhoto.tsx"
      provides: "Server Component exporting HeroPhoto(): JSX.Element — wraps next/image with hairline-border tile + viewTransitionName seam (implements D-02, D-03, D-04, D-21, D-22, D-23, D-24)"
      exports: ["HeroPhoto"]
    - path: "components/home/CurrentlyLine.tsx"
      provides: "Server Component exporting CurrentlyLine({ data: CurrentlyStatement }) — accent dot + statement + ·  + Mono date (implements D-10, D-11, CD-02)"
      exports: ["CurrentlyLine"]
    - path: "lib/format.ts"
      provides: "Pure helper formatDate(iso: string): string returning 'MMM D' format (e.g. 'May 9' — no year, no leading-zero day)"
      exports: ["formatDate"]
  key_links:
    - from: "components/home/HeroPhoto.tsx"
      to: "public/portrait.jpg (Plan 01 asset)"
      via: "import portrait from '@/public/portrait.jpg'"
      pattern: "from\\s+'@/public/portrait\\.jpg'"
    - from: "components/home/CurrentlyLine.tsx"
      to: "lib/format.ts (this plan)"
      via: "import { formatDate } from '@/lib/format'"
      pattern: "from\\s+'@/lib/format'"
    - from: "components/home/CurrentlyLine.tsx"
      to: "lib/motion.ts (Phase 1 seam — fadeInUp + stagger)"
      via: "import { fadeInUp, stagger } from '@/lib/motion'"
      pattern: "fadeInUp,\\s*stagger"
    - from: "components/home/HeroPhoto.tsx (style={{ viewTransitionName: 'hero-photo' }})"
      to: "tests/view-transition-name-present.spec.ts (Plan 01 RED stub)"
      via: "getComputedStyle(wrapper).viewTransitionName === 'hero-photo'"
      pattern: "viewTransitionName:\\s*'hero-photo'"
---

<objective>
Phase 2 Wave 1 — Display-side hero atoms: HeroPhoto (the LCP element), CurrentlyLine (the dynamic-content row), and the formatDate utility. These three files have no dependencies on each other and no Phase 2 dependencies beyond Plan 01's asset + config. Plan 03 (interactive Hero atoms — ChannelButton family + CTAArrowLink) runs in parallel.

Purpose: Establish the LCP-safe photo tile + view-transition seam + accent-dot Currently treatment that Plan 06's Hero.tsx will compose. Every Phase 2 motion + view-transition decision goes through these files: HeroPhoto carries the no-animation invariant (D-24, HOME-04) and the `view-transition-name: hero-photo` seam (D-21); CurrentlyLine is the first real consumer of Phase 1's `lib/motion.ts` seam.

Output: Three Server Component files that compile, type-check, lint-clean, and turn `tests/view-transition-name-present.spec.ts` GREEN (the test only requires the photo tile to mount with the inline style; full hero composition lands in Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-home-page/02-CONTEXT.md
@.planning/phases/02-home-page/02-UI-SPEC.md
@.planning/phases/02-home-page/02-PATTERNS.md
@.planning/phases/01-foundation-design-tokens/01-CONTEXT.md
@CLAUDE.md
@lib/motion.ts
@app/globals.css
@data/currently.ts
@data/site.ts
@components/ui/MonogramMark.tsx

<interfaces>
<!-- Contracts the executor needs — embed verbatim. Executor does NOT explore. -->

## lib/motion.ts (Phase 1 seam — Phase 2 is first real consumer)
```ts
// lib/motion.ts (Phase 1; do not modify in this plan)
export const respectsReducedMotion: boolean;
export const fadeInUp: 'fade-in-up';
export function stagger(i: number): React.CSSProperties;  // { '--stagger': `${i * 80}ms` }
```

Usage:
```tsx
<p className={fadeInUp} style={stagger(2)}>...
```

CSS class `.fade-in-up` is defined in `app/globals.css` lines 76-79:
```css
.fade-in-up {
  animation: fade-in-up 400ms cubic-bezier(0.2, 0, 0, 1) both;
  animation-delay: var(--stagger, 0ms);
}
```
Duration is 400ms globally — do NOT re-tune. CD-03's per-element 80ms stagger increments are the authoritative pacing.

## CurrentlyStatement type (Phase 1, data/currently.ts — unchanged)
```ts
export interface CurrentlyStatement {
  statement: string;
  updatedAt: string;  // ISO YYYY-MM-DD
  link?: string;
}
```

## Phase 1 inline-style color convention (preserve in Phase 2)
Phase 1 uses inline `style={{ color: 'var(--color-muted)' }}` for token colors (see `components/layout/Footer.tsx` lines 18, 22; `app/%5Ftokens/page.tsx` lines 37, 47, 52). Tailwind arbitrary `text-[var(--color-muted)]` also works but inline-style is the shipping convention. Phase 2 follows the same pattern: inline style for color, Tailwind arbitrary for `border-[var(--color-border)]` (no inline shorthand for border-color).

## Photo file (Plan 01 output)
- Path: `public/portrait.jpg`
- Size: ~193KB (Plan 01 verifies ≤ 250KB)
- Dimensions: from the source file in `~/Projects/braehods/images/photo.jpg`. The static import + `next/image` will serve a width-matched `srcset`; do NOT manually resize.

## Decisions / discretion items implemented in this plan
- **D-01** side-by-side hero layout — Plan 06 composes; this plan only ships the photo tile sized for the right column (320px desktop, 240px mobile per CD-04).
- **D-02** hairline border + 8px inner padding + 4-6px corner radius — HeroPhoto wrapper.
- **D-03** 280-320px photo size — content dimensions 320×320; mobile reflow via Tailwind `w-60 h-60 md:w-80 md:h-80`.
- **D-04** photo source = `public/portrait.jpg` (v1 placeholder from braehods repo).
- **D-10** Currently source = `data/currently.ts`.
- **D-11** Currently must include `updatedAt` date in a muted micro-format.
- **D-21** view-transition seam = `view-transition-name: hero-photo` via inline style on HeroPhoto wrapper.
- **D-22** Per-element seam only — no project-wide `<ViewTransition>` wrapper on the root layout.
- **D-23** HeroPhoto uses `next/image` with explicit width=320 + height=320 + priority + static import (blur placeholder).
- **D-24** Hero photo + hero `<h1>` carry NO animation (LCP candidates). This plan ships HeroPhoto unanimated.
- **CD-02** Currently visual treatment = 6px accent dot + Geist Sans 14-15px statement + Geist Mono `· May 9` date suffix in muted.
- **CD-03** Motion choreography — CurrentlyLine = stagger(2) = 160ms delay.
- **CD-04** Mobile reflow — photo 240×240 on <md, 320×320 on md+.

## Pattern analog details (per 02-PATTERNS.md lines 102-154)

### HeroPhoto analog
Closest Phase 1 file: `components/ui/MonogramMark.tsx` (single visual asset wrapper with leading provenance comment + a11y discipline). Inline style + no animation discipline is novel in Phase 2.

### CurrentlyLine analog
Closest Phase 1 file: `app/%5Ftokens/page.tsx` color-row (lines 41-52) — "small visual + Geist Sans label + Geist Mono value in flex row." Transpose with accent dot replacing swatch + `<time>` replacing the second `<span>`.

### lib/format.ts analog
Closest Phase 1 file: `lib/motion.ts` (small pure-function module, leading source-trace comment, named exports, zero deps).

## Test data-attribute convention
Phase 1 uses `data-test="hero-section"` and `data-test="hero-display"` on the home page (`app/page.tsx`). Plan 06's Hero.tsx preserves both. This plan adds `data-test="hero-photo-tile"` on the HeroPhoto wrapper `<div>` so `tests/view-transition-name-present.spec.ts` and `tests/photo-lcp.spec.ts` (Plan 01 RED stubs) can locate it without a brittle CSS selector.

## Required Tailwind class invariants (UI-SPEC line 73)
- Allowed: integer multiples of 4 (`p-1`, `p-2`, `p-3`, `p-4`, `p-6`, `p-8`, `gap-2`, `gap-4`, `mt-4`, `mt-6`, `mt-8`).
- Forbidden: fractional (`p-0.5`, `p-1.5`, `p-2.5`, `gap-1.5`, `translate-x-0.5`).
- Documented exceptions (UI-SPEC line 73, 130-131):
  - Currently accent dot: `w-1.5 h-1.5` (6px diameter — explicit sub-grid exception per CD-02).
  - Image content dimensions (240/320px) — content, not spacers, applied via `w-60 h-60 md:w-80 md:h-80`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/format.ts with formatDate helper</name>
  <files>lib/format.ts, tests/format.spec.ts</files>
  <behavior>
    - Test 1: `formatDate('2026-05-09')` returns `'May 9'` (no year, no leading-zero day).
    - Test 2: `formatDate('2026-01-03')` returns `'Jan 3'` (single-digit day, no leading zero).
    - Test 3: `formatDate('2026-12-25')` returns `'Dec 25'` (two-digit day).
    - Test 4: `formatDate('2026-05-09')` returns the same string regardless of host timezone — invoke once with `process.env.TZ` unset and once with `process.env.TZ = 'America/Los_Angeles'`; both must return `'May 9'` (the UTC anchor prevents server-side timezone drift).
  </behavior>
  <action>
Create `lib/format.ts` following the `lib/motion.ts` shape (leading source-trace comment, named export, zero runtime deps). The function takes an ISO `YYYY-MM-DD` string and returns the formatted `'MMM D'` output. Use `Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', timeZone: 'UTC' })` against a `Date` built from `Date.UTC(year, monthIndex, day)` — this anchors the format to UTC so a `'2026-05-09'` input renders `'May 9'` regardless of host timezone (the "May 9 in PST is May 8 in UTC" hazard for plain `new Date('2026-05-09')` constructions).

Parse the ISO string by splitting on `-` and converting parts to numbers; pass the three numbers to `Date.UTC(year, month - 1, day)` (month is 0-indexed in JS Date).

Add a `tests/format.spec.ts` (Playwright `test` runner — even though this is a pure-function test, mirror the existing test infrastructure rather than introducing Vitest/Jest). The spec imports `formatDate` from `@/lib/format` (the Playwright runner picks up TS via tsx — verify by checking how existing specs import from `@/`; if `@/lib/motion` is not currently imported in any spec, use a relative path `../lib/format`). Each `behavior` case becomes one `test(...)`.

The file ships as a Server-side helper (no `'use client'`). `lib/motion.ts` exports `'use client'`-free code; mirror that.

Leading comment:
```
// lib/format.ts
// Source: PLAN.md 02-02-T1; UI-SPEC § CurrentlyLine (CD-02 date suffix).
//
// Date formatter helper. Server-side (RSC) — no client-side date math, no
// timezone surprises. Input: ISO YYYY-MM-DD. Output: "MMM D" (e.g. "May 9").
```

Function signature:
```ts
export function formatDate(iso: string): string
```

Do NOT export a default; named export only (matches `lib/motion.ts`). Do NOT use `moment`, `date-fns`, `dayjs`, or any other date library — `Intl.DateTimeFormat` is built into Node + browser and matches the "no extra deps" stack philosophy.
  </action>
  <verify>
    <automated>npx playwright test tests/format.spec.ts --project=chromium-desktop 2>&1 | findstr /R "passed failed"</automated>
  </verify>
  <done>
- `lib/format.ts` exists, exports `formatDate(iso: string): string` (named export).
- `tests/format.spec.ts` exists with 4 test cases per behavior block; all 4 pass.
- `npm run typecheck` passes — `formatDate` has explicit string parameter + return types.
- `npm run lint` passes.
- Grep `grep -c "'use client'" lib/format.ts` returns 0.
- `lib/format.ts` does NOT import any third-party date library (`grep -E "from\s+'(moment|date-fns|dayjs|luxon)'" lib/format.ts` returns nothing).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create components/home/CurrentlyLine.tsx consuming lib/format + lib/motion seams</name>
  <files>components/home/CurrentlyLine.tsx</files>
  <behavior>
    - Test 1 (via tests/currently-renders.spec.ts from Plan 01): on `/`, the page contains literal text from `currently.statement` (`'Currently shipping CapitolLens'`).
    - Test 2: page contains `<time dateTime="2026-05-09">` element with visible text `'May 9'`.
    - Test 3: page contains an `aria-hidden` span before the statement whose computed `background-color` is `rgb(124, 135, 255)` (the 6px accent dot per CD-02).
    - Test 4: when CurrentlyLine is mounted, its outermost `<p>` element has class containing `fade-in-up` and an inline `--stagger` custom property of `160ms` (= stagger(2)).
    - Test 5: when `data.link` is set (not the default case), the statement renders as `<a href="...">` with `underline underline-offset-4 decoration-1` classes. (Plan 01's RED spec only checks the default unset-link path; this behavior is in-component logic and verified by inspection.)
  </behavior>
  <action>
Create `components/home/CurrentlyLine.tsx` as a Server Component (no `'use client'`). Follow the analog at `app/%5Ftokens/page.tsx` lines 41-52 (the color-row flex pattern) — adapted per 02-PATTERNS.md lines 157-228.

Leading comment block:
```
// components/home/CurrentlyLine.tsx
// Source: PLAN.md 02-02-T2; UI-SPEC § CurrentlyLine (CD-02, HOME-02).
//
// Server Component (NO 'use client' — FOUND-07 / D-25). Renders the
// "Currently" line: accent dot + statement + middle dot + Geist Mono date.
// First real consumer of Phase 1's lib/motion.ts seam — applies fadeInUp +
// stagger(2) = 160ms delay (CD-03). Date formatting in lib/format.ts
// (server-side UTC-anchored — no client-side date math, no timezone surprises).
```

Imports:
```tsx
import { formatDate } from '@/lib/format';
import { fadeInUp, stagger } from '@/lib/motion';
import type { CurrentlyStatement } from '@/data/currently';
```

Props interface:
```tsx
interface CurrentlyLineProps {
  data: CurrentlyStatement;
}
```

Render structure (per UI-SPEC lines 263-271):
- Outer `<p>` with className combining `mt-6 flex items-center gap-2 text-base` AND `fadeInUp` (a single space-separated string). Inline style is `stagger(2)` — `stagger` returns `{ '--stagger': '160ms' }` cast as `React.CSSProperties`.
- Accent dot `<span aria-hidden className="inline-block w-1.5 h-1.5 rounded-full bg-[var(--color-accent)]" />` — 6px circle prefix (CD-02, w-1.5 = 6px in Tailwind v4 is the documented sub-grid exception per UI-SPEC line 73).
- Statement: if `data.link` is provided, wrap in `<a href={data.link} className="underline underline-offset-4 decoration-1">{data.statement}</a>`; otherwise `<span>{data.statement}</span>`. Use inline `style={{ color: 'var(--color-text)' }}` if Tailwind arbitrary feels heavier; either is accepted per the Phase 1 inline-style preference.
- Separator: `<span aria-hidden style={{ color: 'var(--color-muted)' }}>·</span>` — middle dot.
- Date: `<time dateTime={data.updatedAt} className="font-mono text-sm" style={{ color: 'var(--color-muted)' }}>{formatDate(data.updatedAt)}</time>`.

The component does NOT take `className` or `style` props for outside override — the contract owns its own styling per UI-SPEC. Hero.tsx (Plan 06) imports it and renders `<CurrentlyLine data={currently} />`.

**Per D-25:** zero `'use client'`. Per FOUND-07, the no-client-components.spec scans `app/`, `components/`, `lib/` for the literal string; this file lives under `components/home/` and MUST be clean.

**Per UI-SPEC line 73 invariant:** no fractional Tailwind utilities other than the documented `w-1.5 h-1.5` accent-dot exception (CD-02). Use `gap-2`, `mt-6` integer multiples elsewhere.

**Per CD-02:** the date suffix uses Geist Mono — applied via `font-mono` Tailwind utility (Phase 1's `app/fonts.ts` already wires `--font-geist-mono` and `app/globals.css @theme` maps `font-mono` to it).

Do NOT add `aria-label` or `role` attributes on the outer `<p>` — the visible content reads naturally for screen readers, and `<time dateTime>` already provides machine-readable date semantics.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('components/home/CurrentlyLine.tsx', 'utf8'); if (/'use client'/.test(src)) throw new Error('use client present'); if (!/stagger\(2\)/.test(src)) throw new Error('missing stagger(2)'); if (!/<time\b[^>]*dateTime=/.test(src)) throw new Error('missing <time dateTime= element'); if (!/aria-hidden[^>]*(rounded-full|bg-\[var\(--color-accent\)\])/.test(src) && !/(rounded-full|bg-\[var\(--color-accent\)\])[^>]*aria-hidden/.test(src)) throw new Error('missing aria-hidden accent dot span'); if (!/fadeInUp/.test(src)) throw new Error('missing fadeInUp class application'); if (!/from\s+'@\/lib\/format'/.test(src)) throw new Error('missing @/lib/format import'); if (!/from\s+'@\/lib\/motion'/.test(src)) throw new Error('missing @/lib/motion import'); console.log('OK');"</automated>
  </verify>
  <done>
- `components/home/CurrentlyLine.tsx` exists and exports `CurrentlyLine` as a named function component.
- File is a Server Component — `grep -c "'use client'" components/home/CurrentlyLine.tsx` returns 0.
- Imports use `@/` path alias for `lib/format`, `lib/motion`, `data/currently`.
- Component renders the 4 inner elements: accent dot + statement (or link) + middle dot + `<time>`.
- Outer `<p>` carries `fade-in-up` class + `stagger(2)` inline style.
- `npm run typecheck` passes — `data: CurrentlyStatement` typed param, the optional `data.link` branch is type-safe.
- `npm run lint` passes.
- Note: the Playwright spec `tests/currently-renders.spec.ts` stays RED at this point because it requires the component to be MOUNTED on `/` — Plan 06 lands Hero.tsx and wires it in. Plan 06 Task 2 turns that spec GREEN. This task's static-analysis verify above confirms the component itself is correctly authored; the runtime-mount verification is intentionally deferred to Plan 06 (cannot run before then without faking a host route).
- Spec `tests/format.spec.ts` (Task 1) still passes — no regression.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create components/home/HeroPhoto.tsx — LCP-safe photo tile with view-transition seam</name>
  <files>components/home/HeroPhoto.tsx</files>
  <behavior>
    - Test 1 (via tests/view-transition-name-present.spec.ts from Plan 01): when Hero is mounted on `/`, an element with `data-test="hero-photo-tile"` exists and `getComputedStyle(el).viewTransitionName === 'hero-photo'`.
    - Test 2 (via tests/photo-lcp.spec.ts from Plan 01): the rendered HTML for `/` contains `<img>` with `width="320"` and `height="320"` attributes (PERF-04 — explicit dimensions for LCP optimization).
    - Test 3: HeroPhoto carries NO `fade-in-up` class anywhere in its tree — `grep -E "fade-in-up|fadeInUp" components/home/HeroPhoto.tsx` returns nothing. The photo is the LCP element (D-24, HOME-04) and must not animate.
    - Test 4: the `<img>` carries `loading` of `eager` (browser-default when `priority` is set) — verified by inspecting that `priority` prop is present on `<Image>` in the source.
  </behavior>
  <action>
Create `components/home/HeroPhoto.tsx` as a Server Component (no `'use client'`). Follow the analog at `components/ui/MonogramMark.tsx` (single visual asset wrapper with leading provenance comment + a11y discipline) — adapted per 02-PATTERNS.md lines 102-154.

Leading comment block:
```
// components/home/HeroPhoto.tsx
// Source: PLAN.md 02-02-T3; UI-SPEC § HeroPhoto (D-01..D-04, D-21, D-22, D-23, D-24).
//
// Server Component (NO 'use client' — FOUND-07 / D-25). Wraps the v1 portrait
// asset in a hairline-border tile with 8px inner padding (D-02). Marked with
// `view-transition-name: hero-photo` (D-21) so Phase 3's /about photo can
// wire the matching name for a CSS-only shared-element transition.
// Per-element seam only — NO project-wide <ViewTransition> root wrapper (D-22).
// NO animation — this IS the LCP element (D-24, HOME-04, PERF-06).
```

Imports:
```tsx
import Image from 'next/image';
import portrait from '@/public/portrait.jpg';
```

The static import enables the blur placeholder without a manual `blurDataURL` — Next.js generates it at build time from the imported file (per Next 16 docs, supported in App Router RSC).

Render structure (per UI-SPEC lines 213-228 + 02-PATTERNS.md lines 124-144):

Outer wrapper `<div>`:
- `data-test="hero-photo-tile"` (so the RED specs can locate it without a brittle CSS selector).
- className: `rounded border border-[var(--color-border)] p-2 shrink-0` — 4px corner radius (`rounded` = `border-radius: 0.25rem` = 4px per UI-SPEC D-02), 1px hairline border at the locked border token, 8px inner padding (`p-2` per UI-SPEC), `shrink-0` so the photo column never compresses in the flex parent.
- Inline style: `style={{ viewTransitionName: 'hero-photo' }}` (D-21). This is a static-value style object on a Server Component — valid in React 19.2 RSC; no client state required. Per D-22 this is a PER-ELEMENT seam — no root-level `<ViewTransition>` wrapper anywhere in `app/layout.tsx`.

Inner `<Image>`:
- `src={portrait}` (the static-import variable).
- `alt="Braeden Hodson"` (UI-SPEC line 227 — image is meaningful; do NOT use `aria-hidden`).
- `width={320}` `height={320}` — explicit (PERF-04 — content dimensions on desktop per D-03; mobile size is handled via className `w-60 h-60 md:w-80 md:h-80`, the explicit `width`/`height` props are the layout-stable intrinsic size that `next/image` uses for CLS prevention).
- `priority` — flags as LCP, triggers Next 16's automatic preload (per CLAUDE.md "Image optimization" section + D-23).
- `placeholder="blur"` — uses the static-import-derived blurDataURL.
- `quality={90}` — Next 16 requires `images.qualities` to permit values other than `[75]`; Plan 01 already set `images: { qualities: [75, 90] }` in `next.config.ts`.
- className: `rounded-[2px] w-60 h-60 md:w-80 md:h-80 object-cover` — 2px inner corner radius (slightly smaller than wrapper's 4px so the 8px padding ring is visible per UI-SPEC line 224), responsive content size (240×240 < md, 320×320 ≥ md per CD-04), `object-cover` in case the source file is not perfectly square (per UI-SPEC line 226).

**NO animation classes.** Do NOT add `fade-in-up`, `opacity-0`, `transition-opacity`, or any transform. The component is at first paint and stays there (D-24, HOME-04, Pitfall 4).

**Per UI-SPEC line 230-235:** the view-transition-name lives in inline style (not Tailwind arbitrary, not a CSS class). Two reasons documented in UI-SPEC: (1) it works in Server Components without any `'use client'`; (2) Tailwind v4 supports arbitrary properties but Phase 2 is the first consumer of this CSS property in the codebase. Inline style is the lowest-friction surface.

**Mobile reflow:** the `w-60 h-60 md:w-80 md:h-80` Tailwind classes do the responsive work (240px at mobile, 320px at tablet+). The `width={320} height={320}` props on `<Image>` are the intrinsic dimensions Next.js uses for srcset and CLS calculations — `next/image` correctly serves a smaller rendered size when CSS scales it down. Do NOT use a `sizes` prop yet; Next 16 has good defaults for explicit-width images.

**Per D-25 / FOUND-07:** zero `'use client'`.

The component takes NO props — the photo source, sizing, alt text, and view-transition name are all owned by the contract. Hero.tsx (Plan 06) renders `<HeroPhoto />` with no attributes.

Note: TypeScript may flag the `'@/public/portrait.jpg'` static import if `tsconfig.json` doesn't declare an image-module typing. Phase 1's `next-env.d.ts` (generated by Next 16 dev) should include the necessary declarations; if not, add a `declare module '*.jpg'` ambient declaration to a new `app/types/images.d.ts` file. Verify by running `npm run typecheck` — if it fails on the import, add the declaration and retry; do NOT add `@ts-ignore`.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 && npm run lint -- components/home/HeroPhoto.tsx 2>&1 && (npm run build 2>&1 | findstr /C:"Compiled successfully")</automated>
  </verify>
  <done>
- `components/home/HeroPhoto.tsx` exists and exports `HeroPhoto` as a named Server Component (no props).
- File contains the literal string `viewTransitionName: 'hero-photo'` (verifiable by `grep -F "viewTransitionName: 'hero-photo'" components/home/HeroPhoto.tsx`).
- File contains `priority`, `width={320}`, `height={320}`, `placeholder="blur"`, `quality={90}`.
- File contains `data-test="hero-photo-tile"` on the wrapper `<div>`.
- File does NOT contain any of: `fade-in-up`, `fadeInUp`, `'use client'`, `aria-hidden` (on the `<Image>`).
- `npm run typecheck` passes.
- `npm run lint` passes.
- `npm run build` succeeds (this is the canary that proves `images.qualities` permits `quality={90}` and the static import resolves).
- Spec `tests/format.spec.ts` (Task 1) and `tests/currently-renders.spec.ts` (Task 2 mounted via Plan 06) remain unaffected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → /public/portrait.jpg | Build pipeline reads the static asset; Next.js processes blur-placeholder data at build time |
| browser → image origin | Same-origin (image served from the Vercel preview deployment); no remote patterns |
| RSC → typed data | `currently: CurrentlyStatement` is server-side data, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T (Tampering) | HeroPhoto static import + next/image | accept | Static import resolves at build time; no remote URL, no `remotePatterns`. The image origin is the same Vercel deployment serving the HTML — no third-party fetch surface. |
| T-02-07 | I (Information disclosure) | `view-transition-name: 'hero-photo'` CSS property exposed to client | accept | View Transitions API is a CSS-level seam; no PII transmitted. The string `hero-photo` is a coordination key shared with Phase 3's /about page. Public information by design. |
| T-02-08 | D (Denial of service) | Large blurDataURL inlined in HTML | mitigate | Next.js generates a tiny base64 thumbnail (~1KB) — automatic. Verify `npm run build` output reports `< 5KB` blur data per image; if a future image inflates this, regenerate with `next-image-export-optimizer` or downsample the source. For Plan 02 the 193KB portrait produces a standard blur ≈ 0.5-1KB inline. |
| T-02-09 | T (Tampering) | CurrentlyLine renders `data.statement` as React text | accept | React's JSX escaping prevents script injection. The `currently.statement` value originates from `data/currently.ts` (in-repo, code-reviewed). No external input path. |
| T-02-10 | T (Tampering) | CurrentlyLine renders `data.link` as `<a href={data.link}>` if set | mitigate | `data.link` is currently unset (CONTEXT.md `<deferred>` line 191). When the user populates it, type signature constrains to `string` (no array, no eval). For now, defensive: do NOT add `target="_blank"` on this link without `rel="noopener noreferrer"` — if `data.link` is internal (`/about/foo`), `target="_blank"` is wrong; if external, both attrs are required. Conservative default: omit `target="_blank"` from the statement link in CurrentlyLine; the link opens in the same tab. Revisit when the field is populated. |

`security_threat_model` (from planner-context) cited mandatory `target="_blank" rel="noopener noreferrer"` on external links. CurrentlyLine's `data.link` is type `string` and CONTEXT.md doesn't lock its target; the safer default is in-tab navigation. ChannelButton + SocialIconLink (Plans 03 + 04) own the `target="_blank" rel="noopener noreferrer"` contract because their links are unambiguously external (HOME-03 explicit).
</threat_model>

<verification>
1. `lib/format.ts` exists, exports `formatDate`, passes 4 behavior tests (Task 1).
2. `components/home/CurrentlyLine.tsx` exists, exports `CurrentlyLine`, type-checks against `CurrentlyStatement`, lint-clean (Task 2).
3. `components/home/HeroPhoto.tsx` exists, exports `HeroPhoto`, `npm run build` succeeds (proves `images.qualities` permits quality={90} and static import resolves) (Task 3).
4. Zero `'use client'` added (`grep -rn "'use client'" components/home/ lib/format.ts` returns nothing).
5. `tests/no-client-components.spec.ts` (Phase 1) continues to pass.
6. `tests/format.spec.ts` (this plan's new spec) passes.
7. `tests/view-transition-name-present.spec.ts` (Plan 01 RED stub) is structurally satisfied by HeroPhoto's inline style — but the spec runs against `/` and Hero.tsx is not yet mounted there (lands in Plan 06). The spec will turn GREEN at the end of Plan 06, not this plan. That is acceptable RED-staging.
</verification>

<success_criteria>
- 3 source files created (`lib/format.ts`, `components/home/CurrentlyLine.tsx`, `components/home/HeroPhoto.tsx`) + 1 spec file (`tests/format.spec.ts`).
- All three are Server Components — zero `'use client'` strings.
- `npm run typecheck` + `npm run lint` + `npm run build` all green.
- `tests/format.spec.ts` GREEN with 4 cases passing.
- HeroPhoto carries no animation (D-24 LCP-safe).
- CurrentlyLine wires fade-in-up + stagger(2) (CD-03 = 160ms).
- View-transition-name inline style present on HeroPhoto wrapper (D-21).
- Plan 06 (Wave 2) can begin: Hero.tsx will import and compose all three.
- Plan 03 (Wave 1, parallel) is unaffected — no file overlap.
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-page/02-02-SUMMARY.md` recording:
- Function signatures + export names for all three new files.
- Confirmation that `next.config.ts images.qualities: [75, 90]` (from Plan 01) permitted the build.
- Any TS module declaration added for `*.jpg` imports (if needed).
- File sizes (line counts) for each new component.
- Phase 1 specs that remain GREEN (regression check).
</output>
</output>
</output>
