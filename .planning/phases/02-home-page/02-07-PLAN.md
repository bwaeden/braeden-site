---
phase: 02-home-page
plan: 07
type: execute
wave: 4
depends_on: [02-04, 02-05, 02-06]
files_modified:
  - .planning/phases/02-home-page/02-VERIFICATION.md
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - PERF-04
  - PERF-06
must_haves:
  truths:
    - "All 22 Phase 2 specs (Phase 1's 13 + Plan 02's tests/format.spec.ts + Plan 01's 8 new RED stubs) pass GREEN against a Vercel branch preview URL"
    - "Lighthouse mobile LCP on `/` < 2.5s (PERF-06)"
    - "axe-core a11y scan on `/`, `/about`, `/work` reports zero serious/critical violations"
    - "28-item Phase Exit visual verification checklist from UI-SPEC lines 744-770 confirmed by user against the deployed branch preview URL (NOT the production alias — STATE.md Phase 1 W4-T2 lesson)"
    - "Branch merged to main (or staged for main merge) and the Vercel production deploy pre-stages SSL for braehods.com + www.braehods.com without flipping DNS (Phase 6 owns the DNS swap)"
    - "Zero Phase 1 regressions: tests/monogram.spec.ts + tests/focus-ring.spec.ts + tests/visual.spec.ts + tests/tokens.spec.ts + tests/no-client-components.spec.ts + tests/reduced-motion.spec.ts all pass GREEN"
  artifacts:
    - path: ".planning/phases/02-home-page/02-VERIFICATION.md"
      provides: "Plan 07 deploy + verification log: branch preview URL, all spec results, Lighthouse JSON, 28-item checklist outcomes, any deviations + their dispositions"
  key_links:
    - from: "branch test/phase-2-home"
      to: "Vercel branch preview URL (form: braeden-site-<hash>-<scope>.vercel.app)"
      via: "vercel for github automatic deploy on push (Phase 1 W4-T1 configured)"
      pattern: "vercel\\.app"
    - from: "PLAYWRIGHT_BASE_URL env var"
      to: "Branch preview URL"
      via: "npx playwright test runs against PLAYWRIGHT_BASE_URL=https://<preview-url>"
      pattern: "PLAYWRIGHT_BASE_URL=https://"
    - from: "Phase 2 W4 visual sign-off"
      to: ".planning/phases/02-home-page/02-VERIFICATION.md + commit on main"
      via: "User reviews 28-item checklist against deployed preview; signs off; executor squash-merges to main"
      pattern: "PHASE 2 SHIP"
---

<objective>
Phase 2 Wave 4 — Validate + deploy. Push the integrated Phase 2 build to a Vercel branch preview, run the full 22-spec Playwright + axe + Lighthouse suite against the deployed URL, walk the 28-item Phase Exit visual checklist with the user, and squash-merge to main once everything is green. This is the user-facing acceptance gate for HOME-01..06 + PERF-04 + PERF-06.

Purpose: Phase 1 W4 set the precedent — local-clean is necessary but not sufficient. The Vercel build + deployment must reproduce the local result, the LCP target (PERF-06: <2.5s on mid-tier mobile) MUST be verified on real CDN-served output, and the user MUST eyeball the visual contract before merge. Plan 06 turned 5 RED specs GREEN locally; this plan extends the same coverage to the deployed preview and adds the 3 remaining specs that need real network/Lighthouse data (`photo-lcp`, `footer-socials-render` against deployed footer, and full Lighthouse run).

Output: A Vercel branch preview URL with 22/22 specs GREEN, Lighthouse mobile LCP < 2.5s, axe clean on all three routes, the 28-item visual checklist signed off, and the branch merged to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-home-page/02-CONTEXT.md
@.planning/phases/02-home-page/02-UI-SPEC.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-tokens/01-PLAN.md
@playwright.config.ts

<interfaces>
<!-- Contracts the executor needs — embed verbatim. -->

## Phase 1 W4 lessons (STATE.md — must carry forward)
1. **Branch preview != production alias.** STATE.md Phase 1 W4-T2 documents: `https://braeden-site.vercel.app` is the PRODUCTION alias and serves a cached version. Branch-preview URLs are of the form `braeden-site-<hash>-<scope>.vercel.app` and are emitted by Vercel after every push. Plan 07 must use the BRANCH PREVIEW URL.
2. **CSS serializer quirks** documented in Phase 1 specs (Chromium outline ordering, gradient angle normalization, `0.01ms` → `1e-05s`). Phase 2 specs (Plan 01) already accommodate these — but if a spec flakes against the deployed URL, look here first.
3. **`<vercel-live-feedback>` element** is injected into the page on Vercel previews — tab-walker specs must skip elements with hyphens in the tagName. Phase 2's specs don't have a Tab walker (Phase 1's `tests/focus-ring.spec.ts` does); inherit the pattern if needed.
4. **W4-T4 timing evidence** — Phase 1 stages multiple empty preview-deploys to confirm Vercel build duration ≤ 120s. Phase 2 doesn't need this — Vercel project is already linked from Phase 1.

## Vercel project (Phase 1 W4-T1 — already configured)
- Project name: `braeden-site` (Vercel)
- Linked GitHub repo: `bwaeden/braeden-site` (per STATE.md)
- Production branch: `main`
- Custom domains: `braehods.com` + `www.braehods.com` (status: "Invalid Configuration" pending DNS flip in Phase 6; SSL pre-stages)
- Env vars: `NEXT_PUBLIC_FORMSPREE_ID=xqeypnkw` (Phase 1 W4-T1, unused in Phase 2; Phase 5 consumes)
- Framework: Next.js auto-detected
- Preview deployments: enabled on every branch + PR (default)

## Branching strategy for Phase 2
Phase 1 used branches `test/phase-1-preview` and `test/phase-1-found-05-final`. Phase 2 follows the same pattern: `test/phase-2-home` (or similar) — executor's call. Each push to the branch generates a new preview URL. Final merge to `main` happens at the end of Plan 07.

## Spec inventory (after Plans 02-06 complete)

| # | Spec file | Source | Owner Plan |
|---|-----------|--------|-----------|
| 1 | `tests/build-output.spec.ts` | Phase 1 | n/a |
| 2 | `tests/folder-structure.spec.ts` | Phase 1 | n/a |
| 3 | `tests/no-client-components.spec.ts` | Phase 1 | n/a |
| 4 | `tests/no-bare-outline-none.spec.ts` | Phase 1 | n/a |
| 5 | `tests/visual.spec.ts` | Phase 1 | n/a |
| 6 | `tests/tokens.spec.ts` | Phase 1 | n/a |
| 7 | `tests/lighthouse.spec.ts` | Phase 1 (CLS=0) | n/a |
| 8 | `tests/monogram.spec.ts` | Phase 1 | n/a |
| 9 | `tests/focus-ring.spec.ts` | Phase 1 | n/a |
| 10 | `tests/motion-seam.spec.ts` | Phase 1 (lib/motion contract) | n/a |
| 11 | `tests/reduced-motion.spec.ts` | Phase 1 | n/a |
| 12 | `tests/contrast.spec.ts` | Phase 1 (axe on /_tokens) | n/a |
| 13 | `tests/favicon.spec.ts` | Phase 1 | n/a |
| 14 | `tests/format.spec.ts` | Plan 02 (lib/format unit tests) | Plan 02 |
| 15 | `tests/hero-renders.spec.ts` | Plan 01 (RED) | Plan 06 |
| 16 | `tests/photo-lcp.spec.ts` | Plan 01 (RED) | Plan 07 (deploy-gated) |
| 17 | `tests/currently-renders.spec.ts` | Plan 01 (RED) | Plan 06 |
| 18 | `tests/channels-render.spec.ts` | Plan 01 (RED) | Plan 06 |
| 19 | `tests/ctas-resolve-200.spec.ts` | Plan 01 (RED) | Plans 05 + 06 |
| 20 | `tests/view-transition-name-present.spec.ts` | Plan 01 (RED) | Plans 02 + 06 |
| 21 | `tests/footer-socials-render.spec.ts` | Plan 01 (RED) | Plans 04 + 06 |
| 22 | `tests/mobile-hero-stacks-cleanly.spec.ts` | Plan 01 (RED) | Plan 06 |

22 specs total (Phase 1's 13 + Plan 02's format.spec + Plan 01's 8 new = 22). All must pass for Plan 07 to sign off.

## 28-item Phase Exit visual verification checklist (UI-SPEC lines 744-770)
User walks the deployed branch preview URL with this exact list. The executor stages a checklist in `02-VERIFICATION.md`. Items reproduced verbatim:

1. Hero renders side-by-side on ≥768px — photo right, text-column left.
2. Hero stacks on <768px — photo on top, text below. Photo 240×240. Name clamps to 64px.
3. Hero photo loads visible without animation — visible at first paint.
4. Hero photo has explicit width/height — DevTools shows the rendered `<img>` with `width` and `height` attributes.
5. Hero name renders at correct clamp — 96px desktop, 64px at 320px viewport, fluid in between.
6. Positioning subhead reads as a single line on desktop — does NOT wrap on ≥1024px.
7. Currently line shows accent dot + statement + `· May 9` — accent dot 6px, statement --color-text, date Geist Mono --color-muted.
8. Both channel buttons render with platform icon + `@handle · CTA` — YouTube = "Subscribe", Instagram = "DM me".
9. Channel buttons open in new tab with `target="_blank" rel="noopener noreferrer"`. Verify by clicking.
10. Channel button uses `px-4 py-3` — DevTools computes 12px top/bottom padding; total height 45px.
11. Channel button copy renders at Geist Sans weight 400 — no `font-medium`, no weight 500 anywhere outside the hero `<h1>`.
12. Channel button hover transitions border to accent, verb color to text, lifts -1px.
13. Both CTA arrow-links render in accent color (#7c87ff).
14. CTA arrow-links route to `/about` and `/work` — both stub pages render "Coming soon." No 404s.
15. Hero stagger animation plays in order — positioning (80ms) → Currently (160ms) → ChY (240ms) → ChIG (320ms) → CTA1 (400ms) → CTA2 (480ms). Name + photo do NOT animate.
16. Reduced-motion emulation stops all hero animations.
17. View-transition seam present — DevTools "Elements" tab shows `view-transition-name: hero-photo` on the photo wrapper.
18. Footer extension renders — monogram + © year + GH/IG/YT icons + "View source →" + braehods.com.
19. Footer social aria-labels present in DevTools "Accessibility" pane.
20. Tab navigation order — Nav (4) → channel YT → channel IG → CTA1 → CTA2 → Footer social GH → IG → YT → source link. Logical top-down.
21. All interactive elements show focus ring on Tab — 2px solid accent, 2px offset.
22. Body + muted + accent contrast verified on the hero region via WebAIM checker. All clear AA.
23. 320px viewport renders cleanly — no horizontal overflow.
24. Lighthouse mobile LCP < 2.5s on deployed branch preview.
25. Zero `'use client'` in app/page.tsx, app/about/page.tsx, app/work/page.tsx, components/home/*, components/layout/SocialIconLink.tsx.
26. Phase 1 specs still pass — running Phase 1's existing 13 spec files against the Phase 2 build continues to pass.
27. Vercel branch-preview URL (NOT the production alias) used for all visual checks.
28. (Bonus / informational) Bundle size delta from `next build` output — Phase 2 should add ≤10KB of incremental JS (lucide-react icons + any per-page deltas).

## Required scripts (Phase 1 ships these in package.json)
- `npm run dev` — Turbopack dev server.
- `npm run build` — `next build`.
- `npm run start` — `next start` (production-mode local server, for spec runs against `localhost:3000`).
- `npm run lint` — ESLint.
- `npm run typecheck` — `tsc --noEmit`.
- `npm run format` — Prettier.
- `npm run test` — Playwright suite (chromium-desktop + chromium-mobile).
- `npm run test:e2e` — alias or full suite.

## Vercel CLI invocation patterns
- `vercel link` — link the project (already done Phase 1; reconfirm with `vercel inspect` if doubt).
- `vercel --prod` — promote a preview to production (NOT used in Plan 07 — production is reserved for Phase 6 DNS flip).
- `vercel ls` — list recent deployments + their URLs.
- After `git push origin test/phase-2-home`, Vercel for GitHub auto-deploys; the URL is reported in the GitHub PR (if PR opened) or in `vercel ls`.

## Branch preview URL discovery
After pushing the branch, the executor runs `vercel ls --token=$VERCEL_TOKEN` or visits the Vercel dashboard. The most recent deployment under the project shows the canonical URL. Copy this URL into the `PLAYWRIGHT_BASE_URL` env var for the spec run.

If the user has not authenticated `vercel` CLI locally, the dashboard URL is the fallback (https://vercel.com/<scope>/braeden-site/deployments → click latest → copy URL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build clean locally, run full spec suite against npm start, push branch preview</name>
  <files>.planning/phases/02-home-page/02-VERIFICATION.md</files>
  <action>
Run the local clean-build + spec gate before pushing. This catches every regression before Vercel sees the code.

**Step 1 — Local clean build + lint + typecheck:**
```bash
npm run typecheck && npm run lint && npm run build
```
All three must exit 0. If `next build` fails on the `quality={90}` constraint, Plan 01 missed the `next.config.ts` patch — re-apply per Plan 01 Task 2.

**Step 2 — Start production server + run full spec suite locally:**
```bash
npm run start &
sleep 5
PLAYWRIGHT_BASE_URL=http://localhost:3000 npm run test 2>&1 | tee .playwright-local.log
kill %1
```
Expected: 22/22 spec files pass. The `tests/photo-lcp.spec.ts` may run Lighthouse against `localhost:3000` — Lighthouse against localhost is less reliable than against a CDN-served URL, but the script should still exit 0 if LCP < 2.5s on the local machine. If `photo-lcp` is flaky locally, defer it to the deployed preview run in Task 2 — note the deferral in `02-VERIFICATION.md` Step 2 row.

**Step 3 — Commit + push branch:**
```bash
git checkout -b test/phase-2-home
git add data/ public/portrait.jpg next.config.ts tests/*.spec.ts lib/format.ts components/home/ components/layout/SocialIconLink.tsx components/layout/Footer.tsx components/layout/Nav.tsx app/about app/work app/page.tsx
git status   # sanity check — verify no .planning/ or other large/sensitive paths
git commit -m "feat(phase-2): ship home hero + footer extension + stub routes"
git push -u origin test/phase-2-home
```

If `git status` shows untracked files outside the Phase 2 scope (e.g. accidentally generated `.playwright-local.log`, `.vercel/output/`), DO NOT add them. Add only the Plan 01-06 files_modified union.

**Step 4 — Capture the Vercel branch preview URL:**
After the push completes, Vercel auto-deploys. Wait up to 90 seconds. Then either:
- (a) Run `vercel ls` if the CLI is authenticated locally — the most recent deployment for the project is the branch preview.
- (b) Visit `https://vercel.com/<scope>/braeden-site/deployments` in a browser — click the most recent entry, copy the URL field labeled "Deployment URL" (NOT "Custom Domain" — that's the production alias).
- (c) If a GitHub PR is opened from this branch, the `vercel[bot]` comment posts the preview URL.

The URL has the form `https://braeden-site-<hash>-<scope>.vercel.app`. Save it to `02-VERIFICATION.md` Step 4.

**Step 5 — Create `.planning/phases/02-home-page/02-VERIFICATION.md`** with a structured log:
```markdown
# Phase 2 Verification Log

**Started:** {ISO date}
**Branch:** test/phase-2-home
**Branch preview URL:** {fill in after Step 4}

## Step 1 — Local clean build
- npm run typecheck: PASS / FAIL ({duration})
- npm run lint: PASS / FAIL
- npm run build: PASS / FAIL ({duration}, bundle size: {kB})

## Step 2 — Full spec suite vs npm start (localhost:3000)
- Specs collected: 22
- Specs passed: {N}
- Specs failed: {list with brief failure reason}
- photo-lcp.spec.ts local result: PASS / DEFERRED / FAIL ({LCP value})

## Step 3 — Branch push
- Commit SHA: {sha}
- Files staged: {count} ({list or "see git log"})

## Step 4 — Vercel branch preview URL
- URL: {fill in}
- Deploy duration: {seconds}
- Discovery method: {vercel CLI / dashboard / PR comment}

## Step 5 — Remaining gates
- Task 2: spec suite vs branch preview (next)
- Task 3: 28-item visual checklist (user)
- Task 4: merge to main + SUMMARY (after all green)
```

DO NOT proceed to Task 2 until the branch is pushed AND the Vercel deploy has succeeded (deployment status "Ready" in dashboard). If the deploy fails, debug from `vercel inspect <deployment-url> --logs` and fix on the same branch.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const v = fs.readFileSync('.planning/phases/02-home-page/02-VERIFICATION.md', 'utf8'); if (!/Branch preview URL:\s*https:\/\/[^\s]+\.vercel\.app/.test(v)) throw new Error('branch preview URL missing or not vercel.app'); if (/braeden-site\.vercel\.app/.test(v)) throw new Error('production alias detected — must use BRANCH preview URL per STATE.md W4-T2 lesson'); if (!/Specs collected:\s*22/.test(v) && !/Specs collected:\s*2[12]/.test(v)) throw new Error('expected 22 specs collected (or close)'); console.log('OK');"</automated>
  </verify>
  <done>
- `npm run typecheck` + `npm run lint` + `npm run build` all exit 0.
- 22/22 specs collected; ≥ 21 pass locally (photo-lcp may defer to deployed run).
- Branch `test/phase-2-home` pushed to GitHub.
- Vercel branch-preview URL captured in `02-VERIFICATION.md` — NOT the production alias `braeden-site.vercel.app`.
- Vercel deploy status "Ready" (not "Building", "Failed", or "Cancelled").
- 02-VERIFICATION.md exists with Steps 1-4 populated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full spec suite + axe + Lighthouse against the Vercel branch preview URL</name>
  <files>.planning/phases/02-home-page/02-VERIFICATION.md</files>
  <action>
Re-run all 22 specs against the Vercel branch-preview URL captured in Task 1, plus an axe-core a11y scan on the three Phase 2 routes.

**Step 1 — Spec suite vs Vercel branch preview:**
```bash
PLAYWRIGHT_BASE_URL=https://<branch-preview-url> npm run test 2>&1 | tee .playwright-preview.log
```

Expected: 22/22 GREEN. Common failure modes + fixes:
- `photo-lcp.spec.ts` fails with LCP > 2500ms: investigate via Vercel Speed Insights dashboard. Likely causes: (a) `priority` prop missing on `<Image>` (re-verify Plan 02 Task 3); (b) `images.qualities: [75, 90]` not in `next.config.ts` (Plan 01 Task 2); (c) portrait.jpg over-sized — check `Content-Length` header in DevTools Network tab; should be ≤ 200KB after Next.js compression.
- `ctas-resolve-200.spec.ts` fails with 404: Plan 05 stubs not deployed. Verify `git log --oneline test/phase-2-home -- app/about/page.tsx app/work/page.tsx` shows both files.
- `footer-socials-render.spec.ts` fails on missing aria-label: Plan 04 SocialIconLink. Verify deployed footer HTML via `curl https://<preview-url> | grep -E "aria-label=\"(GitHub|Instagram|YouTube)"`.
- `view-transition-name-present.spec.ts` fails: HeroPhoto inline style stripped by some build optimization. Re-verify Plan 02 Task 3 — `style={{ viewTransitionName: 'hero-photo' }}` literal must survive build.

**Step 2 — axe-core a11y scan on /, /about, /work:**
```bash
# Use @axe-core/playwright (Phase 1 ships it)
# Inline test or one-liner: add to 02-VERIFICATION.md a snippet asserting zero serious/critical violations
```

If Phase 1 ships an axe-core invocation script (check `tests/contrast.spec.ts`), reuse it. Otherwise the deferred Phase 6 a11y audit is the canonical owner; Plan 07 captures a smoke check only:
```ts
// Inline pseudo-test (do NOT add as a new spec file — capture inline):
import { test } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

for (const route of ['/', '/about', '/work']) {
  test(`axe smoke on ${route}`, async ({ page }) => {
    await page.goto(route);
    const result = await new AxeBuilder({ page }).analyze();
    const serious = result.violations.filter(v => ['serious', 'critical'].includes(v.impact ?? ''));
    if (serious.length > 0) {
      throw new Error(JSON.stringify(serious, null, 2));
    }
  });
}
```

Either run this as an inline script via `node` + Playwright's runner, or save the result count + violation list to `02-VERIFICATION.md`. If serious/critical violations are reported, Phase 2 is NOT shippable — fix or defer to a follow-up plan with developer sign-off. Phase 6 owns the full a11y audit (A11Y-01..07 per ROADMAP); Phase 2's gate is "no NEW serious/critical violations introduced."

**Step 3 — Lighthouse mobile LCP verification:**
The `photo-lcp.spec.ts` from Plan 01 runs Lighthouse against `/` and asserts `lhr.audits['largest-contentful-paint'].numericValue < 2500`. Run separately for clarity:
```bash
PLAYWRIGHT_BASE_URL=https://<branch-preview-url> npx playwright test tests/photo-lcp.spec.ts --project=chromium-mobile 2>&1 | tee .lighthouse-preview.log
```

The chromium-mobile project uses the Pixel 5 device emulation; Lighthouse runs against it.

Expected: LCP value reported in the log; assertion `< 2500` passes. Record the exact value in `02-VERIFICATION.md`. If LCP is between 2000-2500ms (close to the threshold), note the headroom — Phase 6 will audit again with the real designed monogram swap (which may swap favicon and add minimal overhead).

**Step 4 — Update 02-VERIFICATION.md Step 5 with results:**
```markdown
## Step 5 — Spec + a11y + Lighthouse vs branch preview
- Spec suite vs branch preview: {N}/22 GREEN
- Failures: {list with diagnosis + fix link}
- axe smoke on /: {N} serious/critical
- axe smoke on /about: {N} serious/critical
- axe smoke on /work: {N} serious/critical
- Lighthouse mobile LCP on /: {value}ms (target < 2500ms)
- Lighthouse mobile Performance score: {0-100} (informational; Phase 6 owns the formal 95+ gate)
- Lighthouse mobile A11Y / Best Practices / SEO scores: {informational}
```

DO NOT proceed to Task 3 until all of these conditions hold:
- 22/22 spec GREEN against branch preview.
- 0 serious/critical axe violations on all three routes.
- Mobile LCP < 2500ms.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const v = fs.readFileSync('.planning/phases/02-home-page/02-VERIFICATION.md', 'utf8'); const greenMatch = v.match(/Spec suite vs branch preview:\s*(\d+)\/22\s*GREEN/); if (!greenMatch || parseInt(greenMatch[1]) < 22) throw new Error('spec gate not 22/22'); const lcpMatch = v.match(/Lighthouse mobile LCP on \/:\s*(\d+(?:\.\d+)?)/); if (!lcpMatch || parseFloat(lcpMatch[1]) >= 2500) throw new Error('LCP gate failed: ' + (lcpMatch ? lcpMatch[1] : 'no value')); const axeRoutes = ['\\/', '\\/about', '\\/work']; for (const r of axeRoutes) { const m = v.match(new RegExp('axe smoke on ' + r + ':\\s*(\\d+)')); if (!m || parseInt(m[1]) > 0) throw new Error('axe violations on route: ' + r); } console.log('OK');"</automated>
  </verify>
  <done>
- 22/22 specs GREEN against the Vercel branch-preview URL (captured in Step 5).
- 0 serious/critical axe violations on `/`, `/about`, `/work`.
- Mobile LCP < 2500ms on `/` (recorded value).
- 02-VERIFICATION.md Step 5 populated with concrete numbers + any deviations + their resolutions.
- All Phase 1 specs continue GREEN (regression check — should be subset of the 22).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 28-item Phase Exit visual verification checklist</name>
  <what-built>
    The full Phase 2 home page is now deployed at the Vercel branch-preview URL. Plans 01-06 shipped: data populated, photo asset committed, next.config patched, 8 new specs RED→GREEN, 4 hero atoms + Hero composition mounted on `/`, footer extended with social row + source link, Nav rewired to /about and /work, both stub routes shipped, view-transition seam in place, motion stagger choreography active, LCP-safe rendering on the photo + h1.

    All 22 automated specs pass. axe a11y smoke is clean. Lighthouse mobile LCP < 2.5s.

    What remains is the 28-item visual contract from UI-SPEC § Phase Exit Visual Verification (lines 744-770). Items 1-23 are user-eye items (visual + interaction); items 24-27 are automated and already verified above; item 28 is informational. The user walks the 28-item list against the deployed branch preview URL and confirms each.
  </what-built>
  <how-to-verify>
    The user has Task 2's `02-VERIFICATION.md` file in front of them and the branch-preview URL open in:
    - Desktop browser (Chrome / Safari / Firefox per user preference)
    - Mobile browser (real iPhone Safari + Android Chrome — STATE.md notes Phase 1 deferred this to user; same applies here)
    - DevTools "Elements" + "Accessibility" + "Lighthouse" panes
    - WebAIM contrast checker tab (https://webaim.org/resources/contrastchecker/) for visual contrast verification

    Walk items 1-23 + 28 from the UI-SPEC checklist (items 24-27 already verified by Task 2's spec/Lighthouse/axe runs). For each item:
    1. Visit the deployed branch-preview URL.
    2. Inspect / interact per the item's instruction (full list reproduced in `<interfaces>` above).
    3. Mark PASS / FAIL in `02-VERIFICATION.md` Step 6 (executor stages a checklist with all 28 items; user fills in).
    4. If FAIL: describe what was observed; executor opens a Plan 08 (gap-closure) OR fixes inline on the branch.

    Items most likely to surface issues based on Phase 1's W4-T3 experience:
    - **Item 23 (320px viewport)** — Phase 1 needed a mobile-nav hamburger fix here. Phase 2 may need: photo size adjustment if the 240×240 + 8px padding crowds the viewport; channel-button wrap behavior; CTA arrow-link text overflow.
    - **Item 22 (contrast)** — verify accent #7c87ff on the gradient endpoints; UI-SPEC line 140 notes accent-on-#1a1a1f is the tightest pair (~5.2:1) — Phase 1 didn't have this surface, Phase 2 introduces it.
    - **Item 15 (stagger order)** — the 80ms increments are short; some users find the cascading load distracting. If so, capture in deferred (Phase 6 polish may slow the cascade).
    - **Item 17 (view-transition seam)** — DevTools must show `view-transition-name: hero-photo` on the photo wrapper. If it's missing, the inline style was stripped — re-check Plan 02 Task 3.
    - **Item 12 (channel button hover)** — desktop only; verify hover transitions ARE smooth (not jittery) and reduced-motion DOES neutralize them.
    - **Item 14 (CTA routes)** — click both arrow-links; both stub pages render "Coming soon." and show the same Nav + Footer chrome. No 404 page.

    Items 24-27 (auto) are already PASS — Task 2 verified.

    User signals: type "approved" if all 28 items pass; OR describe the items that need rework. Executor either:
    - Marks all 28 items PASS in `02-VERIFICATION.md` Step 6 and moves to Task 4.
    - For any FAIL, opens a Plan 08 (gap-closure mode via `/gsd-plan-phase 2 --gaps`) OR fixes inline and re-runs Task 2 before re-walking the checklist.
  </how-to-verify>
  <resume-signal>Type "approved" if all 28 items pass; OR describe the specific items that need rework (executor will gap-close).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Squash-merge to main + write Phase 2 SUMMARY</name>
  <files>.planning/phases/02-home-page/02-VERIFICATION.md</files>
  <action>
After user approves Task 3, merge `test/phase-2-home` → `main` and write the Phase 2 completion summary.

**Step 1 — Pull latest main + rebase or merge:**
```bash
git fetch origin main
git checkout main
git pull origin main
git checkout test/phase-2-home
git rebase main   # OR git merge main if rebase has conflicts
```

If rebase reports conflicts: investigate (Plan 04 touched `components/layout/Nav.tsx` and `components/layout/Footer.tsx` — if main has any tweaks, manual merge). Resolve conflicts preserving Phase 2's contract values; re-run `npm run test` after resolution.

**Step 2 — Merge to main:**
```bash
git checkout main
git merge --squash test/phase-2-home
git commit -m "feat(phase-2): ship home page (hero + Currently + channels + CTAs + footer extension + stub routes)

Phase 2 closes HOME-01..06, PERF-04, PERF-06.
- 8 new components (Hero, HeroPhoto, CurrentlyLine, ChannelButton, ChannelButtonRow, CTAArrowLink, SocialIconLink + Hero composition)
- 2 stub pages (/about, /work)
- Footer extended (social row + View source)
- Nav LINKS rewired to /about, /work
- View-transition seam on hero photo (D-21) ready for Phase 3 wiring
- 22/22 specs GREEN on Vercel branch preview; mobile LCP {value}ms; axe clean

Closes #{issue if any}"
git push origin main
```

The squash-merge keeps `main` history clean: each phase becomes a single commit (matches Phase 1's `01-01-SUMMARY.md` workflow per STATE.md).

**Step 3 — Verify production deploy:**
After pushing to main, Vercel auto-deploys to production (alias `braeden-site.vercel.app`). DNS is still pointing at GitHub Pages (Phase 6 handles the DNS flip), so `braehods.com` does NOT serve the new site — but the Vercel production alias does. Verify:
```bash
curl -I https://braeden-site.vercel.app/ 2>&1 | grep -i "X-Vercel-Cache"
```
Wait up to 90 seconds; the new deploy replaces the stale production cache. Visit `https://braeden-site.vercel.app/` and confirm the new hero renders.

If a production deploy is undesired before Phase 6's DNS flip (e.g. the user wants to keep the production alias stale until launch), DON'T squash-merge yet — keep the branch live and only merge at Phase 6 W0. The default behavior here is to merge: Phase 1 W4-T5 deferred merge until visual sign-off; once signed, merge.

**Step 4 — Update 02-VERIFICATION.md Step 6 (visual checklist) + Step 7 (merge):**
```markdown
## Step 6 — 28-item visual checklist
{Mark all 28 items PASS / FAIL; capture any user comments}

## Step 7 — Merge to main
- Squash commit on main: {sha}
- Branch test/phase-2-home: {deleted / retained for reference}
- Vercel production deploy: {URL + duration}
- Production alias serves Phase 2 hero: {true / false / waiting}
```

**Step 5 — Write the Phase 2 SUMMARY:**
Create `.planning/phases/02-home-page/02-SUMMARY.md` (NOTE: Phase 1 used `01-01-SUMMARY.md` because it had a single plan; Phase 2 has 7 plans — the phase-level summary is `02-SUMMARY.md`, and each individual plan's summary is its own `02-NN-SUMMARY.md`). Use the template at `~/.claude/get-shit-done/templates/summary.md`. Capture:

- **What shipped:** the 8 component files + 2 stub pages + Footer + Nav rewires + view-transition seam + 8 new specs + Plan 01's data + photo asset + next.config patch.
- **Requirement coverage:** HOME-01..06, PERF-04, PERF-06 — all green.
- **Decisions consumed:** D-01..D-25 (all 25) + CD-01..CD-05 (all 5).
- **Specs delta:** 13 → 22.
- **LCP measurement:** the actual Lighthouse mobile LCP value.
- **axe smoke results:** 0 serious/critical on all three routes.
- **Vercel preview URL:** the canonical branch preview URL (already captured).
- **Production alias status:** "Phase 2 hero deployed to production alias; DNS still pointing at GitHub Pages — Phase 6 handles the flip."
- **Carry-forward to Phase 3:** the `view-transition-name: hero-photo` seam awaits Phase 3's matching name on `/about`'s real photo. CTAArrowLink "More about me →" already routes to the (stub) `/about` page.
- **Carry-forward to Phase 4:** /work stub awaits Phase 4's project grid.
- **Carry-forward to Phase 5:** Nav.tsx LINKS[2] (`Contact`) still routes to `/` — Phase 5 wires the modal trigger.
- **Carry-forward to Phase 6:** real designed monogram swap (Phase 1 D-01); per-route OG image generation; sitemap + robots + JSON-LD; full Lighthouse 95+ audit.
- **Deviations from UI-SPEC:** capture any Plan 02-06 SUMMARY-flagged deviations (e.g. the Plan 03 Option-B `group-hover` choice vs UI-SPEC's `[a:hover_&]:` arbitrary-descendant pattern).

**Step 6 — Update STATE.md:**
Append a session log entry mirroring Phase 1's format (STATE.md last-session block). Increment `progress.completed_phases` (1 → 2), `progress.completed_plans` (1 → 8), and the percent.

**Step 7 — Update ROADMAP.md:**
- Check off Phase 2: `- [x] **Phase 2: Home Page** — ...`
- Update the Plans entry for Phase 2: `**Plans:** 7 plans, 4 waves`
- Update the plan list under Phase 2 to enumerate all 7 plans with checkboxes ticked.
- Update the Progress Table: `2. Home Page | 7/7 | Complete | {date}`.

**Step 8 — Final commit:**
```bash
git add .planning/phases/02-home-page/02-VERIFICATION.md .planning/phases/02-home-page/02-SUMMARY.md .planning/STATE.md .planning/ROADMAP.md
git commit -m "docs(phase-2): VERIFICATION + SUMMARY + STATE + ROADMAP updates"
git push origin main
```

(Or include these doc updates in the squash-merge commit from Step 2 — Phase 1 separated them; Plan 07 mirrors that choice.)
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); if (!fs.existsSync('.planning/phases/02-home-page/02-SUMMARY.md')) throw new Error('02-SUMMARY.md missing'); const v = fs.readFileSync('.planning/phases/02-home-page/02-VERIFICATION.md', 'utf8'); if (!/Squash commit on main:\s*[0-9a-f]{7,}/.test(v)) throw new Error('main merge SHA missing'); const roadmap = fs.readFileSync('.planning/ROADMAP.md', 'utf8'); if (!/\[x\]\s+\*\*Phase 2:\s*Home Page\*\*/.test(roadmap)) throw new Error('ROADMAP not marked Phase 2 complete'); const state = fs.readFileSync('.planning/STATE.md', 'utf8'); if (!/Phase 2/.test(state)) throw new Error('STATE.md missing Phase 2 session log'); console.log('OK');"</automated>
  </verify>
  <done>
- `test/phase-2-home` squash-merged into `main`; commit SHA recorded.
- Vercel production deploy succeeds (the alias `braeden-site.vercel.app` serves the new hero within ~90s of push).
- `.planning/phases/02-home-page/02-SUMMARY.md` exists with the full Phase 2 retrospective.
- `.planning/phases/02-home-page/02-VERIFICATION.md` Steps 6 + 7 populated.
- `.planning/STATE.md` updated with session log + progress counters.
- `.planning/ROADMAP.md` Phase 2 marked `- [x]`; progress table row updated.
- All Plan-level SUMMARYs (`02-01-SUMMARY.md` through `02-07-SUMMARY.md`) exist per their respective plan output sections.
- ROADMAP.md `**Plans:** 7 plans, 4 waves` for Phase 2.
- Carry-forward notes captured for Phases 3, 4, 5, 6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → Vercel CDN | `next build` artifacts uploaded; Vercel signs the deployment URL |
| Vercel branch preview → public internet | Anyone with the URL can visit (intentional — visual review depends on it) |
| user → Vercel dashboard | OAuth via GitHub; the user authenticates separately |
| main branch → production alias | `vercel for github` integration auto-promotes main → production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29 | I (Information disclosure) | Vercel branch-preview URLs are publicly accessible | mitigate | Phase 6 SEO-09 will set `X-Robots-Tag: noindex` on preview deploys to keep them out of search engines. Phase 2's preview is short-lived (closes when merged to main) — narrow window. The "Coming soon" stubs and hero are public-facing copy anyway. |
| T-02-30 | T (Tampering) | Squash-merge to main triggers a production deploy that the user did not see | mitigate | Production deploys to `braeden-site.vercel.app` alias (NOT `braehods.com` — DNS still points at GitHub Pages until Phase 6). The user CAN visit the production alias post-merge to confirm; rollback is `vercel rollback <previous-deploy>` if needed. Phase 6 owns the DNS flip; Phase 2's "production" is functionally a second preview channel. |
| T-02-31 | T (Tampering) | Plan 04 modifies Phase 1 chrome (Footer + Nav) — risk of latent regressions only visible at deploy | mitigate | Task 2 runs Phase 1's 13 spec files against the Vercel branch preview before sign-off. If any Phase 1 spec regresses, Plan 07 fails Task 2 verify-gate; executor diagnoses and fixes on the branch before re-running. |
| T-02-32 | D (Denial of service) | Lighthouse mobile LCP > 2.5s on real Vercel CDN despite local pass | mitigate | Task 2 explicitly runs Lighthouse against the deployed preview, NOT localhost. If LCP > 2500ms, escalation path: (a) check `priority` prop survived build; (b) audit `next/image` srcset output for the actual rendered size; (c) compress portrait.jpg further if > 200KB; (d) measure on a real mobile device via Vercel Speed Insights dashboard. PERF-06 is the contract; Plan 07 does not ship if it fails. |
| T-02-33 | I (Information disclosure) | 02-VERIFICATION.md captures the branch-preview URL and Lighthouse JSON — committed to the repo | accept | The branch-preview URL is public anyway. Lighthouse JSON contains DOM snippets but no PII. STATE.md already commits preview URLs as a Phase 1 precedent. |
</threat_model>

<verification>
1. Local clean build + spec gate (Task 1) — 22/22 GREEN on localhost (modulo photo-lcp deferral).
2. Branch pushed; Vercel deploy "Ready" status (Task 1).
3. Vercel branch-preview URL is NOT the production alias (Task 1 verify regex).
4. Spec suite vs deployed URL: 22/22 GREEN (Task 2).
5. axe smoke on `/`, `/about`, `/work`: 0 serious/critical (Task 2).
6. Mobile LCP on `/`: < 2500ms (Task 2 — PERF-06 binding).
7. 28-item visual checklist (Task 3): all PASS or all FAILs gap-closed.
8. Squash-merge to main + Phase 2 SUMMARY + STATE + ROADMAP updates (Task 4).
9. Phase 1 specs continue to pass after merge — final regression check via `npm run test` on main HEAD.
10. Production alias `braeden-site.vercel.app` serves the new hero post-merge (or production deploy explicitly deferred to Phase 6 per user preference).
</verification>

<success_criteria>
- 22/22 Playwright specs GREEN against the Vercel branch-preview URL.
- 0 serious/critical axe-core violations on all three Phase 2 routes.
- Lighthouse mobile LCP < 2.5s on `/` (PERF-06 binding).
- 28-item Phase Exit visual checklist signed off by user (Task 3 checkpoint).
- `test/phase-2-home` squash-merged to `main`; production alias serves the new hero.
- All Phase 2 documentation updated: 7 plan-level SUMMARYs + 02-SUMMARY.md + 02-VERIFICATION.md + STATE.md + ROADMAP.md.
- Phase 3 (About page) can begin: the view-transition seam is in place; CTAArrowLink "More about me →" routes to the (stub) `/about`; the photo treatment pattern is shipped as the Phase 3 analog.
</success_criteria>

<output>
After completion, Phase 2 is officially shipped. The Vercel production alias serves the new hero. DNS still points at GitHub Pages (Phase 6 flips it). Carry-forward items are captured in 02-SUMMARY.md.

Next phase: `/gsd-plan-phase 3` (About page).
</output>
