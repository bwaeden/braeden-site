---
phase: 02-home-page
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - data/channels.ts
  - data/site.ts
  - public/portrait.jpg
  - next.config.ts
  - tests/hero-renders.spec.ts
  - tests/photo-lcp.spec.ts
  - tests/currently-renders.spec.ts
  - tests/channels-render.spec.ts
  - tests/ctas-resolve-200.spec.ts
  - tests/view-transition-name-present.spec.ts
  - tests/footer-socials-render.spec.ts
  - tests/mobile-hero-stacks-cleanly.spec.ts
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - PERF-04
  - PERF-06
must_haves:
  truths:
    - "data/channels.ts exports a non-empty Channel[] with both 'youtube' and 'instagram' entries pointing at real https:// URLs"
    - "data/site.ts exports `socials` with at least github, instagram, youtube populated as real https:// URLs"
    - "public/portrait.jpg exists and is the v1 placeholder copied from ~/Projects/braehods/images/photo.jpg (≤200KB)"
    - "next.config.ts includes images.qualities: [75, 90] so HeroPhoto's quality={90} is permitted in Next 16"
    - "Eight Phase 2 Playwright spec files exist as RED stubs (assertions present, will fail until Waves 1-3 land source)"
    - "Zero 'use client' added to source code in this plan (FOUND-07 invariant)"
    - "All channel/social/source URLs use HTTPS"
  artifacts:
    - path: "data/channels.ts"
      provides: "Populated Channel[] (YouTube + Instagram) — typed export consumed by ChannelButtonRow"
      contains: "platform: 'youtube'"
    - path: "data/site.ts"
      provides: "SiteMeta with socials.github + socials.instagram + socials.youtube populated"
      contains: "socials:"
    - path: "public/portrait.jpg"
      provides: "v1 placeholder hero photo (193KB JPEG copied from ~/Projects/braehods/images/photo.jpg)"
    - path: "next.config.ts"
      provides: "images.qualities: [75, 90] addition so HeroPhoto's quality={90} is permitted (Next 16 default narrowed to [75] only)"
      contains: "qualities"
    - path: "tests/hero-renders.spec.ts"
      provides: "RED stub asserting [data-test=\"hero-section\"] resolves on / + name + positioning + Currently locators present"
    - path: "tests/photo-lcp.spec.ts"
      provides: "RED stub running playwright-lighthouse on / and asserting lhr.audits['largest-contentful-paint'].numericValue < 2500"
    - path: "tests/currently-renders.spec.ts"
      provides: "RED stub asserting accent dot + statement text + <time dateTime=...> elements present"
    - path: "tests/channels-render.spec.ts"
      provides: "RED stub asserting page.locator('a[target=\"_blank\"][rel=\"noopener noreferrer\"]').count() === channels.length within hero region"
    - path: "tests/ctas-resolve-200.spec.ts"
      provides: "RED stub doing request.get('/about') + request.get('/work') and asserting status() === 200"
    - path: "tests/view-transition-name-present.spec.ts"
      provides: "RED stub asserting getComputedStyle(photoWrapper).viewTransitionName === 'hero-photo'"
    - path: "tests/footer-socials-render.spec.ts"
      provides: "RED stub asserting footer contains ≥3 <a> with target='_blank' and aria-label matching 'GitHub profile' | 'Instagram profile' | 'YouTube channel'"
    - path: "tests/mobile-hero-stacks-cleanly.spec.ts"
      provides: "RED stub running in chromium-mobile project asserting hero flex-direction is column-reverse and no horizontal overflow at 320px"
  key_links:
    - from: "data/channels.ts"
      to: "components/home/ChannelButtonRow.tsx (Wave 1) — imports channels and maps to ChannelButton"
      via: "import { channels } from '@/data/channels'"
      pattern: "channels:\\s*Channel\\[\\]\\s*=\\s*\\["
    - from: "data/site.ts (socials field)"
      to: "components/layout/Footer.tsx (Wave 2) — renders SocialIconLink per non-empty social"
      via: "site.socials.github | site.socials.instagram | site.socials.youtube"
      pattern: "socials:\\s*\\{[^}]*github"
    - from: "public/portrait.jpg"
      to: "components/home/HeroPhoto.tsx (Wave 1) — static import next/image source"
      via: "import portrait from '@/public/portrait.jpg'"
      pattern: "portrait\\.jpg"
    - from: "next.config.ts images.qualities"
      to: "components/home/HeroPhoto.tsx quality={90} prop (Wave 1) — Next 16 build-time gate"
      via: "images: { qualities: [75, 90] }"
      pattern: "qualities:\\s*\\[75,\\s*90\\]"
    - from: "tests/*.spec.ts (this plan's RED stubs)"
      to: "Source files landing in Waves 1-3 turn each spec GREEN; Plan 07 (Wave 4) runs the full suite against Vercel branch preview"
      via: "Playwright + playwright-lighthouse; PLAYWRIGHT_BASE_URL = branch preview URL"
      pattern: "test\\(\\s*'.*HOME-"
---

<objective>
Phase 2 Wave 0 — Validation infrastructure + data scaffolds + photo asset + next/image config. Stand up everything Waves 1-3 will consume, and stub 8 RED Playwright specs that turn GREEN as components land.

Purpose: Phase 1 W0 set the precedent — every requirement gets an automated verify before any source code is written, and external content (URLs, asset bytes) blocks W1. This plan is the gate: real channel/social URLs must enter the repo (otherwise the Vercel preview serves broken external links), the v1 portrait must exist (otherwise HeroPhoto can't import it), and next.config.ts must permit quality={90} (otherwise the Wave-1 build fails).

Output: data/channels.ts + data/site.ts populated with real values; public/portrait.jpg copied; next.config.ts patched; 8 RED test specs ready for Waves 1-3 to turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-page/02-CONTEXT.md
@.planning/phases/02-home-page/02-UI-SPEC.md
@.planning/phases/02-home-page/02-PATTERNS.md
@.planning/phases/01-foundation-design-tokens/01-PLAN.md
@CLAUDE.md
@playwright.config.ts
@next.config.ts
@data/channels.ts
@data/site.ts
@data/currently.ts

<interfaces>
<!-- Contracts the executor needs — embed verbatim. -->

## Channel type (locked Phase 1, unchanged)
```ts
// data/channels.ts (already exists; this plan only POPULATES the array)
export interface Channel {
  platform: 'youtube' | 'instagram';
  handle: string;   // no '@' prefix in storage; rendered with '@' in ChannelButton
  url: string;      // must be https://
}
export const channels: Channel[] = [/* populate */];
```

## SiteMeta socials shape (locked Phase 1, unchanged)
```ts
// data/site.ts (already exists; this plan only POPULATES socials)
export interface SiteMeta {
  name: string;
  tagline: string;
  domain: string;
  email?: string;
  socials: { github?: string; instagram?: string; youtube?: string };
}
```

## Next.js 16 images.qualities — required addition
Next 16 narrowed `images.qualities` default from `[1..100]` to `[75]`. HeroPhoto (Plan 02) uses `quality={90}`. Without this config, `next build` will fail in Plan 02. Set `images: { qualities: [75, 90] }` in `next.config.ts` (current file uses `withMDX(nextConfig)` wrapper — add inside `nextConfig` object before the wrapper).

## Photo asset source
`C:/Users/Braeden/Projects/braehods/images/photo.jpg` (verified present 2026-05-11, 193KB). Copy to `C:/Users/Braeden/Projects/braeden-site/public/portrait.jpg`.

## Playwright spec layering (inherited from Phase 1 — do not invent new infra)
- `playwright.config.ts` already ships two projects: `chromium-mobile` (Pixel 5), `chromium-desktop` (1280×800).
- `PLAYWRIGHT_BASE_URL` env var lets specs hit Vercel preview in Wave 4; locally defaults to `http://localhost:3000`.
- Phase 1 ships `playwright-lighthouse` + axe-core; reuse them — no new test deps.
- Top-of-file pattern (mirror `tests/monogram.spec.ts` lines 1-14):
  ```typescript
  /**
   * HOME-XX: <what this verifies>.
   *
   * RED until <Plan NN> lands <files>.
   */
  import { test, expect } from '@playwright/test';
  ```

## Browser-CSS-serializer caveats (Phase 1 learned — Phase 2 must respect)
- Outline shorthand may serialize as `<color> <style> <width>` (Chromium) instead of authored `<width> <style> <color>`. Match both with regex.
- `linear-gradient(180deg, ...)` may serialize without `180deg`. Accept both.
- `0.01ms` normalizes to `1e-05s`. Accept either form.
- Vercel preview injects `<vercel-live-feedback>` custom element into tab order — spec walkers skip elements whose tagName contains a hyphen.
- View Transition: `getComputedStyle(el).viewTransitionName` returns the literal string `"hero-photo"` when applied via inline style.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Collect channel + social URLs from user</name>
  <what-built>Nothing yet — this is the W0 user-input task per 02-CONTEXT.md `<decisions>` "Data the User Must Supply" (lines 95-100). The executor must collect real URLs from the user before populating data/* files.</what-built>
  <how-to-verify>
    The executor pauses and asks the user for the following 5 values:

    1. **YouTube channel URL** — full https:// URL (e.g. `https://youtube.com/@braehods`)
    2. **YouTube handle** — without the `@` (e.g. `braehods`)
    3. **Instagram URL** — full https:// URL (e.g. `https://instagram.com/braehods`)
    4. **Instagram handle** — without the `@`
    5. **GitHub profile URL** — full https:// URL (e.g. `https://github.com/braeden`)

    User pastes the 5 values in chat. Executor proceeds to Task 2 with the values in hand. Do NOT proceed to Task 2 with placeholder URLs — Vercel preview must serve real working external links from day one (per 02-CONTEXT.md line 100).

    If the user wants the Instagram social-row URL to differ from the Instagram channel URL (e.g. main account vs braehods account), the executor asks for both. Otherwise reuse the same Instagram URL for both `data/channels.ts` and `data/site.ts.socials.instagram`. Same for YouTube.
  </how-to-verify>
  <resume-signal>User has provided 5 URLs; executor confirms back and proceeds to Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Populate data/channels.ts + data/site.ts.socials; copy portrait.jpg; patch next.config.ts</name>
  <files>data/channels.ts, data/site.ts, public/portrait.jpg, next.config.ts</files>
  <action>
Populate the two data files with the URLs collected in Task 1, copy the v1 portrait into `public/`, and patch `next.config.ts` so Plan 02's `next/image` build does not fail.

**data/channels.ts** — keep the existing `Channel` interface and `channels` export. Replace the empty array with two entries per D-12: one `{ platform: 'youtube', handle: '<YT handle>', url: '<YT url>' }` and one `{ platform: 'instagram', handle: '<IG handle>', url: '<IG url>' }`. Both URLs MUST start with `https://`. The handle field stores the bare name (no `@` prefix) — ChannelButton (Plan 03) prepends `@` at render time per UI-SPEC line 329.

**data/site.ts** — keep the existing `SiteMeta` interface and `site` export. Replace `socials: {}` with `socials: { github: '<GH url>', instagram: '<IG url>', youtube: '<YT url>' }`. All three URLs must start with `https://`. Per D-15, all three are required for the footer social row (Plan 04). Per D-17, the `socials.github` value doubles as the "View source →" link target (UI-SPEC line 492 — planner's call resolved here: reuse `socials.github` for the source link; no new `socials.repo` field added).

**public/portrait.jpg** — copy the file from `C:/Users/Braeden/Projects/braehods/images/photo.jpg` to `C:/Users/Braeden/Projects/braeden-site/public/portrait.jpg`. Use PowerShell `Copy-Item` since the working environment is Windows: `Copy-Item -Path "C:\Users\Braeden\Projects\braehods\images\photo.jpg" -Destination "C:\Users\Braeden\Projects\braeden-site\public\portrait.jpg"`. Per D-04, this is the v1 placeholder; a real shoot is out-of-band (PROJECT.md "Out of Scope (v1)").

**next.config.ts** — modify the existing `nextConfig: NextConfig` object to add `images: { qualities: [75, 90] }` (per UI-SPEC lines 218-219, 728, 734-735). Preserve the existing `pageExtensions` field, the `createMDX` wrapper, and the `withMDX(nextConfig)` default export. Do NOT add `remotePatterns` — Phase 2 uses only local static assets (per security_threat_model in the planner-context: no remote image URLs in Phase 2).

Do NOT modify the `CurrentlyStatement` shape or the existing `currently` value — Phase 1 ships it and Plan 02 reads it as-is.

Do NOT add `'use client'` anywhere — these are data + config files, server-side only (FOUND-07 / D-25).
  </action>
  <verify>
    <automated>node -e "const c = require('./data/channels.ts'); /* TS file — use ts-node if needed, or: */ const fs = require('fs'); const ch = fs.readFileSync('data/channels.ts','utf8'); const si = fs.readFileSync('data/site.ts','utf8'); const nc = fs.readFileSync('next.config.ts','utf8'); if (!/platform:\s*'youtube'/.test(ch) || !/platform:\s*'instagram'/.test(ch)) throw new Error('channels.ts not populated'); if (!/https:\/\//.test(ch)) throw new Error('channels.ts has non-https URL'); if (!/socials:\s*\{[^}]*github:\s*'https/.test(si)) throw new Error('site.ts socials.github not https'); if (!/socials:\s*\{[^}]*instagram:\s*'https/.test(si)) throw new Error('site.ts socials.instagram not https'); if (!/socials:\s*\{[^}]*youtube:\s*'https/.test(si)) throw new Error('site.ts socials.youtube not https'); if (!fs.existsSync('public/portrait.jpg')) throw new Error('portrait.jpg missing'); const size = fs.statSync('public/portrait.jpg').size; if (size > 250*1024) throw new Error('portrait.jpg > 250KB: '+size); if (!/qualities:\s*\[75,\s*90\]/.test(nc)) throw new Error('next.config.ts missing images.qualities [75, 90]'); console.log('OK');"</automated>
  </verify>
  <done>
- `data/channels.ts` exports a 2-entry array with platform = `'youtube'` and `'instagram'`, real https:// URLs, real handles (no `@` prefix).
- `data/site.ts.socials` has all three keys (github, instagram, youtube) populated with real https:// URLs.
- `public/portrait.jpg` exists, file size between 50KB and 250KB.
- `next.config.ts` contains `images: { qualities: [75, 90] }` inside `nextConfig`, still uses `withMDX(nextConfig)` wrapper.
- `npm run typecheck` passes (data/* type signatures unchanged).
- No `'use client'` strings added (grep -rn "'use client'" data/ next.config.ts returns nothing).
  </done>
</task>

<task type="auto">
  <name>Task 3: Stub 8 RED Phase 2 Playwright specs</name>
  <files>tests/hero-renders.spec.ts, tests/photo-lcp.spec.ts, tests/currently-renders.spec.ts, tests/channels-render.spec.ts, tests/ctas-resolve-200.spec.ts, tests/view-transition-name-present.spec.ts, tests/footer-socials-render.spec.ts, tests/mobile-hero-stacks-cleanly.spec.ts</files>
  <action>
Create 8 RED Playwright spec stubs that turn GREEN as Waves 1-3 land source. Each file follows the top-of-file pattern from `tests/monogram.spec.ts` lines 1-14: JSDoc header referencing the HOME-XX requirement, a one-line description of what's verified, and a "RED until Plan NN" provenance line. Import `{ test, expect } from '@playwright/test'`. Specs run in the existing `chromium-mobile` + `chromium-desktop` projects from `playwright.config.ts` — do NOT add new projects, do NOT touch `playwright.config.ts`.

**tests/hero-renders.spec.ts** (HOME-01, HOME-02, HOME-03, HOME-05):
- `test('hero section renders with name + positioning + Currently + channels + CTAs', ...)` → `await page.goto('/')`, assert `page.locator('[data-test="hero-section"]')` has count 1, `page.locator('[data-test="hero-display"]')` text contains `'Braeden'`, page contains the literal positioning copy from `data/site.ts.tagline` (`'Business student and entrepreneur in LA, building things and running a small content brand'`), page contains the `currently.statement` literal (`'Currently shipping CapitolLens'`), and page contains both CTA strings (`'More about me'` and `'See the work'`).
- RED until Plan 06 lands Hero.tsx + app/page.tsx rewrite.

**tests/photo-lcp.spec.ts** (HOME-04, PERF-04, PERF-06):
- Mirror `tests/lighthouse.spec.ts` lines 35-72 invocation pattern: chromium-only skip via `test.skip(browserName !== 'chromium', ...)`, launch with `--remote-debugging-port=9222`, invoke `playAudit({ url, port: 9222, thresholds: { performance: 0 } })`.
- Assert `lhr.audits['largest-contentful-paint'].numericValue < 2500` (PERF-06: mid-tier mobile LCP < 2.5s).
- Assert `lhr.audits['largest-contentful-paint'].numericValue` is defined (no `undefined` flake — Phase 1 tolerated this only on chromium-desktop; here the value is required because LCP is the success metric).
- Assert the rendered HTML for `/` contains `width="320"` and `height="320"` attributes on the `<img>` inside the hero photo wrapper (PERF-04: explicit width/height).
- RED until Plan 02 lands HeroPhoto.tsx + Plan 06 wires the hero composition.

**tests/currently-renders.spec.ts** (HOME-02):
- `await page.goto('/')`.
- Assert page contains `currently.statement` literal text.
- Assert `page.locator('time[dateTime]')` exists within the hero region and `getAttribute('dateTime')` equals `currently.updatedAt` (`'2026-05-09'`).
- Assert visible date text matches the formatted output `'May 9'` (formatter spec — `formatDate(iso)` from `lib/format.ts`).
- Assert accent dot is present: `page.locator('p:has-text("Currently") span[aria-hidden]:first-child')` resolves to an element whose `getComputedStyle` returns `background-color` matching `rgb(124, 135, 255)` or `rgba(124, 135, 255, ...)` (the `--color-accent` value `#7c87ff`).
- RED until Plan 02 lands CurrentlyLine.tsx + lib/format.ts.

**tests/channels-render.spec.ts** (HOME-03):
- `await page.goto('/')`.
- Import `{ channels }` from the data file dynamically (`import('../data/channels.ts')` may not resolve in the test runner — read `data/channels.ts` file and parse out the count, OR hardcode the expectation `2` since both YT and IG must always appear per D-12).
- Assert `page.locator('a[target="_blank"][rel*="noopener"][rel*="noreferrer"]').filter({ has: page.locator('text=/Subscribe|DM me/') }).count()` equals `2`.
- Assert one button contains text matching `/Subscribe/` (YouTube per D-12) and one contains text matching `/DM me/` (Instagram per D-12 + memory feedback_reel_cta_dm_format).
- Assert the channel buttons' `<a>` elements have `target="_blank"` (HOME-03: opens in new tab) and `rel` contains both `noopener` and `noreferrer` (security_threat_model: tabnabbing prevention).
- RED until Plan 03 lands ChannelButton + ChannelButtonRow.

**tests/ctas-resolve-200.spec.ts** (HOME-05):
- Mirror `tests/favicon.spec.ts` lines 14-29 pattern: `test('/about resolves 200', async ({ request, baseURL }) => { const r = await request.get(baseURL + '/about'); expect(r.status()).toBe(200); })`.
- Same for `/work`.
- Two separate tests so failures are isolated.
- RED until Plan 05 lands the stub routes.

**tests/view-transition-name-present.spec.ts** (HOME-05, D-21):
- `await page.goto('/')`.
- Locate the hero photo wrapper (`page.locator('[data-test="hero-photo-tile"]')` — Plan 02 adds this `data-test` attribute on the wrapper `<div>` per the patterns map test-data-attribute convention).
- Assert `await el.evaluate(node => getComputedStyle(node).viewTransitionName) === 'hero-photo'` (D-21 — CSS-level seam).
- RED until Plan 02 lands HeroPhoto.tsx with the inline style or Plan 06's globals.css utility.

**tests/footer-socials-render.spec.ts** (HOME-06):
- `await page.goto('/')`.
- Assert `page.locator('footer a[target="_blank"]').count()` is at least `4` (3 social icons + 1 source link).
- Assert `page.locator('footer a[aria-label="GitHub profile"]')` resolves to exactly 1.
- Same for `aria-label="Instagram profile"` and `aria-label="YouTube channel"` (D-15).
- Assert `page.locator('footer a:has-text("View source")')` resolves to 1 element with `target="_blank"`, `rel*="noopener"`, `rel*="noreferrer"`.
- Assert `page.locator('footer svg').count()` is at least `4` (1 monogram + 3 lucide icons).
- RED until Plan 04 lands Footer extension + SocialIconLink.

**tests/mobile-hero-stacks-cleanly.spec.ts** (HOME-04, CD-04, LNCH-05 carry-forward):
- Add `test.describe.configure({ project: 'chromium-mobile' })` or rely on the `chromium-mobile` Pixel-5 project running it via the project filter.
- `await page.goto('/')`.
- Assert `await heroFlexContainer.evaluate(el => getComputedStyle(el).flexDirection) === 'column-reverse'` at viewport <768px (per UI-SPEC line 185).
- Assert `await page.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth)` (no horizontal overflow at 320×height).
- Run a second iteration at viewport 320×640 (`page.setViewportSize({ width: 320, height: 640 })`) and re-assert no horizontal overflow.
- RED until Plan 06 lands the Hero.tsx flex layout.

All 8 specs use `import { test, expect } from '@playwright/test'`. None use axe-core in this plan — axe runs already exist in Phase 1's contrast.spec; no new axe specs needed for Phase 2 functional surface (Phase 6 owns the full a11y sweep per ROADMAP).

Do NOT modify `playwright.config.ts`. Do NOT add `'use client'` (these are Node-side test files — `'use client'` is irrelevant here but the no-client-components spec only scans `app/`, `components/`, `lib/`, not `tests/`, so no risk).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const required = ['hero-renders.spec.ts','photo-lcp.spec.ts','currently-renders.spec.ts','channels-render.spec.ts','ctas-resolve-200.spec.ts','view-transition-name-present.spec.ts','footer-socials-render.spec.ts','mobile-hero-stacks-cleanly.spec.ts']; const present = fs.readdirSync('tests'); const missing = required.filter(f => !present.includes(f)); if (missing.length) throw new Error('MISSING SPECS: ' + missing.join(', ')); for (const f of required) { const src = fs.readFileSync('tests/' + f, 'utf8'); if (!/expect\(/.test(src)) throw new Error('no expect() in ' + f); if (!/HOME-|PERF-/.test(src)) throw new Error('no HOME-/PERF- requirement header in ' + f); } console.log('OK — 8/8 specs present with assertions');"</automated>
  </verify>
  <done>
- All 8 spec files exist under `tests/` and `npx playwright test --list` enumerates them (8 new entries beyond Phase 1's 13).
- Each spec has a JSDoc header referencing its HOME-XX / PERF-XX requirement and a "RED until Plan NN" provenance line.
- Each spec contains at least one `expect(...)` assertion (verified by the explicit-filename `fs.readdirSync` + per-file content check in the verify above — `expect(` must appear in each).
- Specs collect cleanly — `npx playwright test --list` returns success (exit 0) when run separately for diagnostic.
- All 8 specs FAIL when run against the current state (no Phase 2 source yet) — this is the RED contract. Run `npx playwright test tests/hero-renders.spec.ts tests/currently-renders.spec.ts tests/channels-render.spec.ts tests/view-transition-name-present.spec.ts tests/footer-socials-render.spec.ts --project=chromium-desktop 2>&1` and confirm failures (do NOT need to run `photo-lcp.spec.ts` or `ctas-resolve-200.spec.ts` locally yet — `photo-lcp` requires a running build and `ctas-resolve-200` requires the stub routes; they pass through to Plan 07 deploy verification).
- No `'use client'` directives added anywhere (this plan touches `tests/`, `data/`, `public/`, `next.config.ts` — none of which are scanned by no-client-components.spec.ts).
- `npm run lint` and `npm run typecheck` pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → repo (URLs) | User-supplied external URLs enter `data/channels.ts` and `data/site.ts`; rendered later as `<a href>` |
| repo → external services | Channel + social buttons send users off-site (YouTube, Instagram, GitHub) |
| external image origin | None in Phase 2 — `public/portrait.jpg` is a local static asset; no `next/image` remotePatterns added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | data/channels.ts URLs collected from user (Task 1) | mitigate | Task 2 verify-step regex `https:\/\/` rejects http:// / non-URL pastes before commit; channels.ts type signature enforces `Channel.url: string` (TS doesn't validate https but the verify gate does). |
| T-02-02 | I (Information disclosure) | external `<a>` to YT/IG/GH (Plans 03 + 04 render; Plan 01 supplies values) | mitigate | All external `<a>` in Plans 03 + 04 will use `target="_blank" rel="noopener noreferrer"` (tabnabbing prevention — `noopener` zeroes `window.opener`, `noreferrer` strips Referer). Plan 01 ensures the URLs themselves are https. Specs `tests/channels-render.spec.ts` and `tests/footer-socials-render.spec.ts` assert the `rel` attribute. |
| T-02-03 | T (Tampering) | next.config.ts (build config change) | accept | Single field added (`images.qualities`); no remote image patterns. Build is private and runs on Vercel preview; no untrusted code path introduced. |
| T-02-04 | D (Denial of service) | public/portrait.jpg asset size (PERF-06) | mitigate | Verify gate caps file size ≤ 250KB (current source is 193KB). PERF-06 requires mobile LCP < 2.5s; the size cap is a static guard against accidentally committing a 5MB original photo. |
| T-02-05 | S (Spoofing) | User-provided handles displayed verbatim as `@<handle>` (Plan 03) | accept | Handles are non-secret display text controlled by the site owner. No XSS surface — React escapes string interpolation by default and the handle is rendered inside `<span>`, never `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
**Wave-0 verification gate** (must all pass before Wave 1 starts):
1. `data/channels.ts` exports non-empty array, both platforms, both https URLs.
2. `data/site.ts.socials` exports three https URLs (github, instagram, youtube).
3. `public/portrait.jpg` exists and is 50KB–250KB.
4. `next.config.ts` contains `images: { qualities: [75, 90] }`.
5. 8 new spec files exist; `npx playwright test --list` enumerates them with no parse errors.
6. `npm run typecheck` passes.
7. `npm run lint` passes.
8. `grep -rn "'use client'" data/ next.config.ts tests/` returns nothing.

**RED-spec discipline:** specs FAIL at the end of Wave 0 (no Phase 2 source yet); this is the intended RED state.
</verification>

<success_criteria>
- All 5 must-have URLs collected from user and committed to `data/channels.ts` + `data/site.ts`.
- `public/portrait.jpg` committed (≤250KB).
- `next.config.ts` patched with `images.qualities: [75, 90]`.
- 8 RED Playwright specs committed under `tests/`.
- Wave-0 verification gate (above) all green.
- Plan 02 (Wave 1) can begin: HeroPhoto.tsx will compile because (a) `next.config.ts` permits `quality={90}`, (b) `public/portrait.jpg` exists for static import, (c) `tests/photo-lcp.spec.ts` is staged to assert the contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-page/02-01-SUMMARY.md` recording:
- URLs collected (redact if user wants, but commit hashes of the data files are public).
- portrait.jpg size and SHA-256 (so Phase 6 can verify the swap-to-real-photo replaces it cleanly).
- Confirmation that all 8 specs are RED and the requirement IDs they cover.
- Any user-provided values that differ from the planning-context defaults (e.g. if Instagram channel URL ≠ Instagram social URL).
</output>
</output>
