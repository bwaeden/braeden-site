---
phase: 03-about-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/about/page.tsx
  - tests/about-renders.spec.ts
  - tests/about-photo-shared-transition.spec.ts
autonomous: false
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
must_haves:
  truths:
    - "Visiting /about renders a two-column composition: text column (bio paragraphs + 'Get in touch' CTA) on the left and HeroPhoto on the right at md+; photo reflows to top via flex-col-reverse below 768px (D-07 / CD-04 inherited from Phase 2)"
    - "The /about photo is the same byte-identical public/portrait.jpg as the home hero (D-16), wrapped by the existing components/home/HeroPhoto.tsx (Option A reuse per D-08), carrying `view-transition-name: hero-photo` so Chromium-class browsers cross-fade the photo during `/` ↔ `/about` navigation while older browsers gracefully fall back to standard navigation (D-17 / D-21 inherited)"
    - "Bio prose is real first-person content drafted at execute time by Claude AND user-reviewed in place BEFORE commit (D-01 — no placeholder, no 'will edit later', no AI-template phrasing); first sentence anchors identity (Braeden + LA + business student per D-03 bucket 1) and location lives inline in the prose per D-19 (no labeled 'Location:' field)"
    - "Bio covers exactly three content buckets in order: (1) who + where + studying, (2) current focus naming CapitolLens + content brand + dev tools per D-03 bucket 2, (3) open-to invitation per D-03 bucket 3 — and explicitly EXCLUDES any 'origin / why I build' biographical paragraph per D-04"
    - "Bio length is 150-250 words across 2 OR 3 paragraphs (≤4 sentences each), reads in ~60 seconds at 200wpm (ABOUT-04 / D-06); no emoji, no exclamation points, no labeled metadata strip, no separate CurrentlyLine reuse (D-18)"
    - "Bio prose passes the cliché-ban regex /passionate|I love to learn|driven by|innovative|cutting.edge|lifelong learner|wear many hats|results.oriented|outcome.driven/i (ABOUT-01 / D-05) — verified by tests/about-renders.spec.ts"
    - "A single <CTAArrowLink href='/' staggerIndex=N>Get in touch</CTAArrowLink> sits in an <div className='mt-6'> wrapper after the last bio paragraph (D-12 / D-14 / D-15 — placeholder /; Phase 5 wires modal trigger atomically across Nav + /about CTA in one commit); no pill button, no duplicate top CTA, no inline-woven CTA"
    - "Photo carries NO entry animation (D-24 inherited LCP discipline); bio paragraphs and the CTA animate via fadeInUp + stagger(i) at 80ms increments — paragraphs at stagger(1/2[/3]) and CTA last (stagger(3) for 2-paragraph bio, stagger(4) for 3-paragraph bio)"
    - "Page is a Server Component (FOUND-07 / D-25 inherited) — zero 'use client' in app/about/page.tsx; no event handlers, no React state, no client-side fetches"
    - "Phase 1 + Phase 2 specs still pass after this plan — Hero on / still renders, Nav + Footer chrome unchanged, no regression on view-transition-name-present (which now must resolve on BOTH / and /about per the shared seam)"
  artifacts:
    - path: "app/about/page.tsx"
      provides: "Phase 3 /about Server Component — replaces the Phase 2 'Coming soon.' stub body with the two-column bio + photo + CTA composition. Imports HeroPhoto from @/components/home/HeroPhoto (Option A reuse — D-08), CTAArrowLink from @/components/home/CTAArrowLink, fadeInUp + stagger from @/lib/motion. Preserves the existing `export const metadata = { title: 'About' }` so the root layout template composes 'About · Braeden Hodson' (Plan 02-05 already shipped this metadata)."
      exports: ["default", "metadata"]
      contains: "view-transition-name on the photo wrapper resolves via HeroPhoto reuse — same data-test='hero-photo-tile' selector resolves on both / and /about after this plan"
    - path: "tests/about-renders.spec.ts"
      provides: "Playwright spec asserting /about renders the photo tile + a CTA reading 'Get in touch' (or approved variant) pointing at '/' + ABOUT-01 cliché-ban regex over the page body text + word count 150-300 + at least 2 paragraphs"
      exports: []
    - path: "tests/about-photo-shared-transition.spec.ts"
      provides: "Playwright spec asserting (1) the photo tile resolves on /about, (2) getComputedStyle(wrapper).viewTransitionName === 'hero-photo' on /about, (3) the same selector with the same computed view-transition-name still resolves on / (regression guard for Phase 2's existing tests/view-transition-name-present.spec.ts)"
      exports: []
  key_links:
    - from: "app/about/page.tsx"
      to: "components/home/HeroPhoto.tsx (Plan 02-02 — reused unchanged)"
      via: "import { HeroPhoto } from '@/components/home/HeroPhoto'"
      pattern: "from\\s+['\"]@/components/home/HeroPhoto['\"]"
    - from: "app/about/page.tsx"
      to: "components/home/CTAArrowLink.tsx (Plan 02-03 — reused unchanged)"
      via: "import { CTAArrowLink } from '@/components/home/CTAArrowLink'"
      pattern: "from\\s+['\"]@/components/home/CTAArrowLink['\"]"
    - from: "app/about/page.tsx"
      to: "lib/motion.ts (Phase 1 seam)"
      via: "import { fadeInUp, stagger } from '@/lib/motion'"
      pattern: "from\\s+['\"]@/lib/motion['\"]"
    - from: "app/about/page.tsx photo wrapper"
      to: "tests/about-photo-shared-transition.spec.ts AND tests/view-transition-name-present.spec.ts (Plan 02-02 — still passes on /)"
      via: "view-transition-name: hero-photo (same literal string on both routes, applied inline via HeroPhoto.tsx style prop)"
      pattern: "viewTransitionName.*hero-photo|hero-photo"
    - from: "tests/about-renders.spec.ts cliché-ban regex"
      to: "ABOUT-01 enforcement (spec gate)"
      via: "expect(text).not.toMatch(/passionate|I love to learn|driven by|innovative|cutting.edge|lifelong learner|wear many hats|results.oriented|outcome.driven/i)"
      pattern: "passionate|I love to learn|driven by|innovative|cutting"
---

<objective>
Phase 3 (sole plan) — Replace the Phase 2 `Coming soon.` stub at `/about` with the real About page: a two-column composition mirroring the home hero (text column with 2-3 short first-person bio paragraphs + a single `Get in touch →` CTA on the left; the same `<HeroPhoto>` from `/` on the right via Option A reuse), carrying the matching `view-transition-name: hero-photo` so the cross-route shared-element transition fires in Chromium-class browsers.

Purpose: Phase 2 shipped the seam on the home side; Phase 3 closes the loop by wiring the matching name on `/about` AND delivering the real bio content the seam transitions INTO. This is also the first real consumer of the cross-route view-transition contract — after this plan ships, the `/ ↔ /about` navigation reads as "the photo stays put while the words swap," which is the load-bearing motion feature for the whole site.

Bio prose itself is a deliverable. Per D-01, Claude drafts the first-pass prose during execution; the user reads it and edits in-place BEFORE the commit lands. The plan does NOT encode the literal sentences — it locks the CONTRACT (length 150-250 words, 2-3 short paragraphs, warm + personal first-person voice, 3 content buckets in order, cliché-ban regex enforced, no exclamation points / emoji / origin paragraph). Task 3 is the deliberate human-in-the-loop checkpoint inside execution.

Output: One file rewritten (`app/about/page.tsx`) + two new Playwright spec files (one for bio cliché-scrub + CTA resolution, one for the view-transition shared-element seam). Zero new components — D-08 Option A reuses `<HeroPhoto>` directly, so the existing `data-test="hero-photo-tile"` selector and the existing `view-transition-name: hero-photo` inline style do double duty across `/` and `/about`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-about-page/03-CONTEXT.md
@.planning/phases/03-about-page/03-UI-SPEC.md
@.planning/phases/02-home-page/02-CONTEXT.md
@.planning/phases/02-home-page/02-UI-SPEC.md
@.planning/phases/02-home-page/02-PATTERNS.md
@.planning/phases/02-home-page/02-06-PLAN.md
@app/about/page.tsx
@app/page.tsx
@app/layout.tsx
@components/home/Hero.tsx
@components/home/HeroPhoto.tsx
@components/home/CTAArrowLink.tsx
@lib/motion.ts
@data/site.ts
@tests/view-transition-name-present.spec.ts
@tests/hero-renders.spec.ts
@tests/ctas-resolve-200.spec.ts
@playwright.config.ts

<interfaces>
<!-- Contracts the executor needs verbatim. No codebase exploration required. -->

## components/home/HeroPhoto.tsx (REUSE unchanged — Option A per D-08)

```tsx
// components/home/HeroPhoto.tsx — Server Component
// Carries the seam: data-test="hero-photo-tile" + inline style viewTransitionName: 'hero-photo'.
// 240x240 mobile (w-60 h-60), 320x320 desktop (md:w-80 md:h-80). priority + placeholder="blur" + quality={90}.
// alt="Braeden Hodson". Image source: import portrait from '@/public/portrait.jpg'.
// Already shipping on /. Same component used on /about — no edits to this file in Phase 3.

export function HeroPhoto(): JSX.Element; // zero props
```

The `data-test="hero-photo-tile"` selector now resolves on BOTH `/` and `/about` once this plan ships. Phase 2's `tests/view-transition-name-present.spec.ts` scopes to `/` via `page.goto('/')` and will continue to pass without modification; the new Phase 3 spec scopes to `/about` independently.

## components/home/CTAArrowLink.tsx (REUSE unchanged)

```tsx
// components/home/CTAArrowLink.tsx — Server Component
// Accent-colored Next.js <Link> with Unicode '→' glyph + group-hover:translate-x-1 on the arrow.
// Reduced-motion globally defeats the translate via app/globals.css.

interface CTAArrowLinkProps {
  href: string;            // internal route only (/ for v1; #contact post-Phase-5)
  children: string;        // plain text label, NOT React.ReactNode — keeps underline-offset clean
  staggerIndex: number;    // caller owns the choreography number; passed to inline stagger()
}

export function CTAArrowLink(props: CTAArrowLinkProps): JSX.Element;
```

The arrow glyph is appended by the component itself — pass `children="Get in touch"` (no `→` in the children string).

## lib/motion.ts (Phase 1 seam, reused)

```ts
export const respectsReducedMotion = true;
export const fadeInUp: 'fade-in-up'; // class name applying @keyframes fade-in-up
export function stagger(i: number): React.CSSProperties; // returns { '--stagger': `${i*80}ms` }
```

Stagger sequence on /about (UI-SPEC Motion Choreography):
- Photo: NO animation (D-24 inherited — LCP element).
- Bio paragraph 1: fadeInUp + stagger(1) = 80ms delay.
- Bio paragraph 2: fadeInUp + stagger(2) = 160ms delay.
- Bio paragraph 3 (if 3-paragraph split): fadeInUp + stagger(3) = 240ms delay.
- CTAArrowLink: staggerIndex = 3 (2-paragraph bio) OR 4 (3-paragraph bio).

## app/layout.tsx (inherited Phase 1 chrome — DO NOT MODIFY)

```tsx
<main className="mx-auto max-w-3xl px-6 py-16 lg:px-12">{children}</main>
```

/about page inherits `max-w-3xl` from this root layout per D-10 (does NOT extend to `max-w-5xl` — only the home page warrants the wider container). The page-level `<section className="py-8 md:py-12">` adds vertical padding within this container, mirroring Hero.tsx but without the `max-w-5xl` nested wrapper.

## app/about/page.tsx (CURRENT Phase 2 stub — Task 2 REPLACES the body, KEEPS the metadata export)

```tsx
// app/about/page.tsx (Phase 2 stub from Plan 02-05 — REPLACE the body in Task 2)
export const metadata = { title: 'About' }; // KEEP — composes "About · Braeden Hodson" via root layout

export default function AboutPage() {
  return (
    <section className="py-24">
      <p className="font-sans text-base" style={{ color: 'var(--color-muted)' }}>
        Coming soon.
      </p>
    </section>
  );
}
```

The `export const metadata = { title: 'About' }` line MUST survive the rewrite. Strip everything else inside the default export and replace with the bio + photo + CTA composition.

## Page composition target (UI-SPEC § Page Composition lines 159-167)

| Slot | Element | Notes |
|------|---------|-------|
| Page root | `<section data-test="about-section" className="py-8 md:py-12">` | Same vertical padding as home hero per CD-05 |
| Layout container | `<div data-test="about-flex" className="flex flex-col-reverse gap-8 md:flex-row md:items-center md:gap-16">` | Photo on top mobile (flex-col-reverse), photo right desktop; no max-w-5xl wrapper per D-10 |
| Text column | `<div className="flex flex-col items-start">` | Wraps bio paragraphs + CTA |
| Bio paragraphs | `<p className="max-w-[44ch] font-sans text-base leading-relaxed fade-in-up" style={{ color: 'var(--color-text)', ...stagger(i) }}>` | First paragraph: no `mt-*`. Subsequent paragraphs: `mt-4` (16px). |
| CTA wrapper | `<div className="mt-6"><CTAArrowLink href="/" staggerIndex={N}>Get in touch</CTAArrowLink></div>` | N=3 if 2-paragraph bio, N=4 if 3-paragraph bio |
| Photo column | `<HeroPhoto />` | Reused unchanged; carries data-test + view-transition seam inline-style from its own source |

## data/site.ts (read-only on /about — informs voice anchor only)

```ts
site.tagline = 'Business student and entrepreneur in LA, building things and running a small content brand';
site.socials = { github: 'https://github.com/bwaeden', instagram: 'https://instagram.com/braehods' };
```

The bio prose may mirror the tagline's framing but MUST NOT copy it verbatim (the home page already shows it; /about extends into longer prose). The `@braehods` handle may appear inline as plain text in the open-to bucket if user voice references DMs (per memory `feedback_reel_cta_dm_format.md` — "DM me @braehods", not "follow on Instagram").

## Test patterns to mirror

- `tests/view-transition-name-present.spec.ts` (lines 18-28) — the pattern for asserting `getComputedStyle(tile).viewTransitionName === 'hero-photo'`. Copy this approach for the /about side.
- `tests/hero-renders.spec.ts` (lines 14-50) — the pattern for `page.getByText(literal, { exact: false }).toHaveCount(1)` to assert page-text presence.
- `tests/ctas-resolve-200.spec.ts` (lines 11-21) — `request.get('/about')` returning 200 is already covered; the new spec asserts page-content presence, not status.

Both new specs follow the top-of-file `@playwright/test` import + numbered REQ-tagged `test()` blocks pattern used throughout `tests/`.

## Cliché-ban regex (binding — ABOUT-01 / D-05)

Exact regex to use in `tests/about-renders.spec.ts`:

```ts
const CLICHE_BAN_REGEX = /passionate|I love to learn|driven by|innovative|cutting.edge|lifelong learner|wear many hats|results.oriented|outcome.driven/i;
```

The regex is case-insensitive (`/i`) so `Passionate Developer` and `passionate developer` both fail. Apply against `await page.locator('[data-test="about-section"]').innerText()` (the section's full text content).

## Coverage audit (Phase 3 source artifacts)

SOURCE   | ID      | Item                                                              | Plan  | Status  | Notes
-------- | ------- | ----------------------------------------------------------------- | ----- | ------- | ------
GOAL     | —       | /about page with short bio, photo, contact-modal nudge in <60s    | 03-01 | COVERED | Task 2 composes; Task 3 user-reviews
REQ      | ABOUT-01| Short bio in Braeden's voice; no AI-template phrases              | 03-01 | COVERED | Task 1 cliché-ban spec; Task 2 draft; Task 3 user review
REQ      | ABOUT-02| Photo treated tastefully on charcoal (v1 placeholder)             | 03-01 | COVERED | Task 2 reuses HeroPhoto unchanged per D-08 Option A
REQ      | ABOUT-03| Location, current focus, friendly CTA wired to contact-modal trigger | 03-01 | COVERED | Task 2 (D-19 inline location, D-18 woven current focus, D-12+D-15 CTA placeholder→Phase 5)
REQ      | ABOUT-04| <60-second read, short scannable paragraphs                       | 03-01 | COVERED | Task 2 follows D-06 length contract; Task 3 user verifies feel
RESEARCH | —       | n/a — RESEARCH.md intentionally skipped (design fully specified)  | —     | n/a     | UI-SPEC.md is the design contract instead
CONTEXT  | D-01    | Claude drafts bio at execution time; user edits in-place pre-commit | 03-01 | COVERED | Task 2 drafts; Task 3 checkpoint reviews
CONTEXT  | D-02    | Warm + personal first-person voice                                | 03-01 | COVERED | Task 2 draft contract + Task 3 user verifies
CONTEXT  | D-03    | 3 buckets in order: who+where+studying / current focus / open-to  | 03-01 | COVERED | Task 2 + Task 3
CONTEXT  | D-04    | Excludes 'origin / why I build' bucket                            | 03-01 | COVERED | Task 2 (negative constraint in draft contract)
CONTEXT  | D-05    | ABOUT-01 cliché ban — explicit phrase list                        | 03-01 | COVERED | Task 1 regex spec; Task 2 self-scrub before review
CONTEXT  | D-06    | 150-250 words, 2-3 paragraphs, ≤4 sentences each                  | 03-01 | COVERED | Task 2 draft contract + Task 1 word-count spec
CONTEXT  | D-07    | Two-column layout mirroring home hero (flex-col-reverse / md:flex-row) | 03-01 | COVERED | Task 2 page composition
CONTEXT  | D-08    | Photo dimensions match home; Option A reuse <HeroPhoto> default   | 03-01 | COVERED | Task 2 imports HeroPhoto unchanged
CONTEXT  | D-09    | Text-column reading width max-w-[44ch]                            | 03-01 | COVERED | Task 2 paragraph className
CONTEXT  | D-10    | Page inherits max-w-3xl from root layout; no max-w-5xl extension  | 03-01 | COVERED | Task 2 (no nested wider container)
CONTEXT  | D-11    | CD-05 vertical rhythm (py-8 md:py-12, mt-4/6/8)                   | 03-01 | COVERED | Task 2 section + paragraph classes
CONTEXT  | D-12    | Reuse <CTAArrowLink> (NOT a pill button)                          | 03-01 | COVERED | Task 2 import
CONTEXT  | D-13    | CTA text 'Get in touch' (variants 'Drop me a line' / 'Say hi' OK) | 03-01 | COVERED | Task 2 default + Task 3 user-edit window
CONTEXT  | D-14    | Single CTA at end of bio (no duplicate, no inline-woven)          | 03-01 | COVERED | Task 2 composition
CONTEXT  | D-15    | CTA href='/' v1 placeholder; Phase 5 atomic swap to #contact      | 03-01 | COVERED | Task 2 href + plan comment
CONTEXT  | D-16    | Same public/portrait.jpg as home (Phase 6 swaps both atomically)  | 03-01 | COVERED | Task 2 via HeroPhoto reuse (same static import)
CONTEXT  | D-17    | view-transition-name: hero-photo on /about photo wrapper          | 03-01 | COVERED | Task 2 via HeroPhoto reuse + Task 1 spec asserts
CONTEXT  | D-18    | Current focus woven into prose; no CurrentlyLine; no metadata strip | 03-01 | COVERED | Task 2 draft contract + negative constraint
CONTEXT  | D-19    | Location inline in prose; no labeled 'Location:' field            | 03-01 | COVERED | Task 2 draft contract

Every D-XX from CONTEXT.md, every ABOUT-XX from REQUIREMENTS.md, and the phase GOAL from ROADMAP.md is covered by 03-01. RESEARCH.md was intentionally skipped (no research artifact exists for Phase 3). No gaps. No phase split needed.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub two RED Playwright specs for /about (cliché-scrub + view-transition seam)</name>
  <files>tests/about-renders.spec.ts, tests/about-photo-shared-transition.spec.ts</files>
  <action>
Create both spec files under `tests/`. Mirror the top-of-file header / import / `test()` shape used in `tests/view-transition-name-present.spec.ts` and `tests/hero-renders.spec.ts`. Both are Playwright + `@playwright/test` (no Lighthouse, no axe) — they run under both the `chromium-mobile` and `chromium-desktop` projects in `playwright.config.ts`.

`tests/about-renders.spec.ts` MUST cover (ABOUT-01, ABOUT-02, ABOUT-03, ABOUT-04 per D-05/D-06/D-13/D-14):
- `await page.goto('/about')`
- Assert `[data-test="about-section"]` resolves to exactly 1 element (proves the page composition shipped, replacing the Phase 2 stub).
- Assert `[data-test="hero-photo-tile"]` resolves on /about (proves Option A HeroPhoto reuse landed — ABOUT-02 photo treatment).
- Assert exactly one `<a>` whose accessible text matches `/Get in touch|Drop me a line|Say hi/i` (D-13 — default + approved variants; tolerates user edit at Task 3 review) AND whose `href` attribute equals `/` (D-15 — placeholder pending Phase 5 atomic swap).
- Extract the section's full text via `await page.locator('[data-test="about-section"]').innerText()` and assert:
  - `expect(text).not.toMatch(/passionate|I love to learn|driven by|innovative|cutting.edge|lifelong learner|wear many hats|results.oriented|outcome.driven/i)` — the ABOUT-01 / D-05 cliché ban. Verbatim regex string (do NOT abbreviate or split — both Task 2 self-scrub and Task 3 user review depend on this exact pattern).
  - Word count: split the extracted text on whitespace, filter empties, count tokens. Assert `count >= 150 && count <= 300` (D-06 target band 150-250 + 300 hard ceiling). Note: this counts ALL words in the section including the CTA label — that's fine because "Get in touch" adds ~3 words to a 150-250 bio.
  - Paragraph count: `await page.locator('[data-test="about-section"] p').count()` — assert `>= 2 && <= 3` (D-06 two-or-three paragraphs).
  - No exclamation points: `expect(text).not.toContain('!')` (D-05 voice guardrail).

`tests/about-photo-shared-transition.spec.ts` MUST cover (HOME-05 / D-17 / D-21 / D-22):
- `await page.goto('/about')` — assert `[data-test="hero-photo-tile"]` resolves to count=1, then read `getComputedStyle(node).viewTransitionName` and assert it equals the literal string `'hero-photo'`. Mirror the exact evaluate() pattern from `tests/view-transition-name-present.spec.ts` lines 24-27 (the CSSStyleDeclaration & { viewTransitionName?: string } cast handles Chromium's incomplete typedef).
- Regression guard: a second test block — `await page.goto('/')`, assert the SAME selector resolves with the SAME computed `viewTransitionName === 'hero-photo'` (proves Phase 2's seam still works after Plan 03-01 lands; documents the cross-route contract in one place).
- Both tests must work under `chromium-mobile` AND `chromium-desktop` (don't add any project-scoping `test.skip` — the seam is browser-CSS not viewport-specific).

Top-of-file leading comment for each: numbered REQ tags (`/** ABOUT-01..04: ... */` and `/** HOME-05 / D-17 / D-21 ... */`), the `RED until Plan 03-01 Task 2 lands ...` pacing note (matches Phase 2 spec convention), and a short note on the Chromium computed-style serializer caveat (the literal value is `"hero-photo"` without quoting — same as Phase 2's spec).

Both specs MUST fail BEFORE Task 2 lands (RED contract — `[data-test="about-section"]` resolves to 0 against the current Phase 2 stub). Run `npx playwright test tests/about-renders.spec.ts tests/about-photo-shared-transition.spec.ts` against a local `npm run dev` and confirm both fail with the expected assertion message before moving to Task 2. Commit as `test(phase-3/w1): stub 2 RED Playwright specs for /about (cliché-scrub + view-transition seam)`.

DO NOT inline any bio text guesses in the specs — the contract is structural (count, regex, ban list), not textual. If a future variant of "Get in touch" is approved at Task 3, the spec's `/Get in touch|Drop me a line|Say hi/i` regex tolerates it.
  </action>
  <verify>
    <automated>npx playwright test tests/about-renders.spec.ts tests/about-photo-shared-transition.spec.ts</automated>
    Expected: BOTH spec files RED. `tests/about-renders.spec.ts` fails on `[data-test="about-section"]` resolving to 0 elements. `tests/about-photo-shared-transition.spec.ts` fails on `[data-test="hero-photo-tile"]` resolving to 0 elements on /about. The regression-guard test block (on /) MAY pass — that's fine; it's documenting the home side's already-shipping seam. Capture the failure output in the commit body so Task 2 can verify the transition to GREEN.
  </verify>
  <done>
    - `tests/about-renders.spec.ts` exists, imports `@playwright/test`, contains the cliché-ban regex verbatim, word-count gate (150-300), paragraph-count gate (2-3), no-exclamation gate, CTA href='/' gate, CTA text regex.
    - `tests/about-photo-shared-transition.spec.ts` exists, asserts the seam on /about AND the regression on /.
    - Both specs RED against the current codebase (Phase 2 stub still shipping at /about).
    - One atomic commit: `test(phase-3/w1): stub 2 RED Playwright specs for /about (cliché-scrub + view-transition seam)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app/about/page.tsx — compose layout, wire HeroPhoto + CTAArrowLink, DRAFT bio prose (DO NOT COMMIT — hand off to Task 3 review)</name>
  <files>app/about/page.tsx</files>
  <action>
Replace the body of `app/about/page.tsx` (default export only — KEEP the existing `export const metadata = { title: 'About' };` line so the root layout's `template: '%s · Braeden Hodson'` continues to compose "About · Braeden Hodson" per Plan 02-05's contract).

Imports (in this order — matches Hero.tsx convention):
- `import { HeroPhoto } from '@/components/home/HeroPhoto';` (per D-08 Option A reuse — UI-SPEC § HeroPhoto Reuse Contract; recommended default)
- `import { CTAArrowLink } from '@/components/home/CTAArrowLink';` (per D-12 — reuse, no new component, no API extension)
- `import { fadeInUp, stagger } from '@/lib/motion';` (per CD-03 inherited — fadeInUp + stagger(i) seam)

Compose the page per UI-SPEC § Page Composition (lines 159-167) and § Motion Choreography (lines 374-417):

1. Page root `<section data-test="about-section" className="py-8 md:py-12">` — D-11 / CD-05 vertical rhythm matches home hero (PRESERVE the `data-test="about-section"` selector — Task 1's spec gates on it).
2. Layout container `<div data-test="about-flex" className="flex flex-col-reverse gap-8 md:flex-row md:items-center md:gap-16">` — D-07 / CD-04 mobile-stacks + desktop-side-by-side. NO `max-w-5xl` wrapper per D-10 (the home hero uses one; /about deliberately inherits the narrower `max-w-3xl` from `app/layout.tsx`). The `data-test="about-flex"` selector is decorative-only but follows the Hero.tsx precedent (`[data-test="hero-flex"]`).
3. Text column `<div className="flex flex-col items-start">` — wraps bio paragraphs + CTA.
4. Bio paragraphs — 2 OR 3 `<p>` elements (executor's call within D-06 — see "Bio draft contract" below). Each carries `className="max-w-[44ch] font-sans text-base leading-relaxed fade-in-up"` (use the literal class `fade-in-up`, or `${fadeInUp}` template if preferred — they expand to the same string). First paragraph: NO `mt-*` prefix. Second + third paragraphs: `mt-4` prefix (16px gap per D-11 / CD-05 reading rhythm). Color via inline style `style={{ color: 'var(--color-text)', ...stagger(i) }}` where i = 1, 2, [3] for paragraphs 1, 2, [3] respectively. The inline-color pattern matches the established Phase 2 inheritance (see CurrentlyLine.tsx + Hero.tsx — Tailwind v4 inline `style={{ color: ... }}` is the project convention for tokenized colors, NOT arbitrary `text-[var(--token)]`).
5. CTA wrapper `<div className="mt-6"><CTAArrowLink href="/" staggerIndex={N}>Get in touch</CTAArrowLink></div>` — `N = 3` if 2-paragraph bio, `N = 4` if 3-paragraph bio. The CTAArrowLink component appends the `→` glyph automatically — pass children `"Get in touch"` only, NO `→` in the string. `href="/"` is the v1 placeholder per D-15 (Phase 5 swaps to `#contact` atomically across Nav.tsx + this file in one commit).
6. Photo column `<HeroPhoto />` — Option A reuse per D-08. NO props (component is zero-arg). The component carries its own `data-test="hero-photo-tile"` + inline `style={{ viewTransitionName: 'hero-photo' }}` + 240/320 responsive sizing + priority + blur — Task 1's `tests/about-photo-shared-transition.spec.ts` asserts the seam resolves on /about via this reuse.

Leading file comment (mirrors Hero.tsx convention):

```
// app/about/page.tsx
// Source: PLAN.md 03-01-T2; UI-SPEC § Page Composition + § HeroPhoto Reuse Contract + § Motion Choreography.
//
// Server Component (NO 'use client' — FOUND-07 / D-25 inherited). Replaces
// the Plan 02-05 'Coming soon.' stub with the Phase 3 bio + photo + CTA
// composition. Reuses components/home/HeroPhoto (D-08 Option A) so the
// view-transition-name: hero-photo seam matches the home side, and reuses
// components/home/CTAArrowLink for the 'Get in touch →' nudge (D-12).
// Bio prose drafted in this commit per D-01; user edits in place at Task 3
// review BEFORE the final commit lands. NO new components, NO new tokens.
```

Bio draft contract (binding — do not skip or shortcut):

The bio prose IS this task's deliverable. Draft it directly into the JSX, satisfying ALL of:

- **Length:** 150-250 words total (hard ceiling 300). Use a quick word count after drafting (split on whitespace, filter empties) — verify in the executor's commit body.
- **Paragraph count:** 2 OR 3 `<p>` elements (D-06 — executor's call; UI-SPEC Bio Prose Contract notes 3-paragraph splits hit "scannable" goal best while 2-paragraph is acceptable if each ≤4 sentences). Each paragraph ≤4 sentences (D-06 hard).
- **Voice:** Warm + personal, first-person. Open with "Hi, I'm Braeden — " or similar identity-anchor opening that lands name + LA + business-student framing in the first ~15 words (D-03 bucket 1, D-19 location inline). Conversational, hints at curiosity beyond the resume (D-02). NOT dry/minimal third-person. NOT earnest/why-I-build (D-04 explicit exclusion).
- **Content buckets in order** (D-03):
  1. **Paragraph 1 (or first sentence of paragraph 1):** Who + where + studying. Name introduction + LA + business student.
  2. **Paragraph 2 (or end of paragraph 1):** Current focus. MUST name at least 2 specific projects from this set, with concrete framing pulled from memory: **CapitolLens** = Form 4 insider-buy signals, paper-trading the MEDIUM-tier strategy (do NOT claim live trading — user is paper-only); **two-channel YT Shorts brand** = Channel A wholesome POV stories + Channel B single-meme deadpan reactions (project_two_channel_split memory); **dev tools** pick 1-2 from: GSD (Claude Code skill suite), shorts-factory (Remotion CLI), meme-dashboard (Streamlit backtest scorer), reel-research-agent (Streamlit AI-news drafting tool). Stay specific over generic — D-05 explicit ("Specific projects > generic categories").
  3. **Paragraph 3 (or end of last paragraph):** Open-to invitation. Concrete examples within voice: "open to trading-desk internships, content collabs, or just a chat about what's working in AI-finance." Lowest-friction conversion signal. NOT "always looking to chat."
- **Cliché ban (D-05 / ABOUT-01 — enforceable):** The drafted prose MUST NOT contain any of: `passionate`, `I love to learn`, `lifelong learner`, `I am driven by` / `driven to`, `innovative` (as self-descriptor), `cutting-edge`, `I am a [role] with experience in [list]`, `wear many hats`, `results-oriented`, `outcome-driven`. The Task 1 regex enforces a subset of these — your draft must pass the regex AND the broader spirit (recruiter-resume template language). Self-scrub: re-read the draft, mentally substitute "would a human writing for themselves say this?" — if no, rewrite.
- **Voice guardrails (D-05 / Phase 2 inherited):** Zero emoji. Zero exclamation points. Zero mono-font / `<time>` element / labeled metadata strip — bio is unsectioned prose (D-18). Location surfaces inline per D-19 (e.g., "...in LA..." or "...based in Los Angeles...") — NOT a dedicated "Location: LA" line, NOT pulled from typed data.
- **Optional Instagram inline mention** (only if the open-to bucket touches DMs / content collabs): render the handle as plain body-text `@braehods`, NOT an accent-colored link, NOT an inline social icon. Phrasing should mirror memory `feedback_reel_cta_dm_format.md`: "DM me @braehods" rather than "follow me on Instagram." If the prose doesn't naturally lead there, skip the handle mention — the Footer's IG icon already covers discoverability.

Self-scrub gate (binding — execute BEFORE handing off to Task 3):
1. Word count is 150-300 (use a quick split-and-count on the prose extracted from the JSX text nodes).
2. Cliché-ban regex passes manually (grep the draft prose against `/passionate|I love to learn|driven by|innovative|cutting.edge|lifelong learner|wear many hats|results.oriented|outcome.driven/i`).
3. No `!` characters and no emoji codepoints in the prose.
4. The three required buckets are present in order (manual checklist read-through).
5. No "origin / why I build" backstory paragraph (D-04 — read each paragraph and confirm none answers "why I started building" or "growing up I..." or similar).

After drafting + self-scrub: run `npx playwright test tests/about-renders.spec.ts tests/about-photo-shared-transition.spec.ts` against `npm run dev`. Both specs should flip GREEN. If `tests/about-renders.spec.ts` fails on word-count, paragraph-count, exclamation-point, or cliché-ban: REVISE THE PROSE — do NOT relax the spec. If `tests/about-photo-shared-transition.spec.ts` fails on /about: re-check the HeroPhoto import + JSX wiring.

Also re-run the Phase 1 + Phase 2 spec sweep locally (`npx playwright test` full suite) — Hero on /, view-transition seam on /, Footer chrome, Nav rewire, focus ring, monogram, reduced motion, lighthouse mono assertion, no-client-components, contrast — MUST all still pass. The only allowed delta in spec results is the +2 new specs flipping GREEN.

**DO NOT COMMIT YET.** This task ends with the prose drafted in the working tree but uncommitted, and the working tree clean except for `app/about/page.tsx`. Hand off to Task 3 for user review. The next commit is Task 3's after user approval.

DEVIATION discipline (Phase 2 inherited): If during execution any UI-SPEC line conflicts with a CLAUDE.md / codebase constraint, document the deviation in a code-comment under the file's leading source-trace block (matches Hero.tsx + Footer.tsx precedent). Do NOT silently scope-reduce, do NOT relax the cliché ban, do NOT skip a bucket. Constraints from UI-SPEC are non-negotiable.
  </action>
  <verify>
    <automated>npx playwright test tests/about-renders.spec.ts tests/about-photo-shared-transition.spec.ts</automated>
    Expected: BOTH specs GREEN (`tests/about-renders.spec.ts` passes word-count + paragraph-count + cliché-ban + CTA href + no-exclamation; `tests/about-photo-shared-transition.spec.ts` passes /about photo tile resolution + viewTransitionName === 'hero-photo' + regression guard on /). Also run `npm run typecheck` + `npm run lint` + `npm run build` — all must exit 0 (Turbopack should compile the new page as a static route at build time per the Server Component contract). Working tree shows ONE modified file (`app/about/page.tsx`) and NO new files (Task 1's specs are already committed). Working tree intentionally UNCOMMITTED.
  </verify>
  <done>
    - `app/about/page.tsx` rewritten: imports HeroPhoto + CTAArrowLink + fadeInUp + stagger; default export composes <section data-test="about-section"> → <div data-test="about-flex"> → text column with 2 or 3 <p> paragraphs (max-w-[44ch] + fade-in-up + stagger(i)) → <div className="mt-6"><CTAArrowLink href="/" staggerIndex=N>Get in touch</CTAArrowLink></div> + <HeroPhoto />.
    - Bio prose drafted in-place: 150-300 words, 2 or 3 paragraphs, warm + personal first-person, 3 buckets in order (who+where+studying / current focus naming ≥2 specific projects with concrete memory-anchored framing / open-to invitation), passes cliché-ban regex, zero `!`, zero emoji, zero origin paragraph, location inline.
    - `export const metadata = { title: 'About' };` line preserved verbatim.
    - Zero `'use client'` directive in the rewritten file.
    - Both Phase 3 specs GREEN locally. Full Phase 1 + Phase 2 spec sweep still GREEN (no regressions). `npm run typecheck` + `lint` + `build` exit 0.
    - File is staged in the working tree but NOT committed — handing off to Task 3 for user review per D-01.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User reviews drafted bio prose in place, edits if needed, then approves commit</name>
  <files>app/about/page.tsx (in-place user edits) — review-only; no test files modified</files>
  <what-built>
The executor (Task 2) drafted real first-person bio prose directly into `app/about/page.tsx` per the D-01 contract (Claude drafts; user edits in-place before commit). The prose satisfies the structural contract (150-250 words, 2-3 paragraphs, 3 buckets in order, cliché-ban regex passes, no emoji, no exclamation points, no origin paragraph, location inline, ≥2 specific current-focus projects with memory-anchored framing). The page also reuses `<HeroPhoto>` (Option A per D-08) so the `view-transition-name: hero-photo` seam matches the home side, and includes the single `<CTAArrowLink href="/">Get in touch</CTAArrowLink>` per D-12 + D-14 + D-15.

Working tree status (pre-checkpoint):
- `app/about/page.tsx` modified, NOT committed (waiting for this review).
- Both Phase 3 specs (`tests/about-renders.spec.ts`, `tests/about-photo-shared-transition.spec.ts`) GREEN locally.
- Full Phase 1 + Phase 2 spec sweep still GREEN.
- `npm run typecheck`, `lint`, `build` all exit 0.
  </what-built>
  <how-to-verify>
**Step 1 — Read the drafted bio prose end-to-end.**

Open `app/about/page.tsx` in your editor. Read the bio paragraphs out loud (or silently — your call). Ask yourself:

1. **Does it sound like you?** Warm + personal first-person, conversational, hints at curiosity beyond the resume. NOT dry/third-person. NOT earnest/why-I-build. The voice anchor is the home hero tagline ("Business student and entrepreneur in LA, building things and running a small content brand") — the /about prose should extend that voice into longer form without contradicting it or repeating it verbatim.
2. **Does the first sentence land identity in ~15 words?** Name + LA + business-student framing in the opening. (D-03 bucket 1, D-19 location inline.)
3. **Does paragraph 2 (or end of paragraph 1) name ≥2 specific current projects?** CapitolLens + content brand + dev tools, with concrete framing — not generic categories. Memory-anchored: CapitolLens = Form 4 insider-buy paper-trading (NOT live trading); two-channel brand = wholesome POV stories + deadpan meme reads; dev tools = pick 1-2 specific names (GSD, shorts-factory, meme-dashboard, reel-research-agent).
4. **Does the last paragraph make an open-to invitation that primes the CTA?** Concrete examples ("trading-desk internships, content collabs, or just a chat about what's working in AI-finance"), not "always looking to chat." Specific over vague.
5. **Cliché scan:** any of `passionate`, `I love to learn`, `lifelong learner`, `driven by`, `innovative`, `cutting-edge`, `wear many hats`, `results-oriented`, `outcome-driven` slip in despite the regex? Re-read with a recruiter-resume-template ear.
6. **No exclamation points.** No emoji. No origin / why-I-build backstory (D-04).
7. **Length feels right under 60 seconds?** Average reader at 200wpm reads 200 words in 60s. The target band 150-250 has slack.

**Step 2 — Edit in place if needed.**

You're the final arbiter (D-01). Open `app/about/page.tsx`, edit any sentences directly, save. As long as your edits keep:
- 150-300 words total (the spec gate is 150-300 — gives slack for editing),
- 2 or 3 `<p>` paragraphs (no more, no less),
- The cliché ban (regex + spirit),
- The 3 buckets in order,
- No `!`, no emoji, no origin paragraph,

... the spec stays GREEN. If you make heavy edits, re-run `npx playwright test tests/about-renders.spec.ts` to confirm.

**Step 3 — Optional: tweak the CTA text.**

D-13 allows variants of `Get in touch`: `Drop me a line`, `Say hi`. The Task 1 spec regex `/Get in touch|Drop me a line|Say hi/i` tolerates any of the three. Edit the `<CTAArrowLink>` children string if a different verb fits the voice better. If you want a completely different verb, also update the spec's regex.

**Step 4 — Optional: visually verify in browser (recommended).**

```
npm run dev
```

Open http://localhost:3000/about. Confirm:
- Photo on the right at ≥768px, on top at <768px (mobile).
- Photo carries the editorial hairline border + 8px inner padding (inherited HeroPhoto treatment).
- Bio paragraphs read at `max-w-[44ch]` line length.
- `Get in touch →` CTA at the end in accent color (#7c87ff).
- Hover the CTA: underline appears, arrow translates +4px right.
- Click the CTA: routes to `/` (placeholder).
- Navigate `/` → `/about` via the Nav "About" link (or by typing the URL). In Chrome ≥111, the photo should cross-fade smoothly during the navigation (view-transition seam) — the photo "stays put" while the bio swaps in. In Firefox/older Safari, the navigation happens with no transition — that's the graceful fallback (D-21 / D-22 inherited).
- Tab order: Nav → CTA → Footer socials. Bio paragraphs + photo are NOT focusable.
- Focus the CTA via Tab: 2px solid accent ring with 2px offset appears.
- (Optional) Toggle Chrome DevTools "Emulate prefers-reduced-motion: reduce" — the bio paragraphs + CTA should render with NO fade-in delay; navigation between / and /about should fire with NO transition cross-fade.

**Step 5 — When satisfied, type `approved` and the executor will commit.**

If you find issues, describe them and the executor will revise + re-present.

**Commit-time action (executor performs after `approved`):**

The executor commits the working-tree change as:

```
feat(phase-3/w1): Phase 3 /about page — bio + photo + CTA composition (D-01..D-19)

Replaces the Plan 02-05 'Coming soon.' stub with the real /about page.
Bio prose drafted at execute time per D-01, user-reviewed in place before
commit. Reuses components/home/HeroPhoto (Option A per D-08) so the
view-transition-name: hero-photo seam matches the home side. Reuses
components/home/CTAArrowLink for the 'Get in touch →' nudge (D-12).
NO new components, NO new tokens, NO new motion keyframes.

Flips tests/about-renders.spec.ts and tests/about-photo-shared-transition.spec.ts
from RED to GREEN. Full Phase 1 + Phase 2 spec sweep still GREEN.
```

After commit, run the full Playwright suite one more time on the committed tree to confirm no regression:

```
npx playwright test
```

Expected: all Phase 1 + Phase 2 + Phase 3 specs GREEN. Phase 2 PERF-06 LCP on local `npm start` may still be RED — that's the Phase 6 carry-forward per STATE.md, not a regression.
  </how-to-verify>
  <resume-signal>Type "approved" to commit, or describe edits you've already saved + any other revisions needed</resume-signal>
  <action>
Human-in-the-loop checkpoint per D-01. Executor presents the drafted bio prose (working tree, uncommitted) to the user via the <what-built> + <how-to-verify> guidance above. User reads, optionally edits in place, and signals approval. On `approved` resume-signal, executor commits the working-tree change with the message in `<how-to-verify>` Step 5. If user describes revisions, executor applies them, re-runs the spec suite locally, then re-presents this checkpoint. No code-authoring work is performed by the executor in this task beyond applying user-requested revisions and the final commit. See `<what-built>` and `<how-to-verify>` for the full review workflow.
  </action>
  <verify>
    <automated>npx playwright test tests/about-renders.spec.ts tests/about-photo-shared-transition.spec.ts</automated>
    Executor re-runs the suite AFTER any user edits AND AFTER the final commit. Both specs must remain GREEN. If user edits cause a spec to flip RED (e.g., word count drops below 150 after a heavy trim), executor flags it to the user before committing.
  </verify>
  <done>
    - User has read the bio prose in `app/about/page.tsx` and either approved it as-shipped or applied in-place edits.
    - If user edited: revised prose still passes `tests/about-renders.spec.ts` (word count 150-300, paragraph count 2-3, cliché-ban regex, no exclamation points).
    - Executor has committed the change with the prescribed `feat(phase-3/w1): ...` message.
    - Full local Playwright suite re-run post-commit shows all Phase 1 + Phase 2 + Phase 3 specs GREEN. The only allowed RED is the pre-existing `tests/photo-lcp.spec.ts` PERF-06 LCP gate on local `npm start` (documented Phase 6 carry-forward — NOT a Phase 3 regression).
    - `.planning/phases/03-about-page/03-01-SUMMARY.md` written per the `<output>` block at the end of this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | `/about` is a fully static Server Component route with zero user input, zero forms, zero auth, zero database access, zero third-party SDKs, zero environment variables. Bio prose + photo + outbound link to `/` (placeholder for future modal trigger). No untrusted input crosses any boundary in Phase 3 scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information Disclosure) | bio prose authored by user | accept | Prose is intentionally public — this IS the About page. User reviews + approves prose at Task 3 before commit per D-01. No private info exposed by design (no SSN, no DOB beyond what's already public, no address beyond "LA"). |
| T-03-02 | T (Tampering) | view-transition CSS contract (`hero-photo` literal string) | mitigate | The string `'hero-photo'` lives in two places: `components/home/HeroPhoto.tsx` (inline style — already shipping) and `tests/about-photo-shared-transition.spec.ts` (assertion). Drift between them silently breaks the cross-route transition. Mitigation: spec assertion is the regression guard — if the literal ever changes on either side, the spec turns RED. Single source of truth is `<HeroPhoto>` (Option A reuse means both `/` and `/about` derive from the same component). |
| T-03-03 | I (Information Disclosure) | photo asset `public/portrait.jpg` | accept | v1 placeholder image is the same already shipping on `/` since Phase 2 (193KB, SHA-256 logged in `02-01-SUMMARY.md`). Public-by-design. Phase 6 swaps to a real shoot atomically per the D-16 / Phase 2 carry-forward. |
| T-03-04 | D (Denial of Service) | static page render performance | accept | No new bundle weight (Server Component, no new client JS, no new components if Option A chosen — D-08). HeroPhoto reuse means LCP image is byte-identical to `/` (browser cache hit on cross-route navigation). View-transition cross-fade is GPU-accelerated and does not block layout. Phase 6 owns the Lighthouse 95+ audit. |
| T-03-05 | S (Spoofing) | external links on /about | accept | The CTA href is `/` (internal placeholder, not external). Footer (inherited Phase 2 chrome) uses `target="_blank" rel="noopener noreferrer"` on its external social icons — already mitigated upstream. /about itself introduces zero new external links. If bio prose mentions `@braehods` it renders as plain text (not a link) per UI-SPEC § Optional inline mentions. |
| T-03-06 | E (Elevation of Privilege) | server component scope | accept | FOUND-07 / D-25 inherited: zero `'use client'`. No event handlers, no state, no client-side data fetching, no React refs to DOM. The page renders the same JSX on every request. No privilege boundary inside the component. |

**Net assessment:** Phase 3 is the lowest threat surface in the project to date — it's a single static informational page composed of two already-vetted Server Components plus author-supplied static prose. The one real attack surface (the view-transition `hero-photo` literal-string contract) is mitigated by the spec assertion in Task 1.
</threat_model>

<verification>
After Task 3's approved commit, the following must all hold:

**Spec coverage:**
- `tests/about-renders.spec.ts` GREEN — proves ABOUT-01 cliché ban + ABOUT-03 CTA presence + ABOUT-04 length/paragraph contract.
- `tests/about-photo-shared-transition.spec.ts` GREEN — proves ABOUT-02 photo presence (via HeroPhoto reuse) + D-17 view-transition seam matches on both routes.
- `tests/view-transition-name-present.spec.ts` (Phase 2) GREEN — proves no regression on the home side.
- `tests/hero-renders.spec.ts` (Phase 2) GREEN — Hero on / still composes.
- `tests/footer-socials-render.spec.ts` (Phase 2) GREEN — Footer chrome unchanged.
- `tests/no-client-components.spec.ts` (Phase 1) GREEN — zero `'use client'` in app/about/page.tsx.
- `tests/lighthouse.spec.ts` (Phase 1) GREEN — mono assertion on `/` still passes (no regression).
- Full local suite: 64+/66 GREEN (PERF-06 LCP on local `npm start` remains the documented Phase 6 carry-forward — NOT a Phase 3 regression).

**Build + lint + typecheck:**
- `npm run typecheck` exits 0.
- `npm run lint` exits 0.
- `npm run build` exits 0 (Turbopack compiles `/about` as static — appears as `○ (Static)` in build output, like the other pages).

**Source-code invariants:**
- Zero `'use client'` in `app/about/page.tsx` and any new file.
- No new component files created (Option A per D-08).
- No new design tokens, no new motion keyframes, no new spacing tokens.
- The literal string `'hero-photo'` appears in source ONLY via `components/home/HeroPhoto.tsx` (already shipping) — no duplicate in `app/about/page.tsx`.
- The literal string `'Get in touch'` (or approved variant `Drop me a line` / `Say hi`) appears in `app/about/page.tsx` exactly once as a CTAArrowLink child.

**Manual verification (Task 3 user-approval gate):**
- Bio reads as Braeden's voice (warm + personal first-person, ≤60s read).
- Three buckets in order (who+where+studying / current focus / open-to invitation).
- Photo treatment matches `/` (Option A reuse — identical hairline border + padding + radius).
- View-transition cross-fade fires in Chrome ≥111 on `/` ↔ `/about` nav.
- Reduced-motion emulation defeats both the stagger fade-in and the cross-fade.
- 320px viewport renders cleanly (no horizontal overflow per LNCH-05 inherited).
</verification>

<success_criteria>
Phase 3 is complete when ALL of the following are true:

1. **ABOUT-01 satisfied:** `/about` renders a short first-person bio with no AI-template phrases. Cliché-ban regex passes against `[data-test="about-section"]` innerText. User-approved at Task 3.
2. **ABOUT-02 satisfied:** Photo on /about uses the same `<HeroPhoto>` component as `/` (Option A per D-08) — same hairline-border tile, same `public/portrait.jpg` source, same 240/320 responsive sizing.
3. **ABOUT-03 satisfied:** Location appears inline in the bio prose per D-19. Current focus (CapitolLens + content brand + dev tools) appears in bio paragraph 2 per D-18. A single `<CTAArrowLink href="/">Get in touch</CTAArrowLink>` sits at the end of the bio per D-14 — placeholder pending Phase 5's atomic swap to `#contact` per D-15.
4. **ABOUT-04 satisfied:** Bio is 150-250 words across 2-3 short paragraphs (≤4 sentences each). Reads in ~60 seconds at 200wpm. Spec gates word count 150-300 and paragraph count 2-3.
5. **D-17 / view-transition seam:** `view-transition-name: hero-photo` resolves on both `/` and `/about` via the shared `<HeroPhoto>` component. Spec `tests/about-photo-shared-transition.spec.ts` asserts the seam on both routes.
6. **No regressions:** Phase 1 + Phase 2 full spec suite still GREEN. Hero on / still composes. Nav + Footer chrome unchanged. Focus ring, monogram, reduced-motion, lighthouse mono, no-client-components all GREEN.
7. **Server Component invariant held:** Zero `'use client'` introduced in Phase 3 source. FOUND-07 / D-25 preserved.
8. **One atomic commit lands the page** (after Task 1's spec-stub commit): `feat(phase-3/w1): Phase 3 /about page — bio + photo + CTA composition (D-01..D-19)`. Two commits total for the phase plan.
</success_criteria>

<output>
After all tasks complete, create `.planning/phases/03-about-page/03-01-SUMMARY.md` capturing:
- Which option was chosen for the photo wrapper (A direct reuse — default — OR B `<PortraitTile>` factor — only if executor surfaced a per-route selector-disambiguation need at execution time).
- Whether the bio shipped as a 2-paragraph or 3-paragraph split, and the corresponding CTA staggerIndex used.
- The final bio word count (post-user-edit).
- Any user-applied edits to the drafted prose (high-level — what changed, not the diff).
- Whether the CTA shipped as `Get in touch` or one of the approved variants (`Drop me a line` / `Say hi`).
- Phase 6 carry-forwards: the `href="/"` → `#contact` (or modal-button) atomic swap that Phase 5 owns across Nav.tsx + app/about/page.tsx in one commit.
- Spec scoreboard delta: +2 GREEN (about-renders, about-photo-shared-transition); regression count = 0.
- Any deviations from UI-SPEC documented inline in the file's leading comment (mirrors Hero.tsx / Footer.tsx precedent — DEVIATION (Rule N — category): explanation).
- (Optional) A note that /about is the first real cross-route view-transition consumer; manual visual verification (Chrome ≥111, /↔/about nav, photo cross-fade) was OK / NOT OK — and if NOT OK, the symptom for Phase 6's polish sweep.
</output>
