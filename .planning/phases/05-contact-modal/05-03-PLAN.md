---
phase: 5
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - app/layout.tsx
  - components/layout/Nav.tsx
  - app/about/page.tsx
  - tests/about-renders.spec.ts
autonomous: false
requirements: [CTCT-01, CTCT-02, CTCT-03, CTCT-04, CTCT-05, CTCT-06, CTCT-07, A11Y-03]
tags: [phase-5, contact-modal, atomic-trigger-swap, d-05, mount-island, phase-exit-verify]
must_haves:
  truths:
    - "ContactModal mounts in app/layout.tsx as a body-level sibling AFTER <Footer /> (top-layer dialog convention)"
    - "components/layout/Nav.tsx LINKS array line 15: `href: '/'` is replaced with `href: '#contact'` for the Contact entry"
    - "app/about/page.tsx line 75 CTAArrowLink: `href=\"/\"` is replaced with `href=\"#contact\"`"
    - "tests/about-renders.spec.ts lines 41-47 assertion is atomically updated from `href === '/'` to `href === '#contact'` in the SAME commit (otherwise this spec goes RED on Nav/about source swap)"
    - "All 4 file edits land in ONE atomic commit per D-05 binding (single Wave, single commit; plan-checker refuses split-Wave variants)"
    - "Trigger surfaces stay locked at exactly 2 per D-04: Nav `Contact` link (global, every page) + /about `Get in touch` CTA (warm post-bio nudge) — NO Hero CTA added, NO Footer Contact link added"
    - "Mobile nav placement per D-07: Contact stays inside the existing `<details>` hamburger; the single LINKS array source covers BOTH desktop nav AND mobile hamburger from one edit (no special-case promotion)"
    - "Modal opens cross-route: clicking Contact from /, /about, /work all open the same ContactModal instance (mounted once in layout, available everywhere)"
    - "Final 4 of 9 Plan 01 RED specs flip GREEN: contact-modal-opens-from-nav, contact-modal-opens-from-about, contact-modal-esc-closes, contact-trigger-rewire"
    - "FOUND-07 carve-out preserved: `git grep -l \"use client\" -- '*.tsx' '*.ts'` STILL returns ONLY components/contact/ContactModal.tsx (no new client directives introduced)"
    - "Phase 1+2+3+4 chrome canaries all GREEN (no regressions in nav, footer, focus-ring, monogram, work grid, about renders)"
    - "Phase Exit Visual Verification (UI-SPEC lines 600-628) walked manually by user — modal opens from both triggers, ESC closes + focus returns, success/error states render verbatim copy, mailto fallback fires on click, reduced-motion clamps the fade"
  artifacts:
    - path: "app/layout.tsx"
      provides: "ContactModal mounted as body-level sibling, available globally on all routes"
      contains: "<ContactModal />"
    - path: "components/layout/Nav.tsx"
      provides: "Contact LINKS entry uses hash-based trigger href"
      contains: "href: '#contact'"
    - path: "app/about/page.tsx"
      provides: "Get in touch CTAArrowLink uses hash-based trigger href"
      contains: "href=\"#contact\""
    - path: "tests/about-renders.spec.ts"
      provides: "CTA href assertion atomically updated to match new ground truth"
      contains: "'#contact'"
  key_links:
    - from: "app/layout.tsx"
      to: "components/contact/ContactModal.tsx"
      via: "named import + sibling mount after Footer"
      pattern: "import\\s*\\{\\s*ContactModal\\s*\\}\\s*from\\s*['\"]@/components/contact/ContactModal['\"]"
    - from: "Nav LINKS Contact entry"
      to: "ContactModal hash listener"
      via: "browser fires `hashchange` on `<Link href='#contact'>` click; modal opens"
      pattern: "href:\\s*['\"]#contact['\"]"
    - from: "/about CTAArrowLink"
      to: "ContactModal hash listener"
      via: "same hashchange path; same modal instance (single mount in layout)"
      pattern: "href=['\"]#contact['\"]"
---

<objective>
Wave 2 / Wave 3 — the atomic D-05 trigger rewire + Phase Exit verification. Three source edits + one spec assertion update land in a SINGLE atomic commit per D-05 binding (Phase 3 D-15 carry-forward + Phase 5 D-05 explicit). After this commit:
- ContactModal is mounted globally (one instance in `app/layout.tsx`, available on every route — `/`, `/about`, `/work`).
- Both trigger surfaces (Nav `Contact` link + /about `Get in touch` CTA) point at `#contact`, firing the hash listener inside the client island.
- The existing `tests/about-renders.spec.ts` assertion (lines 41-47, currently asserts `href === '/'`) is updated atomically to assert `href === '#contact'` — otherwise it would go RED the moment the source swaps.

The final 4 of 9 Plan 01 RED specs flip GREEN: contact-modal-opens-from-nav, contact-modal-opens-from-about, contact-modal-esc-closes, contact-trigger-rewire. Combined with Plans 01 + 02, all 9 Phase 5 specs are now GREEN.

After the atomic commit, Task 2 walks the manual Phase Exit Visual Verification checklist (UI-SPEC lines 600-628) — this is a `checkpoint:human-verify` because pixel-level visual fidelity, screen reader announcement audibility, and real-device iOS Safari behavior cannot be automated.

Output: 1 atomic commit (4 file edits, all coupled per D-05 plan-checker gate), 1 manual Phase Exit checkpoint that pauses for user sign-off, 1 final SUMMARY commit closing Phase 5 at implementation level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-contact-modal/05-CONTEXT.md
@.planning/phases/05-contact-modal/05-RESEARCH.md
@.planning/phases/05-contact-modal/05-PATTERNS.md
@.planning/phases/05-contact-modal/05-UI-SPEC.md
@.planning/phases/05-contact-modal/05-VALIDATION.md
@.planning/phases/05-contact-modal/05-01-PLAN.md
@.planning/phases/05-contact-modal/05-02-PLAN.md
@app/layout.tsx
@components/layout/Nav.tsx
@app/about/page.tsx
@tests/about-renders.spec.ts

<interfaces>
<!-- Critical contracts the executor needs. Pulled from existing source + Plan 02 deliverable. -->

From `app/layout.tsx` (current state — Plan 02 did NOT modify this file):
```tsx
import { Analytics } from '@vercel/analytics/next';
import { SpeedInsights } from '@vercel/speed-insights/next';
import { Nav } from '@/components/layout/Nav';
import { Footer } from '@/components/layout/Footer';
// ...
<body>
  <Nav />
  <main className="mx-auto max-w-3xl px-6 py-16 lg:px-12">{children}</main>
  <Footer />
  <Analytics />
  <SpeedInsights />
</body>
```
- Phase 5 adds: `import { ContactModal } from '@/components/contact/ContactModal';` and `<ContactModal />` as a sibling AFTER `<Footer />` and BEFORE `<Analytics />`. Layout stays a Server Component (importing a client island into RSC is the standard Next.js boundary pattern).

From `components/layout/Nav.tsx` (current state — Plan 02 did NOT modify this file):
- Line 12-16 LINKS array. Line 15: `{ href: '/', label: 'Contact' }, // stub until Phase 5 wires the modal trigger`
- Plan 5 swaps to: `{ href: '#contact', label: 'Contact' }, // Phase 5: hash-driven trigger per D-06 strong recommendation` (also removes the "stub" trailing comment).
- The LINKS array is consumed by BOTH desktop nav (line 32-36) AND mobile hamburger nav (line 77-81 region) — the single value flip covers both surfaces from one edit per D-07.

From `app/about/page.tsx` (current state — Plan 02 did NOT modify this file):
- Line 75: `<CTAArrowLink href="/" staggerIndex={4}>`
- Plan 5 swaps to: `<CTAArrowLink href="#contact" staggerIndex={4}>`
- CTAArrowLink itself does NOT change — it accepts any `href: string` and passes through to `next/link`, which honors hash-only hrefs natively.

From `tests/about-renders.spec.ts` lines 41-47 (current state — asserts the placeholder):
```ts
const cta = page.getByRole('link', { name: CTA_TEXT_REGEX });
await expect(cta, 'exactly one CTA link matching /Get in touch|Drop me a line|Say hi/i').toHaveCount(1);
await expect(cta, 'CTA href should be "/" placeholder (D-15 — Phase 5 atomic swap)').toHaveAttribute(
  'href',
  '/',
);
```
- Plan 5 swaps the assertion target from `'/'` to `'#contact'` AND updates the message arg from "CTA href should be '/' placeholder (D-15 — Phase 5 atomic swap)" to "CTA href should be '#contact' (D-05 atomic swap landed in Plan 05-03)".

From PATTERNS.md (line 357-388 — exact diff format for the 3 source files + companion test update is documented; the executor should match this verbatim).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ATOMIC — mount ContactModal in layout + rewire Nav + /about triggers + update about-renders assertion (D-05 single-commit binding)</name>
  <files>app/layout.tsx, components/layout/Nav.tsx, app/about/page.tsx, tests/about-renders.spec.ts</files>
  <read_first>
    - C:\Users\Braeden\Projects\braeden-site\app\layout.tsx (current 25-line Server Component — must understand structure to add the import + sibling mount cleanly)
    - C:\Users\Braeden\Projects\braeden-site\components\layout\Nav.tsx (current LINKS array structure — line 15 is the swap target; preserve line 15's leading comment style and the trailing "stub until Phase 5" remark migrates to "Phase 5: hash-driven trigger per D-06")
    - C:\Users\Braeden\Projects\braeden-site\app\about\page.tsx (lines 70-85 region — verify line 75 CTAArrowLink is the only `href="/"` in the file; the swap target is line 75 only)
    - C:\Users\Braeden\Projects\braeden-site\tests\about-renders.spec.ts (lines 41-47 assertion block — the spec assertion update must be atomic with the source swap)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-CONTEXT.md (D-05 atomic-swap binding — read this in full before starting; D-06 hash-based architecture rationale; D-07 mobile nav placement — single LINKS array source covers both)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-RESEARCH.md (Pattern 5 lines 548-578 — the literal diff for both source files)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-PATTERNS.md (lines 357-435 — atomic update sequence)
  </read_first>
  <action>
    All FOUR file edits must land in ONE atomic git commit. Do NOT commit between edits. Do NOT split across two waves. The D-05 binding is from Phase 3 `.continue-here.md` carry-forward — plan-checker refuses split-Wave variants.

    **Edit 1: `app/layout.tsx` — mount the ContactModal client island**
    - Add the import (group with the other component imports, after the Footer import):
      `import { ContactModal } from '@/components/contact/ContactModal';`
    - Add `<ContactModal />` as a body-level sibling AFTER `<Footer />` and BEFORE `<Analytics />`. The dialog should sit at the END of the body content (per UI-SPEC + RESEARCH `## Recommended Project Structure`) so it's a top-level sibling for the native top-layer behavior. Final body shape:
      ```
      <body>
        <Nav />
        <main className="mx-auto max-w-3xl px-6 py-16 lg:px-12">{children}</main>
        <Footer />
        <ContactModal />
        <Analytics />
        <SpeedInsights />
      </body>
      ```
    - Layout stays a Server Component (importing a client island into RSC is the standard Next.js App Router boundary pattern — no `'use client'` is added to layout). Verify: `git grep -l "use client" -- '*.tsx' '*.ts'` STILL returns exactly `components/contact/ContactModal.tsx` after this edit.

    **Edit 2: `components/layout/Nav.tsx` line 15 — Contact LINKS entry hash swap (D-05 source 1 of 2)**
    - Current: `{ href: '/', label: 'Contact' }, // stub until Phase 5 wires the modal trigger`
    - Swap to: `{ href: '#contact', label: 'Contact' }, // Phase 5: hash-driven trigger per D-06 (ContactModal listens for #contact via hashchange)`
    - Do NOT touch any other line in Nav.tsx. The LINKS array is consumed by BOTH desktop and mobile hamburger nav from a single source per D-07 — one href flip covers both surfaces (verified in Pattern 2 of PATTERNS.md).

    **Edit 3: `app/about/page.tsx` line 75 — CTAArrowLink href swap (D-05 source 2 of 2)**
    - Current: `<CTAArrowLink href="/" staggerIndex={4}>`
    - Swap to: `<CTAArrowLink href="#contact" staggerIndex={4}>`
    - Do NOT touch the children "Get in touch" text (verbatim D-08 continuity per CONTEXT.md). Do NOT touch `staggerIndex={4}` (Phase 3 motion contract). Do NOT touch any other line.

    **Edit 4: `tests/about-renders.spec.ts` lines 41-47 — atomic assertion update**
    - Current message arg: `'CTA href should be "/" placeholder (D-15 — Phase 5 atomic swap)'`
    - Update to: `'CTA href should be "#contact" — D-05 atomic swap landed in Plan 05-03'`
    - Current assertion: `await expect(cta, ...).toHaveAttribute('href', '/');`
    - Update to: `await expect(cta, ...).toHaveAttribute('href', '#contact');`
    - Update the JSDoc header banner line 10: `AND whose href === '/' (D-13 default + variants; D-15 placeholder pending Phase 5)` → `AND whose href === '#contact' (D-13 default + variants; D-15 → D-05 atomic swap landed in Plan 05-03)`
    - Do NOT change the cliché-ban regex, word-count gate, paragraph-count gate, or any other assertion. The CTA href is the only thing changing.

    **Verify ALL 4 edits land in one commit**:
    - Stage all 4 files: `git add app/layout.tsx components/layout/Nav.tsx app/about/page.tsx tests/about-renders.spec.ts`
    - Single commit message: `feat(phase-5/w2): atomic — mount ContactModal in layout + rewire Nav + /about triggers to #contact (D-05)` (per RESEARCH.md Recommended Skeleton W2-T1 + project commit-message convention; NO emoji or boilerplate footer per CLAUDE.md project guidelines)
    - Verify single-commit atomicity: `git log -n 1 --name-only` should list all 4 files in the same commit. If two commits accidentally land, soft-reset and recommit as one (do NOT use `--amend` per CLAUDE.md GSD safety rule on amends).

    Do NOT introduce any new `'use client'` directive (FOUND-07 carve-out invariant). Do NOT modify Footer, Analytics, SpeedInsights, MonogramMark, fonts, globals.css, or any other file. The atomic discipline is THE plan-checker gate for this Wave.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const layout=fs.readFileSync('app/layout.tsx','utf8');const nav=fs.readFileSync('components/layout/Nav.tsx','utf8');const about=fs.readFileSync('app/about/page.tsx','utf8');const spec=fs.readFileSync('tests/about-renders.spec.ts','utf8');const checks={'layout-import':/import\\s*\\{\\s*ContactModal\\s*\\}\\s*from\\s*['\\\"]@\\/components\\/contact\\/ContactModal['\\\"]/.test(layout),'layout-mount':/<ContactModal\\s*\\/>/.test(layout),'layout-after-footer':layout.indexOf('<ContactModal')>layout.indexOf('<Footer'),'layout-before-analytics':layout.indexOf('<ContactModal')<layout.indexOf('<Analytics'),'nav-hash':/href:\\s*['\\\"]#contact['\\\"][\\s\\S]*label:\\s*['\\\"]Contact['\\\"]/.test(nav),'nav-no-old':!/href:\\s*['\\\"]\\/'\\s*,?\\s*label:\\s*['\\\"]Contact['\\\"]/.test(nav),'about-hash':/<CTAArrowLink\\s+href=['\\\"]#contact['\\\"]\\s+staggerIndex=\\{4\\}/.test(about),'about-no-old':!/<CTAArrowLink\\s+href=['\\\"]\\/[\\\"]\\s+staggerIndex=\\{4\\}/.test(about),'spec-hash':/toHaveAttribute\\(\\s*['\\\"]href['\\\"]\\s*,\\s*['\\\"]#contact['\\\"]\\s*\\)/.test(spec),'spec-no-old':!/toHaveAttribute\\(\\s*['\\\"]href['\\\"]\\s*,\\s*['\\\"]\\/'\\s*\\)/.test(spec),'spec-message-updated':/D-05 atomic swap landed in Plan 05-03/.test(spec)};let fail=false;for(const[k,v]of Object.entries(checks)){if(!v){console.error('FAIL:',k);fail=true}}if(fail)process.exit(1);console.log('all atomic-swap checks pass')" && node -e "const{execSync}=require('child_process');const out=execSync('git log -n 1 --name-only --pretty=format:',{encoding:'utf8'}).trim().split(/\\r?\\n/).filter(Boolean).map(s=>s.replace(/\\\\/g,'/'));const expected=['app/layout.tsx','components/layout/Nav.tsx','app/about/page.tsx','tests/about-renders.spec.ts'];const missing=expected.filter(f=>!out.includes(f));if(missing.length){console.error('Atomic-commit gate FAIL — files NOT in last commit:',missing,'\\nFound:',out);process.exit(1)}console.log('atomic commit verified — all 4 files in one commit')" && node -e "const{execSync}=require('child_process');const out=execSync('git grep -l \\\"use client\\\" -- *.tsx *.ts',{encoding:'utf8'}).trim().split(/\\r?\\n/).filter(Boolean).map(s=>s.replace(/\\\\/g,'/'));if(out.length!==1||out[0]!=='components/contact/ContactModal.tsx'){console.error('FOUND-07 violation:',out);process.exit(1)}console.log('FOUND-07 carve-out preserved')" && npm run typecheck && npm run lint && npm run build && npx playwright test tests/contact-modal-opens-from-nav.spec.ts tests/contact-modal-opens-from-about.spec.ts tests/contact-modal-esc-closes.spec.ts tests/contact-modal-states.spec.ts tests/contact-modal-honeypot.spec.ts tests/contact-modal-min-time.spec.ts tests/contact-modal-mailto-fallback.spec.ts tests/contact-trigger-rewire.spec.ts tests/single-client-island.spec.ts tests/no-client-components.spec.ts tests/about-renders.spec.ts tests/focus-ring.spec.ts tests/no-bare-outline-none.spec.ts tests/reduced-motion.spec.ts tests/monogram.spec.ts tests/footer-socials-render.spec.ts tests/work-grid-renders.spec.ts --project=chromium-mobile</automated>
  </verify>
  <acceptance_criteria>
    - All 4 file edits present (verified by node-script regexes; exits 1 if any pattern missing OR any old pattern still present)
    - Single atomic commit verified — `git log -n 1 --name-only` lists all 4 files (atomic-commit node script exits 1 otherwise)
    - FOUND-07 carve-out preserved: `git grep -l "use client" -- '*.tsx' '*.ts'` returns ONLY `components/contact/ContactModal.tsx`
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm run build` exits 0 (Turbopack compiles cleanly; ContactModal client chunk still in build output)
    - **All 9 Phase 5 specs from Plan 01 are now GREEN**:
      - tests/contact-modal-opens-from-nav.spec.ts ✅
      - tests/contact-modal-opens-from-about.spec.ts ✅
      - tests/contact-modal-esc-closes.spec.ts ✅
      - tests/contact-modal-states.spec.ts ✅ (was GREEN since Plan 02)
      - tests/contact-modal-honeypot.spec.ts ✅ (was GREEN since Plan 02)
      - tests/contact-modal-min-time.spec.ts ✅ (was GREEN since Plan 02)
      - tests/contact-modal-mailto-fallback.spec.ts ✅ (was GREEN since Plan 02)
      - tests/contact-trigger-rewire.spec.ts ✅ (D-05 atomicity gate — both Nav + about CTA assert href === '#contact')
      - tests/single-client-island.spec.ts ✅ (was GREEN since Plan 02)
    - Phase 1+2+3+4 chrome canaries unchanged GREEN: tests/no-client-components.spec.ts (atomically updated in Plan 02), tests/about-renders.spec.ts (atomically updated in this plan to assert #contact — was GREEN against '/' before this commit, now GREEN against '#contact'), tests/focus-ring.spec.ts, tests/no-bare-outline-none.spec.ts, tests/reduced-motion.spec.ts, tests/monogram.spec.ts, tests/footer-socials-render.spec.ts, tests/work-grid-renders.spec.ts
    - Commit message follows project convention: `feat(phase-5/w2): atomic — mount ContactModal in layout + rewire Nav + /about triggers to #contact (D-05)`
  </acceptance_criteria>
  <done>1 atomic commit lands all 4 file edits per D-05 binding. All 9 Phase 5 specs GREEN. Phase 1+2+3+4 chrome canaries GREEN. FOUND-07 carve-out preserved at exactly 1 client island.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase Exit Visual Verification (UI-SPEC lines 600-628 manual checklist)</name>
  <what-built>
    The Wave-2 atomic commit completes Phase 5 at the implementation level:
    - ContactModal client island (`components/contact/ContactModal.tsx`) is the SOLE `'use client'` directive in the entire codebase per FOUND-07 carve-out
    - Mounted globally in `app/layout.tsx` (one instance, available cross-route)
    - Nav `Contact` link + /about `Get in touch` CTA both fire the hash-driven trigger (`href="#contact"`)
    - All 9 new Playwright specs are GREEN (CTCT-01..07 + A11Y-03 covered)
    - Phase 1+2+3+4 chrome canaries unchanged GREEN

    What CANNOT be automated and needs human eyes (UI-SPEC § Phase Exit Visual Verification, lines 600-628):
    - Pixel-level visual fidelity (modal centering, padding, hairline borders, typography rendering, char-counter color flip past 800 chars)
    - Real Formspree submission delivers an actual email to the inbox
    - Screen reader (NVDA on Windows OR VoiceOver on macOS) announces all 4 states via `aria-live` regions
    - `prefers-reduced-motion: reduce` (OS-level toggle) disables the 240ms fade-in (modal appears instantly)
    - Mobile viewport (320px on actual device or DevTools emulation) renders modal cleanly with mx-4 page margins
    - iOS Safari does NOT auto-zoom on input focus (Pitfall 7 — verifies `text-base` 16px lock holds)
  </what-built>
  <how-to-verify>
    Start the dev server (or use a Vercel branch preview if you've pushed): `npm run dev` — visit http://localhost:3000/.

    Walk this 12-item checklist (sourced from `05-UI-SPEC.md` Phase Exit Visual Verification, lines 600-628):

    **1. Open from Nav (cross-route)**:
       - On /, click the Nav `Contact` link → modal opens (240ms fade)
       - Press ESC → modal closes; URL hash cleared from `#contact`
       - Click `Contact` again → modal reopens (Pitfall 1 verified — same-hash click after close still fires)
       - Repeat from /about and /work — same dialog opens (single mount in layout)

    **2. Open from /about CTA**:
       - On /about, scroll to the "Get in touch →" CTA at the bottom of the bio
       - Click → modal opens (same instance as Nav-triggered)

    **3. Modal heading + form rendering**:
       - Heading reads exactly "Get in touch" (D-08)
       - 3 fields visible: Name, Email, Message — each with accent `*` asterisk after label
       - Char counter under textarea reads "0 / 1000" in muted gray
       - Send button reads "Send message" in accent blue, hairline border
       - "Or just email me directly →" mailto link below Send (accent blue, arrow shifts on hover)

    **4. Char counter color flip (D-02 + Phase 4 D-09 reuse)**:
       - Type 801+ characters in the message textarea
       - Counter text color flips from muted gray (`#a8a8a8`) to muted amber (`#c8a86a`) — same exact hue as the Phase 4 paper-trading status dot

    **5. Required-field validation (CTCT-05)**:
       - Click Send with empty fields → browser-native validation prevents submit + highlights first empty required field

    **6. Submitting state (D-12 + CTCT-03)**:
       - Wait at least 1.6 seconds after modal open (defeats the 1500ms min-time gate)
       - Fill all 3 fields with realistic data (e.g., "Test", "you@example.com", "Hello, this is a test message that is reasonably long.")
       - Click Send → button label changes to "Sending…" (U+2026 ellipsis); button + fields disable (opacity 0.6, cursor not-allowed); typed content remains visible
       - If the network is fast, the submitting state may flash briefly — open DevTools Network tab, set throttling to "Slow 3G" to make the state observable

    **7. Success state (D-10 verbatim — REAL Formspree submit)**:
       - With network restored, complete a real submit
       - Form replaces with: "Thanks — I'll get back to you within a day or two." (em-dash U+2014, curly apostrophe U+2019)
       - "Send another →" link appears below
       - Check the configured Formspree inbox (xqeypnkw → fakegoat1@gmail.com) — a real email should arrive within ~30s

    **8. Error state (D-11 verbatim)**:
       - Open DevTools Network tab; right-click the Formspree request URL → Block request URL (or set throttling to "Offline")
       - Re-fill form (after click "Send another →" or close + reopen + wait 1.6s) and submit
       - Error renders ABOVE the form (form remains visible + populated): "Something went wrong sending that. Try the email link below."
       - Form fields retain typed content (Pitfall 3 verification on real device)

    **9. mailto fallback (CTCT-06)**:
       - Click the "Or just email me directly →" link in idle state
       - OS mail client opens with "Hi Braeden" prefilled subject + `fakegoat1@gmail.com` recipient

    **10. Reduced-motion (A11Y-06)**:
       - System Preferences → Accessibility → Display → Reduce Motion (macOS) OR Settings → Ease of Access → Display → Animations Off (Windows)
       - Reload page; click Contact in Nav
       - Modal appears INSTANTLY (no 240ms fade); backdrop dims instantly with the same 60% opacity but no fade transition

    **11. Mobile viewport (320px) + iOS Safari auto-zoom (Pitfall 7)**:
       - DevTools → Device toolbar → iPhone SE (375x667) or smaller (320x568)
       - Modal renders with `mx-4` page margins (16px each side)
       - Tap input field — page does NOT zoom in (input font-size is 16px per `text-base` lock — Pitfall 7 verified)
       - All 3 fields + counter + Send button + mailto link visible without horizontal scroll
       - If a real iPhone is available, repeat on actual hardware — DevTools emulation doesn't catch all iOS Safari quirks

    **12. Tab order + focus trap (A11Y-03)**:
       - Open modal via Nav
       - Press Tab repeatedly — focus cycles through: Name input → Email input → Message textarea → Send button → Mailto link → wraps back to Name (browser-managed focus trap via `<dialog>.showModal()`)
       - Honeypot `company` field is SKIPPED (tabindex=-1)
       - Focus ring is the locked accent blue 2px outline + 2px offset (Phase 1 DSGN-06; verifies Pitfall 4 transition-property discipline holds)
       - Press ESC → focus returns to the Nav `Contact` link (the original trigger)

    Type your verification result.
  </how-to-verify>
  <resume-signal>
    - Type "approved" if all 12 items pass and a real test email arrives at fakegoat1@gmail.com → executor proceeds to Task 3 (close Phase 5 docs).
    - Type "issues: <list>" with specific items that failed → executor opens an investigation (Rule 1 deviation) and either fixes locally or files a Phase 6 carry-forward, depending on severity.
    - Type "defer" if any item is blocked by a deploy concern (Vercel preview not yet pushed, real iOS device unavailable, etc.) — executor logs the deferred items as Phase 6 carry-forwards and proceeds to Task 3 (matching Phase 4's deliberate-deferral precedent per STATE.md).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close Phase 5 — write SUMMARY + flip ROADMAP/REQUIREMENTS/STATE traceability</name>
  <files>.planning/phases/05-contact-modal/05-03-SUMMARY.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - C:\Users\Braeden\Projects\braeden-site\.planning\ROADMAP.md (find Phase 5 section to flip checkbox + plan-list checkboxes; update Progress Table row "5. Contact Modal" from `Not started` to `Complete`)
    - C:\Users\Braeden\Projects\braeden-site\.planning\REQUIREMENTS.md (Traceability table — flip CTCT-01..07 + A11Y-03 from `Pending` to `Complete` (or `Complete (local; deploy-verify deferred to Phase 6)` if Task 2 user response was `defer` for the real-Formspree-submit item))
    - C:\Users\Braeden\Projects\braeden-site\.planning\STATE.md (append a new "Last Session" entry following the Phase 4 pattern; update progress percentage to ~98% — 5/6 phases complete; carry-forward note for Phase 6 about the pre-existing 7 / route specs + any Phase 5 manual deferrals)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-01-PLAN.md, 05-02-PLAN.md, 05-03-PLAN.md (reference these in the SUMMARY's "Plans Executed" section)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\04-work-projects\04-01-SUMMARY.md (use as the structural template for SUMMARY format — this project's established convention)
  </read_first>
  <action>
    **Step 1 — Write `.planning/phases/05-contact-modal/05-03-SUMMARY.md`** covering all 3 plans (single consolidated SUMMARY per Phase 4 precedent — no separate 05-01-SUMMARY.md or 05-02-SUMMARY.md; this consolidated file is THE Phase 5 summary):

    Required sections (mirror `04-01-SUMMARY.md` structure):
    - Header: phase, plans (3), waves (3), commits (5 expected: 3 from Plan 01 + 1 from Plan 02 + 1 atomic from Plan 03 Task 1; +1 final docs commit = 6 total).
    - **Plans Executed**: brief 1-line-per-plan recap of what each plan landed (Plan 01 = test infra + dep + CSS; Plan 02 = ContactModal client island; Plan 03 = atomic mount + trigger rewire).
    - **Verbatim Copy Lock Verification**: table of D-08..D-15 with the literal string + `grep -c` count + file location confirming each lands exactly once.
    - **FOUND-07 Carve-Out Final State**: `git grep -l "use client"` output showing exactly `components/contact/ContactModal.tsx`.
    - **Spec Scoreboard Delta**: 9 new Phase 5 specs all GREEN (list each with the requirement IDs covered); existing `tests/no-client-components.spec.ts` updated atomically; existing `tests/about-renders.spec.ts` updated atomically (CTA href assertion `/` → `#contact`); Phase 1+2+3+4 chrome canaries net 0 regressions.
    - **D-05 Atomic Swap Confirmation**: `git log -n 1 --name-only` output for the Plan 03 Task 1 commit showing all 4 files in one commit (the plan-checker gate evidence).
    - **Phase Exit Visual Verification Result**: User checkpoint response from Task 2 — pasted verbatim ("approved" / "issues: ..." / "defer").
    - **Deviations**: Any Rule 1-4 deviations across the 3 plans, with rationale + commit refs.
    - **Phase 6 Carry-Forwards**: 
      - Real Formspree end-to-end email delivery verification (if Task 2 was "defer")
      - Real-device iOS Safari verification (if no iPhone available locally)
      - Screen-reader manual audit (NVDA/VoiceOver) of all 4 state announcements
      - The 7 pre-existing `/`-route Phase 1 spec failures (lighthouse, photo-lcp, no-bare-outline-none) — Phase 4 carry-forward, still pending
      - WebAIM contrast measurement on `#c8a86a` against `#0a0a0a` (Phase 5 second consumer of the literal — confirms Phase 4 D-09 audit still holds; a 3rd consumer would trigger `@theme` promotion per Phase 6 polish per D-02 inheritance discipline)
      - PERF-03 first-page bundle size measurement now that the single client island ships (~5KB chunk for ContactModal+useForm graph; verify it lands ≤50KB total target)
    - **Net Phase 5 Movement**: +9 newly-GREEN specs, 0 regressions, +1 client island carve-out (FOUND-07 exception activated as planned), +1 production dep (`@formspree/react@3.0.0`).

    **Step 2 — Update `.planning/ROADMAP.md`**:
    - Find the `### Phase 5: Contact Modal` section. Change the leading `- [ ]` checkbox in the top-of-file phases list to `- [x]` with the completion date suffix: `- [x] **Phase 5: Contact Modal** — Shipped 2026-05-14. Native <dialog> client island posts to Formspree xqeypnkw with focus trap + ESC + scroll lock; honeypot (name=company) + 1500ms min-time bot defense; mailto fallback; 9 new specs GREEN; D-05 atomic Nav + /about trigger swap landed in single commit; FOUND-07 carve-out activated (1 client island, 0 elsewhere).`
    - Update the **Plans** field from `**Plans**: TBD` to `**Plans**: 3 plans, 3 waves (W0/Plan 01 = validation infra + dep + CSS; W1/Plan 02 = ContactModal client island + atomic spec update; W2/Plan 03 = atomic mount + trigger rewire + Phase Exit verify)`.
    - Add the 3 plan-list lines (mirror Phase 4 format):
      ```
      - [x] 05-01-PLAN.md — W0: install @formspree/react@3.0.0, append modal-fade-in keyframe + dialog rules to globals.css (D-16/D-18), stub 9 RED Playwright specs (verbatim copy + atomic swap + single-client-island)
      - [x] 05-02-PLAN.md — W1: ship components/contact/ContactModal.tsx (the SOLE 'use client' island per FOUND-07 carve-out — D-01..D-19); wire data/site.ts.email per D-15a; atomically update tests/no-client-components.spec.ts to allow ContactModal
      - [x] 05-03-PLAN.md — W2: atomic D-05 commit (mount in app/layout.tsx + Nav LINKS href swap + /about CTA href swap + tests/about-renders.spec.ts assertion update — all in one commit); Phase Exit Visual Verification checkpoint; consolidated SUMMARY
      ```
    - Update the Progress Table row for Phase 5: `| 5. Contact Modal | 3/3 | Complete | 2026-05-14 |`
    - If user's Task 2 response was `defer`, append " (deploy-verify deferred to Phase 6)" to the Status cell.

    **Step 3 — Update `.planning/REQUIREMENTS.md` Traceability table**:
    - Flip CTCT-01..07 + A11Y-03 from `Pending` to `Complete` (or `Complete (local; deploy verify deferred to Phase 6)` if Task 2 was `defer`).

    **Step 4 — Update `.planning/STATE.md`**:
    - Update the top-of-file `progress:` block: `completed_phases: 4` → `completed_phases: 5`, `completed_plans: 10` → `completed_plans: 13` (10 + 3 new), `percent: 91` → `percent: 98` (5/6 phases done; minor adjustment for plan count change).
    - Append a new "Last Session" entry following the Phase 4 pattern, dated 2026-05-14, summarizing all 3 Phase 5 plans + the FOUND-07 carve-out activation + the 9 newly-GREEN specs + 0 regressions + Phase 6 hand-off list.
    - Update the `### Next Session` section: rename "Phase 5 (Contact Modal)" to "Phase 6 (Polish + SEO + Launch)" with a brief note about inheriting the Phase 4 + 5 deferred-verify cycle, the 7 pre-existing `/`-route spec failures, and the OG/JSON-LD/sitemap/DNS-swap workload.
    - Update the `**Current focus:**` line near the top: replace Phase 4 references with "Phase 6 — Polish + SEO + Launch (final phase)".

    **Step 5 — Final docs commit** (this commit):
    - Stage all 4 modified files: `git add .planning/phases/05-contact-modal/05-03-SUMMARY.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`
    - Commit: `docs(phase-5): close Phase 5 — 9 newly-GREEN specs, FOUND-07 carve-out activated, traceability flipped`
    - Do NOT amend prior commits per CLAUDE.md GSD safety rule. New commit only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const sum=fs.readFileSync('.planning/phases/05-contact-modal/05-03-SUMMARY.md','utf8');const roadmap=fs.readFileSync('.planning/ROADMAP.md','utf8');const reqs=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const state=fs.readFileSync('.planning/STATE.md','utf8');const checks={'summary-has-d05':/D-05/.test(sum),'summary-has-found07':/FOUND-07/.test(sum),'summary-has-9-specs':/9\\s+(new\\s+)?(specs|Phase 5)/i.test(sum),'roadmap-phase5-checked':/- \\[x\\] \\*\\*Phase 5/.test(roadmap),'roadmap-3-plans':/3 plans/.test(roadmap),'roadmap-progress-complete':/\\| 5\\. Contact Modal \\| 3\\/3 \\| Complete/.test(roadmap),'reqs-ctct01-complete':/\\| CTCT-01 \\| Phase 5 \\| Complete/.test(reqs),'reqs-ctct07-complete':/\\| CTCT-07 \\| Phase 5 \\| Complete/.test(reqs),'reqs-a11y03-complete':/\\| A11Y-03 \\| Phase 5 \\| Complete/.test(reqs),'state-percent-98':/percent:\\s*98/.test(state),'state-completed-5':/completed_phases:\\s*5/.test(state)};let fail=false;for(const[k,v]of Object.entries(checks)){if(!v){console.error('FAIL:',k);fail=true}}if(fail)process.exit(1);console.log('Phase 5 close docs verified')" && git log -n 1 --pretty=format:'%s' | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{if(!/docs\\(phase-5\\):/.test(s)){console.error('Last commit message must be docs(phase-5): close ...; got:',s);process.exit(1)}console.log('docs commit verified')})"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-contact-modal/05-03-SUMMARY.md` exists with all required sections (Plans Executed, Verbatim Copy Lock Verification, FOUND-07 Carve-Out, Spec Scoreboard, D-05 Atomic Swap Confirmation, Phase Exit Result, Deviations, Phase 6 Carry-Forwards, Net Movement)
    - `.planning/ROADMAP.md` Phase 5 row checked `[x]`, Plans field updated to "3 plans, 3 waves", Plan list has 3 `- [x]` lines, Progress Table row reads `| 5. Contact Modal | 3/3 | Complete | 2026-05-14 |`
    - `.planning/REQUIREMENTS.md` Traceability rows for CTCT-01..07 + A11Y-03 flipped to `Complete` (or `Complete (local; deploy verify deferred to Phase 6)` per user response)
    - `.planning/STATE.md` `completed_phases: 5`, `percent: 98`, new Last Session entry dated 2026-05-14 covering all 3 plans, Next Session points at Phase 6
    - Final docs commit message starts with `docs(phase-5):` (last-commit grep gate above)
    - No new code committed — this is a docs-only closing commit
    - FOUND-07 carve-out STILL exactly 1 client island (no regression introduced by docs work)
  </acceptance_criteria>
  <done>Phase 5 closed at implementation level: SUMMARY written, traceability flipped across ROADMAP + REQUIREMENTS + STATE, final docs commit landed. Phase 6 inherits the deferred-verify cycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → client island import | `app/layout.tsx` imports `ContactModal` (a client component). Next.js handles the RSC→client boundary at the import site automatically — no security-sensitive data crosses (the layout passes no props to ContactModal). |
| Trigger href value → ContactModal hashchange listener | Plan 03 swaps Nav `href: '/'` → `href: '#contact'` and /about CTA `href="/"` → `href="#contact"`. The hash value is hard-coded in source — no user input concatenation. |
| Atomic-commit gate (D-05 binding) | Plan-checker enforces single-commit landing of the 4 file edits. Splitting across waves would leave a stale `'/'` href next to a `#contact` href — UX confusion (one trigger fires modal, other navigates to home). Atomic commit prevents this transient state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Atomic-commit split (D-05 binding violation) | mitigate | The verify automation includes a `git log -n 1 --name-only` check listing all 4 files in the same commit; exits 1 on split. The plan body explicitly forbids amending or splitting. The commit message convention names the atomic intent. |
| T-05-03-02 | Information Disclosure | Layout RSC importing client island | accept | Standard Next.js App Router pattern. The import statement does not leak server-only state to the client (no `use server` modules involved); ContactModal receives no props from the layout. |
| T-05-03-03 | Tampering | Spec assertion drift (`tests/about-renders.spec.ts` `/` vs `#contact`) | mitigate | Atomically updated in the same commit. Without the atomic update, the spec would go RED on Plan 03 commit (the source swap). The verify automation includes a regex confirming the new assertion is present + the old one is gone. |
| T-05-03-04 | Tampering / DoS | Re-introducing a 2nd `'use client'` directive | mitigate | The verify automation re-runs the FOUND-07 carve-out check (`git grep -l "use client" -- '*.tsx' '*.ts'`). The 4 files modified in this plan are all RSC/test/spec — none should add `'use client'`. Exits 1 if carve-out invariant violated. |

ASVS L1 cleared; Plan 03 is a wiring + verification plan with no new attack surface beyond what Plan 02 introduced (which itself was ASVS L1 cleared per RESEARCH.md `## Security Domain`).
</threat_model>

<verification>
End-of-plan gate (run after Task 3 final docs commit):

1. Atomic-commit gate (Task 1): `git log --all --oneline | head -10` shows the Plan 03 atomic commit with `feat(phase-5/w2): atomic — mount ContactModal in layout + rewire Nav + /about triggers to #contact (D-05)` as a single commit, with `git show --name-only HEAD~N` (where N steps back to it) listing all 4 files.

2. FOUND-07 carve-out gate: `git grep -l "use client" -- '*.tsx' '*.ts'` returns ONLY `components/contact/ContactModal.tsx` (one file).

3. All 9 Phase 5 specs GREEN: `npx playwright test tests/contact-modal-*.spec.ts tests/contact-trigger-rewire.spec.ts tests/single-client-island.spec.ts --project=chromium-mobile` exits 0 with all tests passing.

4. Phase 1+2+3+4 regression canaries GREEN: `npx playwright test tests/no-client-components.spec.ts tests/about-renders.spec.ts tests/focus-ring.spec.ts tests/no-bare-outline-none.spec.ts tests/reduced-motion.spec.ts tests/monogram.spec.ts tests/footer-socials-render.spec.ts tests/work-grid-renders.spec.ts --project=chromium-mobile` exits 0.

5. Build canary: `npm run typecheck && npm run lint && npm run build` exits 0.

6. Docs flip canary: ROADMAP Phase 5 checked `[x]`, REQUIREMENTS CTCT-01..07 + A11Y-03 marked Complete, STATE.md percent 98 + Last Session dated 2026-05-14.

7. Phase Exit Visual Verification: user checkpoint response logged in SUMMARY (whether "approved", "issues:", or "defer" — and resulting Phase 6 carry-forwards correctly listed).

Total commits this plan: 2 (1 atomic Wave-2 + 1 final docs). Combined with Plan 01's 3 commits + Plan 02's 1 commit = 6 commits across Phase 5.
</verification>

<success_criteria>
- 1 atomic D-05 commit lands 4 file edits (`app/layout.tsx`, `components/layout/Nav.tsx`, `app/about/page.tsx`, `tests/about-renders.spec.ts`)
- ContactModal globally mounted; cross-route open verified (Nav from /, /about, /work all open the same instance)
- All 9 Phase 5 specs GREEN
- Phase 1+2+3+4 chrome canaries unchanged GREEN (no regressions)
- FOUND-07 carve-out preserved at exactly 1 `'use client'` file
- Phase Exit Visual Verification checkpoint reached + user response logged
- ROADMAP + REQUIREMENTS + STATE traceability flipped to reflect Phase 5 close
- Phase 6 carry-forwards documented (any deferred-verify items + the inherited Phase 4 + 1 cycle)
- 2 atomic commits direct to main (1 atomic feature + 1 docs close)
</success_criteria>

<output>
After completion, the consolidated `.planning/phases/05-contact-modal/05-03-SUMMARY.md` is THE Phase 5 summary (per Phase 4 single-consolidated-SUMMARY precedent — no separate 05-01-SUMMARY.md or 05-02-SUMMARY.md). It covers all 3 plans + the Phase Exit user response + Phase 6 hand-off.
</output>
