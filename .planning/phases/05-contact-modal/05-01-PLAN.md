---
phase: 5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app/globals.css
  - tests/contact-modal-opens-from-nav.spec.ts
  - tests/contact-modal-opens-from-about.spec.ts
  - tests/contact-modal-esc-closes.spec.ts
  - tests/contact-modal-states.spec.ts
  - tests/contact-modal-honeypot.spec.ts
  - tests/contact-modal-min-time.spec.ts
  - tests/contact-modal-mailto-fallback.spec.ts
  - tests/contact-trigger-rewire.spec.ts
  - tests/single-client-island.spec.ts
autonomous: true
requirements: [CTCT-01, CTCT-02, CTCT-03, CTCT-04, CTCT-05, CTCT-06, CTCT-07, A11Y-03]
tags: [phase-5, contact-modal, validation-infrastructure, formspree, dialog]
must_haves:
  truths:
    - "@formspree/react@3.0.0 is pinned in package.json and installs cleanly with React 19.2"
    - "9 new RED Playwright spec files exist under tests/ and parse without errors"
    - "The 9 spec files assert verbatim against the Phase 5 post-Wave-2 ground truth (locked copy, hash href, single client island)"
    - "app/globals.css contains a NEW opacity-only `modal-fade-in` keyframe + `dialog[open]` rule + `dialog::backdrop` rule (D-16/D-18)"
    - "Existing `tests/no-bare-outline-none.spec.ts`, `tests/reduced-motion.spec.ts`, `tests/focus-ring.spec.ts` remain GREEN (no Phase 1 chrome regression)"
    - "Existing `tests/no-client-components.spec.ts` remains GREEN at end of this plan (no `'use client'` introduced yet)"
    - "FOUND-07 invariant intact at end of plan: `git grep -l \"use client\" -- '*.tsx' '*.ts'` returns ZERO files"
  artifacts:
    - path: "package.json"
      provides: "Pinned @formspree/react@3.0.0 dependency"
      contains: "\"@formspree/react\": \"3.0.0\""
    - path: "app/globals.css"
      provides: "modal-fade-in keyframe + dialog[open] animation rule + dialog::backdrop rule"
      contains: "@keyframes modal-fade-in"
    - path: "tests/contact-modal-opens-from-nav.spec.ts"
      provides: "RED spec: clicking Nav Contact opens dialog; trigger href asserts #contact"
      min_lines: 15
    - path: "tests/contact-modal-opens-from-about.spec.ts"
      provides: "RED spec: clicking /about Get in touch opens dialog"
      min_lines: 15
    - path: "tests/contact-modal-esc-closes.spec.ts"
      provides: "RED spec: ESC closes dialog, focus returns to trigger, hash cleared via history.replaceState"
      min_lines: 20
    - path: "tests/contact-modal-states.spec.ts"
      provides: "RED spec: 5 verbatim copy strings asserted via :text-is() exact-match (D-08/D-09/D-10/D-11/D-12 + D-15)"
      min_lines: 30
    - path: "tests/contact-modal-honeypot.spec.ts"
      provides: "RED spec: honeypot=company filled triggers silent success + ZERO formspree.io network call"
      min_lines: 25
    - path: "tests/contact-modal-min-time.spec.ts"
      provides: "RED spec: submit within 1500ms of mount triggers silent success + ZERO formspree.io network call"
      min_lines: 25
    - path: "tests/contact-modal-mailto-fallback.spec.ts"
      provides: "RED spec: mailto link href + visible text exact-match in idle/submitting/success states"
      min_lines: 15
    - path: "tests/contact-trigger-rewire.spec.ts"
      provides: "RED spec: BOTH Nav Contact AND /about Get in touch resolve href === '#contact' (D-05 atomicity)"
      min_lines: 20
    - path: "tests/single-client-island.spec.ts"
      provides: "RED spec: filesystem scan asserts exactly 1 'use client' directive AND that file is components/contact/ContactModal.tsx (FOUND-07 carve-out)"
      min_lines: 25
  key_links:
    - from: "tests/single-client-island.spec.ts"
      to: "tests/no-client-components.spec.ts"
      via: "shared SCAN_ROOTS + stripLeadingComments helper signature"
      pattern: "SCAN_ROOTS\\s*=\\s*\\['app',\\s*'components',\\s*'lib'\\]"
    - from: "app/globals.css"
      to: "@media (prefers-reduced-motion: reduce)"
      via: "global animation-duration override (existing lines 54-63) automatically defeats new modal-fade-in"
      pattern: "modal-fade-in"
---

<objective>
Wave 0 validation infrastructure for Phase 5: install `@formspree/react@3.0.0`, append the new opacity-only `modal-fade-in` keyframe + `dialog::backdrop` rules to `app/globals.css` (D-16/D-18), and stub all 9 new Playwright specs in their final asserting form (RED at end of this plan, flip GREEN as Plans 02 and 03 land).

Purpose: establish the gate Phase 5 will be measured against — the 9 specs encode the locked verbatim copy strings (D-08 "Get in touch" / D-09 "Send message" / D-12 "Sending…" / D-10 "Thanks — I'll get back to you within a day or two." / D-11 "Something went wrong sending that. Try the email link below." / D-15 "Or just email me directly →"), the locked honeypot field name (`company` per D-13), the locked min-time threshold (1500ms per D-14), the FOUND-07 single-client-island invariant, and the D-05 atomic-trigger-swap requirement. The CSS surface lands here so Plans 02/03 don't have to touch globals.css and can stay focused.

Output: 1 dependency pinned, ~12 lines appended to `app/globals.css`, 9 new spec files committed RED. Plan 02's ContactModal flips 8 of them GREEN; Plan 03's atomic swap flips the last one GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-contact-modal/05-CONTEXT.md
@.planning/phases/05-contact-modal/05-RESEARCH.md
@.planning/phases/05-contact-modal/05-PATTERNS.md
@.planning/phases/05-contact-modal/05-UI-SPEC.md
@.planning/phases/05-contact-modal/05-VALIDATION.md
@CLAUDE.md
@app/globals.css
@tests/no-client-components.spec.ts
@tests/focus-ring.spec.ts
@tests/about-renders.spec.ts
@tests/work-grid-renders.spec.ts
@playwright.config.ts
@package.json

<interfaces>
<!-- Key contracts the executor needs. Sourced from research + existing tests. -->

From `tests/no-client-components.spec.ts` (the analog for `single-client-island.spec.ts`):
- `SCAN_ROOTS = ['app', 'components', 'lib']`
- `collectSourceFiles(dir: string): string[]` — recursive .ts/.tsx walker
- `stripLeadingComments(source: string): string` — strips // and /* */ from top-of-file before directive check
- Detection regex: `/^\s*['"]use client['"]/.test(stripped)`
- Test framework: `@playwright/test` `test()` + `expect()`; filesystem-only test, no `page` fixture

From `tests/work-grid-renders.spec.ts` lines 60-69 (the verbatim-copy assertion analog):
- Pattern: `await expect(page.locator(\`span:text-is("${literal}")\`), '<message>').toHaveCount(1)`
- Reason: substring `text=` matchers cause cross-DOM collisions — `:text-is()` exact-match is the locked form

From `tests/about-renders.spec.ts` lines 41-47 (the trigger-href analog):
- `page.getByRole('link', { name: <regex> })` returns the matching `<a>`
- `await expect(cta).toHaveAttribute('href', <expected>)` is the assertion shape

From `app/globals.css` (the keyframe analog):
- Existing keyframe shape: `@keyframes fade-in-up { from {...} to {...} }` followed by `.fade-in-up { animation: fade-in-up 400ms cubic-bezier(0.2,0,0,1) both; }`
- Existing reduced-motion gate (lines 54-63) covers `*, *::before, *::after` — the new `dialog[open]` and `dialog::backdrop` rules inherit the override automatically; NO new reduced-motion clause needed.

From `playwright.config.ts`:
- `testDir: './tests'` — all spec files go directly under `tests/`
- baseURL configurable via `process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'`
- Two projects: `chromium-mobile` (default for quick runs), `chromium-desktop`

From `package.json` (current state — verify with Read tool before installing):
- React 19.2.x is the locked target; `@formspree/react@3.0.0` peer-deps include `react@^19.0` (verified in RESEARCH.md `npm view`)
- Dependency must be pinned EXACTLY (`"3.0.0"`, not `"^3.0.0"`) per CLAUDE.md "version pinning" convention used throughout the project.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @formspree/react@3.0.0 (pinned)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - C:\Users\Braeden\Projects\braeden-site\package.json (verify current dependencies; confirm @formspree/react not already present)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-RESEARCH.md (lines 117-158 — version verification, peer-deps, install command)
    - C:\Users\Braeden\Projects\braeden-site\CLAUDE.md (Recommended Stack row for `@formspree/react@3.0.0`)
  </read_first>
  <action>
    Run `npm install @formspree/react@3.0.0 --save-exact` to pin the dependency at exactly `"3.0.0"` in `package.json` (NOT `"^3.0.0"`). The `--save-exact` flag is mandatory — every other production dep in this project is exactly-pinned per the established convention from Phase 1. After install, verify package.json shows the literal string `"@formspree/react": "3.0.0"` (no caret). Verify the install introduced ZERO additional client-side runtime deps beyond what `@formspree/react` itself declares (only `@formspree/core` is expected as transitive). Do NOT add `--legacy-peer-deps` or any flag that masks peer-dep issues — React 19.2 is in the supported peer range per RESEARCH.md verification.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies['@formspree/react']!=='3.0.0'){process.exit(1)}" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` has the literal entry `"@formspree/react": "3.0.0"` (verified by node script above; exits 1 if caret present or version drift)
    - `package-lock.json` records `@formspree/react@3.0.0` resolution
    - `npm run typecheck` exits 0 (no stale type definitions broken)
    - `npm run lint` exits 0 (no new lint errors)
    - `git grep -l "use client" -- '*.tsx' '*.ts'` STILL returns ZERO files (no source change introduced a client directive)
  </acceptance_criteria>
  <done>Dependency pinned + typecheck/lint clean + FOUND-07 still ZERO client islands.</done>
</task>

<task type="auto">
  <name>Task 2: Append modal-fade-in keyframe + dialog rules to app/globals.css (D-16/D-18)</name>
  <files>app/globals.css</files>
  <read_first>
    - C:\Users\Braeden\Projects\braeden-site\app\globals.css (the entire file — must understand existing structure to append cleanly without disrupting reduced-motion ordering)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-RESEARCH.md (Pattern 4 lines 512-544 — the literal CSS to add, plus the rationale for opacity-only NOT translateY)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-UI-SPEC.md (D-16 + D-18 — backdrop opacity 0.6, blur 2px, durations 240ms / 150ms)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-PATTERNS.md (lines 196-260 — analog mirror against existing fade-in-up shape)
  </read_first>
  <action>
    Append the following block to the END of `app/globals.css` (after the existing `.divider-top` rule at line 87, after the `nav-mobile` block at line 105 — i.e., truly end-of-file). Add a leading comment banner identifying the source as Phase 5 D-16/D-18.

    Required additions (literal — match exactly):
    1. A comment banner: `/* Phase 5 — Contact Modal entrance (D-16) + backdrop (D-18). Opacity-only keyframe — translateY would conflict with browser-centered <dialog> top-layer positioning per 05-RESEARCH.md Pitfall + Pattern 4. */`
    2. `@keyframes modal-fade-in { from { opacity: 0; } to { opacity: 1; } }` — opacity-only, NO transform property (Pitfall 6 / UI-SPEC line 422 explicit).
    3. `dialog[open] { animation: modal-fade-in 240ms cubic-bezier(0.2, 0, 0, 1) both; }` — duration matches D-16 (200-240ms range, picks the upper bound for editorial polish).
    4. `dialog[open]::backdrop { animation: modal-fade-in 150ms cubic-bezier(0.2, 0, 0, 1) both; background-color: rgba(10, 10, 10, 0.6); backdrop-filter: blur(2px); }` — backdrop opacity 0.6 + 2px blur per D-18; 150ms entrance duration per D-16.

    Do NOT modify any existing rule. Do NOT touch the `@media (prefers-reduced-motion: reduce)` block (lines 54-63) — its `*` selector with `animation-duration: 0.01ms !important` automatically clamps both new animations (verified in research lines 540-542 + UI-SPEC reduced-motion contract). Do NOT add a separate reduced-motion override for `dialog`.

    Do NOT use `@starting-style` (the modern alternative). Plain keyframe + `dialog[open]` selector is broader-supported and matches the established `fade-in-up` pattern shape per PATTERNS.md.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('app/globals.css','utf8');const checks={'keyframe':/@keyframes\s+modal-fade-in\s*\{[^}]*opacity:\s*0[^}]*opacity:\s*1[^}]*\}/s,'dialog-open':/dialog\[open\]\s*\{[^}]*animation:\s*modal-fade-in\s+240ms/s,'backdrop':/dialog\[open\]::backdrop\s*\{[^}]*backdrop-filter:\s*blur\(2px\)[^}]*\}/s,'no-translate':!/@keyframes\s+modal-fade-in[^}]*translateY/s.test(css),'reduced-motion-untouched':/animation-duration:\s*0\.01ms\s*!important/.test(css)};for(const[k,v]of Object.entries(checks)){if(!(k==='no-translate'?v:v.test?v.test(css):v)){console.error('FAIL:',k);process.exit(1)}}console.log('all CSS checks pass')" && npx playwright test tests/no-bare-outline-none.spec.ts tests/reduced-motion.spec.ts tests/focus-ring.spec.ts --project=chromium-mobile</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` contains `@keyframes modal-fade-in` with ONLY `opacity` property in `from`/`to` (NO `transform`, NO `translateY` — verified by negative regex above)
    - `app/globals.css` contains `dialog[open] { animation: modal-fade-in 240ms cubic-bezier(0.2, 0, 0, 1) both; }` (literal duration match)
    - `app/globals.css` contains `dialog[open]::backdrop { ... background-color: rgba(10, 10, 10, 0.6); backdrop-filter: blur(2px); ... }` (literal D-18 values)
    - The existing `@media (prefers-reduced-motion: reduce)` block (lines 54-63) is byte-identical to its pre-edit state
    - `tests/no-bare-outline-none.spec.ts` GREEN (no new bare `outline: none` introduced)
    - `tests/reduced-motion.spec.ts` GREEN (existing reduced-motion contract unchanged)
    - `tests/focus-ring.spec.ts` GREEN (Phase 1 focus-ring chrome unaffected by new dialog rules)
  </acceptance_criteria>
  <done>CSS surface for D-16/D-18 in place; 3 Phase 1 chrome specs still GREEN; reduced-motion still inherits-and-clamps automatically.</done>
</task>

<task type="auto">
  <name>Task 3: Stub 9 RED Playwright specs (verbatim copy + atomic swap + single-client-island)</name>
  <files>tests/contact-modal-opens-from-nav.spec.ts, tests/contact-modal-opens-from-about.spec.ts, tests/contact-modal-esc-closes.spec.ts, tests/contact-modal-states.spec.ts, tests/contact-modal-honeypot.spec.ts, tests/contact-modal-min-time.spec.ts, tests/contact-modal-mailto-fallback.spec.ts, tests/contact-trigger-rewire.spec.ts, tests/single-client-island.spec.ts</files>
  <read_first>
    - C:\Users\Braeden\Projects\braeden-site\tests\no-client-components.spec.ts (the analog for single-client-island.spec.ts — copy SCAN_ROOTS, collectSourceFiles, stripLeadingComments helpers verbatim; only invert the assertion)
    - C:\Users\Braeden\Projects\braeden-site\tests\focus-ring.spec.ts (the analog for esc-closes.spec.ts — keyboard.press + activeElement evaluate pattern)
    - C:\Users\Braeden\Projects\braeden-site\tests\work-grid-renders.spec.ts (the analog for states.spec.ts — `:text-is()` exact-match selectors per Phase 4 lesson)
    - C:\Users\Braeden\Projects\braeden-site\tests\about-renders.spec.ts (the analog for trigger-rewire.spec.ts — `getByRole('link', { name })` + toHaveAttribute('href', ...) shape)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-VALIDATION.md (the 9-test verification map — IDs 5-V-01..09 + which requirement each covers)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-PATTERNS.md (lines 316-435 — exact spec patterns for states, trigger-rewire, single-client-island)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-CONTEXT.md (D-08..D-15 verbatim copy strings — these MUST be the assertion literals)
  </read_first>
  <action>
    Create 9 new spec files under `tests/`. Each file gets a JSDoc header banner that names: (a) the requirement ID(s) it covers from REQUIREMENTS.md, (b) the source decision IDs from 05-CONTEXT.md, (c) "RED until Plan 05-02 lands" or "RED until Plan 05-03 lands" so the executor knows the gate flip point, (d) any anti-pattern carry-forward (e.g., "Phase 4 lesson: use `:text-is()` exact-match, not `text=`").

    All specs use the existing `tests/` infrastructure — `import { test, expect } from '@playwright/test';` — and target the running Next dev server via the configured baseURL.

    --- File 1: `tests/contact-modal-opens-from-nav.spec.ts` (covers CTCT-01) ---
    - Goes to `/`. Locates the Nav `Contact` link via `page.getByRole('link', { name: 'Contact' }).first()` (the .first() handles desktop+mobile both rendering it).
    - Asserts the link's `href` attribute equals `'#contact'` (post-Plan-03 ground truth — RED until Plan 03 lands).
    - Clicks the link. Awaits the dialog: `page.locator('dialog[data-test="contact-modal"]')`. Asserts dialog is `[open]` via attribute selector OR `dialog.open` JS evaluate.
    - Asserts focus has moved INSIDE the dialog: `await page.evaluate(() => document.activeElement?.closest('dialog[data-test=\"contact-modal\"]') !== null)` returns true.

    --- File 2: `tests/contact-modal-opens-from-about.spec.ts` (covers CTCT-01) ---
    - Goes to `/about`. Locates `page.getByRole('link', { name: /Get in touch/i })`.
    - Asserts `href === '#contact'`.
    - Clicks it. Asserts the same `dialog[data-test="contact-modal"]` opens (same dialog instance — mounted in layout, available on every route).
    - Asserts focus inside dialog (same evaluate as File 1).

    --- File 3: `tests/contact-modal-esc-closes.spec.ts` (covers CTCT-01 + CTCT-07 + A11Y-03) ---
    - Goes to `/`. Clicks Nav Contact to open. Records the trigger element handle BEFORE pressing ESC: `const triggerHandle = await page.getByRole('link', { name: 'Contact' }).first().elementHandle()`.
    - Presses ESC: `await page.keyboard.press('Escape')`.
    - Asserts dialog is no longer `[open]` (negation of File 1's assertion).
    - Asserts URL hash is empty: `expect(page.url()).not.toContain('#contact')` (per Pitfall 1 — `history.replaceState` cleared it).
    - Asserts focus returned to the trigger: `await page.evaluate((el) => document.activeElement === el, triggerHandle)` returns true.
    - REOPEN test: click trigger again. Assert dialog opens again (Pitfall 1 — same-hash click after close MUST still fire).

    --- File 4: `tests/contact-modal-states.spec.ts` (covers CTCT-02 + CTCT-03 + CTCT-05 + A11Y-03) ---
    - Goes to `/`. Opens modal via Nav.
    - Mocks Formspree: `await page.route('**/formspree.io/**', (route) => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ ok: true }) }))` — established BEFORE the click that triggers submit.
    - **Idle state assertions (verbatim per D-08, D-09, D-15)**:
      - `page.locator('h2:text-is("Get in touch")').toHaveCount(1)` — D-08 verbatim modal heading
      - `page.locator('button:text-is("Send message")').toHaveCount(1)` — D-09 verbatim idle submit
      - `page.locator(':text-is("Or just email me directly →")').toHaveCount(1)` — D-15 verbatim mailto wording
    - **Empty submit blocks (CTCT-05)**: clicking Send with empty fields does NOT trigger Formspree network call (`required` HTML attributes prevent submit).
    - **Submitting state**: fill name="Test", email="test@example.com", message="Hello world this is at least 1500ms after mount." Wait for mount-time gate (sleep 1600ms BEFORE filling, OR Playwright `clock` API). Click Send. Assert `button:text-is("Sending…")` appears (D-12 verbatim — em-character `…` is U+2026, NOT three dots) AND `form[aria-busy="true"]` is set (D-12 explicit).
    - **Success state (D-10 verbatim)**: After Formspree mock fulfills, assert `page.locator(':text-is("Thanks — I\'ll get back to you within a day or two.")').toHaveCount(1)` — exact em-dash U+2014 NOT hyphen-minus, exact apostrophe U+2019 (typographic) — match the locked string from D-10 byte-for-byte. Assert success copy lives in a `[role="status"][aria-live="polite"]` OR adjacent visible region (per D-10 + UI-SPEC State 3 contract).
    - **Error state (separate test)**: re-mock Formspree with `route.fulfill({ status: 500 })` (or `route.abort()`). Repeat fill+wait+submit. Assert `page.locator(':text-is("Something went wrong sending that. Try the email link below.")').toHaveCount(1)` (D-11 verbatim). Assert wrapped in `[role="alert"][aria-live="assertive"]` per D-11.

    Each verbatim assertion includes a message arg referencing the D-XX it implements (Phase 4 spec convention). Use the locked Unicode characters: em-dash `—` (U+2014), horizontal ellipsis `…` (U+2026), curly apostrophe `'` (U+2019). Do NOT use hyphen-minus or three-dot ellipsis or straight apostrophe — D-10/D-11/D-12 lock the typography.

    --- File 5: `tests/contact-modal-honeypot.spec.ts` (covers CTCT-04 honeypot half) ---
    - Goes to `/`. Opens modal.
    - **Network gate first**: `let formspreeCallCount = 0; await page.route('**/formspree.io/**', (route) => { formspreeCallCount++; route.fulfill({ status: 200, body: '{}' }); })`
    - Sleeps past the 1500ms mount gate (so min-time alone wouldn't trip).
    - Fills the honeypot field directly: `await page.locator('input[name="company"]').fill('I am a bot')` — this requires the field to exist in DOM (RED until Plan 02). The field is visually hidden but addressable via locator.
    - Fills name + email + message normally.
    - Clicks Send.
    - Asserts `formspreeCallCount === 0` (silent rejection — no network call) per Pitfall 5 + D-13.
    - Asserts the success copy renders anyway (the `bypassedSuccess` state per Pitfall 5): `page.locator(':text-is("Thanks — I\'ll get back to you within a day or two.")').toHaveCount(1)`.

    --- File 6: `tests/contact-modal-min-time.spec.ts` (covers CTCT-04 min-time half) ---
    - Same network-gate pattern as File 5.
    - Open modal. Immediately (within ~100ms — well under 1500) fill name + email + message + click Send. The honeypot `company` field stays empty.
    - Assert `formspreeCallCount === 0` (silent rejection per D-14).
    - Assert success copy renders (bypassedSuccess UI per Pitfall 5).
    - **Second test in same file**: re-open, sleep 1600ms (past gate), fill, submit, assert `formspreeCallCount === 1` (real submission goes through after mount-time elapses).

    --- File 7: `tests/contact-modal-mailto-fallback.spec.ts` (covers CTCT-06) ---
    - Goes to `/`. Opens modal.
    - Locates `page.locator('a[href^="mailto:"]')`. Asserts exactly 1.
    - Asserts `href === 'mailto:fakegoat1@gmail.com?subject=Hi%20Braeden'` (verbatim per D-15).
    - Asserts visible text matches `:text-is("Or just email me directly →")` (D-15 verbatim).
    - Asserts visible in idle state (default after open), in submitting state (mock formspree to delay → fill+submit → assert mailto still visible while spinner-equivalent active), and in success state (mock formspree 200 → assert still visible after success render). Skip checking error state visibility — error copy explicitly references the link, so its visibility is implicit.

    --- File 8: `tests/contact-trigger-rewire.spec.ts` (covers D-05 atomic swap binding) ---
    - **Single test asserting BOTH triggers in one go** (per PATTERNS.md — atomicity gate, plan-checker refuses split-Wave variants).
    - Goes to `/about`. Asserts `page.getByRole('link', { name: /Get in touch/i }).first().toHaveAttribute('href', '#contact')`.
    - Goes to `/`. Asserts `page.getByRole('link', { name: 'Contact' }).first().toHaveAttribute('href', '#contact')`.
    - Each assertion includes a message arg: "D-05 atomic rewire — Nav.tsx line 15 + about/page.tsx line 75 must BOTH point at #contact (single commit, single Wave)".

    --- File 9: `tests/single-client-island.spec.ts` (covers FOUND-07 carve-out) ---
    - Copy `SCAN_ROOTS`, `collectSourceFiles`, `stripLeadingComments` from `tests/no-client-components.spec.ts` verbatim (same helpers, same logic).
    - Define `const ALLOWED_CLIENT_ISLANDS = ['components/contact/ContactModal.tsx'];` at module scope.
    - Test name: `'exactly 1 \\'use client\\' directive and it is the ContactModal (FOUND-07 Phase 5 carve-out)'`.
    - In the test: scan with the same loop pattern, collect offenders into `offenders` array.
    - Assert: `expect(offenders.sort(), \`Phase 5 carve-out: exactly 1 'use client' allowed (\${ALLOWED_CLIENT_ISLANDS.join(',')}). Found: \${offenders.join(', ')}\`).toEqual(ALLOWED_CLIENT_ISLANDS.sort())`.
    - Use cross-platform path comparison: `path.relative(root, file).split(path.sep).join('/')` so the assertion works on Windows (current dev env per CLAUDE.md env block — Windows 10 / PowerShell) AND CI Linux.
    - File header banner explains: "RED until Plan 05-02 ships components/contact/ContactModal.tsx with the directive. The companion update to tests/no-client-components.spec.ts (rename or replace) is also a Plan 05-02 concern — atomically with the new client file landing."

    All 9 specs at end of this task are RED (asserting against post-Plan-03 ground truth). Existing `tests/no-client-components.spec.ts` is UNCHANGED in this plan (it stays GREEN until Plan 02 lands the client file; Plan 02 will atomically update it).

    Do NOT include `// @ts-nocheck` or skip annotations. Specs must parse + collect cleanly via `npx playwright test --list`.
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | node -e "let data='';process.stdin.on('data',d=>data+=d);process.stdin.on('end',()=>{const required=['contact-modal-opens-from-nav','contact-modal-opens-from-about','contact-modal-esc-closes','contact-modal-states','contact-modal-honeypot','contact-modal-min-time','contact-modal-mailto-fallback','contact-trigger-rewire','single-client-island'];const missing=required.filter(n=>!data.includes(n+'.spec.ts'));if(missing.length){console.error('Missing specs:',missing);process.exit(1)}console.log('all 9 new specs collect cleanly')})" && npm run lint && npx playwright test tests/no-client-components.spec.ts tests/no-bare-outline-none.spec.ts tests/reduced-motion.spec.ts tests/focus-ring.spec.ts --project=chromium-mobile</automated>
  </verify>
  <acceptance_criteria>
    - All 9 new spec files exist under `tests/` (verified by `playwright test --list` collection above; exits 1 if any missing)
    - All 9 specs parse without TypeScript or syntax errors (`playwright test --list` would fail otherwise)
    - `npm run lint` exits 0 (no ESLint errors in new specs — typed imports, no `any`-as-escape)
    - `tests/no-client-components.spec.ts` STILL GREEN (Plan 01 introduces zero `'use client'`)
    - `tests/no-bare-outline-none.spec.ts` GREEN (Phase 1 chrome unaffected)
    - `tests/reduced-motion.spec.ts` GREEN (Phase 1 reduced-motion unaffected by new keyframe)
    - `tests/focus-ring.spec.ts` GREEN (Phase 1 focus ring unaffected by new dialog rules)
    - **All 9 new specs are RED when run against current `npm run dev` server** (gate is correctly inverted — they will flip GREEN as Plans 02 + 03 land). Verify by running `npx playwright test tests/contact-modal-*.spec.ts tests/contact-trigger-rewire.spec.ts tests/single-client-island.spec.ts --project=chromium-mobile` — expect ≥9 failures (these are the gate flips for downstream plans, not a Plan 01 failure).
    - File ownership: each new spec touches ONLY its own file (no edits to existing specs)
  </acceptance_criteria>
  <done>9 spec files RED + collect-clean + lint-clean + Phase 1 chrome canaries GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→test runner | Playwright reads spec files from local filesystem; specs only read source/config, never write |
| npm registry→local node_modules | `@formspree/react@3.0.0` installed from registry; integrity verified by package-lock.json |
| dev server→browser | Tests in this plan don't yet drive the dialog (RED state); future Plan 02/03 specs will |

## STRIDE Threat Register (per RESEARCH.md `## Security Domain` — references that section verbatim)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering (supply chain) | `@formspree/react@3.0.0` install | mitigate | Pin EXACTLY `"3.0.0"` (no caret) via `--save-exact`. package-lock.json records integrity hash. Version verified against npm registry in RESEARCH.md (published 2025-03-17, latest stable). Re-research trigger documented at v4.x release. |
| T-05-01-02 | Information Disclosure | `NEXT_PUBLIC_FORMSPREE_ID` exposure in client bundle | accept | Form ID `xqeypnkw` is non-secret by design (Formspree CSRF model — RESEARCH.md `## Security Domain` row 3). `NEXT_PUBLIC_` prefix is the correct convention; Phase 1 D-13 set it intentionally. ASVS L1 cleared. |
| T-05-01-03 | Denial of Service | future Formspree submit endpoint | accept | Phase 1 plan T-01-08 explicitly accepted with note "Phase 5 ships honeypot + min-time-to-submit per CTCT-04." Plan 01 stubs the gates (specs RED); Plan 02 implements them. ASVS L1 cleared per RESEARCH.md `## Security Domain`. |
| T-05-01-04 | Tampering (CSS injection) | new `dialog::backdrop` rule | mitigate | Pure CSS append, no user-controlled values. `backdrop-filter: blur(2px)` is a static value (per D-18). No string interpolation surface. |

No high-severity findings — Plan 01 is validation-infrastructure-only with no user-facing surface. Full ASVS L1 audit for the modal itself is in Plan 02's threat model.
</threat_model>

<verification>
End-of-plan gate (run after all 3 tasks commit):

1. `node -e "const p=require('./package.json');console.log('formspree:',p.dependencies['@formspree/react'])"` → must print `formspree: 3.0.0` (literal, no caret).
2. `node -e "const fs=require('fs');const css=fs.readFileSync('app/globals.css','utf8');console.log('keyframe:',/@keyframes modal-fade-in/.test(css),'dialog-open:',/dialog\[open\]/.test(css),'backdrop:',/dialog\[open\]::backdrop/.test(css))"` → all three booleans `true`.
3. `npx playwright test --list 2>&1 | grep -c "contact-modal-\|contact-trigger-rewire\|single-client-island"` → should report 9 (or more — depending on multi-test files like `min-time` having 2 tests).
4. `npx playwright test tests/no-client-components.spec.ts tests/no-bare-outline-none.spec.ts tests/reduced-motion.spec.ts tests/focus-ring.spec.ts --project=chromium-mobile` → 4 GREEN (Phase 1 chrome canaries unaffected).
5. **Inverted-gate verification (the new specs MUST be RED at end of Plan 01):** Start dev server (`npm run dev` in background), then `npx playwright test tests/contact-modal-*.spec.ts tests/contact-trigger-rewire.spec.ts tests/single-client-island.spec.ts --project=chromium-mobile` → expect ≥ 9 failing tests. (Plan 02 + 03 flip them GREEN; this plan stages the gates, doesn't satisfy them.)
6. `git grep -l "use client" -- '*.tsx' '*.ts'` → ZERO files (FOUND-07 invariant intact).
</verification>

<success_criteria>
- Pinned dependency `@formspree/react@3.0.0` in `package.json`
- `app/globals.css` has the new opacity-only `modal-fade-in` keyframe + `dialog[open]` rule + `dialog[open]::backdrop` rule (D-16/D-18 surface)
- 9 new RED Playwright specs encoding the locked verbatim copy + atomic swap + single-client-island invariants
- All 9 specs assert against post-Plan-03 ground truth (RED at Plan 01 end, flip GREEN as Plans 02/03 land)
- Existing Phase 1 chrome canaries (no-client-components, no-bare-outline-none, reduced-motion, focus-ring) still GREEN
- FOUND-07 invariant: ZERO `'use client'` directives across `app/`, `components/`, `lib/`
- 3 atomic commits direct to main per project branching strategy
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-modal/05-01-SUMMARY.md` covering:
- Dependency verification (`@formspree/react@3.0.0` exact-pin confirmation + transitive count)
- CSS surface diff (lines added to globals.css, with the literal keyframe block)
- 9 spec files committed with line counts each
- Pre-flip RED count (expected ≥9 against current dev server)
- Phase 1 chrome canary GREEN count (4 specs verified unchanged)
- Any deviations from plan (Rules 1-4) with rationale
- Hand-off note to Plan 05-02: "ContactModal client island lands here; updates `tests/no-client-components.spec.ts` atomically with the new file"
</output>
