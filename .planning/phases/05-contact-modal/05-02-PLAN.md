---
phase: 5
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - components/contact/ContactModal.tsx
  - data/site.ts
  - tests/no-client-components.spec.ts
autonomous: true
requirements: [CTCT-01, CTCT-02, CTCT-03, CTCT-04, CTCT-05, CTCT-06, CTCT-07, A11Y-03]
tags: [phase-5, contact-modal, client-island, formspree, found-07-carve-out]
must_haves:
  truths:
    - "components/contact/ContactModal.tsx exists with `'use client'` at line 1 (after banner) — the SOLE client island in the entire codebase per FOUND-07 carve-out"
    - "ContactModal opens via `window.location.hash === '#contact'` listener (D-06 hash-driven; deep-link friendly: braehods.com/about#contact opens modal directly)"
    - "ContactModal closes via ESC + backdrop-click; on close the hash is cleared via `history.replaceState` (Pitfall 1 fix — NOT `location.hash = ''`) AND useForm.reset() + local state cleared (Pitfall 2 fix)"
    - "Form posts to Formspree `xqeypnkw` via `useForm(process.env.NEXT_PUBLIC_FORMSPREE_ID ?? 'xqeypnkw')` (D-19)"
    - "Honeypot field name=`company` (D-13, NOT `_gotcha`); silent rejection sets a separate `bypassedSuccess` state so the success UI renders (Pitfall 5 fix)"
    - "Min-time-to-submit threshold = 1500ms (D-14); silent rejection mirrors the honeypot path"
    - "5 verbatim copy strings render exactly: D-08 `Get in touch`, D-09 `Send message`, D-12 `Sending…` (U+2026 ellipsis), D-10 `Thanks — I'll get back to you within a day or two.` (U+2014 em-dash + U+2019 curly apostrophe), D-11 `Something went wrong sending that. Try the email link below.`, D-15 `Or just email me directly →`"
    - "Mailto link href = `mailto:fakegoat1@gmail.com?subject=Hi%20Braeden` verbatim per D-15; visible in idle/submitting/success states"
    - "data/site.ts has `email: 'fakegoat1@gmail.com'` field set (D-15a forward-compat — Phase 6 v1.x can swap to `hi@braehods.com` in one line)"
    - "tests/no-client-components.spec.ts is updated atomically in this plan to allow EXACTLY `components/contact/ContactModal.tsx` (otherwise it would go RED the moment the directive lands)"
    - "Send button + input fields use `transition-[border-color,color,opacity]` (or similar arbitrary list) NOT `transition-colors` shorthand (Pitfall 4 — Tailwind v4 outline-color clobbering carry-forward from Phase 2 + 4)"
    - "All input + textarea content text is `text-base` (16px) NOT `text-sm` (Pitfall 7 — iOS Safari auto-zoom)"
    - "Form has exactly 3 fields per D-01: name (text, required), email (type=email, required), message (textarea, required) — NO subject field, NO topic dropdown"
    - "Message field shape per D-02: 4-row `<textarea>` with `maxLength=1000` soft cap, character counter below in Geist Mono micro-format that turns inline `#c8a86a` past 800 chars (NOT promoted to `@theme` — scoped inline-only per Phase 4 D-09 carry-forward)"
    - "Required-field marking per D-03: visible `<label>` above each field with `*` accent asterisk suffix on required fields; `<input required>` HTML attribute; native browser validation on submit + ValidationError component for server-side errors"
    - "Modal shape per D-17: centered native `<dialog>` on all viewports with `max-w-md` (~28rem), full-width on mobile via `mx-4`, internal padding `p-6 md:p-8` — NOT mobile bottom-sheet, NOT full-screen mobile"
    - "6 of the 9 RED specs from Plan 01 are GREEN after this plan: contact-modal-states, contact-modal-honeypot, contact-modal-min-time, contact-modal-mailto-fallback, single-client-island, no-client-components (atomically updated). 4 specs remain RED until Plan 03 atomically swaps Nav.tsx + about/page.tsx hrefs: contact-modal-opens-from-nav, contact-modal-opens-from-about, contact-modal-esc-closes, contact-trigger-rewire."
  artifacts:
    - path: "components/contact/ContactModal.tsx"
      provides: "The single 'use client' island — native <dialog> + useForm + honeypot/min-time + 4 state renders + 2 aria-live regions"
      min_lines: 130
      contains: "'use client'"
    - path: "data/site.ts"
      provides: "contactEmail value wired into existing `email?: string` field on SiteMeta"
      contains: "email: 'fakegoat1@gmail.com'"
    - path: "tests/no-client-components.spec.ts"
      provides: "Updated to allow exactly 1 client island (ContactModal); replaces the empty-array assertion. Acts as the FOUND-07 carve-out invariant gate alongside `single-client-island.spec.ts`."
      contains: "ContactModal"
  key_links:
    - from: "components/contact/ContactModal.tsx"
      to: "window.location.hash"
      via: "useEffect hashchange listener + initial-mount sync"
      pattern: "addEventListener\\(['\"]hashchange['\"]"
    - from: "components/contact/ContactModal.tsx"
      to: "@formspree/react useForm hook"
      via: "useForm(FORMSPREE_ID) returns [state, handleSubmit, reset]"
      pattern: "useForm\\("
    - from: "components/contact/ContactModal.tsx"
      to: "process.env.NEXT_PUBLIC_FORMSPREE_ID"
      via: "build-time inlined env var with literal fallback 'xqeypnkw'"
      pattern: "process\\.env\\.NEXT_PUBLIC_FORMSPREE_ID"
    - from: "components/contact/ContactModal.tsx"
      to: "history.replaceState"
      via: "dialog 'close' event handler clears hash without scroll-to-top side-effect (Pitfall 1)"
      pattern: "history\\.replaceState"
---

<objective>
Build the ContactModal client island — the FIRST and ONLY `'use client'` directive in the entire codebase per FOUND-07. ~140-160 line React component composing native `<dialog>` (browser-managed focus trap + scroll lock + ESC + return-focus), `useForm` from `@formspree/react@3.0.0` (state machine for submitting/succeeded/errors), hash-driven open trigger via `useEffect` + `hashchange` listener (D-06), honeypot + min-time bot defense with separate `bypassedSuccess` state (Pitfall 5), char counter with conditional inline `#c8a86a` past 800 chars (D-02 + Phase 4 D-09 reuse), 4 state renders with verbatim locked copy (D-08..D-12), mailto fallback link (D-15), and two `aria-live` regions (CTCT-03 + A11Y-03).

Atomically update `tests/no-client-components.spec.ts` in the SAME plan as the new client file lands — otherwise the existing spec goes RED on this commit. The update repurposes the existing spec as the FOUND-07 carve-out gate (it now allows EXACTLY ContactModal.tsx, complementing the new `tests/single-client-island.spec.ts` from Plan 01).

Surface `data/site.ts.email` per D-15a forward-compat — the slot already exists on the `SiteMeta` interface (line 5: `email?: string`); only a value wire-up is needed.

Output: 1 new client component (~150 lines, the single client island), 1 data field added (1 line), 1 spec updated (allow-list of 1 file). 8 of 9 Plan 01 RED specs flip to GREEN. Plan 03 (Wave 2) will flip the 9th by atomically swapping the trigger hrefs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-contact-modal/05-CONTEXT.md
@.planning/phases/05-contact-modal/05-RESEARCH.md
@.planning/phases/05-contact-modal/05-PATTERNS.md
@.planning/phases/05-contact-modal/05-UI-SPEC.md
@.planning/phases/05-contact-modal/05-VALIDATION.md
@.planning/phases/05-contact-modal/05-01-PLAN.md
@CLAUDE.md
@app/globals.css
@app/layout.tsx
@components/home/ChannelButton.tsx
@components/home/CTAArrowLink.tsx
@components/work/ProjectCard.tsx
@components/layout/Nav.tsx
@data/site.ts
@tests/no-client-components.spec.ts

<interfaces>
<!-- Critical contracts the executor needs. Pulled from research + existing files. -->

From `@formspree/react@3.0.0` (verified API surface — RESEARCH.md Pattern 2):
```ts
import { useForm, ValidationError } from '@formspree/react';
const [state, handleSubmit, reset] = useForm(hashid: string);
// state shape:
//   submitting: boolean
//   succeeded:  boolean
//   errors:     SubmissionError | null
//   result:     object | null
// reset(): void  -- clears errors + result, sets submitting/succeeded false
// <ValidationError prefix?: string field: string errors: state.errors className?: string />
```

From `data/site.ts` (existing — D-15a slot already on interface):
```ts
export interface SiteMeta {
  name: string;
  tagline: string;
  domain: string;
  email?: string;             // ← Phase 5 D-15a wires a value here
  socials: { github?: string; instagram?: string; youtube?: string };
}
export const site: SiteMeta = { /* ...existing... */ };
```

From `app/globals.css` (Plan 01 will land — Plan 02 consumes):
- `dialog[open]` selector triggers `modal-fade-in` 240ms opacity-only animation
- `dialog[open]::backdrop` gets dim+2px-blur with 150ms fade

From `components/home/ChannelButton.tsx` (analog: hairline-tile button family — Send button mirrors):
- `className="group inline-flex items-center gap-2 whitespace-nowrap rounded-md border border-[var(--color-border)] px-4 py-3 text-sm font-sans transition-[border-color,color,transform] duration-200 ease-[cubic-bezier(0.2,0,0,1)] hover:-translate-y-px hover:border-[var(--color-accent)]"`
- Send button differences: drop `hover:-translate-y-px` (modal CTA stays static); replace `transform` with `opacity` in transition list (enables disabled-state fade per D-12); add `disabled:opacity-60 disabled:cursor-not-allowed disabled:pointer-events-none`.

From `components/home/CTAArrowLink.tsx` (analog: mailto + Send-another arrow links):
- `<a href={...} className="group inline-flex items-center gap-2 text-base font-sans hover:underline hover:decoration-1 hover:underline-offset-4" style={{ color: 'var(--color-accent)' }}>{children}<span aria-hidden className="inline-block transition-transform group-hover:translate-x-1">→</span></a>`

From `components/work/ProjectCard.tsx` lines 33-38 (analog: inline `#c8a86a` literal for char counter):
- `style={{ color: charCount > 800 ? '#c8a86a' : 'var(--color-muted)' }}` on the counter `<span>`. Inline-style scoped (NOT in `@theme`) per D-02 + Phase 4 D-09 carry-forward.

From RESEARCH.md Pattern 1 lines 264-422 (the paste-ready ContactModal skeleton — use as the implementation source).

From RESEARCH.md Pattern 3 + Pitfall 5 (the silent-success bypass pattern):
```ts
const [bypassedSuccess, setBypassedSuccess] = useState(false);
const wrappedHandleSubmit = (event) => {
  event.preventDefault();
  const formData = new FormData(event.currentTarget);
  if (formData.get('company')) { setBypassedSuccess(true); return; }
  if (Date.now() - mountTime < 1500) { setBypassedSuccess(true); return; }
  return handleSubmit(event);
};
const showSuccess = state.succeeded || bypassedSuccess;
```

From RESEARCH.md Pitfall 2 (close-handler must reset useForm state + local state):
- On dialog `close` event: clear hash via `history.replaceState`, call `reset()`, clear `setMessage('')`, clear `setBypassedSuccess(false)`.

From `tests/no-client-components.spec.ts` (the spec to update atomically):
- Test name + helper structure stays identical — only the assertion target changes.
- New ALLOWED constant: `const ALLOWED_CLIENT_ISLANDS = ['components/contact/ContactModal.tsx'];`
- Cross-platform path normalization: `path.relative(root, file).split(path.sep).join('/')` for Windows compat (current dev OS per CLAUDE.md env).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build components/contact/ContactModal.tsx — the SINGLE client island + wire data/site.ts.email</name>
  <files>components/contact/ContactModal.tsx, data/site.ts</files>
  <read_first>
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-RESEARCH.md (lines 264-422 Pattern 1 paste-ready skeleton; lines 475-510 Pattern 3 honeypot wrapper; lines 630-715 Pitfalls 2 + 4 + 5; lines 1002-1040 Recommended Task Skeleton)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-UI-SPEC.md (lines 200-275 Modal Composition contract; lines 277-380 State Visual Treatments verbatim copy; lines 105-135 Typography contract for input font-size = text-base; lines 140-200 Color contract; lines 380-450 Tab Order contract)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-PATTERNS.md (lines 35-100 ChannelButton + ProjectCard analog snippets; lines 432-505 Shared Patterns — transition-property discipline + inline-style + #c8a86a)
    - C:\Users\Braeden\Projects\braeden-site\.planning\phases\05-contact-modal\05-CONTEXT.md (D-01..D-19 — every decision is binding; verify each is implemented before commit)
    - C:\Users\Braeden\Projects\braeden-site\components\home\ChannelButton.tsx (the hairline-tile button analog — copy comment-banner shape, invert FOUND-07 line)
    - C:\Users\Braeden\Projects\braeden-site\components\home\CTAArrowLink.tsx (mailto link styling analog)
    - C:\Users\Braeden\Projects\braeden-site\components\work\ProjectCard.tsx (lines 18-38 — `#c8a86a` inline literal discipline + comment block rationale)
    - C:\Users\Braeden\Projects\braeden-site\data\site.ts (existing SiteMeta interface — `email?: string` slot already exists at line 5)
    - C:\Users\Braeden\Projects\braeden-site\app\globals.css (verify Plan 01 Task 2 landed the dialog rules — `dialog[open]`, `dialog::backdrop`, `modal-fade-in` keyframe must all be present)
  </read_first>
  <behavior>
    - Test 1 (contact-modal-opens-from-nav): Clicking Nav Contact (after Plan 03 swap, but href asserted in spec) opens dialog and traps focus inside.
    - Test 2 (contact-modal-opens-from-about): Clicking /about Get in touch opens the same dialog instance from any route.
    - Test 3 (contact-modal-esc-closes): ESC closes dialog; URL hash cleared; focus returns to trigger; reopen via same trigger works (Pitfall 1).
    - Test 4 (contact-modal-states): All 5 verbatim copy strings render exact-match with locked Unicode (em-dash U+2014, ellipsis U+2026, curly apostrophe U+2019). aria-live regions present. Empty submit blocked by HTML required attrs. Submitting state shows "Sending…" + form aria-busy. Success state shows D-10 verbatim. Error state shows D-11 verbatim with role="alert".
    - Test 5 (contact-modal-honeypot): Filling `name="company"` field + submitting triggers ZERO formspree.io network call AND renders the success copy via bypassedSuccess.
    - Test 6 (contact-modal-min-time): Submitting within 1500ms of mount triggers ZERO formspree.io call AND renders success via bypassedSuccess. Submit after 1600ms goes through (1 network call).
    - Test 7 (contact-modal-mailto-fallback): Mailto link present with exact href + visible text in idle/submitting/success states.
    - Test 9 (single-client-island): Filesystem scan finds EXACTLY components/contact/ContactModal.tsx as the only `'use client'` file.
    - Test (existing) no-client-components.spec.ts: After atomic update inside this plan, allows the same single file.
    - Tab order inside open dialog: Name → Email → Message → Send button → Mailto link → (loop). Honeypot field is `tabindex={-1}` so it's skipped.
    - Reduced-motion: dialog appears instantly (no fade) due to existing globals.css rule clamping animation-duration to 0.01ms.
  </behavior>
  <action>
    Create `components/contact/ContactModal.tsx` (NEW file, ~150 lines). Use the paste-ready skeleton from RESEARCH.md Pattern 1 (lines 272-422) as the implementation source — it is already verified against MDN dialog spec, Formspree help docs, and the UI-SPEC component contract table. Apply these specific refinements before writing:

    1. **File header banner** (mirror ChannelButton.tsx + ProjectCard.tsx convention):
       - Source line: `// Source: PLAN.md 05-02-T1; UI-SPEC § Modal Composition; CONTEXT D-01..D-19.`
       - **INVERTED FOUND-07 line** (the only file in the codebase that gets this treatment): `// Client Component ('use client' — THE single client island per FOUND-07 carve-out / 05-CONTEXT D-25 inheritance / Phase 5 D-04..D-19). Verified by tests/single-client-island.spec.ts AND tests/no-client-components.spec.ts (the latter updated atomically in this same commit to allow exactly this file).`
       - Rationale lines for the 3 most load-bearing patterns in the file: (a) hash-driven open via useEffect+hashchange (D-06 strong recommendation; preserves Nav.tsx + CTAArrowLink.tsx as Server Components — only `href` value flips in Plan 03), (b) `bypassedSuccess` separate state for honeypot/min-time silent rejection (Pitfall 5 — without it bots detect "no state change after submit" and learn the gate), (c) `transition-[border-color,color,opacity]` arbitrary-list NOT `transition-colors` shorthand (Pitfall 4 — carry-forward from Phase 2 Plan 02-04 e3ed657 + Phase 4 ProjectCard).

    2. **Module directive at line 1 (after the header banner)**: `'use client';` — this is THE single client directive in the codebase. Single-quoted to match Phase 1 + 2 + 3 + 4 string-literal convention.

    3. **Imports** (in this order — convention from existing components):
       - React: `import { useEffect, useRef, useState } from 'react';`
       - Formspree: `import { useForm, ValidationError } from '@formspree/react';`
       - Site data: `import { site } from '@/data/site';` (consumes the new `email` field — single source of truth per D-15a)

    4. **Module-level constants**:
       - `const FORMSPREE_ID = process.env.NEXT_PUBLIC_FORMSPREE_ID ?? 'xqeypnkw';` — env var is already set in Vercel + .env.local from Phase 1 D-13; the literal fallback is dev-safety.
       - `const MAILTO_HREF = \`mailto:\${site.email ?? 'fakegoat1@gmail.com'}?subject=Hi%20Braeden\`;` — uses data layer per D-15a; literal subject hard-coded per D-15 verbatim.
       - `const MIN_TIME_MS = 1500;` — D-14 threshold.
       - `const MAX_LEN = 1000;` — D-02 soft cap.
       - `const WARN_LEN = 800;` — D-02 counter color-flip threshold.

    5. **Component structure** (`export function ContactModal()` — named export, not default; consumers in app/layout.tsx import by name):
       - Refs: `const dialogRef = useRef<HTMLDialogElement>(null);`
       - useForm: `const [state, handleSubmit, reset] = useForm(FORMSPREE_ID);`
       - Local state:
         - `const [mountTime] = useState(() => Date.now());` — lazy init runs ONCE per mount (Pitfall verified — `useState(() => ...)` not `useState(Date.now())`)
         - `const [message, setMessage] = useState('');` — controlled textarea so the counter can read length
         - `const [bypassedSuccess, setBypassedSuccess] = useState(false);` — Pitfall 5 separate success state for honeypot/min-time rejections

    6. **useEffect 1: hash-driven open/close** (D-06):
       - On mount: define `sync = () => { if (window.location.hash === '#contact') { if (!dialog.open) dialog.showModal(); } else { if (dialog.open) dialog.close(); } }`
       - Call `sync()` once for initial-mount deep-link support (e.g., `braehods.com/about#contact` opens the modal directly per D-06)
       - `window.addEventListener('hashchange', sync)` — listen for hash changes
       - Cleanup: `window.removeEventListener('hashchange', sync)`
       - Empty deps `[]` — sync closure captures `dialogRef.current` at call time, no stale-ref issue (verified in research)

    7. **useEffect 2: dialog close handler** (Pitfall 1 + Pitfall 2):
       - Listen for the dialog's native `close` event
       - On close: clear hash via `history.replaceState(null, '', window.location.pathname + window.location.search)` — NOT `location.hash = ''` (Pitfall 1: same-hash click after close doesn't fire `hashchange`)
       - Then call `reset()` (clears useForm state — Pitfall 2: state survives across open/close otherwise)
       - Then `setMessage('')` and `setBypassedSuccess(false)` (clear local state too)
       - Deps `[reset]` — reset is stable but listed for ESLint exhaustive-deps compliance

    8. **wrappedHandleSubmit** (Pitfall 5 silent-rejection bypass):
       ```
       const wrappedHandleSubmit = (event) => {
         event.preventDefault();
         const formData = new FormData(event.currentTarget);
         if (formData.get('company')) { setBypassedSuccess(true); return; }
         if (Date.now() - mountTime < MIN_TIME_MS) { setBypassedSuccess(true); return; }
         return handleSubmit(event);
       };
       ```
       Type: `React.FormEvent<HTMLFormElement>`.

    9. **Backdrop-click handler** (Pitfall 6):
       - `const onDialogClick = (e: React.MouseEvent<HTMLDialogElement>) => { if (e.target === e.currentTarget) dialogRef.current?.close(); };`
       - Pattern relies on browser's native top-layer centering (clicks on the backdrop area register on the `<dialog>` element, not its content children).

    10. **JSX** — follow UI-SPEC § Modal Composition table verbatim:
       - Root: `<dialog id="contact" ref={dialogRef} onClick={onDialogClick} data-test="contact-modal" aria-labelledby="contact-modal-heading" className="mx-4 max-w-md max-h-[calc(100vh-2rem)] overflow-y-auto rounded border border-[var(--color-border)] bg-[var(--color-bg-end)] p-6 md:p-8">`
       - Heading: `<h2 id="contact-modal-heading" className="text-lg font-sans leading-relaxed" style={{ color: 'var(--color-text)' }}>Get in touch</h2>` — D-08 verbatim.
       - Conditional: `const showSuccess = state.succeeded || bypassedSuccess;` — render success state OR form.
       - Success state JSX: `<div role="status" aria-live="polite" aria-atomic="true" className="mt-6"><p className="text-base font-sans" style={{ color: 'var(--color-text)' }}>Thanks — I'll get back to you within a day or two.</p><div className="mt-4"><button type="button" onClick={() => { reset(); setBypassedSuccess(false); setMessage(''); }} className="group inline-flex items-center gap-2 text-base font-sans hover:underline hover:decoration-1 hover:underline-offset-4" style={{ color: 'var(--color-accent)' }}>Send another <span aria-hidden className="inline-block transition-transform group-hover:translate-x-1">→</span></button></div></div>` — D-10 verbatim copy, em-dash U+2014, curly apostrophe U+2019, "Send another →" Claude's-discretion CTA per D-10.
       - Error state JSX (renders ABOVE form per D-11 — form remains visible + populated): `{state.errors && (<div role="alert" aria-live="assertive" aria-atomic="true" className="mt-6"><p className="text-base font-sans" style={{ color: 'var(--color-text)' }}>Something went wrong sending that. Try the email link below.</p></div>)}` — D-11 verbatim.
       - Form: `<form onSubmit={wrappedHandleSubmit} aria-busy={state.submitting} className="mt-6 flex flex-col gap-4">`
       - Honeypot wrapper: `<div className="absolute -left-[9999px] opacity-0 pointer-events-none" aria-hidden="true"><input type="text" name="company" tabIndex={-1} autoComplete="off" /></div>` — D-13 field name `company`, NOT `_gotcha`.
       - 3 field groups (Name / Email / Message). Each: `<div className="flex flex-col gap-2"><label htmlFor="contact-{field}" className="text-sm font-sans" style={{ color: 'var(--color-muted)' }}>{Label} <span aria-hidden style={{ color: 'var(--color-accent)' }}>*</span></label><input id="contact-{field}" type="{type}" name="{name}" required disabled={state.submitting} className="px-3 py-2 text-base font-sans rounded border border-[var(--color-border)] bg-transparent transition-[border-color,color] duration-200 ease-[cubic-bezier(0.2,0,0,1)] focus-visible:border-[var(--color-accent)]" style={{ color: 'var(--color-text)' }} /><ValidationError prefix="{Label}" field="{name}" errors={state.errors} className="text-sm font-sans" /></div>` — `text-base` is 16px to defeat iOS Safari auto-zoom (Pitfall 7); `transition-[border-color,color]` arbitrary list to defeat outline-color clobbering (Pitfall 4).
       - Message field is a controlled `<textarea>` instead: `<textarea id="contact-message" name="message" rows={4} maxLength={MAX_LEN} required disabled={state.submitting} value={message} onChange={(e) => setMessage(e.target.value)} className="px-3 py-2 text-base font-sans rounded border border-[var(--color-border)] bg-transparent resize-y transition-[border-color,color] duration-200 ease-[cubic-bezier(0.2,0,0,1)] focus-visible:border-[var(--color-accent)]" style={{ color: 'var(--color-text)' }} />` — `rows={4}` per D-02 fixed height (NOT auto-grow); `maxLength={1000}` browser-enforced cap; `resize-y` allows user to drag vertically.
       - Char counter (lives BELOW the textarea, ABOVE the next sibling): `<div className="mt-1 flex justify-end"><span className="text-sm font-mono" style={{ color: message.length > WARN_LEN ? '#c8a86a' : 'var(--color-muted)' }}>{message.length} / {MAX_LEN}</span></div>` — inline-style `#c8a86a` literal scoped to this counter only per D-02 (Phase 4 D-09 reuse, NOT promoted to `@theme`).
       - Send button row + mailto block: `<div className="mt-6 flex flex-col gap-4"><div className="flex justify-end"><button type="submit" disabled={state.submitting} className="inline-flex items-center gap-2 whitespace-nowrap rounded-md border border-[var(--color-border)] px-4 py-3 text-sm font-sans transition-[border-color,color,opacity] duration-200 ease-[cubic-bezier(0.2,0,0,1)] hover:border-[var(--color-accent)] disabled:opacity-60 disabled:cursor-not-allowed disabled:pointer-events-none" style={{ color: 'var(--color-accent)' }}>{state.submitting ? 'Sending…' : 'Send message'}</button></div><a href={MAILTO_HREF} className="group inline-flex items-center gap-2 text-base font-sans hover:underline hover:decoration-1 hover:underline-offset-4" style={{ color: 'var(--color-accent)' }}>Or just email me directly <span aria-hidden className="inline-block transition-transform group-hover:translate-x-1">→</span></a></div>` — D-09 + D-12 button label verbatim (use U+2026 ellipsis `…` for "Sending…", NOT three dots); D-15 mailto link verbatim with U+2192 right-arrow `→`.
       - End the conditional render: form + mailto block are wrapped in `{!showSuccess && (<>...</>)}` so success state replaces form per D-10 contract.
       - The error message stays visible IN the success-false branch (above the form) so the user retains typed content per D-11.
       - Two `aria-live` sr-only regions at the END of the dialog (after success/form conditional) — `role="status" aria-live="polite"` for "Sending your message." / "Message sent. Thanks." announcements; `role="alert" aria-live="assertive"` for error announcements. The visible UI also communicates state — these are screen-reader-additive per A11Y-03 + CTCT-03.

    11. **Verbatim copy verification gate** (do this BEFORE commit):
        - `grep -c "Get in touch" components/contact/ContactModal.tsx` → exactly 1
        - `grep -c "Send message" components/contact/ContactModal.tsx` → exactly 1
        - `grep -c "Sending…" components/contact/ContactModal.tsx` → exactly 1 (use file with proper UTF-8 — the … is U+2026)
        - `grep -c "Thanks — I'll get back to you within a day or two." components/contact/ContactModal.tsx` → exactly 1 (em-dash U+2014, curly apostrophe U+2019)
        - `grep -c "Something went wrong sending that. Try the email link below." components/contact/ContactModal.tsx` → exactly 1
        - `grep -c "Or just email me directly" components/contact/ContactModal.tsx` → exactly 1
        - If ANY count is wrong, the spec assertions in Plan 01 Task 3 will fail — stop and fix before commit.

    12. **`data/site.ts` edit** (D-15a — single-line addition):
        - Add `email: 'fakegoat1@gmail.com',` to the `site` object literal between `domain: 'braehods.com',` and `socials: { ... }`. The `email?: string` field already exists on the SiteMeta interface (line 5) — just wire a value. Add a comment: `// Phase 5 D-15 — modal mailto target. Future: hi@braehods.com per D-15a once domain mail is configured.`

    13. **`tests/no-client-components.spec.ts` atomic update** (MUST land in same commit as ContactModal.tsx — otherwise this spec goes RED):
        - Update the existing test to allow exactly 1 client island (the new ContactModal). Two implementation paths — pick (b):
          (a) Replace assertion: `expect(offenders).toEqual([])` → `expect(offenders).toEqual(['components/contact/ContactModal.tsx'])` and rename test description.
          (b) Define `const ALLOWED_CLIENT_ISLANDS = ['components/contact/ContactModal.tsx'];` near the top, normalize paths to forward-slash for cross-platform match (`path.relative(root, file).split(path.sep).join('/')`), assert `expect(offenders.sort()).toEqual(ALLOWED_CLIENT_ISLANDS.sort())`. Test name updates from "no `use client` directives anywhere" to "exactly 1 'use client' directive (ContactModal — Phase 5 carve-out)".
        - Update the JSDoc header banner to reflect "Phase 5 carve-out" wording (replace the "RED until those directories exist" line with "GREEN at Phase 5 once ContactModal lands as the sole carve-out per FOUND-07").

    14. **Commit**: ALL THREE files (ContactModal.tsx + data/site.ts + tests/no-client-components.spec.ts) in ONE commit per atomic-coupling rule. Commit message: `feat(phase-5/w1): ContactModal client island (D-01..D-19; the single 'use client' carve-out per FOUND-07)`. NO emoji or boilerplate footer per CLAUDE.md project conventions.

    Do NOT use `'use client'` ANYWHERE else in the project. Do NOT introduce a `ContactTrigger` wrapper component (the hash-based architecture per D-06 makes one unnecessary; a wrapper would force a 2nd client directive — FOUND-07 violation). Do NOT add a visible close-X button (UI-SPEC silence + editorial restraint — ESC + backdrop-click are the close affordances per D-06 carry-forward + Pitfall 6 architecture). Do NOT use `transition-colors` shorthand on ANY interactive element in the modal (Pitfall 4 — Tailwind v4 outline-color clobbering regresses focus-ring spec). Do NOT reuse `fade-in-up` keyframe for the modal entrance (UI-SPEC line 422 + Pitfall 6: 8px translateY conflicts with browser-centered dialog — Plan 01 Task 2 already added the new opacity-only `modal-fade-in` keyframe). Do NOT promote `#c8a86a` to `@theme` (D-02 + Phase 4 D-09 inline-only discipline). Do NOT use straight apostrophe / hyphen-minus / three-dot ellipsis where D-10/D-11/D-12 lock the typographic Unicode forms.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && node -e "const fs=require('fs');const src=fs.readFileSync('components/contact/ContactModal.tsx','utf8');const checks={\"'use client'\":src.startsWith('//')?/^\\s*['\\\"]use client['\\\"]/.test(src.split('\\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('/*')&&l.trim()).slice(0,1).join('')||''):src.trimStart().startsWith(\"'use client'\")||src.trimStart().startsWith('\"use client\"'),'D-08 heading':/Get in touch/.test(src),'D-09 send':/Send message/.test(src),'D-12 sending':/Sending\\u2026/.test(src),'D-10 thanks':/Thanks \\u2014 I\\u2019ll get back to you within a day or two\\./.test(src),'D-11 error':/Something went wrong sending that\\. Try the email link below\\./.test(src),'D-15 mailto wording':/Or just email me directly/.test(src),'D-15 mailto href':/mailto:fakegoat1@gmail\\.com\\?subject=Hi%20Braeden/.test(src),'D-13 honeypot':/name=['\\\"]\\bcompany\\b['\\\"]/.test(src),'D-14 1500ms':/1500/.test(src),'D-19 useForm':/useForm\\(/.test(src),'no transition-colors':!/transition-colors/.test(src),'no _gotcha':!/_gotcha/.test(src),'history.replaceState':/history\\.replaceState/.test(src),'bypassedSuccess':/bypassedSuccess/.test(src),'showModal':/showModal/.test(src),'aria-busy':/aria-busy/.test(src),'aria-labelledby':/aria-labelledby/.test(src),'role=status':/role=['\\\"]status['\\\"]/.test(src),'role=alert':/role=['\\\"]alert['\\\"]/.test(src),'no fade-in-up reuse':!/className=['\\\"][^'\\\"]*fade-in-up[^'\\\"]*['\\\"][^>]*>(?!.*<dialog)/.test(src)};let fail=false;for(const[k,v]of Object.entries(checks)){if(!v){console.error('FAIL:',k);fail=true}}if(fail)process.exit(1);console.log('all ContactModal checks pass')" && node -e "const s=require('./data/site.ts'.replace('.ts','')); /* skip - TS file. Use grep instead. */" 2>/dev/null; node -e "const fs=require('fs');const s=fs.readFileSync('data/site.ts','utf8');if(!/email:\\s*['\\\"]fakegoat1@gmail\\.com['\\\"]/.test(s)){console.error('data/site.ts missing email field');process.exit(1)}console.log('data/site.ts.email wired')" && node -e "const{execSync}=require('child_process');const out=execSync('git grep -l \\\"use client\\\" -- *.tsx *.ts',{encoding:'utf8'}).trim().split(/\\r?\\n/).filter(Boolean).map(s=>s.replace(/\\\\/g,'/'));if(out.length!==1||out[0]!=='components/contact/ContactModal.tsx'){console.error('FOUND-07 violation. Found:',out);process.exit(1)}console.log('FOUND-07 carve-out: exactly 1 client island =',out[0])" && npx playwright test tests/no-client-components.spec.ts tests/single-client-island.spec.ts tests/contact-modal-states.spec.ts tests/contact-modal-honeypot.spec.ts tests/contact-modal-min-time.spec.ts tests/contact-modal-mailto-fallback.spec.ts tests/focus-ring.spec.ts tests/no-bare-outline-none.spec.ts tests/reduced-motion.spec.ts --project=chromium-mobile</automated>
  </verify>
  <acceptance_criteria>
    - `components/contact/ContactModal.tsx` exists with `'use client';` as the first non-comment statement
    - All 5 verbatim copy strings present with locked Unicode (em-dash U+2014, ellipsis U+2026, curly apostrophe U+2019) — verified by node-script regexes above; exits 1 if any literal is wrong
    - Honeypot field name = `company` (NOT `_gotcha`); negative regex `!/_gotcha/.test(src)` PASS
    - Min-time threshold literal `1500` present
    - `useForm(` invocation present (D-19)
    - `history.replaceState` used (NOT `location.hash = ''`) — Pitfall 1
    - `bypassedSuccess` state defined — Pitfall 5
    - `showModal` called (NOT `show`) — RESEARCH anti-pattern
    - `aria-busy`, `aria-labelledby`, `role="status"`, `role="alert"` all present (CTCT-03 + A11Y-03)
    - Negative regex `!/transition-colors/.test(src)` PASS — only `transition-[...]` arbitrary lists used (Pitfall 4)
    - `data/site.ts` has `email: 'fakegoat1@gmail.com',` literal
    - `git grep -l "use client" -- '*.tsx' '*.ts'` returns EXACTLY `components/contact/ContactModal.tsx` (FOUND-07 carve-out enforced)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm run build` exits 0 (Turbopack compiles cleanly; the new client island shows up in the bundle output)
    - `tests/no-client-components.spec.ts` GREEN (atomically updated to allow ContactModal)
    - `tests/single-client-island.spec.ts` GREEN (Plan 01 RED → Plan 02 GREEN flip)
    - `tests/contact-modal-states.spec.ts` GREEN (4 state copy strings render correctly)
    - `tests/contact-modal-honeypot.spec.ts` GREEN (silent rejection + bypassedSuccess UI)
    - `tests/contact-modal-min-time.spec.ts` GREEN (silent rejection + post-1500ms passthrough)
    - `tests/contact-modal-mailto-fallback.spec.ts` GREEN (link href + visible text)
    - `tests/focus-ring.spec.ts` GREEN (Pitfall 4 discipline verified — no outline-color regression)
    - `tests/no-bare-outline-none.spec.ts` GREEN (no new bare outline:none introduced)
    - `tests/reduced-motion.spec.ts` GREEN (modal animations clamped via existing globals.css gate)
    - Commit message references D-XX decisions implemented + the FOUND-07 carve-out
  </acceptance_criteria>
  <done>The single client island ships, all verbatim copy locked, FOUND-07 carve-out enforced, 6 of 9 Plan 01 RED specs flip GREEN (contact-modal-states, contact-modal-honeypot, contact-modal-min-time, contact-modal-mailto-fallback, single-client-island, plus the atomic update to no-client-components). The remaining 4 (`tests/contact-modal-opens-from-nav.spec.ts`, `tests/contact-modal-opens-from-about.spec.ts`, `tests/contact-modal-esc-closes.spec.ts`, `tests/contact-trigger-rewire.spec.ts`) all assert `href === '#contact'` on Nav/about source — they need Plan 03's atomic href swap to flip and remain RED at end of Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → Formspree (third-party) | Form fields POST over HTTPS to `formspree.io/f/xqeypnkw`; values never re-rendered in our app DOM |
| browser → DOM (top-layer dialog) | `<dialog>.showModal()` puts the dialog in browser-managed top-layer; focus trap + scroll lock + ESC are browser-delivered, not our code |
| user URL → location.hash | Read-only consumption (`window.location.hash === '#contact'`); cleared via `history.replaceState` (no programmatic SET to a fixed value) |
| OS → mailto: handler | `mailto:fakegoat1@gmail.com?subject=Hi%20Braeden` handed to OS mail client; subject is hard-coded literal (no user input concatenation) |

## STRIDE Threat Register (full enumeration; references RESEARCH.md `## Security Domain` for shared rationale)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Bot DOM enumeration + form submit (form spam) | mitigate | Honeypot field `company` (D-13) + min-time-to-submit 1500ms (D-14). BOTH silent-reject (no error feedback bots can train against) per Pitfall 5. `bypassedSuccess` state ensures the success UI renders identically to a real submit so bots can't distinguish gates from passthroughs. |
| T-05-02-02 | Tampering / XSS | Form input echoed back into DOM | mitigate | Form values are POSTed to Formspree (third-party). They are NEVER re-rendered into our DOM. The success-state copy is locked verbatim (D-10 — does not echo user input). The error-state copy is locked verbatim (D-11). Even `<ValidationError>` from `@formspree/react` renders only Formspree-side validation strings, not user-typed values. Zero XSS surface. |
| T-05-02-03 | Information Disclosure | `NEXT_PUBLIC_FORMSPREE_ID` in client bundle | accept | Form ID is non-secret by design — Formspree's CSRF model uses the ID as the token. Inlined at build time per Next.js convention. RESEARCH.md `## Security Domain` row 3. |
| T-05-02-04 | Tampering | CSRF against Formspree | accept | Formspree handles CSRF on their side (form ID acts as the token). Our POST originates directly from the user's browser. RESEARCH.md `## Security Domain` row 4. |
| T-05-02-05 | Denial of Service | Formspree endpoint abuse | accept | Phase 1 plan T-01-08 explicitly accepted with the note "Phase 5 ships honeypot + min-time-to-submit per CTCT-04." Implemented in this plan. Migration target if needed: Cloudflare Turnstile (free, no Google tracking). reCAPTCHA explicitly excluded per REQUIREMENTS.md Out-of-Scope + memory `feedback_avoid_paid_tools.md`. |
| T-05-02-06 | Tampering | mailto: link XSS via subject param | accept | Subject `Hi%20Braeden` is hard-coded in source. No user input concatenation. Zero XSS surface. The `site.email` field is also a hard-coded literal (D-15a slot wired to `fakegoat1@gmail.com` for v1). |
| T-05-02-07 | Tampering | Open redirect via `window.location.hash` | accept | Hash is read-only consumption (decision tree only checks `=== '#contact'`). On close we `replaceState` to clear it (no programmatic SET to a fixed value). No redirect surface. |
| T-05-02-08 | Tampering | Disabled-input bypass via DevTools | mitigate | `useForm`'s `state.submitting` tracks the in-flight request; even if a malicious user re-enables the disabled input via DevTools, `useForm` will not double-fire. The disabled state is a UX affordance, not a security boundary. |
| T-05-02-09 | Repudiation | Bot detection via state-change observation | mitigate | Pitfall 5 fix: `bypassedSuccess` state renders the success UI identically when the honeypot OR min-time gate trips. From the bot's perspective, a successful Formspree submit and a silent-rejected submit look IDENTICAL. No state-change-after-submit signal for the bot to learn. |
| T-05-02-10 | Information Disclosure | Reduced-motion user feature-detection | accept | The reduced-motion gate is global (existing `app/globals.css` lines 54-63 universal selector). Modal entrance is opacity-only, defeated by the gate. No new information channel. |

ASVS L1 cleared per RESEARCH.md `## Security Domain` summary. No high-severity findings; the modal's threat surface is small + well-understood + every flow is browser-bounded.
</threat_model>

<verification>
End-of-plan gate (run after the single atomic Task 1 commit):

1. FOUND-07 carve-out: `git grep -l "use client" -- '*.tsx' '*.ts'` → output is EXACTLY `components/contact/ContactModal.tsx` (one file).
2. Verbatim copy locks all present in source: 5 grep counts above all return 1.
3. `npm run typecheck && npm run lint && npm run build` all exit 0; build output shows ContactModal client chunk. (Run `npm run build` after this plan completes; not required per-task — per-task feedback stays under 10s with typecheck + lint + Playwright only, per WARNING 2 fix.)
4. Plan 01 RED specs flip status:
   - `tests/single-client-island.spec.ts` — RED → GREEN
   - `tests/no-client-components.spec.ts` — GREEN (was GREEN, now atomically updated to allow ContactModal — verifies the same invariant from a different angle)
   - `tests/contact-modal-states.spec.ts` — RED → GREEN
   - `tests/contact-modal-honeypot.spec.ts` — RED → GREEN
   - `tests/contact-modal-min-time.spec.ts` — RED → GREEN
   - `tests/contact-modal-mailto-fallback.spec.ts` — RED → GREEN
5. Plan 01 specs that REMAIN RED at end of Plan 02 (Plan 03 flips them):
   - `tests/contact-modal-opens-from-nav.spec.ts` — needs Nav href swap (Plan 03)
   - `tests/contact-modal-opens-from-about.spec.ts` — needs /about href swap (Plan 03)
   - `tests/contact-modal-esc-closes.spec.ts` — needs trigger-via-Nav (Plan 03)
   - `tests/contact-trigger-rewire.spec.ts` — needs both href swaps (Plan 03 atomic)
6. Phase 1 chrome canaries: `tests/focus-ring.spec.ts` + `tests/no-bare-outline-none.spec.ts` + `tests/reduced-motion.spec.ts` all GREEN.
7. Phase 2/3/4 regression canaries: `tests/about-renders.spec.ts` (note: still asserts href === '/' — this STAYS GREEN until Plan 03 atomically swaps both Nav source AND this spec's assertion); `tests/work-grid-renders.spec.ts`; `tests/footer-socials-render.spec.ts` — all GREEN.
8. Bundle size sanity check (NOT a gate; document in SUMMARY): client island chunk size ≤ 8KB gzipped for the ContactModal+useForm graph (Phase 6 PERF-03 target is ≤50KB total; this single island should be well under).
</verification>

<success_criteria>
- `components/contact/ContactModal.tsx` shipped as the SOLE `'use client'` file in the entire codebase (FOUND-07 carve-out)
- 5 verbatim copy strings locked per D-08..D-15 with correct typographic Unicode
- Honeypot + min-time gates implemented with `bypassedSuccess` separate state (Pitfall 5)
- Hash-driven open via `useEffect` + `hashchange` listener with `history.replaceState` close cleanup (Pitfall 1)
- `useForm.reset()` + local state clearance on dialog close (Pitfall 2)
- Backdrop-click handler uses `e.target === e.currentTarget` (Pitfall 6)
- All interactive elements use `transition-[<list>]` arbitrary-list (Pitfall 4 — focus-ring spec stays GREEN)
- Input + textarea content text is `text-base` (16px) defeating iOS auto-zoom (Pitfall 7)
- `data/site.ts.email` wired with literal value (D-15a forward-compat)
- `tests/no-client-components.spec.ts` atomically updated to allow ContactModal (single commit with the new file)
- 6 of 9 Plan 01 RED specs flip GREEN (3 remain RED waiting for Plan 03)
- Existing Phase 1+2+3+4 chrome canaries unchanged GREEN
- 1 atomic commit direct to main per project branching strategy
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-modal/05-02-SUMMARY.md` covering:
- ContactModal.tsx line count, structure summary (header banner, directive, imports, state, effects, JSX layout)
- Verbatim copy verification table (D-08..D-15 each with literal grep count)
- FOUND-07 carve-out verification (`git grep` output showing exactly 1 file)
- 6 Plan 01 spec flips RED → GREEN with assertion summaries
- 3 Plan 01 specs that remain RED (Plan 03 territory, listed for hand-off clarity)
- `data/site.ts.email` wire-up confirmation (1-line diff)
- `tests/no-client-components.spec.ts` atomic update diff (assertion change)
- Bundle size measurement for the new client island chunk (informational, not a gate)
- Any deviations from plan (Rules 1-4) with rationale
- Hand-off note to Plan 05-03: "atomic mount in app/layout.tsx + Nav href swap + about CTA href swap + tests/about-renders.spec.ts assertion update — all in ONE commit per D-05"
</output>
