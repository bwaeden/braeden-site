---
phase: 04-work-projects
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/work/page.tsx
  - components/work/ProjectCard.tsx
  - tests/work-status-badges.spec.ts
  - .planning/phases/04-work-projects/04-01-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - A11Y-05
tags:
  - nextjs
  - app-router
  - rsc
  - tailwind
  - playwright
  - editorial-dark
  - card-grid
  - deploy
must_haves:
  truths:
    - "Visiting /work on the deployed Vercel branch preview shows all N (user-confirmed in Plan 04-01 Wave 0a) projects rendered as a grid of cards"
    - "Each project card displays the locked composition: status badge (colored dot + lowercase schema label) + title + accent ↗ glyph + one-line description + comma-joined tags line"
    - "Hovering any card transitions its hairline border from #2a2a2f to accent #7c87ff and translates the trailing ↗ glyph +2px right; no card lifts vertically"
    - "Pressing Tab through /work focuses each card with the Phase 1 accent focus ring (no muted-color flash on first frame — verified against tests/focus-ring.spec.ts)"
    - "Clicking any card opens the project's canonical URL in a new tab (target=\"_blank\" rel=\"noopener noreferrer\")"
    - "On viewports ≥768px the grid renders 2 columns with cards in the same row matching height via items-stretch; on <768px the grid renders 1 column"
    - "If N is odd, the orphan card sits left-aligned at half-width in the last row — NOT centered, NOT spanning 2 columns; if N is even, all rows are full"
    - "Status dots display the locked color map: shipped→accent #7c87ff, paper-trading→#c8a86a, in-dev→muted #a8a8a8, archived→#707070 (or #7a7a7a fallback if WebAIM check fails on deployed preview)"
    - "Toggling DevTools 'Emulate prefers-reduced-motion: reduce' on the deployed preview defeats per-card stagger AND the card hover border-color transition AND the arrow translate"
    - "Zero 'use client' directive in app/work/, components/work/, or any new file added across Plans 04-01 + 04-02"
    - "All 4 Wave-0 RED specs (work-grid-renders, work-status-badges, work-no-flagship, work-descriptions-cliche-scrub) flip GREEN against the deployed Vercel branch preview"
    - "All 22+ pre-existing Playwright specs (Phase 1 + 2 + 3 chrome) remain GREEN — zero regression"
    - "data/projects.ts contains zero `// TODO(user)` markers at deploy time (deploy gate enforces; if any remain, route back to Plan 04-01 Task 1 to refine descriptions before deploy approval)"
    - "WebAIM contrast check on the rendered archived dot color against the rendered background (with grain) returns ≥3.0:1; if not, the documented #7a7a7a fallback ships and re-verifies"
    - "CONTEXT.md decisions covered by Plan 04-02 (page composition + grid + deploy scope): D-05 (1-col mobile / 2-col md+ / 2-col lg grid), D-06 (orphan card = left-aligned half-width default Grid behavior, NOT centered, NOT spanning), D-07 (items-stretch row-aligned card heights), D-08 (archived braehods uses same card style — equal-weight, no demotion), D-13 (sr-only h1 'Work' + aria-labelledby section landmark per Phase 3 /about convention)"
  artifacts:
    - path: "app/work/page.tsx"
      provides: "Server Component composing the work grid (sr-only h1 + section landmark + ul of staggered ProjectCards)"
      contains: "ProjectCard"
      must_contain: ["data-test=\"work-section\"", "data-test=\"work-grid\"", "aria-labelledby=\"work-heading\"", "id=\"work-heading\"", "sr-only", "grid-cols-1", "md:grid-cols-2", "items-stretch", "fadeInUp"]
      must_not_contain: ["'use client'", "Coming soon"]
    - path: ".planning/phases/04-work-projects/04-01-SUMMARY.md"
      provides: "Consolidated SUMMARY covering Plans 04-01 + 04-02 — frontmatter, wave-by-wave recap (Tasks 0-7), User Decisions subsection, net spec movement, deviations, Phase 6 carry-forwards, visual milestone"
      must_contain: ["WORK-01", "WORK-02", "WORK-03", "WORK-04", "WORK-05", "WORK-06", "A11Y-05", "User Decisions"]
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 row checked + completion date set + Plans count updated to '2 plans, 4 waves'"
    - path: ".planning/STATE.md"
      provides: "progress.completed_phases incremented to 4, completed_plans incremented, Spec scoreboard updated, Last Session entry appended"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table — WORK-01..06 + A11Y-05 flipped from Pending to Complete; section checkboxes flipped"
  key_links:
    - from: "app/work/page.tsx"
      to: "data/projects.ts"
      via: "import { projects } from '@/data/projects'"
      pattern: "import.*projects.*from.*@/data/projects"
    - from: "app/work/page.tsx"
      to: "components/work/ProjectCard.tsx"
      via: "import { ProjectCard } from '@/components/work/ProjectCard'"
      pattern: "import.*ProjectCard.*from.*@/components/work/ProjectCard"
    - from: "app/work/page.tsx"
      to: "lib/motion.ts"
      via: "import { fadeInUp, stagger } from '@/lib/motion'"
      pattern: "import.*fadeInUp.*stagger.*from.*@/lib/motion"
---

<objective>
Replace the Phase 2 `Coming soon.` stub at `app/work/page.tsx` with the real `/work` route — an equal-weight responsive grid composition that maps over the projects array (populated by Plan 04-01 Wave 0b) and renders each as a `<ProjectCard>` (built by Plan 04-01 Wave 1). This flips the 4 RED specs (stubbed by Plan 04-01 Wave 0c) GREEN. Then deploy to a Vercel branch preview, verify the full Playwright suite GREEN against the CDN-served URL, walk the 28-item visual checklist, run the WebAIM contrast check on the borderline `#707070`-on-`#1a1a1f` archived dot pair (with `#7a7a7a` fallback documented), and write the consolidated `04-01-SUMMARY.md` + flip ROADMAP / STATE / REQUIREMENTS traceability.

Purpose: Closes Phase 4's 7 requirement IDs (WORK-01..06 + A11Y-05) and ships the deployable Vercel preview. The `/work` route becomes the second cross-route consumer of the editorial-tile pattern family established by Phases 1 + 2 + 3, translated to card scale.

Output:
- 1 page rewrite (`app/work/page.tsx`)
- 0–2 conditional source edits (only if WebAIM contrast check fails: `STATUS_DOT_COLOR['archived']` literal in `ProjectCard.tsx` + `ARCHIVED_RGB` regex in `tests/work-status-badges.spec.ts`)
- Deployed Vercel branch preview verified against the 28-item visual checklist in UI-SPEC § Phase Exit Visual Verification
- Consolidated SUMMARY at `.planning/phases/04-work-projects/04-01-SUMMARY.md` covering Plans 04-01 + 04-02
- ROADMAP / STATE / REQUIREMENTS updated to reflect Phase 4 completion
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-work-projects/04-CONTEXT.md
@.planning/phases/04-work-projects/04-RESEARCH.md
@.planning/phases/04-work-projects/04-UI-SPEC.md
@.planning/phases/04-work-projects/04-PATTERNS.md
@.planning/phases/04-work-projects/04-VALIDATION.md
@.planning/phases/04-work-projects/04-01-PLAN.md
@.planning/phases/02-home-page/02-SUMMARY.md
@.planning/phases/03-about-page/03-01-SUMMARY.md
@CLAUDE.md

# Source-of-truth files (Phase 1/2/3 patterns the executor MUST mirror)
@app/work/page.tsx
@app/about/page.tsx
@app/layout.tsx
@app/globals.css
@lib/motion.ts
@components/home/Hero.tsx
@components/home/ChannelButtonRow.tsx

# Plan 04-01 outputs (must exist before this plan starts)
@data/projects.ts
@components/work/ProjectCard.tsx
@tests/work-grid-renders.spec.ts
@tests/work-status-badges.spec.ts
@tests/work-no-flagship.spec.ts
@tests/work-descriptions-cliche-scrub.spec.ts

# Regression specs (must remain GREEN)
@tests/no-client-components.spec.ts
@tests/focus-ring.spec.ts
@tests/reduced-motion.spec.ts

<interfaces>
<!-- Page composition contracts the executor needs without re-discovering them. -->

Page <section> attributes (LOCKED — verbatim per UI-SPEC § Page Composition):
- data-test="work-section"
- aria-labelledby="work-heading"
- className="py-8 md:py-12"

Page <h1> (LOCKED):
- id="work-heading"
- className="sr-only"
- text content: "Work"

Page grid <ul> className (LOCKED — verbatim):
"grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-6 items-stretch list-none p-0"

Page grid <ul> data-test (LOCKED — the spec tests/work-no-flagship.spec.ts queries [data-test="work-grid"] > li):
- data-test="work-grid"

Per-card <li> wrapper (LOCKED — animation seam lives HERE not on <a>):
- key={project.slug}
- className={fadeInUp}
- style={stagger(i + 1)}  ← i is 0-indexed map iterator; stagger starts at 1

Page imports (LOCKED — exactly 3 lines):
- import { fadeInUp, stagger } from '@/lib/motion';
- import { projects } from '@/data/projects';
- import { ProjectCard } from '@/components/work/ProjectCard';

Page metadata (LOCKED — preserve verbatim from Plan 02-05 stub):
- export const metadata = { title: 'Work' };

From lib/motion.ts (consumed unchanged):
```typescript
export const fadeInUp = 'fade-in-up' as const;
export function stagger(i: number): React.CSSProperties; // returns { '--stagger': `${i * 80}ms` }
```

Plan 04-01 outputs (consumed):
- data/projects.ts exports projects: Project[] with N user-confirmed entries
- components/work/ProjectCard.tsx exports ProjectCard (Server Component) with the locked STATUS_DOT_COLOR + JSX tree
- 4 Playwright specs exist as RED (this plan's Wave 2 flips them GREEN)

WebAIM fallback contract (LOCKED per D-09 borderline pair documentation):
- If rendered archived dot contrast against rendered background (with grain) measures < 3.0:1:
  - components/work/ProjectCard.tsx STATUS_DOT_COLOR['archived'] changes from '#707070' to '#7a7a7a' (1-line edit)
  - tests/work-status-badges.spec.ts ARCHIVED_RGB regex changes from /rgba?\(\s*112\s*,\s*112\s*,\s*112/ to /rgba?\(\s*122\s*,\s*122\s*,\s*122/ (1-line edit)
  - Single atomic commit ships both: `fix(phase-4/w3): bump archived dot to #7a7a7a per WebAIM contrast verification`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4 (Wave 2): Rewrite app/work/page.tsx (compose grid + flip RED specs GREEN)</name>
  <files>app/work/page.tsx</files>
  <read_first>
    - app/work/page.tsx (current Plan 02-05 stub — verify the 18-line `Coming soon.` body before replacing)
    - app/about/page.tsx (PRIMARY analog — sr-only h1 + aria-labelledby section landmark + per-item map; lines 1-50)
    - components/home/Hero.tsx (composition pattern — multiple data imports + lib/motion stagger; lines 1-90)
    - components/home/ChannelButtonRow.tsx (per-item `<li>`-wrapper stagger pattern — animation seam discipline; lines 1-40)
    - components/work/ProjectCard.tsx (created in Plan 04-01 Task 3 — the consumer)
    - data/projects.ts (populated in Plan 04-01 Task 1 — the source array)
    - lib/motion.ts (verbatim API — `fadeInUp` + `stagger(i)`)
    - app/layout.tsx (the inherited `max-w-3xl mx-auto px-6 py-16 lg:px-12` container — page does NOT widen)
    - .planning/phases/04-work-projects/04-UI-SPEC.md § Page Composition (lines ~174-216) + § Motion Choreography (lines ~352-411)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Code Examples Example 2 (lines ~571-619 — verbatim reference for the page rewrite)
    - .planning/phases/04-work-projects/04-PATTERNS.md § app/work/page.tsx (lines ~201-289 — analog excerpts)
    - tests/work-grid-renders.spec.ts + tests/work-status-badges.spec.ts + tests/work-no-flagship.spec.ts + tests/work-descriptions-cliche-scrub.spec.ts (the RED specs this task flips GREEN — match their selector expectations exactly)
  </read_first>
  <behavior>
    - Test 1: `app/work/page.tsx` exports a default function and `metadata = { title: 'Work' }` (preserve Plan 02-05's metadata export verbatim)
    - Test 2: File contains zero `'use client'` directive
    - Test 3: File imports `projects` from `'@/data/projects'`, `ProjectCard` from `'@/components/work/ProjectCard'`, and `fadeInUp, stagger` from `'@/lib/motion'`
    - Test 4: Rendered HTML contains `<section data-test="work-section" aria-labelledby="work-heading" class="py-8 md:py-12">`
    - Test 5: Rendered HTML contains `<h1 id="work-heading" class="sr-only">Work</h1>`
    - Test 6: Rendered HTML contains `<ul data-test="work-grid"` with className containing `grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-6 items-stretch list-none p-0`
    - Test 7: The `<ul>` contains exactly `projects.length` `<li>` direct children (the user-confirmed N from Plan 04-01 Task 1)
    - Test 8: Each `<li>` has `className` of `fade-in-up` (the resolved value of `fadeInUp`) AND inline `style` containing `--stagger:` with a `${(i+1) * 80}ms` value
    - Test 9: Each `<li>` contains exactly one `<a target="_blank" rel="noopener noreferrer">` (delegated to ProjectCard)
    - Test 10: All 4 Wave-0 RED specs flip GREEN: `tests/work-grid-renders.spec.ts`, `tests/work-status-badges.spec.ts`, `tests/work-no-flagship.spec.ts`, `tests/work-descriptions-cliche-scrub.spec.ts`
    - Test 11: All Phase 1 + 2 + 3 chrome specs remain GREEN (no regression) — `npm run test:full` passes
  </behavior>
  <action>Replace the entire body of `app/work/page.tsx` (currently 18 lines, lines 1-18 from Plan 02-05) with the page composition described in UI-SPEC § Page Composition lines 197-216. The full reference implementation is in `04-RESEARCH.md` Code Examples Example 2 (lines ~571-619) — match its source-attribution header verbatim, its imports, its metadata export, and its JSX tree.

File-header comment block opens with `// app/work/page.tsx` then `// Source: PLAN.md 04-02-T4; 04-UI-SPEC § Page Composition (D-05, D-06, D-07, D-13).` followed by paragraphs documenting:
- Replaces the Plan 02-05 `Coming soon.` stub
- Server Component invariant (zero `'use client'`)
- Inherits Nav + Footer chrome from `app/layout.tsx` automatically
- Inherits `max-w-3xl mx-auto px-6 py-16 lg:px-12` container — does NOT extend to `max-w-5xl` (D-05 + Phase 3 inheritance)
- Grid: `grid-cols-1` mobile / `md:grid-cols-2` desktop / `items-stretch` row equalization per D-05 + D-07
- Orphan-card treatment per D-06 (default CSS Grid behavior; no special markup, no spans, no centering)
- Stagger seam discipline per Pitfall 5 (animation lives on the `<li>` wrapper, NOT on the inner `<a>`, to avoid `transform` collision with the card's hover `translate-x` arrow)

Imports — exactly 3 lines:
- `import { fadeInUp, stagger } from '@/lib/motion';`
- `import { projects } from '@/data/projects';`
- `import { ProjectCard } from '@/components/work/ProjectCard';`

Metadata — preserve verbatim from Plan 02-05:
- `export const metadata = { title: 'Work' };`

JSX tree — match the LOCKED structure from `<interfaces>` block above (verbatim from UI-SPEC § Page Composition):

1. `<section data-test="work-section" aria-labelledby="work-heading" className="py-8 md:py-12">` — page root.
2. `<h1 id="work-heading" className="sr-only">Work</h1>` — sr-only heading per D-13. NO visible heading. Tailwind v4's built-in `sr-only` utility (already verified working on `app/about/page.tsx` per Phase 3 — Assumption A1).
3. `<ul data-test="work-grid" className="grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-6 items-stretch list-none p-0">` — semantic list per UI-SPEC line 203. `list-none p-0` strips browser default `<ul>` styling.
4. `{projects.map((project, i) => (...))}` — map iteration. Each `<li>`:
   - `key={project.slug}`
   - `className={fadeInUp}` (resolves to the literal string `'fade-in-up'`)
   - `style={stagger(i + 1)}` (resolves to `{ '--stagger': '${(i + 1) * 80}ms' }` — first card 80ms, last card N×80ms)
   - Children: `<ProjectCard project={project} staggerIndex={i + 1} />`
5. Close `</ul>` and `</section>`.

CRITICAL discipline (do NOT deviate):
- The animation seam MUST live on the `<li>` wrapper. Do NOT pass `className={fadeInUp}` to `<ProjectCard>` (would put the keyframe on the inner `<a>` and collide with the hover arrow `translate-x` per Pitfall 5).
- The grid container MUST be a `<ul>` (semantic list per UI-SPEC line 203). Do NOT use `<div>` — would weaken screen-reader semantics.
- The `data-test="work-grid"` selector MUST be on the `<ul>` (the spec `tests/work-no-flagship.spec.ts` queries `[data-test="work-grid"] > li`).
- The orphan card (last card if N is odd) MUST sit at the natural CSS Grid default position (left column of the last row, with the right column empty) per D-06. Do NOT add any `col-span-*` or `justify-self-*` utility — the empty cell IS the design.
- The `<section>` className `py-8 md:py-12` MUST match exactly (matches Phase 2 home hero + Phase 3 about per CD-05 inheritance).

After writing, run `npm run dev` (or build + start) and curl `http://localhost:3000/work` to verify the rendered HTML contains all 5 expected `data-test` selectors and all card content. Then run the full Playwright suite to verify the 4 Wave-0 RED specs flip GREEN with zero Phase 1/2/3 regressions.</action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npx playwright test tests/work-grid-renders.spec.ts tests/work-status-badges.spec.ts tests/work-no-flagship.spec.ts tests/work-descriptions-cliche-scrub.spec.ts tests/no-client-components.spec.ts tests/focus-ring.spec.ts tests/reduced-motion.spec.ts && npm run test:full</automated>
  </verify>
  <done>
    - `app/work/page.tsx` body replaced; the `Coming soon.` `<p>` is gone; the new grid composition is in place with all 5 `data-test` selectors (`work-section`, `work-grid`) and the locked `<section>` + `<h1>` + `<ul>` markup
    - `npm run typecheck` + `npm run lint` + `npm run build` all exit 0
    - All 4 Wave-0 RED specs flip GREEN (`work-grid-renders`, `work-status-badges`, `work-no-flagship`, `work-descriptions-cliche-scrub`)
    - Phase 1 + 2 + 3 chrome specs remain GREEN (`npm run test:full` passes — no regression on hero, currently, channels, footer-socials, ctas-resolve-200, view-transition-name-present, mobile-hero-stacks-cleanly, about-renders, no-client-components, focus-ring, reduced-motion, monogram, lighthouse, contrast, etc.)
    - Curl direct verification: `curl -s http://localhost:3000/work | grep -E '(work-section|work-grid|work-heading|sr-only)'` shows all 4 selectors present in the rendered HTML
    - Atomic commit: `feat(phase-4/w2): rewrite app/work/page.tsx to render projects grid (D-05, D-06, D-07, D-13; flips W0 RED specs GREEN)`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (Wave 3a): Deploy to Vercel branch preview + run full Playwright suite against the preview URL — TODO(user) marker count MUST be 0</name>
  <files>(no source files modified — deploy + verify only; if TODO(user) markers remain in data/projects.ts, route back to Plan 04-01 Task 1)</files>
  <read_first>
    - .planning/STATE.md "Preview URL correction (important)" subsection — Vercel production alias is NOT the branch preview; user MUST supply the actual per-branch preview URL from the Vercel dashboard
    - .planning/phases/04-work-projects/04-UI-SPEC.md § Phase Exit Visual Verification (lines ~559-600 — the 28-item checklist)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Common Pitfalls Pitfall 7 (Vercel preview vs production alias confusion)
    - playwright.config.ts (root — verify if a `BASE_URL` env override exists; if not, document the override mechanism in the SUMMARY)
    - data/projects.ts (verify zero `// TODO(user)` markers remain; if any exist, route back to Plan 04-01 Task 1)
  </read_first>
  <what-built>Plan 04-01 (Wave 0a/0b/0c + Wave 1) shipped data + spec stubs + ProjectCard. This plan's Wave 2 (Task 4) shipped the page rewrite. Pushing those commits triggers Vercel to build a branch preview at a URL of the form `braeden-site-<hash>-<vercel-scope>.vercel.app`.

This checkpoint validates the deployed build matches the local build by:
1. **Pre-deploy gate (TODO(user) marker check — checker warning #2 fix):** Run `grep -c 'TODO(user)' data/projects.ts`. The result MUST be 0. If non-zero, the data/projects.ts file still contains placeholder descriptions that the user has not refined — route back to Plan 04-01 Task 1 (description refinement) before pushing or approving deploy. This protects against unrefined placeholder copy leaking to a live preview URL.
2. Pushing the Wave 0-2 commits (already committed to the current branch — just `git push`)
3. User retrieves the actual branch-preview URL from Vercel dashboard → Deployments → most recent
4. Re-running the full Playwright suite against `BASE_URL=<preview-url>` (or equivalent env override per `playwright.config.ts`)
5. Verifying all 22+ specs (Phase 1 + 2 + 3 chrome + the 4 new Phase 4 specs) GREEN against the CDN-served URL — NOT the local dev server (CDN serves the optimized production bundle; local dev server differs)
  </what-built>
  <how-to-verify>
    1. **PRE-DEPLOY GATE (checker warning #2 fix):** Run `grep -c 'TODO(user)' data/projects.ts`. The result MUST be 0. If non-zero:
       - Surface the count and the affected entries to the user with: "data/projects.ts contains <N> unrefined `TODO(user)` markers. These are placeholder descriptions from Plan 04-01 Task 1 that you marked for refinement. The deploy gate blocks until they are resolved. Reply with refined descriptions for each marker, OR type 'approve placeholders for preview' to proceed (NOT recommended — placeholder copy will be visible on the public preview URL)."
       - If user provides refined descriptions: edit `data/projects.ts` inline, remove the `TODO(user)` markers, re-run Plan 04-01 Task 1's verify gate (Project.parse + D-15 honesty checks), commit `fix(phase-4/w3a): refine TODO(user) descriptions per user response`, then continue.
       - If user explicitly approves placeholders: record the override in the SUMMARY and proceed; the marker count is recorded as a known issue.
    2. Confirm Plan 04-01 + 04-02 commits are pushed: `git log --oneline -8` shows the 4 atomic commits from Plan 04-01 (Tasks 1, 2, 3) + Plan 04-02 Task 4 on the current branch.
    3. Open Vercel dashboard → Deployments → find the deploy triggered by the most recent push (NOT `https://braeden-site.vercel.app` — that is the production alias serving stale cache per STATE.md Phase 1 W4-T2 lesson). Copy the per-deploy URL of the form `braeden-site-<hash>-<scope>.vercel.app`.
    4. Reply to Claude with the exact preview URL string. Claude will then run:
       ```
       BASE_URL=https://<preview-hash>.vercel.app npx playwright test tests/work-*.spec.ts && \
       BASE_URL=https://<preview-hash>.vercel.app npm run test:full
       ```
       (If `playwright.config.ts` does not currently honor `BASE_URL`, Claude amends it to read `process.env.BASE_URL ?? 'http://localhost:3000'` as a one-line documented dev-loop change before the test run.)
    5. Visually walk the 28-item Phase Exit checklist in `04-UI-SPEC.md` § Phase Exit Visual Verification (lines ~559-600) on the preview URL in a real browser. Note any items that fail.
    6. Reply with: (a) the TODO(user) marker count (MUST be 0 to proceed); (b) the Playwright counts (passed / failed / skipped); (c) any of the 28 visual items that failed with one-line cause; (d) approval to proceed to Task 6 (the WebAIM contrast check) OR a request to revise.
  </how-to-verify>
  <action>**Step 1 (pre-deploy gate per checker warning #2):** Run `grep -c 'TODO(user)' data/projects.ts`. If result > 0, surface the affected entries to the user and HALT. Either route back to Plan 04-01 Task 1 to refine descriptions (then re-commit + re-push), OR explicitly accept the user's "approve placeholders for preview" override (record in SUMMARY). Do NOT proceed past this gate until count = 0 OR user has explicitly accepted the override.

Step 2: Push the Plan 04-01 + 04-02 commits if not already pushed (git push). Wait for the user to supply the per-branch Vercel preview URL from the dashboard. Once supplied, set BASE_URL=<preview-url> and re-run the targeted Phase 4 spec set first, then the full suite. If playwright.config.ts does not currently honor BASE_URL via process.env, amend it inline as a documented one-line dev-loop change (read process.env.BASE_URL ?? 'http://localhost:3000' on the use.baseURL field) before the test run. Walk the 28-item UI-SPEC § Phase Exit Visual Verification checklist on the preview URL in a real browser. Do NOT proceed to Task 6 until the user explicitly approves or supplies a revision request.</action>
  <verify>
    <automated>TODO_COUNT=$(grep -c 'TODO(user)' data/projects.ts); if [ "$TODO_COUNT" != "0" ]; then echo "DEPLOY GATE FAIL: data/projects.ts contains $TODO_COUNT TODO(user) markers — route back to Plan 04-01 Task 1 OR get explicit user override before proceeding"; exit 1; fi; echo "DEPLOY GATE PASS: data/projects.ts is clean (0 TODO(user) markers); checkpoint:human-verify continues with Playwright suite execution against the deployed preview URL parameterized by user-supplied BASE_URL; verification command is BASE_URL=https://<preview-hash>.vercel.app npm run test:full and must show 0 failed; the 28-item visual checklist is human-verified in browser"</automated>
  </verify>
  <acceptance_criteria>
    - **Pre-deploy gate:** `grep -c 'TODO(user)' data/projects.ts` returns 0 (OR user has explicitly accepted the override per checker warning #2 fix; override is recorded in the SUMMARY)
    - User has supplied the actual per-branch preview URL (NOT `braeden-site.vercel.app` production alias)
    - Full Playwright suite ran against the preview URL with `BASE_URL` override
    - Result reported: passed / failed / skipped counts; any of the 28 visual checklist items that failed are listed with cause
    - User explicitly approves proceeding to Task 6 OR requests revision (in which case orchestrator re-routes to a fix task)
  </acceptance_criteria>
  <resume-signal>Reply with: (a) "TODO(user) count: 0" OR a list of refined descriptions per remaining marker OR "approve placeholders for preview"; (b) the preview URL; (c) Playwright counts (passed/failed/skipped); (d) visual-checklist exceptions; (e) "approved" or "revise: &lt;item&gt;".</resume-signal>
  <done>Deploy gate cleared (TODO(user) count = 0 OR user override recorded); Vercel branch preview deployed; full suite GREEN against the CDN URL (or specific failures triaged); 28-item visual checklist walked; user approved continuation OR a revision task was inserted before Task 6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (Wave 3b): WebAIM contrast verification on archived dot — execute the documented #707070 → #7a7a7a fallback if it fails</name>
  <files>(potentially modifies: components/work/ProjectCard.tsx + tests/work-status-badges.spec.ts — only if fallback triggers)</files>
  <read_first>
    - .planning/phases/04-work-projects/04-UI-SPEC.md § Color (lines ~155-170 — the contrast verification table; the `#707070`-on-`#1a1a1f` row marked ⚠️ borderline)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Common Pitfalls Pitfall 2 (the documented fallback mechanism)
    - .planning/phases/04-work-projects/04-CONTEXT.md `<specifics>` (the `#7a7a7a` fallback note)
    - components/work/ProjectCard.tsx (the `STATUS_DOT_COLOR['archived']` literal — only file changed if fallback triggers)
    - tests/work-status-badges.spec.ts (the `ARCHIVED_RGB` regex — also updates if fallback triggers)
  </read_first>
  <what-built>Wave 3a verified the deployed preview functionally. This checkpoint executes the one pre-flagged contrast risk: the `#707070` archived status dot on `#1a1a1f` background measures ~3.6:1 against the gradient top in clean conditions, but the grain layer (`opacity 0.04 mix-blend-mode: overlay`) MAY drop it below the WCAG AA UI-element minimum of 3.0:1 once compositing is real (Pitfall 2 / Assumption A8 / D-09 borderline pair).

User must run a real-browser contrast check on the deployed preview URL. If the dot passes ≥3.0:1, no source change. If it fails, Claude bumps `STATUS_DOT_COLOR['archived']` from `#707070` to `#7a7a7a` (still in the deep-grey "past" register) AND updates `tests/work-status-badges.spec.ts`'s `ARCHIVED_RGB` regex from `/rgba?\(\s*112\s*,\s*112\s*,\s*112/` to `/rgba?\(\s*122\s*,\s*122\s*,\s*122/`. Both are 1-line edits; a single atomic commit ships both.</what-built>
  <how-to-verify>
    1. Open the Vercel branch-preview URL from Task 5 in a real browser (Chrome / Safari / Firefox — any).
    2. Navigate to `/work`. Locate the archived braehods card (last card; row 4 left column on desktop if N is odd, otherwise wherever it lands).
    3. Open WebAIM Contrast Checker at https://webaim.org/resources/contrastchecker/.
    4. Use a color picker (browser DevTools "Inspect" + computed style on the dot `<span>`, then copy the hex; OR use a screenshot + a desktop color-picker tool) to read the rendered dot color and the rendered background color directly underneath it (with grain overlay applied).
    5. Enter the two hex values into WebAIM. Read the contrast ratio.
    6. Reply with: (a) measured rendered dot color hex; (b) measured rendered background hex (with grain); (c) WebAIM contrast ratio; (d) PASS (ratio ≥ 3.0:1) or FAIL (ratio < 3.0:1).
    7. If FAIL: Claude executes the documented fallback (1-line change in `ProjectCard.tsx` constant + 1-line regex update in `tests/work-status-badges.spec.ts`) and re-runs the targeted spec to confirm the new color satisfies the regex update. Re-deploys via push.
    8. If PASS: Claude proceeds to Task 7 (final SUMMARY + ROADMAP update) with no source change.
  </how-to-verify>
  <action>Wait for the user to supply: (a) measured rendered dot color hex, (b) measured rendered background hex (with grain overlay applied), (c) WebAIM contrast ratio, (d) PASS/FAIL verdict against the WCAG AA UI-element minimum of 3.0:1. If FAIL: edit components/work/ProjectCard.tsx STATUS_DOT_COLOR['archived'] from '#707070' to the user-confirmed fallback value (default '#7a7a7a'); edit tests/work-status-badges.spec.ts ARCHIVED_RGB regex to match the new RGB values (e.g., /rgba?\(\s*122\s*,\s*122\s*,\s*122/ for #7a7a7a); re-run npx playwright test tests/work-status-badges.spec.ts to confirm GREEN; commit with fix(phase-4/w3): bump archived dot per WebAIM contrast verification; push to trigger re-deploy. If PASS: record the measured ratio in the SUMMARY's User Decisions section and proceed to Task 7 with no source change.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — WebAIM measurement is human-supplied via real-browser color-picker on the deployed preview; if FAIL branch executes, npx playwright test tests/work-status-badges.spec.ts must exit 0 after the literal + regex update"</automated>
  </verify>
  <acceptance_criteria>
    - User has supplied: rendered dot color hex, rendered background hex, WebAIM ratio, PASS/FAIL verdict
    - If FAIL: `STATUS_DOT_COLOR['archived']` updated to `#7a7a7a` (or another agreed grey value) in `components/work/ProjectCard.tsx`; `ARCHIVED_RGB` regex updated correspondingly in `tests/work-status-badges.spec.ts`; targeted spec re-run shows GREEN; re-deploy verified; commit `fix(phase-4/w3): bump archived dot to #7a7a7a per WebAIM contrast verification`
    - If PASS: no source change; the borderline-but-passing measurement is recorded in the SUMMARY for Phase 6 reference
  </acceptance_criteria>
  <resume-signal>Reply with the WebAIM measurement + PASS/FAIL. If FAIL, also confirm the `#7a7a7a` (or alternate) value to use.</resume-signal>
  <done>Contrast verification complete; either no source change (PASS) or the documented fallback shipped + re-verified + re-deployed (FAIL). User approved continuation to Task 7.</done>
</task>

<task type="auto">
  <name>Task 7 (Wave 3c): Write 04-01-SUMMARY.md (consolidated, covers Plans 04-01 + 04-02) + update ROADMAP + STATE + REQUIREMENTS</name>
  <files>.planning/phases/04-work-projects/04-01-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (the canonical SUMMARY template — match its sections verbatim)
    - .planning/phases/03-about-page/03-01-SUMMARY.md (the most recent SUMMARY — match its tone, depth, and structure as the closest analog)
    - .planning/phases/02-home-page/02-07-SUMMARY.md if it exists, else 02-06-SUMMARY.md (the Wave-3 deploy + verify SUMMARY pattern)
    - .planning/ROADMAP.md (Phase 4 entry — needs Plans count flip from "1 plan" to "2 plans" + checkbox flip from `[ ]` to `[x]` on both 04-01 and 04-02 + completion date)
    - .planning/STATE.md (Last Session entry to append; Spec scoreboard to update; carry-forwards to Phase 6 to amend)
    - .planning/REQUIREMENTS.md (Traceability table — flip WORK-01..06 + A11Y-05 from `Pending` to `Complete`; flip the equivalent rows under § Work / Projects + § Accessibility from `[ ]` to `[x]`)
    - All commit hashes from Plan 04-01 (Tasks 1, 2, 3) and Plan 04-02 (Task 4, conditionally Task 6) — needed for the wave-by-wave recap
  </read_first>
  <action>Write `.planning/phases/04-work-projects/04-01-SUMMARY.md` per the canonical SUMMARY template (this is a consolidated SUMMARY covering BOTH Plans 04-01 + 04-02 — there is no separate 04-02-SUMMARY.md, since the two plans together close Phase 4). Match the structure and depth of `.planning/phases/03-about-page/03-01-SUMMARY.md`. Required sections:

1. **Frontmatter** — phase, plan: 01 (consolidated), status: complete, completed_at: <today's date>, duration_estimate, requirements_completed: WORK-01..06 + A11Y-05.
2. **Objective recap** — 1 paragraph; what shipped vs the original Phase 4 goal. Note that Phase 4 was split into 2 plans during planning per checker scope_sanity feedback (8 tasks → 2 plans of 4 tasks each).
3. **Wave-by-wave recap** — bulleted list of each task's commit hash + 1-line summary:
   - Plan 04-01 Wave 0a Task 0 (checkpoint:human-action)
   - Plan 04-01 Wave 0b Task 1 (data/projects.ts populate) — commit hash
   - Plan 04-01 Wave 0c Task 2 (4 RED specs) — commit hash
   - Plan 04-01 Wave 1 Task 3 (ProjectCard component) — commit hash
   - Plan 04-02 Wave 2 Task 4 (page rewrite, flips RED → GREEN) — commit hash
   - Plan 04-02 Wave 3a Task 5 (deploy + Playwright suite verify; TODO(user) gate result)
   - Plan 04-02 Wave 3b Task 6 (WebAIM contrast verify; PASS or FAIL + fallback commit hash if applicable)
   - Plan 04-02 Wave 3c Task 7 (this task)
4. **User Decisions** subsection — record verbatim:
   - The Wave-0a OQ#1/OQ#2/OQ#3 user responses (project count, hrefs, archived title)
   - The Wave-3a TODO(user) marker resolution (refined descriptions OR explicit override)
   - The Wave-3b WebAIM measurement (rendered hex values + ratio + PASS/FAIL + fallback decision)
5. **Net spec movement** — list the 4 newly-GREEN specs; confirm 0 regressions; cite full-suite count (passed / failed / skipped).
6. **Deviations from Plan** — any Rule-N deviations executed during execution (e.g., if the optional ExternalArrowIcon SVG fallback shipped, document why; if the WebAIM check forced the `#7a7a7a` bump, document the measured ratio and the new regex; if the Playwright config needed the BASE_URL amendment, document it).
7. **Phase 6 carry-forwards** — list the items the SUMMARY surfaces for Phase 6:
   - archived braehods `href` post-Phase-6 DNS-swap target (TBD — Phase 6 picks Wayback / repo / `/archive` sub-route)
   - `STATUS_DOT_COLOR` literal promotion to `@theme` revisit if status badges propagate beyond ProjectCard
   - CLAUDE.md `chore(docs)` row update for lucide-react brand icons (still outstanding from Phase 2 Plan 04 SUMMARY; Phase 4 did not consume any lucide brand icon so didn't surface again)
   - Phase 6 Lighthouse audit owns the /work LCP gate (per UI-SPEC § Performance Contract — first-card stagger fallback documented if needed)
8. **Visual milestone** — 1 paragraph describing what the deployed preview shows (the N cards in their grid; CapitolLens leading; archived braehods as the orphan if N is odd; hover states animating; reduced-motion verified).

Then update `.planning/ROADMAP.md`:
- Phase 4 entry: flip `[ ]` to `[x]` and append `Shipped <date>. <one-line summary>.` to the entry's first bullet (mirror Phase 2 + 3 patterns).
- Phase 4 Plans line: `Plans: 2 plans, 4 waves (W0 data + spec stubs + user-input checkpoint, W1 ProjectCard component, W2 page rewrite, W3 deploy + verify + WebAIM contrast check)` — note the count change from "1 plan" to "2 plans"
- Plans list: flip both lines (mirror Phase 2's completion lines):
  - `- [x] 04-01-PLAN.md — W0+W1: populate data/projects.ts, stub 4 RED Playwright specs, ship components/work/ProjectCard.tsx (Server Component, hairline-tile)`
  - `- [x] 04-02-PLAN.md — W2+W3: rewrite app/work/page.tsx (flips RED specs GREEN), deploy to Vercel preview, WebAIM contrast verify, SUMMARY + traceability flip`
- Progress Table row for Phase 4: `2/2` plans complete; status `Complete`; completed date.

Then update `.planning/STATE.md`:
- progress.completed_phases: 4 (was 3)
- progress.completed_plans: 11 (was 9; Phase 4 adds 2 plans)
- progress.percent: recompute consistently
- Last Session entry appended: `*State updated: <date> by execute-phase (Phase 4 Plans 01 + 02 / Wave 3 complete — …)*` mirroring the Phase 2 + 3 entry style.
- Current Position: bump Phase to 5 (next milestone target) once Phase 4 ships; update Branch + Status accordingly.
- Spec scoreboard: increment by +4 newly-GREEN Phase 4 specs.
- Append a new "Last Session (Phase 4 Plans 01 + 02)" subsection summarizing the 7-task wave structure across both plans + commit hashes + duration.

Then update `.planning/REQUIREMENTS.md`:
- Traceability table: flip WORK-01..06 + A11Y-05 from `Pending` to `Complete`.
- Section markdown: under `### Work / Projects (WORK)`, flip each `- [ ]` to `- [x]`. Under `### Accessibility (A11Y)`, flip the A11Y-05 line.

After writing all 4 files, run `git add -A && git status` to verify the changes, then commit with: `docs(phase-4): close Phase 4 — 04-01-SUMMARY.md (consolidated 04-01 + 04-02) + ROADMAP + STATE + REQUIREMENTS traceability flip`.</action>
  <verify>
    <automated>test -f .planning/phases/04-work-projects/04-01-SUMMARY.md && grep -q "WORK-01" .planning/phases/04-work-projects/04-01-SUMMARY.md && grep -q "A11Y-05" .planning/phases/04-work-projects/04-01-SUMMARY.md && grep -q "User Decisions" .planning/phases/04-work-projects/04-01-SUMMARY.md && grep -q "\[x\] \*\*Phase 4" .planning/ROADMAP.md && grep -q "04-02-PLAN.md" .planning/ROADMAP.md && grep -q "WORK-01 | Phase 4 | Complete" .planning/REQUIREMENTS.md && grep -q "A11Y-05 | Phase 4 | Complete" .planning/REQUIREMENTS.md && grep -q "completed_phases: 4" .planning/STATE.md</automated>
  </verify>
  <done>
    - `04-01-SUMMARY.md` exists with all 8 required sections + frontmatter + User Decisions (covering OQ responses, TODO(user) resolution, WebAIM measurement) + Phase 6 carry-forwards
    - `ROADMAP.md` Phase 4 row checked, Plans count updated to "2 plans", BOTH 04-01-PLAN.md and 04-02-PLAN.md lines flipped to `[x]`, completion date set, Progress Table row updated to 2/2
    - `STATE.md` `progress.completed_phases` incremented to 4, `completed_plans` incremented by 2, Spec scoreboard updated, Last Session entry appended
    - `REQUIREMENTS.md` Traceability table shows WORK-01..06 + A11Y-05 as `Complete`; Work + A11Y section checkboxes flipped
    - Final atomic commit lands all 4 files: `docs(phase-4): close Phase 4 — SUMMARY (consolidated 04-01 + 04-02) + ROADMAP + STATE + REQUIREMENTS traceability flip`
    - `git status` is clean after the commit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→browser→our DOM (focus, hover, scroll) on /work | Pure CSS interactivity; no JS event handlers; FOUND-07 invariant holds (zero `'use client'`). The page rewrite in Task 4 is the surface where the grid + N card instances become a runtime DOM. |
| build-time→Vercel CDN→user-browser | Wave 0-2 commits push to a feature branch; Vercel builds the preview; the user accesses the preview URL. The CDN serves the optimized production bundle (different from local dev server). |
| user→browser→external (project repo / live site / video) | Page-level instantiation of the N tabnabbing-prevented `<a target="_blank" rel="noopener noreferrer">` cards from Plan 04-01's ProjectCard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | Page rewrite breaks the LCP element on /work (e.g., placeholder content gets shipped to the live preview URL via unrefined `// TODO(user)` markers in descriptions) | mitigate | The pre-deploy gate in Task 5 (checker warning #2 fix) blocks deploy until `grep -c 'TODO(user)' data/projects.ts` returns 0, OR the user explicitly accepts the override (recorded in SUMMARY). This protects the public preview URL from leaking placeholder copy that could confuse external visitors or be cached. |
| T-04-09 | Information Disclosure | The Vercel branch preview URL is shareable; Phase 6 will own the `X-Robots-Tag: noindex` SEO gate (SEO-09); until then, the preview URL is technically discoverable via referrer leak | accept | Phase 1 + 2 + 3 already shipped on the same Vercel preview surface. Phase 4 inherits the same SEO posture; SEO-09 closes this in Phase 6. The preview URL is per-deploy (hash-based) so it is not stable enough to attract crawlers. |
| T-04-10 | Tampering | The WebAIM fallback (Task 6) edits `STATUS_DOT_COLOR['archived']` AND `ARCHIVED_RGB` regex in two files; if only one is edited, the spec drifts from the source | mitigate | Task 6 acceptance criterion explicitly requires BOTH files updated in a single atomic commit (`fix(phase-4/w3): bump archived dot…`). The targeted spec re-run after the edit (`npx playwright test tests/work-status-badges.spec.ts`) verifies they are in sync — if only one file changed, the spec fails (regex mismatch on rendered color OR rendered color mismatch on regex). |
| T-04-11 | Information Disclosure | The deployed preview URL embeds the user-confirmed canonical hrefs from Plan 04-01 Wave 0a — including any private-repo URLs the user supplied | accept | The Wave-0a checkpoint requires explicit user confirmation per href. Private-repo URLs that the user supplies are by definition the user's own decision; no silent disclosure. The preview URL itself is per-hash and not search-indexed (per T-04-09 + SEO-09 deferral). |
</threat_model>

<verification>
**Per-wave verification gates (sampled per `04-VALIDATION.md` § Sampling Rate):**

- After Task 4 (Wave 2 commit): `npm run typecheck && npm run lint && npm run build && npx playwright test tests/work-*.spec.ts tests/no-client-components.spec.ts tests/focus-ring.spec.ts tests/reduced-motion.spec.ts && npm run test:full` — all 4 Wave-0 RED specs flip GREEN; zero regressions.
- After Task 5 (Wave 3a checkpoint): `grep -c 'TODO(user)' data/projects.ts` returns 0 (deploy gate per checker warning #2 fix); full Playwright suite GREEN against the deployed Vercel branch-preview URL with `BASE_URL` override; 28-item visual checklist walked.
- After Task 6 (Wave 3b checkpoint): WebAIM contrast verification on the rendered archived dot color against the rendered background (with grain) returns a ratio ≥ 3.0:1; if not, the documented `#7a7a7a` fallback ships AND `tests/work-status-badges.spec.ts` ARCHIVED_RGB regex updates AND a single atomic commit lands BOTH (T-04-10 mitigation) AND the re-deployed preview re-passes the targeted spec.
- After Task 7 (Wave 3c commit): `04-01-SUMMARY.md` (consolidated 04-01 + 04-02) + ROADMAP + STATE + REQUIREMENTS all updated; final commit lands; `git status` clean.

**Phase-level success criteria (from ROADMAP § Phase 4):**

1. `/work` renders all N projects in equal-weight grid (WORK-01, WORK-06) — proven by `tests/work-grid-renders.spec.ts` + `tests/work-no-flagship.spec.ts` GREEN against Vercel preview.
2. Project entries typed and live in `data/projects.ts`; editing one adds or modifies a card with no JSX hunt (WORK-02) — proven by Plan 04-01 Task 1's populated literal + Plan 04-02 Task 4's `<ProjectCard>` map iteration in `app/work/page.tsx`.
3. Status badges render as colored dot + label, never color-only (WORK-03, A11Y-05) — proven by `tests/work-status-badges.spec.ts` GREEN.
4. Tags render visibly on each card (WORK-04) — proven by `tests/work-grid-renders.spec.ts` Test 3.
5. Each card links out to repo / site / video with correct external-link semantics (WORK-05) — proven by `tests/work-grid-renders.spec.ts` Test 2 + threat T-04-01 mitigation (in Plan 04-01).
</verification>

<success_criteria>
- [ ] All 7 phase requirement IDs (WORK-01..06 + A11Y-05) flipped to `Complete` in `.planning/REQUIREMENTS.md` Traceability table
- [ ] `04-01-SUMMARY.md` (consolidated 04-01 + 04-02) exists with all required sections + User Decisions subsection covering OQ responses + TODO(user) resolution + WebAIM measurement
- [ ] `.planning/ROADMAP.md` Phase 4 row checked + Plans count updated to "2 plans" + both 04-01-PLAN.md AND 04-02-PLAN.md lines flipped to `[x]` + completion date set
- [ ] `.planning/STATE.md` `progress.completed_phases` = 4, `progress.completed_plans` incremented by 2, Spec scoreboard updated, Last Session entry appended
- [ ] All 4 Wave-0 RED specs (`work-grid-renders`, `work-status-badges`, `work-no-flagship`, `work-descriptions-cliche-scrub`) GREEN against Vercel branch preview
- [ ] All 22+ pre-existing Playwright specs (Phase 1 + 2 + 3 chrome) remain GREEN against Vercel branch preview — zero regression
- [ ] `grep -c 'TODO(user)' data/projects.ts` returns 0 at deploy time (OR user-explicit override recorded in SUMMARY per checker warning #2 fix)
- [ ] WebAIM contrast verification on archived dot pair complete; either ≥ 3.0:1 measured (no source change) or the documented `#7a7a7a` fallback shipped + re-verified (with BOTH ProjectCard literal + status-badges spec regex updated in the same atomic commit per T-04-10)
- [ ] User has confirmed all 28 items of UI-SPEC § Phase Exit Visual Verification (or specific items remediated)
- [ ] Zero `'use client'` directive added in any Phase 4 file (verified by `tests/no-client-components.spec.ts` GREEN)
- [ ] Zero new dependencies installed across Plans 04-01 + 04-02 (verified by `package.json` diff = 0 from Phase 3 close)
- [ ] All commits land atomic; commits cited in the consolidated SUMMARY's wave-by-wave recap
</success_criteria>

<output>
After completion, the consolidated `.planning/phases/04-work-projects/04-01-SUMMARY.md` is the load-bearing handoff to Phase 5 (Contact Modal) — the next phase consumes the established Server-Components-only + tile-treatment + sr-only-h1 patterns and references this SUMMARY for prior-art on the cross-route consumer pattern.
</output>
