---
phase: 04-work-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/projects.ts
  - app/work/page.tsx
  - components/work/ProjectCard.tsx
  - tests/work-grid-renders.spec.ts
  - tests/work-status-badges.spec.ts
  - tests/work-no-flagship.spec.ts
  - tests/work-descriptions-cliche-scrub.spec.ts
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - A11Y-05
tags:
  - nextjs
  - app-router
  - rsc
  - tailwind
  - playwright
  - editorial-dark
  - card-grid
must_haves:
  truths:
    - "Visiting /work shows all 7 (or 8 if user opts in to reel-research-agent at W0) projects rendered as a grid of cards on the deployed Vercel branch preview"
    - "Each project card displays: status badge (colored dot + lowercase schema label), project title with trailing accent ↗ glyph, one-line description, and comma-joined tags line"
    - "Hovering any card transitions its hairline border from #2a2a2f to accent #7c87ff and translates the trailing ↗ glyph +2px right; no card lifts vertically"
    - "Pressing Tab through /work focuses each card with the Phase 1 accent focus ring (no muted-color flash on first frame)"
    - "Clicking any card opens the project's canonical URL in a new tab (target=_blank rel=noopener noreferrer)"
    - "On viewports ≥768px the grid renders 2 columns with cards in the same row matching height via items-stretch; on <768px the grid renders 1 column"
    - "The 7th card (archived braehods) sits left-aligned at half-width in row 4 — NOT centered, NOT spanning 2 columns"
    - "Status dots display the locked color map: shipped→accent #7c87ff, paper-trading→#c8a86a, in-dev→muted #a8a8a8, archived→#707070 (or #7a7a7a fallback if WebAIM check fails on deployed preview)"
    - "All 7 descriptions pass the cliché-ban regex inherited from tests/about-renders.spec.ts and the CapitolLens description contains the literal substring 'paper-only' or 'paper-traded'"
    - "Toggling DevTools 'Emulate prefers-reduced-motion: reduce' on the deployed preview defeats per-card stagger AND the card hover border-color transition AND the arrow translate"
    - "All 22+ pre-existing Playwright specs (Phase 1 + 2 + 3 chrome) remain GREEN — no regression"
    - "Zero 'use client' directive in app/work/, components/work/, or any new file"
  artifacts:
    - path: "data/projects.ts"
      provides: "7 (or 8) populated Project literals with confirmed canonical hrefs and drafted descriptions"
      contains: "export const projects: Project[] ="
      min_entries: 7
    - path: "components/work/ProjectCard.tsx"
      provides: "Server Component rendering a single project as a hairline-tile card with status badge + title + arrow + description + tags"
      exports: ["ProjectCard"]
      must_contain: ["STATUS_DOT_COLOR", "target=\"_blank\"", "rel=\"noopener noreferrer\"", "transition-[border-color,color,transform]", "aria-hidden"]
      must_not_contain: ["'use client'", "transition-colors", "from 'lucide-react'", "from 'next/link'"]
    - path: "app/work/page.tsx"
      provides: "Server Component composing the work grid (sr-only h1 + section landmark + ul of staggered ProjectCards)"
      contains: "ProjectCard"
      must_contain: ["data-test=\"work-section\"", "data-test=\"work-grid\"", "aria-labelledby=\"work-heading\"", "id=\"work-heading\"", "sr-only", "grid-cols-1", "md:grid-cols-2", "items-stretch", "fadeInUp"]
      must_not_contain: ["'use client'", "Coming soon"]
    - path: "tests/work-grid-renders.spec.ts"
      provides: "WORK-01 + WORK-02 + WORK-04 + WORK-05 + WORK-06 spec assertions"
    - path: "tests/work-status-badges.spec.ts"
      provides: "WORK-03 + A11Y-05 dot + label assertions per status color"
    - path: "tests/work-no-flagship.spec.ts"
      provides: "WORK-01 equal-weight clientWidth parity assertion"
    - path: "tests/work-descriptions-cliche-scrub.spec.ts"
      provides: "D-15 voice + cliché regression guard mirroring tests/about-renders.spec.ts"
  key_links:
    - from: "app/work/page.tsx"
      to: "data/projects.ts"
      via: "import { projects } from '@/data/projects'"
      pattern: "import.*projects.*from.*@/data/projects"
    - from: "app/work/page.tsx"
      to: "components/work/ProjectCard.tsx"
      via: "import { ProjectCard } from '@/components/work/ProjectCard'"
      pattern: "import.*ProjectCard.*from.*@/components/work/ProjectCard"
    - from: "app/work/page.tsx"
      to: "lib/motion.ts"
      via: "import { fadeInUp, stagger } from '@/lib/motion'"
      pattern: "import.*fadeInUp.*stagger.*from.*@/lib/motion"
    - from: "components/work/ProjectCard.tsx"
      to: "data/projects.ts"
      via: "import type { Project } from '@/data/projects'"
      pattern: "import type \\{ Project \\} from '@/data/projects'"
---

<objective>
Replace the Phase 2 `Coming soon.` stub at `app/work/page.tsx` with the real `/work` route — an equal-weight responsive grid of all 7 (or 8 if user opts in) projects sourced from a populated `data/projects.ts`. Each card is a Server-Component hairline-tile rendering status badge (colored dot + lowercase label per A11Y-05) + title + accent `↗` glyph + description + tags, wrapping a single external `<a target="_blank" rel="noopener noreferrer">` per D-03. Honors all locked decisions D-01..D-16 from `04-CONTEXT.md` and the full visual + interaction contract in `04-UI-SPEC.md`.

Purpose: Closes Phase 4's 7 requirements (WORK-01..06 + A11Y-05). The /work route becomes the second cross-route consumer of the editorial-tile pattern family established by Phases 1 + 2 (photo tile, channel button, footer social row), translated to card scale.

Output:
- 1 populated data file (`data/projects.ts`)
- 1 new Server Component (`components/work/ProjectCard.tsx`)
- 1 page rewrite (`app/work/page.tsx`)
- 4 new Playwright specs (3 required + 1 optional cliché-scrub recommended per RESEARCH OQ#4)
- Deployed Vercel branch preview verified against the 28-item visual checklist in UI-SPEC § Phase Exit Visual Verification
- WebAIM contrast verification on the borderline `#707070`-on-`#1a1a1f` archived dot pair (with `#7a7a7a` fallback documented if it fails)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-work-projects/04-CONTEXT.md
@.planning/phases/04-work-projects/04-RESEARCH.md
@.planning/phases/04-work-projects/04-UI-SPEC.md
@.planning/phases/04-work-projects/04-PATTERNS.md
@.planning/phases/04-work-projects/04-VALIDATION.md
@.planning/phases/02-home-page/02-SUMMARY.md
@.planning/phases/03-about-page/03-01-SUMMARY.md
@CLAUDE.md

# Source-of-truth files (Phase 1/2/3 patterns the executor MUST mirror)
@data/projects.ts
@app/work/page.tsx
@app/layout.tsx
@app/globals.css
@lib/motion.ts
@components/home/ChannelButton.tsx
@components/home/ChannelButtonRow.tsx
@components/home/CTAArrowLink.tsx
@components/home/CurrentlyLine.tsx
@components/icons/InstagramIcon.tsx
@app/about/page.tsx
@tests/hero-renders.spec.ts
@tests/currently-renders.spec.ts
@tests/about-renders.spec.ts
@tests/footer-socials-render.spec.ts
@tests/no-client-components.spec.ts
@tests/focus-ring.spec.ts
@tests/reduced-motion.spec.ts

<interfaces>
<!-- Key contracts the executor needs to write Phase 4 code without re-discovering them. -->
<!-- All extracted from the source-of-truth files referenced above. -->

From data/projects.ts (LOCKED — DO NOT MODIFY the schema; only populate the array literal):
```typescript
export const ProjectStatus = z.enum(['shipped', 'paper-trading', 'in-dev', 'archived']);
export const ProjectTag = z.enum(['trading', 'content', 'tools', 'archived']);
export const Project = z.object({
  slug: z.string(),
  title: z.string(),
  description: z.string().max(140),
  tags: z.array(ProjectTag),
  status: ProjectStatus,
  href: z.string().url(),
});
export type Project = z.infer<typeof Project>;
export const projects: Project[] = [];   // ← Wave-0 populates this literal
```

From lib/motion.ts (consumed unchanged):
```typescript
export const fadeInUp = 'fade-in-up' as const;
export function stagger(i: number): React.CSSProperties; // returns { '--stagger': `${i * 80}ms` }
```

From app/globals.css @theme block (locked palette — do NOT add new tokens):
- --color-bg-start: #1a1a1f
- --color-bg-end:   #0a0a0a
- --color-text:     #e8e8e8
- --color-muted:    #a8a8a8
- --color-accent:   #7c87ff
- --color-border:   #2a2a2f
- @keyframes fade-in-up + .fade-in-up class (consumed)
- *:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }
- @media (prefers-reduced-motion: reduce) { … defeats animations and transitions … }

Status-color map (LOCKED per D-09 — declare verbatim at top of ProjectCard.tsx):
```typescript
const STATUS_DOT_COLOR: Record<Project['status'], string> = {
  'shipped':       'var(--color-accent)', // #7c87ff (token)
  'paper-trading': '#c8a86a',             // muted amber, inline literal (D-09 — NOT in @theme)
  'in-dev':        'var(--color-muted)',  // #a8a8a8 (token)
  'archived':      '#707070',             // deep grey, inline literal (D-09 — NOT in @theme; fallback #7a7a7a documented if WebAIM check fails on deployed preview)
};
```

ProjectCard component API (LOCKED per UI-SPEC § ProjectCard component):
```typescript
interface ProjectCardProps {
  project: Project;
  staggerIndex: number; // 1..N — caller owns stagger; prop currently unused inside component, prefix with _ but keep wired for forward-compat
}
```

Card root <a> className (LOCKED — verbatim — no additions, no removals):
"group block h-full p-5 rounded border border-[var(--color-border)] transition-[border-color,color,transform] duration-200 ease-[cubic-bezier(0.2,0,0,1)] hover:border-[var(--color-accent)]"

Page grid <ul> className (LOCKED — verbatim):
"grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-6 items-stretch list-none p-0"

Page <section> attributes (LOCKED):
- data-test="work-section"
- aria-labelledby="work-heading"
- className="py-8 md:py-12"

Page <h1> (LOCKED):
- id="work-heading"
- className="sr-only"
- text content: "Work"

Per-card <li> wrapper (LOCKED — animation seam lives HERE not on <a>):
- key={project.slug}
- className={fadeInUp}
- style={stagger(i + 1)}  ← i is 0-indexed map iterator; stagger starts at 1
</interfaces>

<phase_4_canonical_titles>
<!-- Default titles for Wave-0 description drafting. User confirms / overrides at the W0 review checkpoint. -->
- capitollens          → "CapitolLens"
- shorts-factory       → "shorts-factory"
- meme-dashboard       → "meme-dashboard"
- prediction-market-bot→ "prediction-market-bot"  (user supplies if memory has no context)
- no-more-short-form   → "no-more-short-form"     (user supplies)
- mc-packet-client     → "mc-packet-client"       (user supplies)
- braehods-archive     → "braehods.com (v0)"      (planner-recommended per RESEARCH OQ#2; user can override)
</phase_4_canonical_titles>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0 (Wave 0a): Resolve OQ#1 — final project count + canonical hrefs</name>
  <files>(no files written until user responds)</files>
  <read_first>
    - .planning/phases/04-work-projects/04-RESEARCH.md (Open Questions section, OQ#1 + OQ#2 + OQ#3)
    - .planning/phases/04-work-projects/04-CONTEXT.md (D-14 default order; D-16 href waterfall; <deferred> reel-research-agent + school-planner notes)
    - $HOME/.claude/projects/C--Users-Braeden/memory/MEMORY.md (project_reel_research_agent, project_school_planner entries — informs the user's optional 8th/9th picks)
  </read_first>
  <what-built>Nothing yet — this is a pre-population gate. Wave 0a surfaces three deterministic questions to the user before `data/projects.ts` is committed:

1. **OQ#1 — Final project count.** WORK-06 lists 7 projects (CapitolLens, shorts-factory, meme-dashboard, prediction-market-bot, no-more-short-form, mc-packet-client, archived braehods). Memory flags two additional active projects NOT in WORK-06: `reel-research-agent` (Streamlit AI-news drafting tool) and `school-planner` (Canvas+Wiley ICS daily checklist). Adding `reel-research-agent` makes 8 cards (re-evens the 2-col grid to 4 full rows). Adding `school-planner` too makes 9 cards (orphan returns).
2. **OQ#3 — Canonical hrefs.** Per D-16 (live > demo > repo), Claude needs the URL string for each confirmed project before committing the array literal. Defaults Claude will draft if user does not override:
   - CapitolLens → `https://github.com/bwaeden/capitollens` (no live site; assume private repo — if not, user supplies)
   - shorts-factory → `https://github.com/bwaeden/shorts-factory` (assume; user confirms or supplies)
   - meme-dashboard → Streamlit Cloud URL if hosted, else GitHub repo (user supplies)
   - prediction-market-bot → user supplies
   - no-more-short-form → YouTube playlist URL or GitHub repo (user supplies)
   - mc-packet-client → user supplies
   - archived braehods → `https://braehods.com` (the OLD GitHub Pages site is currently live there; URL changes after Phase 6 DNS swap — documented as Phase-6 carry-forward)
3. **OQ#2 — archived braehods title.** Default `braehods.com (v0)` (planner-recommended per RESEARCH OQ#2). User can override to `braehods (v0)` or another framing.
  </what-built>
  <how-to-verify>
    Reply in the format below. Claude will not write to `data/projects.ts` until this checkpoint resolves.

    Required answers:
    a) Project count: choose ONE: "ship 7" (WORK-06 verbatim) | "ship 8 with reel-research-agent" | "ship 9 with reel-research-agent + school-planner"
    b) For each project on the chosen list, supply the canonical href OR confirm the default. Format: `slug: <url-or-OK-default>`
    c) archived braehods title: confirm `braehods.com (v0)` or supply override

    Example response:
    ```
    a) ship 8 with reel-research-agent
    b) capitollens: OK-default
       shorts-factory: OK-default
       meme-dashboard: https://meme-dashboard-bwaeden.streamlit.app
       prediction-market-bot: https://github.com/bwaeden/prediction-market-bot
       no-more-short-form: https://www.youtube.com/@nomoreshortform
       mc-packet-client: https://github.com/bwaeden/mc-packet-client
       reel-research-agent: https://github.com/bwaeden/reel-research-agent
       braehods-archive: OK-default
    c) braehods.com (v0)
    ```
  </how-to-verify>
  <action>Pause execution. Surface the three deterministic questions above to the user and wait for the response. Do NOT write to data/projects.ts until the user has supplied (a) project count choice, (b) per-project href confirmations or overrides, and (c) the archived braehods title confirmation. Record the user's exact response (verbatim) in working memory so Task 7 (the SUMMARY writer) can include it in the User Decisions section.</action>
  <verify>
    <automated>echo "checkpoint:human-action — pauses for user response per resume-signal; no automated verification possible at this gate; downstream Task 1 verify gate enforces the consequences (Project.parse on every populated entry)"</automated>
  </verify>
  <acceptance_criteria>
    - User has supplied (a) project count choice (one of: 7 / 8 / 9)
    - User has supplied (b) one href per project on the chosen list (defaults explicitly accepted with `OK-default` are acceptable)
    - User has supplied (c) archived braehods title confirmation or override
    - Claude has recorded the answers in the SUMMARY's "User Decisions" section before proceeding to Task 1
  </acceptance_criteria>
  <resume-signal>Reply with the three-section response above. Type "use all defaults" to ship 7 with all default hrefs and the default `braehods.com (v0)` title (CapitolLens + shorts-factory + meme-dashboard repos at github.com/bwaeden/&lt;slug&gt;, archived braehods at https://braehods.com, others as best-guess GitHub repo URLs flagged for description-review fix).</resume-signal>
  <done>User responses logged; Wave 1 task list adjusted (project count = 7, 8, or 9); Claude proceeds to Task 1 with confirmed inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0b): Populate data/projects.ts with confirmed entries + draft descriptions</name>
  <files>data/projects.ts</files>
  <read_first>
    - data/projects.ts (current state — verify schema + empty array literal still exact)
    - .planning/phases/04-work-projects/04-UI-SPEC.md § Project Order + Data Contract + Copywriting Contract (lines ~322-344, 415-444)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Code Examples Example 3 (verbatim reference for the populated literal shape)
    - .planning/phases/04-work-projects/04-PATTERNS.md § data/projects.ts (sibling typed-array pattern from data/channels.ts)
    - $HOME/.claude/projects/C--Users-Braeden/memory/MEMORY.md (CapitolLens form4 strategy, shorts-factory Remotion CLI, meme-dashboard Streamlit scorer entries — verbatim source for description content)
    - tests/about-renders.spec.ts (cliché-ban regex on lines 27-28 — descriptions MUST pass this)
    - Wave 0a SUMMARY entry "User Decisions" subsection (project count + hrefs + archived title from Task 0 response)
  </read_first>
  <behavior>
    - Test 1: `data/projects.ts` exports `projects: Project[]` with length matching the user-confirmed count (7, 8, or 9 per Task 0 response)
    - Test 2: First entry has slug `'capitollens'` and `status: 'paper-trading'` and `tags: ['trading', 'tools']`
    - Test 3: Last entry has slug `'braehods-archive'` and `status: 'archived'` and `tags` includes `'archived'`
    - Test 4: Every entry's `href` parses with `new URL(entry.href)` (no syntax errors); every `description.length` is `<= 140`
    - Test 5: `Project.parse(entry)` succeeds for every entry (Zod runtime validation passes — slugs, titles, descriptions ≤140, tag enums, status enums, URL strings)
    - Test 6: CapitolLens description contains the literal substring `'paper-only'` OR `'paper-traded'` (D-15 + memory `feedback_capitollens_auto_follow.md` honesty marker)
    - Test 7: Concatenated descriptions do NOT match the cliché-ban regex from `tests/about-renders.spec.ts` line 28: `/passionate|I love to learn|driven by|innovative|cutting[-\s]edge|lifelong learner|wear many hats|results[-\s]oriented|outcome[-\s]driven/i`
    - Test 8: No description contains `!` (exclamation point — D-15 voice guardrail)
    - Test 9: No description contains an emoji (rough heuristic: no chars in the U+1F300–U+1FAFF range)
  </behavior>
  <action>Replace line 16 of `data/projects.ts` (`export const projects: Project[] = [];`) with a populated array literal of the user-confirmed length (7 / 8 / 9 from Task 0). Preserve lines 1-15 (Zod schema + type) verbatim — the schema is locked. Above the array opening bracket, add a leading comment block documenting (a) order rationale per D-14 (CapitolLens leads, archived braehods last), (b) canonical-href waterfall per D-16, (c) post-Phase-6 DNS-swap carry-forward note for the archived braehods entry.

For each entry, Claude drafts:
- `slug`: kebab-case from the canonical project name (`capitollens`, `shorts-factory`, `meme-dashboard`, `prediction-market-bot`, `no-more-short-form`, `mc-packet-client`, `braehods-archive`, plus optional `reel-research-agent` / `school-planner` if Task 0 added them)
- `title`: the canonical display name (e.g., `CapitolLens`, `shorts-factory`, `braehods.com (v0)` for the archived entry per Task 0 confirmation)
- `description`: ≤140 chars, drafted from memory context where available — CapitolLens MUST contain `paper-only` or `paper-traded`; shorts-factory references Remotion-based YT Shorts CLI (`scripts/make-short.mjs`); meme-dashboard references Streamlit backtest scorer; reel-research-agent (if present) references the 6-phase GSD Streamlit drafting tool. For prediction-market-bot, no-more-short-form, mc-packet-client (and school-planner if added) where Claude has no memory context, draft a placeholder that names the artifact concretely (e.g., "Personal trading bot for prediction markets — in-development.") and flag it inline with `// TODO(user): refine description` so the user notices at the SUMMARY review
- `tags`: pick from the locked `ProjectTag` enum (`trading` / `content` / `tools` / `archived`) — multiple per project where appropriate
- `status`: pick from the locked `ProjectStatus` enum — CapitolLens MUST be `paper-trading` per memory `project_capitollens_trading_mode.md`; archived braehods MUST be `archived`; shorts-factory + meme-dashboard default to `in-dev`; others default to `in-dev` unless the user-supplied href hints at a `shipped` artifact
- `href`: the user-confirmed URL string from Task 0 (or the OK-default if `OK-default` was indicated)

Voice contract (D-15 + ABOUT-01 inheritance — match Phase 3 bio voice): warm + concrete; no emoji; no exclamation points; no cliché-ban phrases (`passionate`, `I love to learn`, `driven by`, `innovative`, `cutting-edge`, `lifelong learner`, `wear many hats`, `results-oriented`, `outcome-driven`); honest about status (paper-only / in-dev / archived where applicable).

Run `npx tsc --noEmit` after writing to verify the populated literal type-checks against the locked schema. Run a one-off `node` snippet importing the array and calling `Project.parse()` on each entry to verify Zod runtime validation (per RESEARCH OQ#6 belt-and-suspenders recommendation — execute this as part of `<verify>` not as a permanent test).</action>
  <verify>
    <automated>npm run typecheck && node -e "import('./data/projects.ts').then(m => { const errs = []; for (const p of m.projects) { const r = m.Project.safeParse(p); if (!r.success) errs.push({slug: p.slug, error: r.error.flatten()}); } if (errs.length) { console.error(JSON.stringify(errs, null, 2)); process.exit(1); } console.log('OK ' + m.projects.length + ' entries'); })"</automated>
  </verify>
  <done>
    - `data/projects.ts` exports `projects: Project[]` with the user-confirmed length (verify via the node one-liner above prints `OK <N> entries`)
    - `npm run typecheck` exits 0
    - Every entry passes `Project.parse()` (no Zod errors)
    - First entry slug = `capitollens`; last entry slug = `braehods-archive`; CapitolLens description contains `paper-only` or `paper-traded`
    - Atomic commit: `feat(phase-4/w0): populate data/projects.ts with N entries (per D-14 order, D-16 href waterfall)`
  </done>
</task>

<task type="auto">
  <name>Task 2 (Wave 0c): Stub 4 RED Playwright specs (work-grid-renders, work-status-badges, work-no-flagship, work-descriptions-cliche-scrub)</name>
  <files>tests/work-grid-renders.spec.ts, tests/work-status-badges.spec.ts, tests/work-no-flagship.spec.ts, tests/work-descriptions-cliche-scrub.spec.ts</files>
  <read_first>
    - tests/hero-renders.spec.ts (analog for work-grid-renders.spec.ts — section selector + literal-text + count assertions; lines 1-50)
    - tests/currently-renders.spec.ts (analog for work-status-badges.spec.ts — `getComputedStyle.backgroundColor` regex + aria-hidden dot pattern; lines 1-60)
    - tests/footer-socials-render.spec.ts (analog for work-no-flagship.spec.ts — multi-element count + sibling iteration; lines 1-50)
    - tests/about-renders.spec.ts (analog for work-descriptions-cliche-scrub.spec.ts — REUSE the `CLICHE_BAN_REGEX` literal from line 28 verbatim; full file)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Code Examples Examples 4-6 (lines ~660-751 — verbatim spec reference)
    - .planning/phases/04-work-projects/04-PATTERNS.md § Test specs (analogs + assertion shapes)
    - .planning/phases/04-work-projects/04-VALIDATION.md (per-task verification map; sampling rate)
    - tests/no-client-components.spec.ts (regression spec already exists — do NOT modify; this Wave-0 task adds /work to its scan tree implicitly via the new files)
    - data/projects.ts (the user-confirmed N from Task 1 — specs hard-code the expected count)
  </read_first>
  <action>Create 4 new spec files. Each opens with the project-standard JSDoc header (req IDs + on `/work` bullets + "RED until …" marker per the Phase 1-3 convention documented in `04-PATTERNS.md` § Shared Patterns § Test-spec docstring). Use the user-confirmed count `N` from Task 1 as the `toHaveCount(N)` literal where applicable.

`tests/work-grid-renders.spec.ts` — covers WORK-01, WORK-02, WORK-04, WORK-05, WORK-06:
- Test 1 (`WORK-01, WORK-06: /work renders N ProjectCards in the grid`): goto `/work`; assert `[data-test="work-section"]` toHaveCount(1); assert `[data-test="work-grid"]` toHaveCount(1); assert `[data-test="work-grid"] > li` toHaveCount(N).
- Test 2 (`WORK-02, WORK-05: each card has external <a> + status label matching schema enum`): iterate cards; for each: assert `card.locator('a[target="_blank"]')` toHaveCount(1); assert each `<a>` has `rel` attribute containing both `noopener` and `noreferrer`; assert `card.locator('text=/^(shipped|paper-trading|in-dev|archived)$/')` toHaveCount(1).
- Test 3 (`WORK-04: tags render as visible comma-joined text`): iterate cards; assert each contains a `<p>` whose text matches `/^[a-z, ]+$/` (lowercase letters, commas, spaces only — the tags line). Skip pairing with specific tag values since user can reorder; the shape check is sufficient.

`tests/work-status-badges.spec.ts` — covers WORK-03, A11Y-05:
- Define 4 RGB regex constants at the top: `SHIPPED_RGB = /rgba?\(\s*124\s*,\s*135\s*,\s*255/`, `PAPER_TRADING_RGB = /rgba?\(\s*200\s*,\s*168\s*,\s*106/`, `IN_DEV_RGB = /rgba?\(\s*168\s*,\s*168\s*,\s*168/`, `ARCHIVED_RGB = /rgba?\(\s*112\s*,\s*112\s*,\s*112/`. Add a comment noting `ARCHIVED_RGB` may need to update to `/rgba?\(\s*122\s*,\s*122\s*,\s*122/` if the Wave-3 WebAIM check forces the `#7a7a7a` fallback.
- Test 1 (`WORK-03, A11Y-05: every card has status dot + label`): iterate cards; for each: assert `card.locator('span[aria-hidden].rounded-full')` first toHaveCount(1) (the dot); assert `card.locator('text=/^(shipped|paper-trading|in-dev|archived)$/')` first toHaveCount(1) (the label).
- Test 2 (`WORK-03: dot color matches STATUS_DOT_COLOR for declared status`): iterate cards; read the label text; read `getComputedStyle(dot).backgroundColor`; switch on label and assert `bg.toMatch(<corresponding-RGB>)`.

`tests/work-no-flagship.spec.ts` — covers WORK-01 equal-weight verification:
- Test 1 (`WORK-01, D-08: cards in the same row have matching width at md viewport`): `await page.setViewportSize({ width: 1024, height: 800 })`; goto `/work`; locate `[data-test="work-grid"] > li`; `evaluateAll` to read each `el.clientWidth`; assert `widths[0] === widths[1]`, `widths[2] === widths[3]`, `widths[4] === widths[5]`; if N >= 7 assert `widths[6] === widths[0]` (orphan must NOT span — same width as row-mates per D-06); if N === 8 assert `widths[6] === widths[7]`; if N === 9 assert `widths[6] === widths[7]` and `widths[8] === widths[0]`.
- Test 2 (optional sub-assertion — same row heights via items-stretch per D-07): for each row pair (0,1), (2,3), (4,5): read `clientHeight` of the inner `<a>` (NOT the `<li>`) per Pitfall 6; assert pair heights are equal within 1px tolerance.

`tests/work-descriptions-cliche-scrub.spec.ts` — covers D-15 (per RESEARCH OQ#4 recommendation):
- Reuse `CLICHE_BAN_REGEX` literal from `tests/about-renders.spec.ts` line 28 verbatim — copy-paste, do not re-derive.
- Test 1 (`D-15: all card descriptions pass cliché ban`): goto `/work`; extract description text via `page.locator('[data-test="work-grid"] li p').first()` per card OR `allInnerTexts()` joined; assert `.not.toMatch(CLICHE_BAN_REGEX)`.
- Test 2 (`D-15: no exclamation points in card copy`): same source string; assert `.not.toContain('!')`.

All 4 specs MUST be RED at this point — `tests/work-grid-renders.spec.ts` should fail because `[data-test="work-grid"]` does not exist yet (Wave 2 adds it); the others fail similarly. Verify RED state by running the targeted command in `<verify>`.

Do NOT use fenced code blocks in the action; assertion shapes are described above and verbatim references live in `04-RESEARCH.md` Code Examples 4-6 + `04-PATTERNS.md` excerpts cited in `<read_first>`.</action>
  <verify>
    <automated>npx playwright test tests/work-grid-renders.spec.ts tests/work-status-badges.spec.ts tests/work-no-flagship.spec.ts tests/work-descriptions-cliche-scrub.spec.ts 2>&1 | tee /tmp/work-w0-red.log; grep -E "(failed|passed)" /tmp/work-w0-red.log; grep -q "passed" /tmp/work-w0-red.log || (echo "Wave-0 specs must collect cleanly even when RED — Playwright reports per-test status" && exit 1)</automated>
  </verify>
  <done>
    - All 4 spec files exist at the listed paths with the project-standard JSDoc header
    - `npx playwright test --list` shows the 4 new specs collected (no parse errors)
    - Targeted run shows assertions FAILING (RED) — this is the expected Wave-0 state; Wave 2 flips them GREEN
    - `npm run typecheck` + `npm run lint` both exit 0 (no spec-file syntax errors)
    - Atomic commit: `test(phase-4/w0): stub 4 RED Playwright specs for WORK-01..06 + A11Y-05 + D-15 cliché-scrub`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 1): Create components/work/ProjectCard.tsx (Server Component, hairline-tile card)</name>
  <files>components/work/ProjectCard.tsx</files>
  <read_first>
    - components/home/ChannelButton.tsx (PRIMARY analog — hairline-tile + group-hover + external `<a>` pattern; lines 1-90 in full — Phase 4 ProjectCard is the card-scale version)
    - components/home/CTAArrowLink.tsx (group-hover translate-arrow pattern; lines 28-43)
    - components/home/CurrentlyLine.tsx (aria-hidden colored dot + inline-style color cascade pattern; lines 1-55)
    - components/icons/InstagramIcon.tsx (only if shipping the optional ExternalArrowIcon fallback — pattern reference; lines 1-46)
    - components/layout/SocialIconLink.tsx (transition-property discipline — lines 18-32 are the bug-fix doc; CRITICAL — apply same `transition-[…]` pattern)
    - .planning/phases/04-work-projects/04-UI-SPEC.md § ProjectCard component (lines ~220-318 — the full visual + implementation contract)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Code Examples Example 1 (lines ~478-567 — verbatim reference implementation)
    - .planning/phases/04-work-projects/04-PATTERNS.md § components/work/ProjectCard.tsx (lines ~63-198 — line-cited pattern excerpts)
    - data/projects.ts (the `Project` type the component imports)
    - app/globals.css (the @theme tokens the component references via `var(--color-*)`)
    - tests/work-status-badges.spec.ts (the spec the component MUST satisfy — wired by Task 2 RED)
    - .planning/phases/02-home-page/02-04-SUMMARY.md (the `transition-[color]` Rule 1 fix narrative — same bug class avoided here)
  </read_first>
  <behavior>
    - Test 1: `ProjectCard` is a named export from `components/work/ProjectCard.tsx`
    - Test 2: File contains zero `'use client'` (Server Component)
    - Test 3: File contains zero imports from `'lucide-react'` and zero imports from `'next/link'`
    - Test 4: File declares `STATUS_DOT_COLOR` as a `Record<Project['status'], string>` with exactly 4 keys: `shipped`, `paper-trading`, `in-dev`, `archived`
    - Test 5: `STATUS_DOT_COLOR['shipped'] === 'var(--color-accent)'`; `STATUS_DOT_COLOR['paper-trading'] === '#c8a86a'`; `STATUS_DOT_COLOR['in-dev'] === 'var(--color-muted)'`; `STATUS_DOT_COLOR['archived'] === '#707070'`
    - Test 6: Component root is `<a>` with `target="_blank"` AND `rel` containing `noopener noreferrer`
    - Test 7: `<a>` className contains `transition-[border-color,color,transform]` (substring match) and does NOT contain `transition-colors` (the shorthand — Phase 2 lesson)
    - Test 8: `<a>` className contains `group`, `block`, `h-full`, `p-5`, `rounded`, `border`, `border-[var(--color-border)]`, `hover:border-[var(--color-accent)]`, `duration-200`, `ease-[cubic-bezier(0.2,0,0,1)]`
    - Test 9: Status badge dot is a `<span>` with `aria-hidden`, `rounded-full`, `w-1.5 h-1.5`, and `style={{ backgroundColor: STATUS_DOT_COLOR[project.status] }}`
    - Test 10: Status label is a `<span>` rendering `{project.status}` verbatim (lowercase) with `style={{ color: 'var(--color-muted)' }}` and `text-sm font-sans` classes
    - Test 11: Title section renders `{project.title}` followed by `{' '}` then a `<span aria-hidden>` containing `↗` (Unicode U+2197) with `style={{ color: 'var(--color-accent)' }}` and `inline-block transition-transform group-hover:translate-x-0.5` classes
    - Test 12: Description renders `{project.description}` inside `<p className="mt-2 text-sm font-sans">` with `style={{ color: 'var(--color-text)' }}`
    - Test 13: Tags render `{project.tags.join(', ')}` inside `<p className="mt-2 text-sm font-sans">` with `style={{ color: 'var(--color-muted)' }}`
    - Test 14: Component accepts `staggerIndex: number` prop in its interface (prefix the destructured param with `_` to silence TS unused-var; prop is forward-compat per Pitfall 5 — animation lives on the wrapper `<li>`, not inside the card)
  </behavior>
  <action>Create `components/work/ProjectCard.tsx` as a Server Component. The full reference implementation is in `04-RESEARCH.md` Code Examples Example 1 (lines ~478-567) — match its file-header comment block (provenance + transition-discipline doc + token-discipline note + RSC invariant), `STATUS_DOT_COLOR` constant declaration, `ProjectCardProps` interface, and JSX tree verbatim except where noted below.

File-header comment block opens with `// components/work/ProjectCard.tsx` then `// Source: PLAN.md 04-01-T3; 04-UI-SPEC § ProjectCard component (D-01..D-04, D-09..D-12).` followed by paragraphs documenting:
- Server Component invariant (FOUND-07 / D-25 inherited; mirrors `components/home/ChannelButton.tsx` lines 5-9 source-attribution convention)
- Card-scale translation of `components/home/ChannelButton.tsx` (cite line 78 transition discipline)
- Token discipline for the 2 inline literals (`#c8a86a`, `#707070` — NOT in @theme per D-09)
- The `transition-[border-color,color,transform]` arbitrary-list rationale citing the Phase 2 Plan 02-04 Rule 1 fix in `components/layout/SocialIconLink.tsx` lines 18-32

Imports: `import type { Project } from '@/data/projects';` — that is the only import. (No icon import unless the optional ExternalArrowIcon fallback ships per Claude's Discretion — default to the Unicode `↗` glyph; ship the SVG fallback ONLY if visual verification at Wave 3 flags glyph inconsistency. If the SVG fallback ships, add `import { ExternalArrowIcon } from '@/components/icons/ExternalArrowIcon';`.)

`STATUS_DOT_COLOR` constant declaration — verbatim per `<interfaces>` block above. Place it between the imports and the interface definition. Do NOT export it (project-private to ProjectCard.tsx).

`ProjectCardProps` interface — verbatim per `<interfaces>` block. The `staggerIndex` prop is unused inside the component body (animation lives on the parent `<li>` per Pitfall 5); destructure as `staggerIndex: _staggerIndex` to silence the TS `noUnusedParameters` warning while keeping the prop wired for forward-compat.

JSX tree (top-down inside the component's return — match UI-SPEC § ProjectCard component lines 252-279):

1. **Root `<a>`** — href, target, rel, and the LOCKED className from the `<interfaces>` block above. Do NOT add `cursor-pointer` (browser default). Do NOT add any `outline-*` utility (Phase 1 global `*:focus-visible` rule applies automatically).

2. **Status badge** — `<div className="flex items-center gap-2">` containing:
   - Dot: `<span aria-hidden className="inline-block w-1.5 h-1.5 rounded-full" style={{ backgroundColor: STATUS_DOT_COLOR[project.status] }} />`
   - Label: `<span className="text-sm font-sans" style={{ color: 'var(--color-muted)' }}>{project.status}</span>`

3. **Title row** — `<div className="mt-4 text-base font-sans" style={{ color: 'var(--color-text)' }}>` containing:
   - `{project.title}` as a plain text node
   - `{' '}` (single space — ensures the arrow does not adhere to the title)
   - `<span aria-hidden className="inline-block transition-transform group-hover:translate-x-0.5" style={{ color: 'var(--color-accent)' }}>↗</span>`

4. **Description** — `<p className="mt-2 text-sm font-sans" style={{ color: 'var(--color-text)' }}>{project.description}</p>`

5. **Tags** — `<p className="mt-2 text-sm font-sans" style={{ color: 'var(--color-muted)' }}>{project.tags.join(', ')}</p>`

CRITICAL discipline (do NOT deviate):
- The `transition-[border-color,color,transform]` arbitrary list MUST appear verbatim — `transition-colors` shorthand causes the `:focus-visible` accent ring to interpolate from inherited muted to accent on Tab focus per Phase 2 Plan 02-04 Rule 1 fix (regresses `tests/focus-ring.spec.ts`). This is the SAME bug class fixed in `components/layout/Footer.tsx` line 76 + `components/layout/SocialIconLink.tsx` lines 18-32.
- The animation seam (`fadeInUp` + `stagger()`) lives on the parent `<li>` wrapper added by Wave 2's page rewrite — NOT here. The card's hover `transform` (arrow translate) and the keyframe's `transform: translateY(0)` end-state would collide if both lived on the same element per Pitfall 5.
- The `<a>` inner content must NOT use `next/link` — external destination, no prefetch needed; plain `<a>` is correct per Pitfall 4.
- The `aria-hidden` on the dot AND on the trailing arrow is required: the dot is decorative-redundant per A11Y-05 (the label carries the info); the arrow is decorative because the `<a target="_blank">` already carries external-link semantics.

Run `npm run typecheck` after writing — verify `STATUS_DOT_COLOR[project.status]` lookup compiles cleanly under `noUncheckedIndexedAccess` (TS treats `Record<Project['status'], string>` lookup as total per Anti-Pattern A4 in RESEARCH).</action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx playwright test tests/no-client-components.spec.ts</automated>
  </verify>
  <done>
    - `components/work/ProjectCard.tsx` exists with the source-attribution header, locked `STATUS_DOT_COLOR` constant, `ProjectCardProps` interface, and the locked JSX tree
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `tests/no-client-components.spec.ts` GREEN (verifies zero `'use client'` in `app/`, `components/`, `lib/` — including the new `components/work/`)
    - File contains zero imports from `'lucide-react'` and zero imports from `'next/link'` (grep verification)
    - Atomic commit: `feat(phase-4/w1): components/work/ProjectCard.tsx (hairline-tile card; Server Component; D-01..D-04, D-09..D-12)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (Wave 2): Rewrite app/work/page.tsx (compose grid + flip RED specs GREEN)</name>
  <files>app/work/page.tsx</files>
  <read_first>
    - app/work/page.tsx (current Plan 02-05 stub — verify the 18-line `Coming soon.` body before replacing)
    - app/about/page.tsx (PRIMARY analog — sr-only h1 + aria-labelledby section landmark + per-item map; lines 1-50)
    - components/home/Hero.tsx (composition pattern — multiple data imports + lib/motion stagger; lines 1-90)
    - components/home/ChannelButtonRow.tsx (per-item `<li>`-wrapper stagger pattern — animation seam discipline; lines 1-40)
    - components/work/ProjectCard.tsx (just created in Task 3 — the consumer)
    - data/projects.ts (just populated in Task 1 — the source array)
    - lib/motion.ts (verbatim API — `fadeInUp` + `stagger(i)`)
    - app/layout.tsx (the inherited `max-w-3xl mx-auto px-6 py-16 lg:px-12` container — page does NOT widen)
    - .planning/phases/04-work-projects/04-UI-SPEC.md § Page Composition (lines ~174-216) + § Motion Choreography (lines ~352-411)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Code Examples Example 2 (lines ~571-619 — verbatim reference for the page rewrite)
    - .planning/phases/04-work-projects/04-PATTERNS.md § app/work/page.tsx (lines ~201-289 — analog excerpts)
    - tests/work-grid-renders.spec.ts + tests/work-status-badges.spec.ts + tests/work-no-flagship.spec.ts + tests/work-descriptions-cliche-scrub.spec.ts (the RED specs this task flips GREEN — match their selector expectations exactly)
  </read_first>
  <behavior>
    - Test 1: `app/work/page.tsx` exports a default function and `metadata = { title: 'Work' }` (preserve Plan 02-05's metadata export verbatim)
    - Test 2: File contains zero `'use client'` directive
    - Test 3: File imports `projects` from `'@/data/projects'`, `ProjectCard` from `'@/components/work/ProjectCard'`, and `fadeInUp, stagger` from `'@/lib/motion'`
    - Test 4: Rendered HTML contains `<section data-test="work-section" aria-labelledby="work-heading" class="py-8 md:py-12">`
    - Test 5: Rendered HTML contains `<h1 id="work-heading" class="sr-only">Work</h1>`
    - Test 6: Rendered HTML contains `<ul data-test="work-grid"` with className containing `grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-6 items-stretch list-none p-0`
    - Test 7: The `<ul>` contains exactly `projects.length` `<li>` direct children (the user-confirmed N from Task 1)
    - Test 8: Each `<li>` has `className` of `fade-in-up` (the resolved value of `fadeInUp`) AND inline `style` containing `--stagger:` with a `${(i+1) * 80}ms` value
    - Test 9: Each `<li>` contains exactly one `<a target="_blank" rel="noopener noreferrer">` (delegated to ProjectCard)
    - Test 10: All 4 Wave-0 RED specs flip GREEN: `tests/work-grid-renders.spec.ts`, `tests/work-status-badges.spec.ts`, `tests/work-no-flagship.spec.ts`, `tests/work-descriptions-cliche-scrub.spec.ts`
    - Test 11: All Phase 1 + 2 + 3 chrome specs remain GREEN (no regression) — `npm run test:full` passes
  </behavior>
  <action>Replace the entire body of `app/work/page.tsx` (currently 18 lines, lines 1-18 from Plan 02-05) with the page composition described in UI-SPEC § Page Composition lines 197-216. The full reference implementation is in `04-RESEARCH.md` Code Examples Example 2 (lines ~571-619) — match its source-attribution header verbatim, its imports, its metadata export, and its JSX tree.

File-header comment block opens with `// app/work/page.tsx` then `// Source: PLAN.md 04-01-T4; 04-UI-SPEC § Page Composition (D-05, D-06, D-07, D-13).` followed by paragraphs documenting:
- Replaces the Plan 02-05 `Coming soon.` stub
- Server Component invariant (zero `'use client'`)
- Inherits Nav + Footer chrome from `app/layout.tsx` automatically
- Inherits `max-w-3xl mx-auto px-6 py-16 lg:px-12` container — does NOT extend to `max-w-5xl` (D-05 + Phase 3 inheritance)
- Grid: `grid-cols-1` mobile / `md:grid-cols-2` desktop / `items-stretch` row equalization per D-05 + D-07
- Orphan-card treatment per D-06 (default CSS Grid behavior; no special markup, no spans, no centering)
- Stagger seam discipline per Pitfall 5 (animation lives on the `<li>` wrapper, NOT on the inner `<a>`, to avoid `transform` collision with the card's hover `translate-x` arrow)

Imports — exactly 3 lines:
- `import { fadeInUp, stagger } from '@/lib/motion';`
- `import { projects } from '@/data/projects';`
- `import { ProjectCard } from '@/components/work/ProjectCard';`

Metadata — preserve verbatim from Plan 02-05:
- `export const metadata = { title: 'Work' };`

JSX tree — match the LOCKED structure from `<interfaces>` block above (verbatim from UI-SPEC § Page Composition):

1. `<section data-test="work-section" aria-labelledby="work-heading" className="py-8 md:py-12">` — page root.
2. `<h1 id="work-heading" className="sr-only">Work</h1>` — sr-only heading per D-13. NO visible heading. Tailwind v4's built-in `sr-only` utility (already verified working on `app/about/page.tsx` per Phase 3 — Assumption A1).
3. `<ul data-test="work-grid" className="grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-6 items-stretch list-none p-0">` — semantic list per UI-SPEC line 203. `list-none p-0` strips browser default `<ul>` styling.
4. `{projects.map((project, i) => (...))}` — map iteration. Each `<li>`:
   - `key={project.slug}`
   - `className={fadeInUp}` (resolves to the literal string `'fade-in-up'`)
   - `style={stagger(i + 1)}` (resolves to `{ '--stagger': '${(i + 1) * 80}ms' }` — first card 80ms, last card N×80ms)
   - Children: `<ProjectCard project={project} staggerIndex={i + 1} />`
5. Close `</ul>` and `</section>`.

CRITICAL discipline (do NOT deviate):
- The animation seam MUST live on the `<li>` wrapper. Do NOT pass `className={fadeInUp}` to `<ProjectCard>` (would put the keyframe on the inner `<a>` and collide with the hover arrow `translate-x` per Pitfall 5).
- The grid container MUST be a `<ul>` (semantic list per UI-SPEC line 203). Do NOT use `<div>` — would weaken screen-reader semantics.
- The `data-test="work-grid"` selector MUST be on the `<ul>` (the spec `tests/work-no-flagship.spec.ts` queries `[data-test="work-grid"] > li`).
- The orphan card (7th, or 8th/9th if the user added optional projects) MUST sit at the natural CSS Grid default position (left column of the last row, with the right column empty) per D-06. Do NOT add any `col-span-*` or `justify-self-*` utility — the empty cell IS the design.
- The `<section>` className `py-8 md:py-12` MUST match exactly (matches Phase 2 home hero + Phase 3 about per CD-05 inheritance).

After writing, run `npm run dev` (or build + start) and curl `http://localhost:3000/work` to verify the rendered HTML contains all 5 expected `data-test` selectors and all card content. Then run the full Playwright suite to verify the 4 Wave-0 RED specs flip GREEN with zero Phase 1/2/3 regressions.</action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npx playwright test tests/work-grid-renders.spec.ts tests/work-status-badges.spec.ts tests/work-no-flagship.spec.ts tests/work-descriptions-cliche-scrub.spec.ts tests/no-client-components.spec.ts tests/focus-ring.spec.ts tests/reduced-motion.spec.ts && npm run test:full</automated>
  </verify>
  <done>
    - `app/work/page.tsx` body replaced; the `Coming soon.` `<p>` is gone; the new grid composition is in place with all 5 `data-test` selectors (`work-section`, `work-grid`) and the locked `<section>` + `<h1>` + `<ul>` markup
    - `npm run typecheck` + `npm run lint` + `npm run build` all exit 0
    - All 4 Wave-0 RED specs flip GREEN (`work-grid-renders`, `work-status-badges`, `work-no-flagship`, `work-descriptions-cliche-scrub`)
    - Phase 1 + 2 + 3 chrome specs remain GREEN (`npm run test:full` passes — no regression on hero, currently, channels, footer-socials, ctas-resolve-200, view-transition-name-present, mobile-hero-stacks-cleanly, about-renders, no-client-components, focus-ring, reduced-motion, monogram, lighthouse, contrast, etc.)
    - Curl direct verification: `curl -s http://localhost:3000/work | grep -E '(work-section|work-grid|work-heading|sr-only)'` shows all 4 selectors present in the rendered HTML
    - Atomic commit: `feat(phase-4/w2): rewrite app/work/page.tsx to render projects grid (D-05, D-06, D-07, D-13; flips W0 RED specs GREEN)`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (Wave 3a): Deploy to Vercel branch preview + run full Playwright suite against the preview URL</name>
  <files>(no source files modified — deploy + verify only)</files>
  <read_first>
    - .planning/STATE.md "Preview URL correction (important)" subsection — Vercel production alias is NOT the branch preview; user MUST supply the actual per-branch preview URL from the Vercel dashboard
    - .planning/phases/04-work-projects/04-UI-SPEC.md § Phase Exit Visual Verification (lines ~559-600 — the 28-item checklist)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Common Pitfalls Pitfall 7 (Vercel preview vs production alias confusion)
    - playwright.config.ts (root — verify if a `BASE_URL` env override exists; if not, document the override mechanism in the SUMMARY)
  </read_first>
  <what-built>Wave 0-2 shipped on a feature branch (recommend `phase-4/work-projects` per project branching convention; if the user has been committing direct to `main`, this checkpoint runs against the `main`-branch preview URL Vercel auto-generates per push). Pushing the Wave 0-2 commits triggers Vercel to build a branch preview at a URL of the form `braeden-site-<hash>-<vercel-scope>.vercel.app`.

This checkpoint validates the deployed build matches the local build by:
1. Pushing the Wave 0-2 commits (already committed to the current branch — just `git push`)
2. User retrieves the actual branch-preview URL from Vercel dashboard → Deployments → most recent
3. Re-running the full Playwright suite against `BASE_URL=<preview-url>` (or equivalent env override per `playwright.config.ts`)
4. Verifying all 22+ specs (Phase 1 + 2 + 3 chrome + the 4 new Phase 4 specs) GREEN against the CDN-served URL — NOT the local dev server (CDN serves the optimized production bundle; local dev server differs)
  </what-built>
  <how-to-verify>
    1. Confirm Wave 0-2 commits are pushed: `git log --oneline -5` shows the 4 atomic commits from Tasks 1-4 on the current branch.
    2. Open Vercel dashboard → Deployments → find the deploy triggered by the most recent push (NOT `https://braeden-site.vercel.app` — that is the production alias serving stale cache per STATE.md Phase 1 W4-T2 lesson). Copy the per-deploy URL of the form `braeden-site-<hash>-<scope>.vercel.app`.
    3. Reply to Claude with the exact preview URL string. Claude will then run:
       ```
       BASE_URL=https://<preview-hash>.vercel.app npx playwright test tests/work-*.spec.ts && \
       BASE_URL=https://<preview-hash>.vercel.app npm run test:full
       ```
       (If `playwright.config.ts` does not currently honor `BASE_URL`, Claude amends it to read `process.env.BASE_URL ?? 'http://localhost:3000'` as a one-line documented dev-loop change before the test run.)
    4. Visually walk the 28-item Phase Exit checklist in `04-UI-SPEC.md` § Phase Exit Visual Verification (lines ~559-600) on the preview URL in a real browser. Note any items that fail.
    5. Reply with: (a) the Playwright counts (passed / failed / skipped); (b) any of the 28 visual items that failed with one-line cause; (c) approval to proceed to Task 6 (the WebAIM contrast check) OR a request to revise.
  </how-to-verify>
  <action>Push the Wave 0-2 commits if not already pushed (git push). Wait for the user to supply the per-branch Vercel preview URL from the dashboard. Once supplied, set BASE_URL=<preview-url> and re-run the targeted Phase 4 spec set first, then the full suite. If playwright.config.ts does not currently honor BASE_URL via process.env, amend it inline as a documented one-line dev-loop change (read process.env.BASE_URL ?? 'http://localhost:3000' on the use.baseURL field) before the test run. Walk the 28-item UI-SPEC § Phase Exit Visual Verification checklist on the preview URL in a real browser. Do NOT proceed to Task 6 until the user explicitly approves or supplies a revision request.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — Playwright suite execution against the deployed preview URL is parameterized by user-supplied BASE_URL; verification command is BASE_URL=https://<preview-hash>.vercel.app npm run test:full and must show 0 failed; the 28-item visual checklist is human-verified in browser"</automated>
  </verify>
  <acceptance_criteria>
    - User has supplied the actual per-branch preview URL (NOT `braeden-site.vercel.app` production alias)
    - Full Playwright suite ran against the preview URL with `BASE_URL` override
    - Result reported: passed / failed / skipped counts; any of the 28 visual checklist items that failed are listed with cause
    - User explicitly approves proceeding to Task 6 OR requests revision (in which case orchestrator re-routes to a fix task)
  </acceptance_criteria>
  <resume-signal>Reply with the preview URL + Playwright counts + visual-checklist exceptions + "approved" or "revise: &lt;item&gt;".</resume-signal>
  <done>Vercel branch preview deployed; full suite GREEN against the CDN URL (or specific failures triaged); 28-item visual checklist walked; user approved continuation OR a revision task was inserted before Task 6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (Wave 3b): WebAIM contrast verification on archived dot — execute the documented #707070 → #7a7a7a fallback if it fails</name>
  <files>(potentially modifies: components/work/ProjectCard.tsx + tests/work-status-badges.spec.ts — only if fallback triggers)</files>
  <read_first>
    - .planning/phases/04-work-projects/04-UI-SPEC.md § Color (lines ~155-170 — the contrast verification table; the `#707070`-on-`#1a1a1f` row marked ⚠️ borderline)
    - .planning/phases/04-work-projects/04-RESEARCH.md § Common Pitfalls Pitfall 2 (the documented fallback mechanism)
    - .planning/phases/04-work-projects/04-CONTEXT.md `<specifics>` (the `#7a7a7a` fallback note)
    - components/work/ProjectCard.tsx (the `STATUS_DOT_COLOR['archived']` literal — only file changed if fallback triggers)
    - tests/work-status-badges.spec.ts (the `ARCHIVED_RGB` regex — also updates if fallback triggers)
  </read_first>
  <what-built>Wave 3a verified the deployed preview functionally. This checkpoint executes the one pre-flagged contrast risk: the `#707070` archived status dot on `#1a1a1f` background measures ~3.6:1 against the gradient top in clean conditions, but the grain layer (`opacity 0.04 mix-blend-mode: overlay`) MAY drop it below the WCAG AA UI-element minimum of 3.0:1 once compositing is real (Pitfall 2 / Assumption A8 / D-09 borderline pair).

User must run a real-browser contrast check on the deployed preview URL. If the dot passes ≥3.0:1, no source change. If it fails, Claude bumps `STATUS_DOT_COLOR['archived']` from `#707070` to `#7a7a7a` (still in the deep-grey "past" register) AND updates `tests/work-status-badges.spec.ts`'s `ARCHIVED_RGB` regex from `/rgba?\(\s*112\s*,\s*112\s*,\s*112/` to `/rgba?\(\s*122\s*,\s*122\s*,\s*122/`. Both are 1-line edits; a single atomic commit ships both.</what-built>
  <how-to-verify>
    1. Open the Vercel branch-preview URL from Task 5 in a real browser (Chrome / Safari / Firefox — any).
    2. Navigate to `/work`. Locate the archived braehods card (last card; row 4 left column on desktop).
    3. Open WebAIM Contrast Checker at https://webaim.org/resources/contrastchecker/.
    4. Use a color picker (browser DevTools "Inspect" + computed style on the dot `<span>`, then copy the hex; OR use a screenshot + a desktop color-picker tool) to read the rendered dot color and the rendered background color directly underneath it (with grain overlay applied).
    5. Enter the two hex values into WebAIM. Read the contrast ratio.
    6. Reply with: (a) measured rendered dot color hex; (b) measured rendered background hex (with grain); (c) WebAIM contrast ratio; (d) PASS (ratio ≥ 3.0:1) or FAIL (ratio < 3.0:1).
    7. If FAIL: Claude executes the documented fallback (1-line change in `ProjectCard.tsx` constant + 1-line regex update in `tests/work-status-badges.spec.ts`) and re-runs the targeted spec to confirm the new color satisfies the regex update. Re-deploys via push.
    8. If PASS: Claude proceeds to Task 7 (final SUMMARY + ROADMAP update) with no source change.
  </how-to-verify>
  <action>Wait for the user to supply: (a) measured rendered dot color hex, (b) measured rendered background hex (with grain overlay applied), (c) WebAIM contrast ratio, (d) PASS/FAIL verdict against the WCAG AA UI-element minimum of 3.0:1. If FAIL: edit components/work/ProjectCard.tsx STATUS_DOT_COLOR['archived'] from '#707070' to the user-confirmed fallback value (default '#7a7a7a'); edit tests/work-status-badges.spec.ts ARCHIVED_RGB regex to match the new RGB values (e.g., /rgba?\(\s*122\s*,\s*122\s*,\s*122/ for #7a7a7a); re-run npx playwright test tests/work-status-badges.spec.ts to confirm GREEN; commit with fix(phase-4/w3): bump archived dot per WebAIM contrast verification; push to trigger re-deploy. If PASS: record the measured ratio in the SUMMARY's User Decisions section and proceed to Task 7 with no source change.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — WebAIM measurement is human-supplied via real-browser color-picker on the deployed preview; if FAIL branch executes, npx playwright test tests/work-status-badges.spec.ts must exit 0 after the literal + regex update"</automated>
  </verify>
  <acceptance_criteria>
    - User has supplied: rendered dot color hex, rendered background hex, WebAIM ratio, PASS/FAIL verdict
    - If FAIL: `STATUS_DOT_COLOR['archived']` updated to `#7a7a7a` (or another agreed grey value) in `components/work/ProjectCard.tsx`; `ARCHIVED_RGB` regex updated correspondingly in `tests/work-status-badges.spec.ts`; targeted spec re-run shows GREEN; re-deploy verified; commit `fix(phase-4/w3): bump archived dot to #7a7a7a per WebAIM contrast verification`
    - If PASS: no source change; the borderline-but-passing measurement is recorded in the SUMMARY for Phase 6 reference
  </acceptance_criteria>
  <resume-signal>Reply with the WebAIM measurement + PASS/FAIL. If FAIL, also confirm the `#7a7a7a` (or alternate) value to use.</resume-signal>
  <done>Contrast verification complete; either no source change (PASS) or the documented fallback shipped + re-verified + re-deployed (FAIL). User approved continuation to Task 7.</done>
</task>

<task type="auto">
  <name>Task 7 (Wave 3c): Write 04-01-SUMMARY.md + update ROADMAP + STATE + REQUIREMENTS</name>
  <files>.planning/phases/04-work-projects/04-01-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (the canonical SUMMARY template — match its sections verbatim)
    - .planning/phases/03-about-page/03-01-SUMMARY.md (the most recent SUMMARY — match its tone, depth, and structure as the closest analog)
    - .planning/phases/02-home-page/02-07-SUMMARY.md if it exists, else 02-06-SUMMARY.md (the Wave-3 deploy + verify SUMMARY pattern)
    - .planning/ROADMAP.md (Phase 4 entry — needs Plans count + checkbox flip from `[ ]` to `[x]` + completion date)
    - .planning/STATE.md (Last Session entry to append; Spec scoreboard to update; carry-forwards to Phase 6 to amend)
    - .planning/REQUIREMENTS.md (Traceability table — flip WORK-01..06 + A11Y-05 from `Pending` to `Complete`; flip the equivalent rows under § Work / Projects + § Accessibility from `[ ]` to `[x]`)
  </read_first>
  <action>Write `.planning/phases/04-work-projects/04-01-SUMMARY.md` per the canonical SUMMARY template. Match the structure and depth of `.planning/phases/03-about-page/03-01-SUMMARY.md`. Required sections:

1. **Frontmatter** — phase, plan, status: complete, completed_at: <today's date>, duration_estimate, requirements_completed: WORK-01..06 + A11Y-05.
2. **Objective recap** — 1 paragraph; what shipped vs the plan's stated objective.
3. **Wave-by-wave recap** — bulleted list of each task's commit hash + 1-line summary (Tasks 0-6).
4. **User Decisions** subsection — the Task-0 Wave-0a OQ#1/OQ#2/OQ#3 user responses (project count, hrefs, archived title) recorded verbatim for Phase 6 / future reference.
5. **Net spec movement** — list the 4 newly-GREEN specs; confirm 0 regressions; cite full-suite count (passed / failed / skipped).
6. **Deviations from Plan** — any Rule-N deviations executed during the 6 task waves (e.g., if the optional ExternalArrowIcon SVG fallback shipped, document why; if the WebAIM check forced the `#7a7a7a` bump, document the measured ratio and the new regex).
7. **Phase 6 carry-forwards** — list the items the SUMMARY surfaces for Phase 6:
   - archived braehods `href` post-Phase-6 DNS-swap target (TBD — Phase 6 picks Wayback / repo / `/archive` sub-route)
   - `STATUS_DOT_COLOR` literal promotion to `@theme` revisit if status badges propagate beyond ProjectCard
   - CLAUDE.md `chore(docs)` row update for lucide-react brand icons (still outstanding from Phase 2 Plan 04 SUMMARY; Phase 4 did not consume any lucide brand icon so didn't surface again)
   - Phase 6 Lighthouse audit owns the /work LCP gate (per UI-SPEC § Performance Contract — first-card stagger fallback documented if needed)
8. **Visual milestone** — 1 paragraph describing what the deployed preview shows (the 7 (or 8/9) cards in their grid; CapitolLens leading; archived braehods as the orphan; hover states animating; reduced-motion verified).

Then update `.planning/ROADMAP.md`:
- Phase 4 entry: flip `[ ]` to `[x]` and append `Shipped <date>. <one-line summary>.` to the entry's first bullet (mirror Phase 2 + 3 patterns).
- Phase 4 Plans line: `Plans: 1 plan, 4 waves (W0 data + spec stubs + user-input checkpoint, W1 ProjectCard component, W2 page rewrite, W3 deploy + verify + WebAIM contrast check)`
- Plans list: `- [x] 04-01-PLAN.md — <one-line summary>` (mirror Phase 3's completion line)
- Progress Table row for Phase 4: `1/1` plans complete; status `Complete`; completed date.

Then update `.planning/STATE.md`:
- progress.completed_phases: 4 (was 3)
- progress.completed_plans: 10 (was 9; Phase 4 adds 1)
- progress.percent: recompute (was 100% with 9/9 plans; now 10/10 plans across 4/6 phases — actual percent depends on planning's `total_phases` framing, recompute consistently)
- Last Session entry appended: `*State updated: <date> by execute-phase (Phase 4 Plan 01 / Wave 3 complete — …)*` mirroring the Phase 2 + 3 entry style.
- Current Position: bump Phase to 5 (next milestone target) once Phase 4 ships; update Branch + Status accordingly.
- Spec scoreboard: increment by +4 newly-GREEN Phase 4 specs (or +3 if the user opted out of the optional cliché-scrub spec — match what actually shipped).
- Append a new "Last Session (Phase 4 Plan 01)" subsection summarizing the 6-task wave structure + commit hashes + duration.

Then update `.planning/REQUIREMENTS.md`:
- Traceability table: flip WORK-01..06 + A11Y-05 from `Pending` to `Complete`.
- Section markdown: under `### Work / Projects (WORK)`, flip each `- [ ]` to `- [x]`. Under `### Accessibility (A11Y)`, flip the A11Y-05 line.

After writing all 4 files, run `git add -A && git status` to verify the changes, then commit with: `docs(phase-4): close Phase 4 — 04-01-SUMMARY.md + ROADMAP + STATE + REQUIREMENTS traceability flip`.</action>
  <verify>
    <automated>test -f .planning/phases/04-work-projects/04-01-SUMMARY.md && grep -q "WORK-01" .planning/phases/04-work-projects/04-01-SUMMARY.md && grep -q "A11Y-05" .planning/phases/04-work-projects/04-01-SUMMARY.md && grep -q "\[x\] \*\*Phase 4" .planning/ROADMAP.md && grep -q "WORK-01 | Phase 4 | Complete" .planning/REQUIREMENTS.md && grep -q "A11Y-05 | Phase 4 | Complete" .planning/REQUIREMENTS.md && grep -q "completed_phases: 4" .planning/STATE.md</automated>
  </verify>
  <done>
    - `04-01-SUMMARY.md` exists with all 8 required sections + frontmatter + User Decisions + Phase 6 carry-forwards
    - `ROADMAP.md` Phase 4 row checked, Plans count updated, completion date set, Progress Table row updated
    - `STATE.md` `progress.completed_phases` incremented to 4, `completed_plans` incremented, Spec scoreboard updated, Last Session entry appended
    - `REQUIREMENTS.md` Traceability table shows WORK-01..06 + A11Y-05 as `Complete`; Work + A11Y section checkboxes flipped
    - Final atomic commit lands all 4 files: `docs(phase-4): close Phase 4 — SUMMARY + ROADMAP + STATE + REQUIREMENTS traceability flip`
    - `git status` is clean after the commit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→browser→external (project repo / live site / video) | The card click target navigates the user from this site to a third-party destination via `target="_blank"`. The opened tab is granted `window.opener` access by default unless `rel="noopener"` is set — that opener handle is the tabnabbing surface. |
| build-time→source (data/projects.ts literal) | Project metadata (title, description, tags, status, href) is authored in source; no runtime user input crosses this boundary. React escapes by default; no `dangerouslySetInnerHTML`. |
| user→browser→our DOM (focus, hover, scroll) | Pure CSS interactivity; no JS event handlers; FOUND-07 invariant holds (zero `'use client'`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Spoofing | `<a target="_blank">` on every ProjectCard (7-9 instances) — tabnabbing via `window.opener` | mitigate | `rel="noopener noreferrer"` paired with every `target="_blank"` per D-03. Verbatim in `components/work/ProjectCard.tsx` lines specified in Task 3 acceptance Test 6. Spec `tests/work-grid-renders.spec.ts` Test 2 asserts the `rel` attribute contains both `noopener` and `noreferrer` on every card link. |
| T-04-02 | Spoofing | `project.href` open-redirect risk | accept | All hrefs are TS-string-literals confirmed at the Wave-0a user-input checkpoint (Task 0). No dynamic href construction; no URL parameters parsed at runtime. Zod schema validates URL syntax via `z.string().url()` at the Task 1 build-time validation step (`Project.parse()` per `<verify>` block). The Wave-0a checkpoint requires user explicit confirmation per href — no silent acceptance of attacker-controlled URLs. |
| T-04-03 | Tampering / Information Disclosure | XSS via project description / title injection through React render | accept | All project fields are TS-literals in source (no runtime user input); React's default JSX escaping applies; no `dangerouslySetInnerHTML` used anywhere in the new files. Verified by Task 3 acceptance Test 11 (title rendered as plain text node, not HTML). |
| T-04-04 | Information Disclosure | CapitolLens description framing reveals trading strategy as "live" when it is actually paper-only | mitigate | D-15 + Task 1 behavior Test 6 + memory `feedback_capitollens_auto_follow.md` mandate the literal substring `paper-only` or `paper-traded` in the CapitolLens description. Cliché-scrub spec `tests/work-descriptions-cliche-scrub.spec.ts` (Task 2) is a backstop for voice / honesty regression. |
| T-04-05 | Information Disclosure | Memory disclosure of unrelated personal projects (school-planner, reel-research-agent) without user opt-in | mitigate | Wave-0a checkpoint (Task 0) requires explicit user opt-in via the OQ#1 binary choice (7 / 8 / 9). Default is 7 (WORK-06 verbatim). No silent inclusion of memory-flagged projects. |
| T-04-06 | Tampering | Schema-mismatch silent failure (e.g., a typo in `tags: ['traidng']` would be a runtime issue if `Project.parse()` is never called) | mitigate | Task 1 `<verify>` block runs a `node` one-liner that imports `projects` and calls `Project.parse(entry)` on every entry — any Zod validation error fails the verify gate and blocks the Wave-0 commit. TS strict + `noUncheckedIndexedAccess` (Phase 1 tsconfig) catches enum mismatches at typecheck time. |
| T-04-07 | Tampering | Lucide brand-icon supply-chain risk (already mitigated Phase 2) | accept | Phase 4 does NOT import any brand icon from `lucide-react`. The `↗` arrow glyph is Unicode (or inline SVG fallback in `components/icons/ExternalArrowIcon.tsx` per Claude's Discretion — same `components/icons/` pattern Phase 2 + 3 already use for GitHub + Instagram). Task 3 acceptance Test 3 asserts zero `lucide-react` imports in `ProjectCard.tsx`. |
</threat_model>

<verification>
**Per-wave verification gates (sampled per `04-VALIDATION.md` § Sampling Rate):**

- After Task 1 (Wave 0b commit): `npm run typecheck && node -e "<Project.parse() one-liner>"` per Task 1 `<verify>`.
- After Task 2 (Wave 0c commit): `npx playwright test --list` shows 4 new specs collected; targeted run shows assertions FAILING (RED is the expected Wave-0 state).
- After Task 3 (Wave 1 commit): `npm run typecheck && npm run lint && npx playwright test tests/no-client-components.spec.ts` (Phase 1 regression guard); the 4 Wave-0 specs still RED (Wave 2 flips them).
- After Task 4 (Wave 2 commit): `npm run typecheck && npm run lint && npm run build && npx playwright test tests/work-*.spec.ts tests/no-client-components.spec.ts tests/focus-ring.spec.ts tests/reduced-motion.spec.ts && npm run test:full` — all 4 Wave-0 RED specs flip GREEN; zero regressions.
- After Task 5 (Wave 3a checkpoint): full Playwright suite GREEN against the deployed Vercel branch-preview URL with `BASE_URL` override; 28-item visual checklist walked.
- After Task 6 (Wave 3b checkpoint): WebAIM contrast verification on the rendered archived dot color against the rendered background (with grain) returns a ratio ≥ 3.0:1; if not, the documented `#7a7a7a` fallback ships.
- After Task 7 (Wave 3c commit): `04-01-SUMMARY.md` + ROADMAP + STATE + REQUIREMENTS all updated; final commit lands; `git status` clean.

**Phase-level success criteria (from ROADMAP § Phase 4):**

1. `/work` renders all 7 (or 8 if user opts in) projects in equal-weight grid (WORK-01, WORK-06) — proven by `tests/work-grid-renders.spec.ts` + `tests/work-no-flagship.spec.ts` GREEN against Vercel preview.
2. Project entries typed and live in `data/projects.ts`; editing one adds or modifies a card with no JSX hunt (WORK-02) — proven by Task 1's populated literal + Task 3's `<ProjectCard>` map iteration in `app/work/page.tsx`.
3. Status badges render as colored dot + label, never color-only (WORK-03, A11Y-05) — proven by `tests/work-status-badges.spec.ts` GREEN.
4. Tags render visibly on each card (WORK-04) — proven by `tests/work-grid-renders.spec.ts` Test 3.
5. Each card links out to repo / site / video with correct external-link semantics (WORK-05) — proven by `tests/work-grid-renders.spec.ts` Test 2 + threat T-04-01 mitigation.
</verification>

<success_criteria>
- [ ] All 7 phase requirement IDs (WORK-01..06 + A11Y-05) flipped to `Complete` in `.planning/REQUIREMENTS.md` Traceability table
- [ ] `04-01-SUMMARY.md` exists with all required sections + User Decisions subsection from Task 0 Wave-0a checkpoint
- [ ] `.planning/ROADMAP.md` Phase 4 row checked + completion date set
- [ ] `.planning/STATE.md` `progress.completed_phases` = 4, Spec scoreboard updated, Last Session entry appended
- [ ] All 4 Wave-0 RED specs (`work-grid-renders`, `work-status-badges`, `work-no-flagship`, `work-descriptions-cliche-scrub`) GREEN against Vercel branch preview
- [ ] All 22+ pre-existing Playwright specs (Phase 1 + 2 + 3 chrome) remain GREEN against Vercel branch preview — zero regression
- [ ] WebAIM contrast verification on archived dot pair complete; either ≥ 3.0:1 measured (no source change) or the documented `#7a7a7a` fallback shipped + re-verified
- [ ] User has confirmed all 28 items of UI-SPEC § Phase Exit Visual Verification (or specific items remediated)
- [ ] Zero `'use client'` directive added in any Phase 4 file (verified by `tests/no-client-components.spec.ts` GREEN)
- [ ] Zero new dependencies installed (verified by `package.json` diff = 0 from Phase 3 close)
- [ ] All commits land atomic (one logical change per commit; commits cited in SUMMARY's wave-by-wave recap)
- [ ] CapitolLens description contains the literal substring `paper-only` or `paper-traded` (D-15 honesty marker; verified by Task 1 behavior Test 6 + Task 4 full-suite re-run)
</success_criteria>

<output>
After completion, create `.planning/phases/04-work-projects/04-01-SUMMARY.md` per Task 7. The SUMMARY is the load-bearing handoff to Phase 5 (Contact Modal) — the next phase consumes the established Server-Components-only + tile-treatment + sr-only-h1 patterns and references the Phase 4 SUMMARY for prior-art on the cross-route consumer pattern.
</output>
